---
phase: 44-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/hooks/useSettingsData.ts
  - src/src/hooks/useSettingsData.test.ts
  - src/src/types/ui.ts
  - src/src/types/settings.ts
  - src/src/stores/connection.ts
  - src/src/components/settings/SettingsModal.tsx
  - src/src/components/settings/SettingsModal.test.tsx
  - src/src/components/settings/AgentsTab.tsx
  - src/src/components/settings/AgentsTab.test.tsx
  - src/src/components/settings/AppearanceTab.tsx
  - src/src/components/settings/AppearanceTab.test.tsx
  - src/src/components/settings/ApiKeysTab.tsx
  - src/src/components/settings/CredentialsSection.tsx
  - src/src/components/settings/McpTab.tsx
  - src/src/components/settings/SettingsTabSkeleton.tsx
  - src/src/components/content-area/view/PanelPlaceholder.tsx
  - src/src/components/shared/PlaceholderView.tsx
  - src/src/components/shared/PlaceholderView.test.tsx
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02

must_haves:
  truths:
    - "Settings modal opens with refactored data hooks -- no per-hook fetch boilerplate, useFetch<T> drives simple hooks"
    - "Connection store persists modelId and rehydrates without clobbering ephemeral fields (status, error, reconnectAttempts)"
    - "ModalState is a discriminated union with typed props per modal type"
    - "No placeholder fields, fake data, or non-functional controls are visible anywhere in the application"
  artifacts:
    - path: "src/src/hooks/useSettingsData.ts"
      provides: "Generic useFetch<T> hook, all settings hooks composing it"
      contains: "function useFetch<T>"
    - path: "src/src/types/ui.ts"
      provides: "Discriminated union ModalState"
      contains: "type ModalState"
    - path: "src/src/stores/connection.ts"
      provides: "Deep merge persist function for connection store"
      contains: "merge:"
  key_links:
    - from: "src/src/hooks/useSettingsData.ts"
      to: "src/src/components/settings/AgentsTab.tsx"
      via: "useAgentStatuses hook import"
      pattern: "useAgentStatuses"
    - from: "src/src/types/ui.ts"
      to: "src/src/components/settings/SettingsModal.tsx"
      via: "ModalState type driving initialTab"
      pattern: "modalState\\.initialTab"
---

<objective>
Land the WIP settings refactor and complete dead UI removal.

Purpose: The git working tree contains 16 modified files implementing FOUND-01 (generic `useFetch<T>`, discriminated union `ModalState`, deep-merge persist). All 29 settings tests pass. This plan commits that work, deletes 2 remaining dead UI files (PanelPlaceholder, PlaceholderView), verifies no regressions, and closes FOUND-01 + FOUND-02.

Output: Committed settings refactor, zero dead placeholder components in codebase.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-foundation/44-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit WIP settings refactor and verify</name>
  <files>
    src/src/hooks/useSettingsData.ts,
    src/src/hooks/useSettingsData.test.ts,
    src/src/types/ui.ts,
    src/src/types/settings.ts,
    src/src/stores/connection.ts,
    src/src/components/settings/SettingsModal.tsx,
    src/src/components/settings/SettingsModal.test.tsx,
    src/src/components/settings/AgentsTab.tsx,
    src/src/components/settings/AgentsTab.test.tsx,
    src/src/components/settings/AppearanceTab.tsx,
    src/src/components/settings/AppearanceTab.test.tsx,
    src/src/components/settings/ApiKeysTab.tsx,
    src/src/components/settings/CredentialsSection.tsx,
    src/src/components/settings/McpTab.tsx,
    src/src/components/settings/SettingsTabSkeleton.tsx
  </files>
  <action>
    The WIP changes are already in the working tree (unstaged). The research confirms all 29 settings tests pass and all changes are correct. The task is:

    1. Run `cd /home/swd/loom/src && npx tsc --noEmit` to verify TypeScript compilation passes with all WIP changes.
    2. Run `cd /home/swd/loom/src && npx vitest run src/hooks/useSettingsData.test.ts src/components/settings/ --reporter=verbose` to confirm all 29 settings tests pass.
    3. Run `cd /home/swd/loom/src && npx eslint src/hooks/useSettingsData.ts src/types/ui.ts src/types/settings.ts src/stores/connection.ts src/components/settings/ --max-warnings=0` to confirm no lint violations.
    4. Stage all 15 WIP files (listed above) and commit with message describing the refactor.

    What the WIP changes do (for reference -- do NOT re-implement, just commit):
    - `useSettingsData.ts`: Generic `useFetch<T>(url, initial)` hook. `useApiKeys`, `useCredentials`, `useGitConfig` compose it. `useAgentStatuses` and `useMcpServers` keep custom fetch logic (Promise.all / response transform).
    - `types/ui.ts`: `ModalState` changed to discriminated union: `{ type: 'settings'; initialTab?: SettingsTabId }`.
    - `types/settings.ts`: Removed `PROVIDER_DEFAULT_MODELS` map and `defaultModel` field (client-side fake data).
    - `stores/connection.ts`: Added `merge` function to persist config for deep-merge rehydration. Fixed `reconnectAttempts` nullish coalescing.
    - `SettingsModal.tsx`: Reads `modalState.initialTab`, uses as `defaultValue` with `key` prop.
    - `AgentsTab.tsx`: Removed fake `defaultModel` display, added error state with retry button.
    - `AppearanceTab.tsx`: Removed non-functional density label, fixed code font CSS quoting, fixed `useEffect` deps.
    - `ApiKeysTab.tsx`: `Math.max(0, ...)` prevents negative day counts.
    - `CredentialsSection.tsx`: Changed loading state from `null` to `<SettingsTabSkeleton />`.
    - `McpTab.tsx`: Prop drilling for `ProviderSection`, `text-xs` instead of `text-[10px]`.
    - `SettingsTabSkeleton.tsx`: Removed unnecessary `cn()` wrapping.
    - All 4 test files updated to match.

    If any verification step fails, fix the issue before committing. But research indicates everything is clean.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx tsc --noEmit && npx vitest run src/hooks/useSettingsData.test.ts src/components/settings/ --reporter=verbose && echo "PASS"</automated>
  </verify>
  <done>All 15 WIP files committed. TypeScript compiles clean. All 29 settings tests pass. No lint violations.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead UI components and verify no broken imports</name>
  <files>
    src/src/components/content-area/view/PanelPlaceholder.tsx,
    src/src/components/shared/PlaceholderView.tsx,
    src/src/components/shared/PlaceholderView.test.tsx
  </files>
  <action>
    Remove the two remaining dead UI components identified by research:

    1. Delete `src/src/components/content-area/view/PanelPlaceholder.tsx` -- zero imports in codebase (was for panels not yet built; all panels now exist).
    2. Delete `src/src/components/shared/PlaceholderView.tsx` -- only imported by its own test file. Route placeholders replaced by real views in M1/M2.
    3. Delete `src/src/components/shared/PlaceholderView.test.tsx` -- test for deleted component.

    IMPORTANT: Do NOT delete any of these (they are actively used):
    - `BinaryPlaceholder` -- imported by `CodeEditor.tsx` for binary files
    - `SettingsTabSkeleton` -- used as loading state in settings tabs
    - `GitPanelSkeleton` -- used as loading state in git panel
    - `ProofOfLife` -- dev route, intentionally kept
    - `TokenPreview` -- dev route, intentionally kept

    After deletion:
    4. Run `cd /home/swd/loom/src && grep -r "PanelPlaceholder\|PlaceholderView" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules` to confirm zero remaining imports.
    5. Run `cd /home/swd/loom/src && npx tsc --noEmit` to confirm no broken imports.
    6. Run `cd /home/swd/loom/src && npx vitest run` to confirm full test suite still passes.
    7. Commit the deletions.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>PanelPlaceholder.tsx, PlaceholderView.tsx, and PlaceholderView.test.tsx deleted. Zero broken imports. Full test suite passes. No placeholder fields, fake data, or non-functional controls visible in the application.</done>
</task>

</tasks>

<verification>
- TypeScript compiles with zero errors: `cd /home/swd/loom/src && npx tsc --noEmit`
- All settings tests pass: `cd /home/swd/loom/src && npx vitest run src/hooks/useSettingsData.test.ts src/components/settings/ --reporter=verbose`
- Full test suite passes: `cd /home/swd/loom/src && npx vitest run`
- Zero references to deleted components: `grep -r "PanelPlaceholder\|PlaceholderView" src/src/ --include="*.ts" --include="*.tsx"` returns nothing
- `useFetch<T>` exists in useSettingsData.ts: `grep "function useFetch" src/src/hooks/useSettingsData.ts`
- `ModalState` is discriminated union: `grep "type ModalState" src/src/types/ui.ts`
- Connection store has merge function: `grep "merge:" src/src/stores/connection.ts`
</verification>

<success_criteria>
- FOUND-01: Generic `useFetch<T>` hook eliminates per-hook boilerplate, connection store has deep merge persist, ModalState is discriminated union -- all committed and verified
- FOUND-02: No PanelPlaceholder, no PlaceholderView, no fake defaultModel, no density display -- all dead UI removed
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/44-foundation/44-01-SUMMARY.md`
</output>
