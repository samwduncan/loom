---
phase: 14-message-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/types/message.ts
  - src/src/components/chat/view/MessageContainer.tsx
  - src/src/components/chat/view/MessageList.tsx
  - src/src/components/chat/view/ErrorMessage.tsx
  - src/src/components/chat/view/SystemMessage.tsx
  - src/src/components/chat/view/TaskNotificationMessage.tsx
  - src/src/lib/transformMessages.ts
autonomous: true
requirements: [MSG-03, MSG-04, MSG-05, MSG-10, MSG-11]

must_haves:
  truths:
    - "Error messages display as inline banners with red accent and error icon"
    - "System messages display centered in muted text"
    - "Task notification messages display with checklist icon and surface background"
    - "All 5 message types dispatch correctly from MessageList"
    - "Historical error/system/task messages survive page reload (if backend sends them)"
  artifacts:
    - path: "src/src/components/chat/view/ErrorMessage.tsx"
      provides: "Inline error banner component"
      exports: ["ErrorMessage"]
    - path: "src/src/components/chat/view/SystemMessage.tsx"
      provides: "Centered system message component"
      exports: ["SystemMessage"]
    - path: "src/src/components/chat/view/TaskNotificationMessage.tsx"
      provides: "Task notification component"
      exports: ["TaskNotificationMessage"]
    - path: "src/src/components/chat/view/MessageContainer.tsx"
      provides: "5-role message wrapper"
      exports: ["MessageContainer"]
    - path: "src/src/components/chat/view/MessageList.tsx"
      provides: "5-way message dispatch"
      exports: ["MessageList"]
  key_links:
    - from: "src/src/components/chat/view/MessageList.tsx"
      to: "ErrorMessage, SystemMessage, TaskNotificationMessage"
      via: "switch on msg.role"
      pattern: "case 'error'.*case 'system'.*case 'task_notification'"
    - from: "src/src/lib/transformMessages.ts"
      to: "Message types"
      via: "filter allowlist"
      pattern: "entry\\.type.*error.*system.*task_notification"
---

<objective>
Expand the message rendering pipeline from 2-way (user/assistant) to 5-way dispatch, add three new message type components, and extend the backend message transformer.

Purpose: Enable rendering of error, system, and task notification messages alongside existing user/assistant messages.
Output: MessageContainer handles all 5 roles, MessageList dispatches to all 5 components, transformBackendMessages passes through new entry types.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-message-types/14-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/src/types/message.ts:
```typescript
export type MessageRole = 'user' | 'assistant' | 'system' | 'error' | 'task_notification';

export interface Message {
  id: string;
  role: MessageRole;
  content: string;
  toolCalls?: ToolCall[];
  thinkingBlocks?: ThinkingBlock[];
  metadata: MessageMetadata;
  providerContext: ProviderContext;
}
```

From src/src/components/chat/view/MessageContainer.tsx:
```typescript
interface MessageContainerProps {
  children: ReactNode;
  role: 'user' | 'assistant'; // EXPAND to all 5 roles
}
```

From src/src/components/chat/view/MessageList.tsx:
```typescript
// Currently 2-way ternary: msg.role === 'user' ? <UserMessage> : <AssistantMessage>
// EXPAND to 5-way switch
```

From src/src/lib/transformMessages.ts:
```typescript
// Current filter: entry.type !== 'user' && entry.type !== 'assistant' → false
// EXPAND to include error/system/task_notification
export interface BackendEntry {
  type: string;
  message?: { role: 'user' | 'assistant'; content: string | ContentBlock[]; };
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand MessageContainer + create Error/System/TaskNotification components</name>
  <files>
    src/src/types/message.ts,
    src/src/components/chat/view/MessageContainer.tsx,
    src/src/components/chat/view/ErrorMessage.tsx,
    src/src/components/chat/view/SystemMessage.tsx,
    src/src/components/chat/view/TaskNotificationMessage.tsx,
    src/src/components/chat/view/ErrorMessage.test.tsx,
    src/src/components/chat/view/SystemMessage.test.tsx,
    src/src/components/chat/view/TaskNotificationMessage.test.tsx
  </files>
  <behavior>
    - ErrorMessage renders with border-l-4 border-error, bg-error/10, AlertCircle icon, error text content
    - SystemMessage renders centered text with text-muted text-xs, no background
    - TaskNotificationMessage renders with CheckSquare icon, label, bg-surface-1 background
    - MessageContainer accepts all 5 roles without TypeScript errors
    - System and error messages do NOT use the 80% max-width bubble constraint
    - All components use MessageContainer wrapper with correct role
  </behavior>
  <action>
    1. In `src/src/types/message.ts`: Add `ImageAttachment` interface (`{ id: string; url: string; name: string; width?: number; height?: number }`) and add optional `attachments?: ImageAttachment[]` to Message interface. This prepares the type for Plan 03.

    2. Expand `MessageContainer` role union from `'user' | 'assistant'` to all 5 MessageRole values. Update the inner wrapper logic:
       - `user`: keep existing right-aligned bubble with `max-w-[80%] rounded-2xl` but change bg from `bg-primary-muted` to `bg-card` per user decision
       - `assistant`: keep existing full-width transparent
       - `system`: `flex justify-center` outer, no inner wrapper constraints, text centered
       - `error`: full-width, no bubble constraint
       - `task_notification`: full-width, no bubble constraint
       Import MessageRole type and use it for the role prop.

    3. Create `ErrorMessage.tsx`: Inline banner with `border-l-4 border-error` accent, `bg-error/10` muted background, Lucide `AlertCircle` icon (size-4, text-error, shrink-0), error text. Use `MessageContainer role="error"`. No retry button (deferred to Phase 19 per ENH-04).

    4. Create `SystemMessage.tsx`: Centered horizontally, `text-muted text-xs`, no background. Content is `message.content`. Use `MessageContainer role="system"`.

    5. Create `TaskNotificationMessage.tsx`: Lucide `CheckSquare` icon (size-4, text-muted, shrink-0), label text in `text-sm`, `bg-surface-1 rounded-lg px-4 py-2` inner container. Use `MessageContainer role="task_notification"`.

    6. Write tests for each new component verifying correct rendering, correct CSS classes, correct icon presence, and correct MessageContainer role.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/chat/view/ErrorMessage.test.tsx src/src/components/chat/view/SystemMessage.test.tsx src/src/components/chat/view/TaskNotificationMessage.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Three new message type components render with correct styling. MessageContainer accepts all 5 roles. ImageAttachment type exists on Message interface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 5-way MessageList dispatch + transformBackendMessages expansion</name>
  <files>
    src/src/components/chat/view/MessageList.tsx,
    src/src/lib/transformMessages.ts,
    src/src/lib/transformMessages.test.ts
  </files>
  <behavior>
    - MessageList renders ErrorMessage for role='error', SystemMessage for role='system', TaskNotificationMessage for role='task_notification'
    - transformBackendMessages passes through entries with type 'error', 'system', 'task_notification' (creating Messages with appropriate role)
    - Existing user/assistant rendering is unchanged
    - Pure tool_result filtering still works
    - Assistant entry merging still works
  </behavior>
  <action>
    1. Update `MessageList.tsx`: Replace the ternary `msg.role === 'user' ? ... : ...` with a switch statement dispatching to all 5 message components. Import `ErrorMessage`, `SystemMessage`, `TaskNotificationMessage`. Add a default case that returns null (defensive — should never hit). Wrap each case in `MessageErrorBoundary` as existing pattern.

    2. Update `transformBackendMessages` in `transformMessages.ts`:
       - Expand the filter to also allow entry types `'error'`, `'system'`, and `'task_notification'`
       - For these new types, create Message objects with the matching role, content from `entry.message?.content` (string form), and default metadata
       - Backend currently only sends user/assistant in JSONL, but this prepares for future backend additions. The filter should use a Set allowlist: `new Set(['user', 'assistant', 'error', 'system', 'task_notification'])`
       - Ensure the BackendEntry type's message.role union is expanded to include all 5 roles

    3. Update existing tests in `transformMessages.test.ts`: Add test cases for error/system/task_notification entry passthrough. Verify existing tests still pass (user/assistant filtering, tool_result filtering, assistant merging).
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/lib/transformMessages.test.ts --reporter=verbose</automated>
  </verify>
  <done>MessageList dispatches all 5 message types. transformBackendMessages passes through error/system/task_notification entries. All existing tests pass plus new coverage.</done>
</task>

</tasks>

<verification>
- `cd /home/swd/loom/src && npx vitest run --reporter=verbose` — full suite green
- `cd /home/swd/loom/src && npx tsc --noEmit` — no TypeScript errors
</verification>

<success_criteria>
- All 5 message roles render through MessageList without errors
- Error banner has red accent border and icon
- System message is centered and muted
- Task notification has checklist icon and surface background
- transformBackendMessages handles new entry types
- No regression in existing user/assistant rendering
</success_criteria>

<output>
After completion, create `.planning/phases/14-message-types/14-01-SUMMARY.md`
</output>
