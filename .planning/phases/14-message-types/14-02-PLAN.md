---
phase: 14-message-types
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/components/chat/view/ThinkingDisclosure.tsx
  - src/src/components/chat/view/ThinkingDisclosure.test.tsx
  - src/src/components/chat/view/AssistantMessage.tsx
  - src/src/components/chat/view/ActiveMessage.tsx
  - src/src/components/chat/provider-logos/ClaudeLogo.tsx
  - src/src/components/chat/provider-logos/GeminiLogo.tsx
  - src/src/components/chat/provider-logos/CodexLogo.tsx
  - src/src/components/chat/provider-logos/ProviderHeader.tsx
  - src/src/stores/ui.ts
  - src/src/components/chat/view/ChatView.tsx
autonomous: true
requirements: [MSG-02, MSG-06, MSG-07, MSG-08, MSG-11]

must_haves:
  truths:
    - "Assistant messages show provider logo and name above markdown content"
    - "Thinking blocks show character count when collapsed and duration when available"
    - "Thinking blocks render as plain monospace italic muted text"
    - "Global thinking toggle collapses/expands all thinking blocks"
    - "Individual thinking blocks can override the global toggle"
    - "Historical assistant messages show ThinkingDisclosure for their thinking blocks"
  artifacts:
    - path: "src/src/components/chat/provider-logos/ProviderHeader.tsx"
      provides: "Provider logo + name display"
      exports: ["ProviderHeader"]
    - path: "src/src/components/chat/provider-logos/ClaudeLogo.tsx"
      provides: "16px Claude SVG logo"
      exports: ["ClaudeLogo"]
    - path: "src/src/components/chat/provider-logos/GeminiLogo.tsx"
      provides: "16px Gemini SVG logo"
      exports: ["GeminiLogo"]
    - path: "src/src/components/chat/provider-logos/CodexLogo.tsx"
      provides: "16px Codex SVG logo"
      exports: ["CodexLogo"]
    - path: "src/src/components/chat/view/ThinkingDisclosure.tsx"
      provides: "Unified streaming+historical thinking disclosure"
      exports: ["ThinkingDisclosure"]
    - path: "src/src/stores/ui.ts"
      provides: "thinkingExpanded state + toggleThinking action"
  key_links:
    - from: "src/src/components/chat/view/AssistantMessage.tsx"
      to: "ProviderHeader"
      via: "import and render above MarkdownRenderer"
      pattern: "ProviderHeader.*providerId"
    - from: "src/src/components/chat/view/AssistantMessage.tsx"
      to: "ThinkingDisclosure"
      via: "render when message.thinkingBlocks exists"
      pattern: "ThinkingDisclosure.*blocks.*isStreaming.*false"
    - from: "src/src/stores/ui.ts"
      to: "ThinkingDisclosure"
      via: "globalExpanded prop from useUIStore"
      pattern: "thinkingExpanded"
---

<objective>
Add provider identity headers to assistant messages, refactor ThinkingDisclosure for dual-mode (streaming + historical) use, add global thinking toggle, and wire thinking blocks into historical assistant messages.

Purpose: Assistant messages display provider context, thinking blocks work for both live and historical messages, and users can globally toggle thinking visibility.
Output: ProviderHeader component, refactored ThinkingDisclosure, global toggle in UI store and ChatView header.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-message-types/14-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/src/types/provider.ts:
```typescript
export type ProviderId = 'claude' | 'codex' | 'gemini';
export interface ProviderContext {
  providerId: ProviderId;
  modelId: string;
  agentName: string | null;
}
```

From src/src/types/message.ts:
```typescript
export interface ThinkingBlock {
  id: string;
  text: string;
  isComplete: boolean;
}
export interface Message {
  thinkingBlocks?: ThinkingBlock[];
  providerContext: ProviderContext;
}
```

From src/src/types/stream.ts:
```typescript
export interface ThinkingState {
  isThinking: boolean;
  blocks: ThinkingBlock[];
}
```

Current ThinkingDisclosure props (TO BE REPLACED):
```typescript
interface ThinkingDisclosureProps {
  thinkingState: ThinkingState | null;
}
```

Current AssistantMessage (NO provider header, NO ThinkingDisclosure):
```typescript
export function AssistantMessage({ message }: AssistantMessageProps) {
  // Only renders MarkdownRenderer inside MessageContainer
}
```

Current ActiveMessage uses ThinkingDisclosure:
```typescript
<ThinkingDisclosure thinkingState={thinkingState} />
```

Current useUIStore persist partialize:
```typescript
partialize: (state) => ({
  theme: state.theme,
  sidebarCollapsed: state.sidebarCollapsed,
}),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ThinkingDisclosure refactor + global toggle + provider logos</name>
  <files>
    src/src/components/chat/view/ThinkingDisclosure.tsx,
    src/src/components/chat/view/ThinkingDisclosure.test.tsx,
    src/src/components/chat/view/ActiveMessage.tsx,
    src/src/stores/ui.ts,
    src/src/components/chat/provider-logos/ClaudeLogo.tsx,
    src/src/components/chat/provider-logos/GeminiLogo.tsx,
    src/src/components/chat/provider-logos/CodexLogo.tsx,
    src/src/components/chat/provider-logos/ProviderHeader.tsx
  </files>
  <behavior>
    - ThinkingDisclosure accepts `blocks: ThinkingBlock[]` + `isStreaming: boolean` + `globalExpanded?: boolean` (replaces thinkingState prop)
    - When isStreaming=true: shows pulsing "Thinking..." label
    - When isStreaming=false: shows "Thinking (1,234 chars)" or "Thinking (1,234 chars, 3.2s)" when duration available
    - Content styled as `italic text-muted font-mono text-sm` (plain monospace, no markdown)
    - Global toggle: userToggled (per-instance) overrides globalExpanded. When globalExpanded changes, userToggled resets to null
    - useUIStore gains `thinkingExpanded: boolean` (default true) + `toggleThinking` action, persisted via partialize
    - ProviderHeader shows 16px SVG logo + provider name in muted xs text
    - ClaudeLogo/GeminiLogo/CodexLogo are 16px inline SVG React components
  </behavior>
  <action>
    1. **Create provider logo components** in `src/src/components/chat/provider-logos/`:
       - `ClaudeLogo.tsx`: Anthropic/Claude logomark as inline SVG (orange/salmon circle mark), accepts `className` prop
       - `GeminiLogo.tsx`: Google Gemini star/sparkle mark as inline SVG (blue tones), accepts `className` prop
       - `CodexLogo.tsx`: OpenAI-style mark as inline SVG (green tones), accepts `className` prop
       - `ProviderHeader.tsx`: Renders `<Logo className="size-4" />` + `<span className="text-xs text-muted">{name}</span>` in a `flex items-center gap-1.5 mb-1` container. Maps ProviderId to logo component and display name. Fallback: if unknown providerId, show just the name text.

    2. **Refactor ThinkingDisclosure props:**
       - Change from `{ thinkingState: ThinkingState | null }` to `{ blocks: ThinkingBlock[]; isStreaming: boolean; globalExpanded?: boolean }`
       - Collapsed label logic: if `isStreaming`, show pulsing "Thinking...". Otherwise:
         - Calculate `totalChars = blocks.reduce((sum, b) => sum + b.text.length, 0)`
         - Format: `Thinking (${totalChars.toLocaleString()} chars)`. Add duration if available from block timestamps (currently no timestamps, so char count only for now).
       - Content styling: each block paragraph gets `className="italic text-muted font-mono text-sm"` (JetBrains Mono from design tokens)
       - Three-state expansion: `userToggled` (per-instance, useState) takes priority over `globalExpanded` prop. When `globalExpanded` value changes, reset `userToggled` to null via a useRef tracking previous globalExpanded.
       - Default expansion: if `isStreaming`, default expanded=true (user sees live thinking). If historical, follow `globalExpanded` (default true).

    3. **Update ActiveMessage.tsx** to pass new ThinkingDisclosure props:
       - Change from `<ThinkingDisclosure thinkingState={thinkingState} />` to `<ThinkingDisclosure blocks={thinkingState?.blocks ?? []} isStreaming={thinkingState?.isThinking ?? false} globalExpanded={thinkingExpanded} />`
       - Import `useUIStore` and select `thinkingExpanded`

    4. **Update useUIStore** (`src/src/stores/ui.ts`):
       - Add `thinkingExpanded: boolean` to UIState interface (default: true)
       - Add `toggleThinking: () => void` action that flips `thinkingExpanded`
       - Add `thinkingExpanded` to INITIAL_UI_STATE
       - Add `thinkingExpanded` to `partialize` return object
       - Bump persist `version` to 2 with migration: `(state, version) => version < 2 ? { ...state, thinkingExpanded: true } : state`

    5. **Update ThinkingDisclosure tests:** Test new prop shape, collapsed label with char count, monospace styling class, global toggle behavior (globalExpanded=false collapses, individual click overrides).
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/chat/view/ThinkingDisclosure.test.tsx --reporter=verbose</automated>
  </verify>
  <done>ThinkingDisclosure works with blocks+isStreaming props. Shows char count when collapsed. Monospace italic styling. Global toggle wired in UI store. Provider logo components created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AssistantMessage provider header + historical ThinkingDisclosure + thinking toggle button</name>
  <files>
    src/src/components/chat/view/AssistantMessage.tsx,
    src/src/components/chat/view/AssistantMessage.test.tsx,
    src/src/components/chat/view/ChatView.tsx
  </files>
  <behavior>
    - AssistantMessage renders ProviderHeader above MarkdownRenderer content
    - AssistantMessage renders ThinkingDisclosure when message.thinkingBlocks has entries (isStreaming=false)
    - ThinkingDisclosure appears above the markdown content, below provider header
    - ChatView header area includes a thinking toggle button (Brain icon from Lucide)
    - Toggle button reads/writes thinkingExpanded from useUIStore
    - Historical messages with thinking blocks are visually identical to streamed messages
  </behavior>
  <action>
    1. **Update AssistantMessage.tsx:**
       - Import `ProviderHeader` from `@/components/chat/provider-logos/ProviderHeader`
       - Import `ThinkingDisclosure` from `@/components/chat/view/ThinkingDisclosure`
       - Import `useUIStore` and select `thinkingExpanded`
       - Render order inside MessageContainer: ProviderHeader (using `message.providerContext.providerId`) -> ThinkingDisclosure (if `message.thinkingBlocks?.length`) -> MarkdownRenderer
       - ThinkingDisclosure props: `blocks={message.thinkingBlocks!}`, `isStreaming={false}`, `globalExpanded={thinkingExpanded}`

    2. **Add thinking toggle button to ChatView.tsx:**
       - Import `Brain` from `lucide-react`
       - Import `useUIStore` and select `thinkingExpanded` + `toggleThinking`
       - Add a small toggle button in the header area of the chat grid. Position it as a fixed utility button near the top-right of the message area (absolute positioned within the grid, or a small bar above the message list).
       - Button: `<button onClick={toggleThinking} className="..." title={thinkingExpanded ? 'Collapse all thinking' : 'Expand all thinking'}><Brain className="size-4" /></button>`
       - Visual state: when collapsed-all, icon is muted. When expanded-all, icon is primary color.
       - Keep it minimal — a small icon button, not a toolbar.

    3. **Create AssistantMessage.test.tsx:**
       - Test that ProviderHeader renders with provider name text
       - Test that ThinkingDisclosure renders when message has thinkingBlocks
       - Test that ThinkingDisclosure does NOT render when thinkingBlocks is empty/undefined
       - Test that MarkdownRenderer still renders content correctly
       - Mock useUIStore to provide thinkingExpanded value
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/chat/view/AssistantMessage.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Assistant messages show provider logo + name header. Historical thinking blocks render in collapsed state with char count. Global toggle button in ChatView toggles all disclosures. MSG-02, MSG-11 satisfied.</done>
</task>

</tasks>

<verification>
- `cd /home/swd/loom/src && npx vitest run --reporter=verbose` — full suite green
- `cd /home/swd/loom/src && npx tsc --noEmit` — no TypeScript errors
</verification>

<success_criteria>
- Assistant messages display provider logo (16px SVG) and provider name above content
- Historical assistant messages with thinking blocks render ThinkingDisclosure
- ThinkingDisclosure shows "Thinking (N chars)" when collapsed, monospace italic content
- Global thinking toggle in ChatView header collapses/expands all thinking blocks
- Individual thinking blocks can still be toggled independently
- useUIStore persists thinkingExpanded to localStorage
</success_criteria>

<output>
After completion, create `.planning/phases/14-message-types/14-02-SUMMARY.md`
</output>
