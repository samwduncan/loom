---
phase: 42-session-discovery
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - src/src/hooks/useSessionSelection.ts
  - src/src/hooks/useSessionSelection.test.ts
  - src/src/components/sidebar/BulkActionBar.tsx
  - src/src/components/sidebar/SessionList.tsx
  - src/src/components/sidebar/SessionItem.tsx
  - src/src/components/sidebar/SessionContextMenu.tsx
  - src/src/components/sidebar/DeleteSessionDialog.tsx
  - src/src/components/sidebar/DeleteSessionDialog.test.tsx
  - src/src/components/sidebar/sidebar.css
  - src/src/components/ui/checkbox.tsx
autonomous: true
requirements:
  - SESS-07
  - SESS-08
  - SESS-09

must_haves:
  truths:
    - "Typing in sidebar search bar filters sessions by title across all projects with matches highlighted"
    - "Search forces all project groups visible (bypasses collapsed state) so no matches are hidden"
    - "User can pin a session via context menu and it appears in Pinned group at top of its project"
    - "Pin icon visible on pinned sessions in the list"
    - "User can enter selection mode via context menu Select item"
    - "Checkboxes appear on all visible sessions in selection mode"
    - "Bulk action bar shows selected count with delete and cancel buttons"
    - "Bulk delete fires parallel DELETE requests and removes sessions from both stores"
    - "Deleting the active session navigates to the most recent remaining session"
    - "Escape exits selection mode and clears selection"
  artifacts:
    - path: "src/src/hooks/useSessionSelection.ts"
      provides: "Selection state management and bulk delete logic"
      exports: ["useSessionSelection"]
    - path: "src/src/components/sidebar/BulkActionBar.tsx"
      provides: "Fixed bar showing selection count and delete/cancel actions"
      exports: ["BulkActionBar"]
    - path: "src/src/components/sidebar/SessionList.tsx"
      provides: "Updated list wiring search, pins, and selection"
      min_lines: 50
    - path: "src/src/components/sidebar/DeleteSessionDialog.tsx"
      provides: "Updated dialog supporting count > 1"
      exports: ["DeleteSessionDialog"]
  key_links:
    - from: "src/src/components/sidebar/SessionList.tsx"
      to: "src/src/hooks/useSessionSearch.ts"
      via: "useSessionSearch hook import"
      pattern: "useSessionSearch"
    - from: "src/src/components/sidebar/SessionList.tsx"
      to: "src/src/hooks/useSessionPins.ts"
      via: "useSessionPins hook import"
      pattern: "useSessionPins"
    - from: "src/src/components/sidebar/SessionList.tsx"
      to: "src/src/hooks/useSessionSelection.ts"
      via: "useSessionSelection hook import"
      pattern: "useSessionSelection"
    - from: "src/src/hooks/useSessionSelection.ts"
      to: "src/src/lib/api-client.ts"
      via: "apiFetch DELETE calls for bulk delete"
      pattern: "apiFetch.*DELETE"
    - from: "src/src/components/sidebar/SessionList.tsx"
      to: "src/src/hooks/useMultiProjectSessions.ts"
      via: "passes pinnedIds to groupSessionsByProject via useMultiProjectSessions"
      pattern: "pinnedIds"
---

<objective>
Wire search, pinning, and bulk delete into the sidebar UI. Integrate Plan 01's hooks into SessionList, add selection mode with checkboxes, bulk action bar, and update the delete dialog for multi-session support.

Purpose: Delivers all three user-facing features (SESS-07, SESS-08, SESS-09) by connecting the data layer to the component tree.
Output: Updated sidebar components with search filtering, pin management, and bulk delete capability.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-session-discovery/42-RESEARCH.md
@.planning/phases/42-session-discovery/42-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From src/src/hooks/useSessionSearch.ts:
```typescript
export function filterProjectGroups(groups: ProjectGroup[], query: string): ProjectGroup[];
export function useSessionSearch(): {
  query: string;
  setQuery: (q: string) => void;
  filterGroups: (groups: ProjectGroup[]) => ProjectGroup[];
};
```

From src/src/hooks/useSessionPins.ts:
```typescript
export function useSessionPins(): {
  pinnedIds: Set<string>;
  togglePin: (id: string) => void;
  isPinned: (id: string) => boolean;
};
```

From src/src/components/sidebar/SearchInput.tsx:
```typescript
interface SearchInputProps {
  value: string;
  onChange: (value: string) => void;
}
export function SearchInput({ value, onChange }: SearchInputProps): JSX.Element;
```

From src/src/lib/sessionGrouping.ts (updated in Plan 01):
```typescript
export function groupSessionsByProject(
  projects: ProjectSessionInput[],
  pinnedIds?: Set<string>,
): ProjectGroup[];
```

<!-- Existing interfaces needed by executor -->

From src/src/components/sidebar/SessionItem.tsx:
```typescript
export interface SessionItemProps {
  id: string;
  title: string;
  updatedAt: string;
  providerId: ProviderId;
  isActive: boolean;
  isStreaming?: boolean;
  hasDraft?: boolean;
  isEditing?: boolean;
  onClick: () => void;
  onContextMenu: (e: MouseEvent<HTMLDivElement>) => void;
  onRename: (id: string, newTitle: string) => void;
}
```

From src/src/components/sidebar/SessionContextMenu.tsx:
```typescript
interface SessionContextMenuProps {
  isOpen: boolean;
  position: { x: number; y: number };
  onRename: () => void;
  onDelete: () => void;
  onClose: () => void;
}
```

From src/src/components/sidebar/DeleteSessionDialog.tsx:
```typescript
interface DeleteSessionDialogProps {
  isOpen: boolean;
  onOpenChange: (open: boolean) => void;
  onConfirm: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Selection hook, bulk delete logic, and shadcn checkbox install</name>
  <files>
    src/src/hooks/useSessionSelection.ts,
    src/src/hooks/useSessionSelection.test.ts,
    src/src/components/sidebar/DeleteSessionDialog.tsx,
    src/src/components/sidebar/DeleteSessionDialog.test.tsx,
    src/src/components/ui/checkbox.tsx
  </files>
  <behavior>
    - useSessionSelection.toggle(id) adds ID if not selected, removes if selected
    - useSessionSelection.selectAll(ids) selects all provided IDs
    - useSessionSelection.clear() empties selection and exits selection mode
    - useSessionSelection.isSelecting starts false, becomes true on first toggle
    - useSessionSelection.bulkDelete calls DELETE for each selected session, removes from timeline store, dispatches loom:projects-updated
    - useSessionSelection.bulkDelete navigates to most recent remaining session if active session was deleted
    - useSessionSelection.bulkDelete shows toast on partial failures with failure count
    - DeleteSessionDialog with count=1 shows "Delete session?" (singular)
    - DeleteSessionDialog with count=5 shows "Delete 5 sessions?" (plural)
    - DeleteSessionDialog with count > 1 shows "This will permanently delete 5 sessions..." in description
  </behavior>
  <action>
    0. Install shadcn checkbox: `cd /home/swd/loom/src && npx shadcn@latest add checkbox --yes`. This gives us an accessible, themed checkbox for selection mode.

    1. Create `src/src/hooks/useSessionSelection.ts`:
       - State: `selectedIds: Set<string>`, `isSelecting: boolean`.
       - `toggle(id: string)`: If not selecting, set `isSelecting = true`. Toggle ID in set. Return new Set reference.
       - `selectAll(ids: string[])`: Add all IDs, set `isSelecting = true`.
       - `clear()`: Empty set, set `isSelecting = false`.
       - `bulkDelete(projectGroups: ProjectGroup[], activeSessionId: string | null, navigate: NavigateFunction, removeSession: (id: string) => void)`:
         - Build session-to-project map from projectGroups.
         - Fire `Promise.allSettled` of DELETE calls via `apiFetch` for each selected ID.
         - For each fulfilled result, call `removeSession(id)`.
         - Count rejected results. If any, `toast.error(\`Failed to delete \${count} session(s)\`)`.
         - Dispatch `window.dispatchEvent(new Event('loom:projects-updated'))` to trigger refetch.
         - If `activeSessionId` was in selectedIds, navigate to most recent remaining session or `/chat`.
         - Call `clear()` at end.
       - Export: `useSessionSelection()` returning `{ selectedIds, isSelecting, toggle, selectAll, clear, bulkDelete }`.
       - Add Escape key listener: when `isSelecting` is true, Escape calls `clear()`. Use `useEffect` with `keydown` listener.

    2. Update `src/src/components/sidebar/DeleteSessionDialog.tsx`:
       - Add optional `count?: number` prop (defaults to 1).
       - Title: `count > 1 ? \`Delete \${count} sessions?\` : 'Delete session?'`
       - Description: `count > 1 ? \`This will permanently delete \${count} sessions and all their messages. This cannot be undone.\` : 'This will permanently delete this session and all its messages. This cannot be undone.'`
       - No other changes to the component structure.

    3. Create test files:
       - `src/src/hooks/useSessionSelection.test.ts`: Test toggle, selectAll, clear, isSelecting transitions. Test bulkDelete with mocked apiFetch (mock both success and partial failure scenarios).
       - `src/src/components/sidebar/DeleteSessionDialog.test.tsx`: Render with count=1 and count=5, assert correct text.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/hooks/useSessionSelection.test.ts src/src/components/sidebar/DeleteSessionDialog.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - useSessionSelection manages selection state correctly with toggle/selectAll/clear
    - bulkDelete fires parallel DELETEs and handles partial failures with toast
    - DeleteSessionDialog displays correct singular/plural text based on count
    - shadcn Checkbox component installed
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire search, pins, selection, and bulk delete into sidebar UI</name>
  <files>
    src/src/components/sidebar/SessionList.tsx,
    src/src/components/sidebar/SessionItem.tsx,
    src/src/components/sidebar/SessionContextMenu.tsx,
    src/src/components/sidebar/BulkActionBar.tsx,
    src/src/components/sidebar/sidebar.css,
    src/src/hooks/useMultiProjectSessions.ts
  </files>
  <action>
    1. Update `src/src/hooks/useMultiProjectSessions.ts`:
       - Accept `pinnedIds?: Set<string>` in the hook or pass it through to `groupSessionsByProject`.
       - Modify the `fetchProjects` callback: pass `pinnedIds` to `groupSessionsByProject(projectInputs, pinnedIds)`.
       - Expose `pinnedIds` as part of the return type so SessionList can pass it. Alternatively, accept pinnedIds as a parameter to useMultiProjectSessions.
       - IMPORTANT: Since `pinnedIds` changes on pin toggle, and `groupSessionsByProject` is called inside `fetchProjects`, the simplest approach is to make `useMultiProjectSessions` accept `pinnedIds` and memoize the grouping. OR: apply pinning AFTER the hook returns, inside SessionList, by re-running `groupSessionsByProject`. Either approach works. Choose the one that avoids refetching from the API on pin toggle.
       - Recommended approach: DON'T modify useMultiProjectSessions. Instead, in SessionList, take the returned `projectGroups` and apply pin hoisting as a post-processing step. This avoids coupling the hook to pin state and avoids API refetches on pin toggle. Create a small `applyPins` utility or inline it.

    2. Create `src/src/components/sidebar/BulkActionBar.tsx`:
       - Props: `{ count: number; onDelete: () => void; onCancel: () => void }`.
       - Layout: flex row with "{count} selected" text on left, Trash2 icon button (status-error color) and X icon button (cancel) on right.
       - Sticky at bottom of session list area (use `border-t border-border bg-surface-raised`).
       - Accessible: `aria-label` on both buttons with count context.
       - Named export per Constitution 2.2.

    3. Update `src/src/components/sidebar/SessionContextMenu.tsx`:
       - Add `onPin?: () => void`, `isPinned?: boolean`, `onSelect?: () => void` optional props.
       - Add "Pin to top" / "Unpin" menu item (before Rename). Only render if `onPin` provided.
       - Add "Select" menu item (after Rename, before Delete). Only render if `onSelect` provided.
       - Use Pin/PinOff icons from lucide-react for the pin item.

    4. Update `src/src/components/sidebar/SessionItem.tsx`:
       - Add optional props: `searchQuery?: string`, `isPinned?: boolean`, `isSelecting?: boolean`, `isSelected?: boolean`, `onToggleSelect?: () => void`.
       - When `searchQuery` is non-empty, wrap title in `highlightMatch(title, searchQuery)` helper (inline in the file or a small local function). Use `<mark>` with `bg-accent-primary/20 text-foreground rounded-sm px-0.5` classes.
       - When `isPinned` is true, show a small Pin icon (lucide, size 12) next to the title (after streaming dot / draft dot area).
       - When `isSelecting` is true, show a shadcn Checkbox at the left side of the item (before the title). Checkbox `checked` = `isSelected`. `onCheckedChange` calls `onToggleSelect`. Click on the row itself also toggles selection (don't navigate).
       - When `isSelecting`, suppress `onClick` navigation and instead toggle selection.

    5. Update `src/src/components/sidebar/SessionList.tsx`:
       - Import and wire `useSessionSearch`, `useSessionPins`, `useSessionSelection`, `SearchInput`, `BulkActionBar`.
       - Render `<SearchInput>` between NewChatButton area and the scroll list.
       - Apply `filterGroups(projectGroups)` before rendering when search query is non-empty.
       - Apply pin hoisting: after getting projectGroups from useMultiProjectSessions, post-process each project to hoist pinned sessions. Create a small helper `hoistPinnedSessions(groups: ProjectGroup[], pinnedIds: Set<string>): ProjectGroup[]` that partitions each project's sessions and prepends a Pinned date-group.
       - CRITICAL: During active search (query non-empty), bypass `expandedProjects.has()` check -- show all matching results regardless of collapsed state. This prevents the pitfall where matches in collapsed projects are invisible.
       - Pass `searchQuery`, `isPinned`, `isSelecting`, `isSelected`, `onToggleSelect` props to each SessionItem.
       - Pass `onPin`, `isPinned`, `onSelect` to SessionContextMenu.
       - When `isSelecting` is true and `selectedIds.size > 0`, render `<BulkActionBar>` at the bottom.
       - Wire BulkActionBar's onDelete to open DeleteSessionDialog with `count={selectedIds.size}`, onConfirm calls `bulkDelete(...)`.
       - CONSTITUTION: SessionList is at 204 lines. With hooks extracted, the additions should stay manageable. If it exceeds ~220 lines, extract the rendering loop into a `ProjectGroupRenderer` child component. The hooks do the heavy lifting -- SessionList just composes.

    6. Update `sidebar.css`: Add `.session-item-pinned` style if needed for pin icon positioning. Add `.bulk-action-bar` styles if Tailwind alone doesn't suffice. Minimal additions expected.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx tsc --noEmit --pretty 2>&1 | head -30 && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - SearchInput appears in sidebar, typing filters sessions across all projects
    - Matches are highlighted with mark tags in session titles
    - Collapsed projects expand during active search to show matches
    - Pin/Unpin appears in context menu, pinned sessions show at top of project group with pin icon
    - "Select" in context menu enters selection mode with checkboxes
    - BulkActionBar shows at bottom with count and delete/cancel
    - Bulk delete fires parallel requests and removes sessions from both stores
    - Active session deletion navigates to next available session
    - Escape exits selection mode
    - TypeScript compiles, all tests pass
  </done>
</task>

</tasks>

<verification>
- TypeScript: `cd /home/swd/loom/src && npx tsc --noEmit`
- Full test suite: `cd /home/swd/loom/src && npx vitest run --reporter=verbose`
- Lint: `cd /home/swd/loom/src && npx eslint src/src/components/sidebar/ src/src/hooks/useSession*.ts src/src/lib/sessionGrouping.ts --max-warnings=0`
</verification>

<success_criteria>
- Sidebar search bar filters sessions by title substring, highlights matches, shows results from all projects (even collapsed)
- Context menu has Pin/Unpin option, pinned sessions appear in Pinned group at top of their project
- Context menu has Select option, entering selection mode shows checkboxes on all sessions
- BulkActionBar shows selected count with delete and cancel actions
- Bulk delete removes sessions via API, updates timeline store, triggers refetch
- DeleteSessionDialog shows correct singular/plural text
- Escape exits selection mode
- All tests pass, TypeScript compiles, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/42-session-discovery/42-02-SUMMARY.md`
</output>
