---
status: testing
phase: 57-ios-research
source: [57-01-SUMMARY.md]
started: 2026-03-27T02:45:00Z
updated: 2026-03-27T02:45:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Capacitor devDependencies installed correctly
expected: |
  `src/package.json` devDependencies contains @capacitor/core, @capacitor/cli, and @capacitor/ios all at ^7.6.1.
  These packages do NOT appear in the `dependencies` section (production deps unchanged).
  `npm run build` still succeeds.
awaiting: user response

## Tests

### 1. Capacitor devDependencies installed correctly
expected: `src/package.json` devDependencies contains @capacitor/core, @capacitor/cli, and @capacitor/ios all at ^7.6.1. These packages do NOT appear in the `dependencies` section. `npm run build` still succeeds.
result: [pending]

### 2. Capacitor config file is correct
expected: `src/capacitor.config.ts` exists with appId `com.loom.agent`, webDir `dist`, cleartext enabled, allowNavigation includes Tailscale IP pattern, and webContentsDebuggingEnabled set to true.
result: [pending]

### 3. iOS scaffold generated on Linux
expected: `src/ios/` directory exists with App/ subdirectory containing the Xcode project scaffold. The scaffold was generated by `cap add ios` on Linux without Xcode. `cap sync ios` output shows successful web asset copy and SPM package resolution.
result: [pending]

### 4. Assessment document covers deployment models
expected: IOS-ASSESSMENT.md contains sections for "Model A: Bundled Assets" and "Model B: Remote server.url" with pros, cons, required code changes, and effort estimates for each model.
result: [pending]

### 5. Tailscale DNS analysis documented
expected: IOS-ASSESSMENT.md contains a "Tailscale DNS from WKWebView" section with a HIGH confidence verdict, explanation of the system-wide VPN mechanism, specific caveats (ATS, MagicDNS, VPN disconnect), and a testing protocol for when a Mac becomes available.
result: [pending]

### 6. Effort estimates table exists
expected: IOS-ASSESSMENT.md contains an effort estimates table with at least 4 rows covering prototype (2-4h), local dev (1-2d), App Store minimum (2-3d), and production quality (1-2w).
result: [pending]

### 7. Existing tests unaffected
expected: All 137 test files pass (1405 tests). No production source code was changed — only config files, package metadata, and planning documents.
result: [pending]

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0

## Gaps

[none yet]
