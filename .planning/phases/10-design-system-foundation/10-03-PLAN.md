---
phase: 10-design-system-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified: []
autonomous: false
requirements:
  - DSGN-09
  - DSGN-10
  - DSGN-11
  - DSGN-12
  - DSGN-13
  - DSGN-14
  - DSGN-15

must_haves:
  truths:
    - "User confirms the charcoal base palette looks correct"
    - "User confirms the dusty rose accent appears on interactive elements"
    - "User confirms borders are subtle and premium-feeling"
    - "User confirms transitions work smoothly"
    - "User confirms the ambient gradient is atmospheric, not distracting"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete design system foundation visually. The user confirms palette, borders, transitions, effects, and ambient gradient look correct in the running app.

Purpose: Design systems are visual -- automated tests can verify token existence but not aesthetic quality. Human verification catches issues like wrong contrast, too-prominent borders, jarring transitions, or an overpowering gradient.
Output: User approval or specific feedback for iteration.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-design-system-foundation/10-CONTEXT.md
@.planning/phases/10-design-system-foundation/10-01-SUMMARY.md
@.planning/phases/10-design-system-foundation/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and dev server</name>
  <files></files>
  <action>
Run the build to verify no CSS/JS errors:
```bash
cd /home/swd/loom && npm run build
```

If the dev server is not already running, start it:
```bash
cd /home/swd/loom && npm run dev
```

Verify the app loads at http://localhost:5173 using a quick curl or by confirming the dev server output shows "ready".
  </action>
  <verify>
    <automated>cd /home/swd/loom && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds with zero errors. Dev server is running and serving the app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of design system</name>
  <files></files>
  <action>
Present the running app to the user for visual verification of all design system changes.
  </action>
  <verify>User confirms visual approval or provides specific feedback.</verify>
  <done>User approves the charcoal + dusty rose design system or provides actionable feedback.</done>
  <what-built>Complete charcoal + dusty rose design system: palette swap, surface elevation, borders, focus glow, text selection, scrollbars, ambient gradient, transition fix, streaming cursor color</what-built>
  <how-to-verify>
    1. Open http://100.86.4.57:5173 in your browser
    2. **Palette check**: The app background should be dark charcoal (#1b1a19 range) with very slight warmth -- not pure black, not obviously brown
    3. **Surface elevation**: Cards/panels should be barely lighter than the background -- subtle 2-3% lightness steps, no drop shadows for depth
    4. **Rose accent**: Buttons, links, and interactive elements should show dusty rose (#D4736C range) -- not amber/gold
    5. **Borders**: All borders should be very subtle white at ~8% opacity -- nearly invisible, premium feel. No hard visible borders
    6. **Text selection**: Select some text -- it should highlight in dusty rose at 25% opacity
    7. **Focus glow**: Tab to an input field -- it should show a soft dusty rose glow ring (not a hard outline)
    8. **Scrollbars**: Scroll a long conversation or the sidebar -- scrollbar thumbs should be subtle gray, not warm gold
    9. **Transitions**: Click buttons, hover over elements, expand/collapse panels -- transitions should work smoothly (no more blocked by transition: none)
    10. **Ambient gradient**: Look at the top of the page -- there should be a very subtle, slowly cycling color wash (barely perceptible). Watch for 30+ seconds to see it shift through rose, violet, teal, amber
    11. **Streaming cursor**: If possible, send a message and watch the streaming cursor -- it should blink in dusty rose, not amber
  </how-to-verify>
  <resume-signal>Type "approved" if the design system looks correct, or describe any issues (e.g., "borders too visible", "gradient too strong", "rose accent too orange")</resume-signal>
</task>

</tasks>

<verification>
Human visual confirmation of all design system elements.
</verification>

<success_criteria>
- User approves the charcoal + dusty rose palette
- User confirms borders, focus, selection, scrollbars, transitions, and ambient gradient all look correct
- Any issues are documented for iteration
</success_criteria>

<output>
After completion, create `.planning/phases/10-design-system-foundation/10-03-SUMMARY.md`
</output>
