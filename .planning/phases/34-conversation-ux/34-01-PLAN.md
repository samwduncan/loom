---
phase: 34-conversation-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/hooks/useAutoCollapse.ts
  - src/src/hooks/useAutoCollapse.test.ts
  - src/src/components/chat/view/CollapsibleMessage.tsx
  - src/src/components/chat/view/CollapsibleMessage.test.tsx
  - src/src/components/chat/view/MessageList.tsx
autonomous: true
requirements: [UXR-01, UXR-02]

must_haves:
  truths:
    - "Messages scrolled far above the viewport collapse to a compact summary line"
    - "Clicking a collapsed message expands it back to full content"
    - "Scrolling back to a collapsed message auto-expands it"
    - "Recent messages (last 10) never collapse"
    - "Messages do not flicker between states during fast scroll"
  artifacts:
    - path: "src/src/hooks/useAutoCollapse.ts"
      provides: "IntersectionObserver-based collapse detection hook"
      exports: ["useAutoCollapse"]
    - path: "src/src/components/chat/view/CollapsibleMessage.tsx"
      provides: "Wrapper component with collapsed summary line and expand toggle"
      exports: ["CollapsibleMessage"]
    - path: "src/src/components/chat/view/MessageList.tsx"
      provides: "Message list integration with auto-collapse"
      contains: "useAutoCollapse"
  key_links:
    - from: "src/src/hooks/useAutoCollapse.ts"
      to: "IntersectionObserver API"
      via: "per-message observer with scroll container root"
      pattern: "new IntersectionObserver"
    - from: "src/src/components/chat/view/CollapsibleMessage.tsx"
      to: "src/src/hooks/useAutoCollapse.ts"
      via: "observeRef callback for DOM observation"
      pattern: "useAutoCollapse"
    - from: "src/src/components/chat/view/MessageList.tsx"
      to: "src/src/components/chat/view/CollapsibleMessage.tsx"
      via: "wrapping each message in CollapsibleMessage"
      pattern: "CollapsibleMessage"
---

<objective>
Implement auto-collapsing conversation turns that compress old messages scrolled out of viewport into compact summary lines, and expand them on click or scroll-back.

Purpose: Long conversations (100+ messages) become unwieldy. Collapsing old turns keeps the chat readable while preserving access to history.
Output: useAutoCollapse hook, CollapsibleMessage component, MessageList integration
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-conversation-ux/34-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/src/types/message.ts:
```typescript
export type MessageRole = 'user' | 'assistant' | 'system' | 'error' | 'task_notification';

export interface Message {
  id: string;
  role: MessageRole;
  content: string;
  toolCalls?: ToolCall[];
  thinkingBlocks?: ThinkingBlock[];
  metadata: MessageMetadata;
  providerContext: ProviderContext;
}
```

From src/src/components/chat/view/MessageList.tsx:
```typescript
export interface MessageListProps {
  messages: Message[];
  sessionId: string;
  scrollContainerRef?: RefObject<HTMLDivElement | null>;
  searchQuery?: string;
  highlightText?: (text: string) => ReactNode;
  hasMore?: boolean;
  isFetchingMore?: boolean;
  onLoadMore?: () => void;
}
```

MessageList renders messages in a scrollable div (`ref={assignRef}`, `className="flex-1 overflow-y-auto"`). The scroll container ref is either `scrollContainerRef` prop or internal `internalScrollRef`. Messages are mapped inside `<div className="mx-auto max-w-3xl flex flex-col py-4">`.

MessageContainer (wraps each message) already applies `content-visibility: auto` on finalized messages. Auto-collapse is a separate concern -- it physically replaces the full message with a summary line.

ThinkingDisclosure uses CSS grid `grid-template-rows: 0fr/1fr` for smooth collapse animation -- reuse this pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useAutoCollapse hook and CollapsibleMessage component</name>
  <files>src/src/hooks/useAutoCollapse.ts, src/src/hooks/useAutoCollapse.test.ts, src/src/components/chat/view/CollapsibleMessage.tsx, src/src/components/chat/view/CollapsibleMessage.test.tsx</files>
  <behavior>
    - useAutoCollapse returns observeRef, isCollapsed, toggleExpand
    - Messages more than 10 from the bottom that leave viewport are marked collapsed
    - Messages within last 10 are never collapsed (recent protection)
    - Calling toggleExpand pins a message open (not re-collapsed by IO)
    - Calling toggleExpand again unpins and allows re-collapse
    - On unmount, all IntersectionObservers are disconnected (cleanup)
    - CollapsibleMessage shows role label ("You"/"Claude") + truncated first line (80 chars) + tool count when collapsed
    - CollapsibleMessage shows full children when expanded
    - Clicking collapsed summary calls toggleExpand
    - CSS grid 0fr/1fr transition for smooth expand/collapse (matches ThinkingDisclosure pattern)
  </behavior>
  <action>
    Create useAutoCollapse hook in `src/src/hooks/useAutoCollapse.ts`:
    - Accept `scrollContainerRef: RefObject<HTMLElement | null>`, `messageCount: number`, `collapseThreshold: number = 10`
    - Use a `Map<string, boolean>` ref for collapse state, `Set<string>` ref for pinned messages, `Map<string, IntersectionObserver>` ref for observer tracking
    - `observeRef(messageId, messageIndex)` returns a ref callback that creates an IntersectionObserver with `root: scrollContainerRef.current` and `rootMargin: '200px 0px 0px 0px'` (200px buffer above viewport). Only observe messages where `messageIndex < messageCount - collapseThreshold`.
    - When IO fires `!entry.isIntersecting` and message is not pinned, mark collapsed. When `entry.isIntersecting`, mark expanded.
    - Use a `revision` state counter to trigger re-renders when collapse map changes.
    - `isCollapsed(messageId)` reads from collapse map (depends on revision for reactivity).
    - `toggleExpand(messageId)` toggles pin state and forces expand.
    - useEffect cleanup disconnects all observers on unmount.
    - Add 300ms debounce before collapsing (not expanding) to prevent flicker during fast scroll. Use a `Map<string, number>` ref for pending collapse timeouts, clear on expand or unmount.

    Create CollapsibleMessage in `src/src/components/chat/view/CollapsibleMessage.tsx`:
    - Props: `message: Message`, `isCollapsed: boolean`, `onToggle: () => void`, `children: ReactNode`
    - When collapsed: render a button with role icon ("You" for user, "Claude" for assistant), first line of `message.content` truncated to 80 chars, tool call count if any, and a ChevronRight icon (lucide-react).
    - When expanded: render children normally.
    - Use CSS grid `grid-template-rows: 0fr/1fr` with `overflow: hidden` on inner div for smooth transition (same pattern as ThinkingDisclosure).
    - Collapsed summary styled with `text-xs text-muted hover:bg-surface-hover rounded transition-colors`.
    - Add `data-testid="collapsed-summary"` on the summary button and `data-testid="collapsible-wrapper"` on the outer div.

    Tests:
    - useAutoCollapse.test.ts: Test observeRef creates observers, isCollapsed returns correct state, toggleExpand pins/unpins, cleanup disconnects observers. Mock IntersectionObserver.
    - CollapsibleMessage.test.tsx: Test collapsed state renders summary with truncated content, expanded state renders children, click toggles, tool count shown when toolCalls present.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/hooks/useAutoCollapse.test.ts src/src/components/chat/view/CollapsibleMessage.test.tsx -x</automated>
  </verify>
  <done>useAutoCollapse hook creates per-message IntersectionObservers with debounced collapse, pinning support, and cleanup. CollapsibleMessage renders compact summary when collapsed and full content when expanded with smooth CSS grid transition. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire auto-collapse into MessageList</name>
  <files>src/src/components/chat/view/MessageList.tsx</files>
  <action>
    Modify MessageList.tsx to integrate auto-collapse:

    1. Import `useAutoCollapse` from `@/hooks/useAutoCollapse` and `CollapsibleMessage` from `@/components/chat/view/CollapsibleMessage`.

    2. Call `useAutoCollapse(scrollRef, messages.length)` inside the component to get `{ observeRef, isCollapsed, toggleExpand }`.

    3. In the messages.map() block, wrap each message's content in CollapsibleMessage:
    ```tsx
    {messages.map((msg, idx) => (
      <MessageErrorBoundary key={msg.id}>
        <div
          ref={observeRef(msg.id, idx)}
          className={cn(
            idx >= newStartIndex && 'motion-safe:animate-in motion-safe:fade-in motion-safe:slide-in-from-bottom-2 duration-200'
          )}
        >
          <CollapsibleMessage
            message={msg}
            isCollapsed={isCollapsed(msg.id)}
            onToggle={() => toggleExpand(msg.id)}
          >
            {renderMessage(msg)}
          </CollapsibleMessage>
        </div>
      </MessageErrorBoundary>
    ))}
    ```

    4. Do NOT collapse during streaming: the `collapseThreshold` of 10 already protects the most recent messages, and `ActiveMessage` is rendered separately (not in the messages array), so it's inherently safe.

    5. On session switch (sessionId change), the useAutoCollapse hook's collapse state resets naturally because the component tree remounts with new message IDs.

    Important: The `observeRef` callback must be on the outer wrapper div (not inside CollapsibleMessage) so the IntersectionObserver tracks the full message container, including when collapsed.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>MessageList wraps each message in CollapsibleMessage with auto-collapse behavior. Old messages (more than 10 from bottom) collapse when scrolled out of viewport. Clicking a collapsed message expands it. Full test suite passes with no regressions.</done>
</task>

</tasks>

<verification>
- All existing MessageList tests pass (no regression)
- useAutoCollapse tests cover: observer creation, collapse detection, pinning, cleanup
- CollapsibleMessage tests cover: collapsed summary rendering, expanded children, click toggle
- No new npm dependencies added
</verification>

<success_criteria>
- Messages scrolled far above viewport collapse to single-line summaries showing role + truncated content
- Clicking a collapsed message expands it to full content
- Scrolling back to a collapsed message auto-expands it (IO re-entry)
- Last 10 messages never collapse
- No flicker during fast scrolling (300ms debounce on collapse)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/34-conversation-ux/34-01-SUMMARY.md`
</output>
