---
phase: 62-haptics-motion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/lib/motion.ts
  - src/src/lib/motion.test.ts
  - src/src/styles/tokens.css
  - src/scripts/generate-spring-tokens.mjs
  - src/ios/App/App/Info.plist
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [MOTION-01, MOTION-02, MOTION-03]

must_haves:
  truths:
    - "Info.plist contains CADisableMinimumFrameDurationOnPhone = true for 120Hz ProMotion rendering"
    - "Info.plist is valid XML (xmllint --noout passes)"
    - "SPRING_SNAPPY damping is increased ~20% (from 20 to 24) for tighter feel"
    - "SPRING_BOUNCY damping is increased ~17% (from 12 to 14) for tighter feel"
    - "CSS linear() tokens in tokens.css match the updated spring configs (regenerated)"
    - "generate-spring-tokens.mjs header comment matches the updated damping values"
    - "SPRING_GENTLE is unchanged (stiffness: 120, damping: 14)"
    - "Duration tokens (--duration-fast, --duration-normal, etc.) are unchanged"
    - "REQUIREMENTS.md MOTION-01 text reflects actual implementation (Info.plist opt-in, not runtime detection)"
  artifacts:
    - path: "src/ios/App/App/Info.plist"
      provides: "ProMotion opt-in key"
      contains: "CADisableMinimumFrameDurationOnPhone"
    - path: "src/src/lib/motion.ts"
      provides: "Updated spring configs with increased damping"
      contains: "damping: 24"
    - path: "src/src/styles/tokens.css"
      provides: "Regenerated CSS linear() spring tokens"
      contains: "--ease-spring-snappy"
    - path: "src/scripts/generate-spring-tokens.mjs"
      provides: "Updated header comment + spring params matching motion.ts"
      contains: "damping: 24"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected MOTION-01 requirement text"
      contains: "Info.plist opt-in"
  key_links:
    - from: "src/src/lib/motion.ts"
      to: "src/scripts/generate-spring-tokens.mjs"
      via: "Spring config values must match between both files"
      pattern: "stiffness: 300.*damping: 24"
    - from: "src/scripts/generate-spring-tokens.mjs"
      to: "src/src/styles/tokens.css"
      via: "Script output pasted into CSS custom properties"
      pattern: "--ease-spring-snappy.*linear\\("
---

<objective>
Tune spring animations for ProMotion, enable 120Hz rendering via Info.plist, and correct MOTION-01 requirement text.

Purpose: MOTION-01/02/03 require that CSS animations run at 120Hz on ProMotion displays and spring configs feel snappier. The Info.plist `CADisableMinimumFrameDurationOnPhone` key unlocks 120Hz for compositor-driven CSS animations (transform, opacity, filter). Spring damping increases on SNAPPY (+20%) and BOUNCY (+17%) reduce overshoot and settling time for a tighter, more precise feel. These are physics-based curves -- more frames render the same curve more smoothly, no separate 120Hz variants needed (D-20). Also corrects MOTION-01 requirement text which incorrectly specifies runtime detection when the actual approach is Info.plist build-time opt-in (AR A-2).

Output: Info.plist with ProMotion opt-in, updated motion.ts spring configs, regenerated CSS linear() tokens, updated tests, corrected REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-haptics-motion/62-CONTEXT.md
@.planning/phases/62-haptics-motion/62-RESEARCH.md

@src/src/lib/motion.ts
@src/src/lib/motion.test.ts
@src/src/styles/tokens.css
@src/scripts/generate-spring-tokens.mjs
@src/ios/App/App/Info.plist
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Key types from motion.ts -->

From src/src/lib/motion.ts:
```typescript
export interface SpringConfig {
  stiffness: number;
  damping: number;
  mass?: number;
}

export const SPRING_GENTLE: SpringConfig = { stiffness: 120, damping: 14 };
export const SPRING_SNAPPY: SpringConfig = { stiffness: 300, damping: 20 };
export const SPRING_BOUNCY: SpringConfig = { stiffness: 180, damping: 12 };
```

From src/scripts/generate-spring-tokens.mjs:
```javascript
const springs = [
  { name: 'gentle',  mass: 1, stiffness: 120, damping: 14 },
  { name: 'snappy',  mass: 1, stiffness: 300, damping: 20 },
  { name: 'bouncy',  mass: 1, stiffness: 180, damping: 12 },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ProMotion opt-in to Info.plist and tune spring damping values</name>
  <files>src/ios/App/App/Info.plist, src/src/lib/motion.ts, src/scripts/generate-spring-tokens.mjs, src/src/styles/tokens.css</files>
  <read_first>
    - src/ios/App/App/Info.plist (current plist structure, indentation style -- uses tabs)
    - src/src/lib/motion.ts (current SPRING_SNAPPY and SPRING_BOUNCY damping values)
    - src/scripts/generate-spring-tokens.mjs (header comment with spring params, springs array)
    - src/src/styles/tokens.css lines 92-103 (current spring linear() tokens and durations)
  </read_first>
  <action>
    **Step 1: Info.plist ProMotion opt-in (D-17, D-18, MOTION-03):**

    Add the following key-value pair to `src/ios/App/App/Info.plist` inside the `<dict>` block, before the closing `</dict>` tag. Match existing indentation (tab-based). Insert after the `UIViewControllerBasedStatusBarAppearance` entry (line 49):

    ```xml
    	<key>CADisableMinimumFrameDurationOnPhone</key>
    	<true/>
    ```

    This enables Core Animation to render at 120Hz on ProMotion devices. Without this key, iOS caps WKWebView CSS compositor animations at 60Hz. Only compositor-driven properties (transform, opacity, filter) benefit -- grid-template-rows and other layout properties remain 60Hz (main-thread).

    **Validate Info.plist XML (AR B-2):**
    ```bash
    xmllint --noout src/ios/App/App/Info.plist
    ```
    If xmllint is not installed, fall back to: `python3 -c "import xml.etree.ElementTree; xml.etree.ElementTree.parse('src/ios/App/App/Info.plist')"` to verify the XML is well-formed.

    **Step 2: Update motion.ts spring configs (D-22, MOTION-02):**

    In `src/src/lib/motion.ts`, change:
    - `SPRING_SNAPPY.damping` from `20` to `24` (20% increase)
    - `SPRING_BOUNCY.damping` from `12` to `14` (~17% increase)
    - Leave `SPRING_GENTLE` unchanged (`stiffness: 120, damping: 14`)

    The resulting configs:
    ```typescript
    export const SPRING_SNAPPY: SpringConfig = {
      stiffness: 300,
      damping: 24,
    };

    export const SPRING_BOUNCY: SpringConfig = {
      stiffness: 180,
      damping: 14,
    };
    ```

    **Step 3: Update generate-spring-tokens.mjs to match (Pitfall 5):**

    In `src/scripts/generate-spring-tokens.mjs`, update:
    - The header comment (line 13): change `damping: 20` to `damping: 24` for SPRING_SNAPPY
    - The header comment (line 14): change `damping: 12` to `damping: 14` for SPRING_BOUNCY
    - The springs array (line 23): change snappy `damping: 20` to `damping: 24`
    - The springs array (line 24): change bouncy `damping: 12` to `damping: 14`

    **Step 4: Regenerate CSS spring tokens (AR A-5 -- precise instructions):**

    Run the generator script:
    ```bash
    cd /home/swd/loom/src && node scripts/generate-spring-tokens.mjs
    ```

    Capture the output. The script outputs lines in the format:
    ```
    --ease-spring-gentle: linear(...);
    --duration-spring-gentle: ...ms;
    --ease-spring-snappy: linear(...);
    --duration-spring-snappy: ...ms;
    --ease-spring-bouncy: linear(...);
    --duration-spring-bouncy: ...ms;
    ```

    In `src/src/styles/tokens.css`, locate the 6 CSS custom property lines inside the `:root { }` block that define `--ease-spring-*` and `--duration-spring-*`. These are the lines containing `--ease-spring-gentle`, `--ease-spring-snappy`, `--ease-spring-bouncy`, `--duration-spring-gentle`, `--duration-spring-snappy`, and `--duration-spring-bouncy`. Replace ONLY those 6 property lines with the regenerated output from the script. Preserve:
    - The `:root {` indentation context (2-space indent for each `--` property line)
    - The `/* --- Spring Easing Tokens ... */` comment line above them (do NOT replace the comment)
    - Any non-spring properties or duration tokens (`--duration-fast`, `--duration-normal`, etc.) below them

    Do NOT paste the script's comment header (if any) -- only the property lines. The script output may need indentation adjustment to match the existing 2-space indent.

    Duration values will change because higher damping means faster settling (shorter spring duration). This is expected and correct.
  </action>
  <verify>
    <automated>cd /home/swd/loom && xmllint --noout src/ios/App/App/Info.plist 2>&1 || python3 -c "import xml.etree.ElementTree; xml.etree.ElementTree.parse('src/ios/App/App/Info.plist'); print('XML valid')" && grep CADisableMinimumFrameDurationOnPhone src/ios/App/App/Info.plist && grep 'damping: 24' src/src/lib/motion.ts && grep 'damping: 24' src/scripts/generate-spring-tokens.mjs && grep 'ease-spring-snappy' src/src/styles/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - src/ios/App/App/Info.plist contains `<key>CADisableMinimumFrameDurationOnPhone</key>` followed by `<true/>`
    - src/ios/App/App/Info.plist passes XML validation (xmllint --noout or Python xml.etree parse)
    - src/src/lib/motion.ts SPRING_SNAPPY contains `damping: 24`
    - src/src/lib/motion.ts SPRING_BOUNCY contains `damping: 14`
    - src/src/lib/motion.ts SPRING_GENTLE still contains `damping: 14` and `stiffness: 120`
    - src/scripts/generate-spring-tokens.mjs springs array contains `damping: 24` for snappy
    - src/scripts/generate-spring-tokens.mjs springs array contains `damping: 14` for bouncy
    - src/src/styles/tokens.css contains `--ease-spring-snappy: linear(` with a regenerated value
    - src/src/styles/tokens.css still contains `--duration-fast: 100ms` (unchanged)
    - src/src/styles/tokens.css still contains `--duration-normal: 200ms` (unchanged)
  </acceptance_criteria>
  <done>Info.plist includes CADisableMinimumFrameDurationOnPhone for 120Hz ProMotion and passes XML validation. SPRING_SNAPPY damping=24, SPRING_BOUNCY damping=14. CSS linear() tokens regenerated from updated configs. Duration tokens unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update motion.test.ts for new spring damping values</name>
  <files>src/src/lib/motion.test.ts</files>
  <read_first>
    - src/src/lib/motion.test.ts (current test assertions, especially the regression test on line 88-91)
    - src/src/lib/motion.ts (updated values from Task 1)
  </read_first>
  <action>
    Update the regression test in `src/src/lib/motion.test.ts` that asserts exact spring config values.

    The test at the bottom of the file (inside "CSS spring easing tokens in tokens.css" describe block) currently asserts:
    ```typescript
    expect(SPRING_SNAPPY).toEqual({ stiffness: 300, damping: 20 });
    expect(SPRING_BOUNCY).toEqual({ stiffness: 180, damping: 12 });
    ```

    Change to:
    ```typescript
    expect(SPRING_SNAPPY).toEqual({ stiffness: 300, damping: 24 });
    expect(SPRING_BOUNCY).toEqual({ stiffness: 180, damping: 14 });
    ```

    The SPRING_GENTLE assertion stays unchanged:
    ```typescript
    expect(SPRING_GENTLE).toEqual({ stiffness: 120, damping: 14 });
    ```

    All other tests in motion.test.ts (stiffness comparisons, shape checks, easing pattern checks, duration ordering) should pass without modification since they don't assert specific damping values.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/lib/motion.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/src/lib/motion.test.ts contains `damping: 24` in the SPRING_SNAPPY assertion
    - src/src/lib/motion.test.ts contains `damping: 14` in the SPRING_BOUNCY assertion (for the toEqual line)
    - src/src/lib/motion.test.ts still contains `damping: 14` in the SPRING_GENTLE assertion
    - `cd src && npx vitest run src/src/lib/motion.test.ts -x` exits 0
    - `cd src && npx vitest run` exits 0 (full suite green)
    - `cd src && npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>motion.test.ts updated with new damping values. All tests pass including the regression assertions for SPRING_SNAPPY={stiffness:300, damping:24} and SPRING_BOUNCY={stiffness:180, damping:14}. Full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md MOTION-01 text to match implementation (AR A-2)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (find MOTION-01 line)
  </read_first>
  <action>
    MOTION-01 currently reads:
    ```
    - [ ] **MOTION-01**: Device refresh rate detected (60Hz vs 120Hz) at runtime
    ```

    This is incorrect -- D-19 specifies that runtime detection is NOT needed for CSS animations. The actual implementation is a build-time Info.plist opt-in (`CADisableMinimumFrameDurationOnPhone`) that unlocks 120Hz for the entire WKWebView. CSS compositor animations automatically render at the device's native refresh rate once opted in.

    Update to:
    ```
    - [ ] **MOTION-01**: 120Hz ProMotion rendering enabled via Info.plist opt-in (CADisableMinimumFrameDurationOnPhone) -- CSS compositor animations automatically run at device refresh rate, no runtime detection needed
    ```

    Do NOT change any other requirements. Do NOT change MOTION-01's status (leave it as `- [ ]`).
  </action>
  <verify>
    <automated>grep "MOTION-01" /home/swd/loom/.planning/REQUIREMENTS.md | head -2</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md MOTION-01 line contains "Info.plist opt-in" or "CADisableMinimumFrameDurationOnPhone"
    - REQUIREMENTS.md MOTION-01 line does NOT contain "detected" or "at runtime" as the primary description
    - REQUIREMENTS.md MOTION-01 status is still `- [ ]` (unchecked)
    - No other requirements were modified
  </acceptance_criteria>
  <done>REQUIREMENTS.md MOTION-01 text updated to accurately describe the Info.plist opt-in approach instead of runtime detection. Matches D-19 and actual implementation.</done>
</task>

</tasks>

<verification>
1. `xmllint --noout /home/swd/loom/src/ios/App/App/Info.plist` -- valid XML (AR B-2)
2. `grep CADisableMinimumFrameDurationOnPhone /home/swd/loom/src/ios/App/App/Info.plist` -- returns match
3. `cd /home/swd/loom/src && npx vitest run src/src/lib/motion.test.ts -x` -- passes
4. `cd /home/swd/loom/src && npx vitest run` -- full suite green
5. `cd /home/swd/loom/src && npx tsc --noEmit` -- no TypeScript errors
6. Spring params match across motion.ts, generate-spring-tokens.mjs, and tokens.css
7. `grep 'MOTION-01.*Info.plist' /home/swd/loom/.planning/REQUIREMENTS.md` -- corrected text
</verification>

<success_criteria>
- Info.plist includes CADisableMinimumFrameDurationOnPhone = true and passes XML validation (MOTION-03, AR B-2)
- SPRING_SNAPPY damping increased to 24, SPRING_BOUNCY damping increased to 14 (MOTION-02)
- CSS linear() tokens regenerated from updated spring configs
- generate-spring-tokens.mjs spring params match motion.ts (Pitfall 5 avoided)
- MOTION-01 satisfied: Info.plist opt-in means CSS compositor animations automatically run at device refresh rate -- no JS runtime detection needed (D-19)
- REQUIREMENTS.md MOTION-01 text corrected to match implementation (AR A-2)
- All tests pass, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/62-haptics-motion/62-02-SUMMARY.md`
</output>
