---
phase: 01-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - tailwind.config.js
  - index.html
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-04
  - DSGN-05
  - DSGN-07
  - DSGN-08

must_haves:
  truths:
    - "The app's CSS variables define a warm earthy palette with chocolate brown base, warm surfaces, cream text, and amber/copper/terracotta accents"
    - "Tailwind opacity modifiers work correctly — bg-primary/50 renders at 50% opacity (alpha-value contract fixed)"
    - "JetBrains Mono is loaded from Google Fonts and applied as the universal font at 13px"
    - "Scrollbars are thin, warm-tinted (muted gold), and match the dark theme"
    - "Status color CSS variables exist for connected, reconnecting, disconnected, and error states"
    - "Border color uses warm gold at low default opacity via the alpha-value contract"
  artifacts:
    - path: "src/index.css"
      provides: "Warm earthy CSS variable palette in :root, scrollbar utilities, typography base rules"
      contains: "--background: 10 27.3% 8.6%"
    - path: "tailwind.config.js"
      provides: "Alpha-value contract color references, status color tokens"
      contains: "<alpha-value>"
    - path: "index.html"
      provides: "JetBrains Mono font loading via Google Fonts CDN"
      contains: "JetBrains+Mono"
  key_links:
    - from: "src/index.css"
      to: "tailwind.config.js"
      via: "CSS variable names match Tailwind color config keys"
      pattern: "--background.*--foreground.*--primary"
    - from: "tailwind.config.js"
      to: "src/index.css"
      via: "hsl(var(--variable) / <alpha-value>) references bare HSL channels in :root"
      pattern: "hsl\\(var\\(--.*\\) / <alpha-value>\\)"
    - from: "index.html"
      to: "src/index.css"
      via: "Google Fonts <link> loads JetBrains Mono, body rule in index.css applies it"
      pattern: "fonts.googleapis.com.*JetBrains"
---

<objective>
Define the complete warm earthy CSS variable palette, fix the broken Tailwind alpha-value contract, load JetBrains Mono, and establish the density/typography/scrollbar foundation.

Purpose: This is THE foundational plan — every subsequent phase builds on these CSS variables, the working opacity modifiers, and the consistent typography. Without a correct alpha-value contract, no color work downstream is reliable. Without the warm palette in :root, the app still looks like generic blue/gray shadcn/ui.

Output: Three files transformed — `src/index.css` (warm palette, scrollbar, typography, transitions cleaned), `tailwind.config.js` (alpha-value fix, status colors), `index.html` (font loading, meta tags).
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-CONTEXT.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md

Key source files to read before editing:
@src/index.css
@tailwind.config.js
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CSS variable palette and clean up index.css</name>
  <files>src/index.css</files>
  <action>
Rewrite the `@layer base { :root { ... } }` block in `src/index.css` with the warm earthy palette. All values are bare HSL channels (no `hsl()` wrapper) for Tailwind's alpha-value contract.

**Replace the `:root` block with these exact values:**

```css
:root {
  /* Surface hierarchy — 3-tier depth model */
  --background: 10 27.3% 8.6%;          /* #1c1210 — base layer */
  --foreground: 33.5 63% 89.4%;         /* #f5e6d3 — cream primary text */

  --card: 18.8 23.5% 13.3%;             /* #2a1f1a — surface 1 */
  --card-foreground: 33.5 63% 89.4%;    /* #f5e6d3 — cream */

  --popover: 18.8 23.5% 13.3%;          /* #2a1f1a — surface 1 */
  --popover-foreground: 33.5 63% 89.4%; /* #f5e6d3 — cream */

  --primary: 30.6 52.7% 64.3%;          /* #d4a574 — amber accent */
  --primary-foreground: 10 27.3% 8.6%;  /* #1c1210 — dark text on amber */

  --secondary: 22.5 24.5% 19.2%;        /* #3d2e25 — surface 2 */
  --secondary-foreground: 33.5 63% 89.4%; /* #f5e6d3 — cream */

  --muted: 22.5 24.5% 19.2%;            /* #3d2e25 — surface 2 */
  --muted-foreground: 34.5 35.9% 63.9%; /* #c4a882 — muted gold */

  --accent: 21.9 45.6% 55.3%;           /* #c17f59 — copper */
  --accent-foreground: 33.5 63% 89.4%;  /* #f5e6d3 — cream */

  --destructive: 16.2 52.1% 47.5%;      /* #b85c3a — terracotta */
  --destructive-foreground: 33.5 63% 89.4%; /* #f5e6d3 — cream */

  --border: 34.5 35.9% 63.9%;           /* #c4a882 — warm gold, used at low opacity */
  --input: 18.8 23.5% 13.3%;            /* #2a1f1a — surface 1 */
  --ring: 30.6 52.7% 64.3%;             /* #d4a574 — amber focus ring */

  --radius: 0.5rem;

  /* Status colors — warm tinted (per DSGN-07) */
  --status-connected: 109.4 44.3% 54.9%;    /* #6bbf59 — warm green */
  --status-reconnecting: 30.6 52.7% 64.3%;  /* #d4a574 — amber */
  --status-disconnected: 8.1 49% 52.4%;     /* #c15a4a — warm red */
  --status-error: 16.2 52.1% 47.5%;         /* #b85c3a — terracotta */

  /* Nav design tokens — warm palette */
  --nav-glass-bg: 18.8 23.5% 13.3% / 0.7;
  --nav-glass-blur: 24px;
  --nav-glass-saturate: 1.6;
  --nav-tab-glow: 30.6 52.7% 64.3% / 0.25;
  --nav-tab-ring: 30.6 52.7% 64.3% / 0.15;
  --nav-float-shadow: 0 0% 0% / 0.35;
  --nav-float-ring: 22.5 24.5% 19.2% / 0.3;
  --nav-divider-color: 34.5 35.9% 63.9% / 0.2;
  --nav-input-bg: 22.5 24.5% 19.2% / 0.5;
  --nav-input-focus-ring: 30.6 52.7% 64.3% / 0.25;

  /* Safe area CSS variables — keep unchanged */
  --safe-area-inset-top: env(safe-area-inset-top);
  --safe-area-inset-right: env(safe-area-inset-right);
  --safe-area-inset-bottom: env(safe-area-inset-bottom);
  --safe-area-inset-left: env(safe-area-inset-left);

  /* Mobile navigation dimensions — keep unchanged */
  --mobile-nav-height: 52px;
  --mobile-nav-padding: 20px;
  --mobile-nav-total: calc(var(--mobile-nav-height) + var(--mobile-nav-padding) + env(safe-area-inset-bottom, 0px));

  /* Header safe area dimensions — keep unchanged */
  --header-safe-area-top: env(safe-area-inset-top, 0px);
  --header-base-padding: 8px;
  --header-total-padding: calc(var(--header-safe-area-top) + var(--header-base-padding));
}
```

**Delete the entire `.dark { ... }` block** (lines ~87-119 in current file). This includes the dark nav token overrides.

**Keep** the `@supports (padding-top: constant(safe-area-inset-top))` fallback block unchanged.

**Update the body rule** in `@layer base`:
```css
body {
  @apply bg-background text-foreground;
  font-family: 'JetBrains Mono', 'Fira Code', 'Cascadia Code', 'SF Mono', 'Consolas', monospace;
  font-size: 13px;
  line-height: 1.5;
  letter-spacing: -0.01em;
  margin: 0;
  padding: 0;
}
```

**Remove theme-switching transition CSS** — delete the block (lines ~188-197 in current file):
```css
/* Color transitions for theme switching - exclude interactive elements */
body, div, section, article, aside, header, footer, nav, main,
h1, h2, h3, h4, h5, h6, p, span, blockquote, ... {
  transition: background-color 200ms ease-in-out, ...
}
```
Keep the interactive element transitions (buttons, links, focus, etc.) — those are for hover/interaction, not theme switching.

**Update scrollbar utility** `.scrollbar-thin` to warm-tinted colors:
```css
.scrollbar-thin {
  scrollbar-width: thin;
  scrollbar-color: hsl(34.5 35.9% 63.9% / 0.3) transparent;
}

.scrollbar-thin::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}

.scrollbar-thin::-webkit-scrollbar-track {
  background: transparent;
}

.scrollbar-thin::-webkit-scrollbar-thumb {
  background-color: hsl(34.5 35.9% 63.9% / 0.3);
  border-radius: 3px;
}

.scrollbar-thin::-webkit-scrollbar-thumb:hover {
  background-color: hsl(34.5 35.9% 63.9% / 0.5);
}
```

**Delete ALL `.dark` scoped rules** in `@layer utilities` — the `.dark .scrollbar-thin` blocks, `.dark::-webkit-scrollbar` blocks, `.dark { scrollbar-width: thin; ... }` Firefox block, `.dark input[type="checkbox"]` blocks, `.dark input[type="radio"]` blocks.

**Update the checkbox styling** to use warm palette directly (no `.dark` prefix):
```css
input[type="checkbox"] {
  @apply accent-primary;
  opacity: 1;
}
```

**Update the global wildcard reset** — change:
```css
* {
  @apply border-border;
  box-sizing: border-box;
  transition: none;
}
```
to apply border at the correct default opacity:
```css
* {
  border-color: hsl(var(--border) / 0.15);
  box-sizing: border-box;
  transition: none;
}
```
This ensures all borders default to the warm gold at 15% opacity per DSGN-08, while the `border-border` class remains available for explicit use.
  </action>
  <verify>
    <automated>grep -c "dark" src/index.css | xargs -I{} test {} -lt 5 && grep "#1c1210\|10 27.3% 8.6%" src/index.css | head -3 && grep "JetBrains Mono" src/index.css | head -1 && echo "PASS: index.css updated"</automated>
  </verify>
  <done>
    - `:root` contains warm earthy palette with bare HSL channels
    - `.dark` block is completely removed from index.css
    - All `.dark`-scoped utility rules removed (scrollbar, checkbox, radio)
    - Theme-switching transition CSS removed
    - Scrollbar utilities use warm muted gold colors
    - Body rule uses JetBrains Mono at 13px/1.5
    - Status color variables defined
    - Global border defaults to warm gold at 15% opacity
  </done>
  <depth>
    - Nav glass tokens updated to warm palette (not still blue)
    - Safe area and mobile nav dimension variables preserved unchanged
    - Interactive transitions (button, link, focus, sidebar, modal, message, height) preserved
    - Reduced motion media query preserved
    - PWA mode CSS preserved unchanged
    - Checkbox/radio styling uses warm palette directly without .dark prefix
  </depth>
</task>

<task type="auto">
  <name>Task 2: Fix alpha-value contract in tailwind.config.js and add status colors</name>
  <files>tailwind.config.js</files>
  <action>
Rewrite `tailwind.config.js` to fix the broken alpha-value contract and add status color tokens.

**Full replacement for tailwind.config.js:**

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: {
        "2xl": "1400px",
      },
    },
    extend: {
      colors: {
        border: "hsl(var(--border) / <alpha-value>)",
        input: "hsl(var(--input) / <alpha-value>)",
        ring: "hsl(var(--ring) / <alpha-value>)",
        background: "hsl(var(--background) / <alpha-value>)",
        foreground: "hsl(var(--foreground) / <alpha-value>)",
        primary: {
          DEFAULT: "hsl(var(--primary) / <alpha-value>)",
          foreground: "hsl(var(--primary-foreground) / <alpha-value>)",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary) / <alpha-value>)",
          foreground: "hsl(var(--secondary-foreground) / <alpha-value>)",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive) / <alpha-value>)",
          foreground: "hsl(var(--destructive-foreground) / <alpha-value>)",
        },
        muted: {
          DEFAULT: "hsl(var(--muted) / <alpha-value>)",
          foreground: "hsl(var(--muted-foreground) / <alpha-value>)",
        },
        accent: {
          DEFAULT: "hsl(var(--accent) / <alpha-value>)",
          foreground: "hsl(var(--accent-foreground) / <alpha-value>)",
        },
        popover: {
          DEFAULT: "hsl(var(--popover) / <alpha-value>)",
          foreground: "hsl(var(--popover-foreground) / <alpha-value>)",
        },
        card: {
          DEFAULT: "hsl(var(--card) / <alpha-value>)",
          foreground: "hsl(var(--card-foreground) / <alpha-value>)",
        },
        // Status colors — warm tinted (DSGN-07)
        status: {
          connected: "hsl(var(--status-connected) / <alpha-value>)",
          reconnecting: "hsl(var(--status-reconnecting) / <alpha-value>)",
          disconnected: "hsl(var(--status-disconnected) / <alpha-value>)",
          error: "hsl(var(--status-error) / <alpha-value>)",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      spacing: {
        'safe-area-inset-bottom': 'env(safe-area-inset-bottom)',
        'mobile-nav': 'var(--mobile-nav-total)',
      },
    },
  },
  plugins: [require('@tailwindcss/typography')],
}
```

**Key changes:**
1. Removed `darkMode: ["class"]` — no dark mode toggle, warm palette is always active
2. Every color reference changed from `hsl(var(--x))` to `hsl(var(--x) / <alpha-value>)` — this fixes the opacity modifier bug
3. Added `status` color group referencing the new `--status-*` CSS variables
4. Preserved container, borderRadius, spacing, and plugins unchanged
  </action>
  <verify>
    <automated>grep -c "<alpha-value>" tailwind.config.js | xargs -I{} test {} -ge 20 && ! grep -q "darkMode" tailwind.config.js && grep "status-connected" tailwind.config.js && echo "PASS: tailwind config fixed"</automated>
  </verify>
  <done>
    - Every color in tailwind.config.js uses `hsl(var(--x) / <alpha-value>)` pattern
    - `darkMode: ["class"]` line removed entirely
    - Status color tokens (connected, reconnecting, disconnected, error) available via `text-status-connected`, `bg-status-error`, etc.
    - No `hsl(var(--x))` without alpha-value remains
  </done>
</task>

<task type="auto">
  <name>Task 3: Add JetBrains Mono font loading and update HTML meta tags</name>
  <files>index.html</files>
  <action>
Update `index.html` to load JetBrains Mono from Google Fonts and update meta tags for the warm theme.

**Add these tags inside `<head>`, after the favicon links and before the viewport meta tag:**

```html
<!-- Font: JetBrains Mono from Google Fonts -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:ital,wght@0,400;0,500;0,700;1,400&display=swap" rel="stylesheet">
```

**Update the `<title>` tag:**
```html
<title>Loom</title>
```

**Update the theme-color meta tags** to match the warm base color:
```html
<meta name="theme-color" content="#1c1210" />
<meta name="msapplication-TileColor" content="#1c1210" />
```

**Update the iOS status bar style** for the dark theme:
```html
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
```
(This is already set to `black-translucent` — verify it stays.)

**Update the app title meta:**
```html
<meta name="apple-mobile-web-app-title" content="Loom" />
```
  </action>
  <verify>
    <automated>grep "JetBrains" index.html && grep "1c1210" index.html && grep "<title>Loom</title>" index.html && echo "PASS: index.html updated"</automated>
  </verify>
  <done>
    - JetBrains Mono loaded via Google Fonts with preconnect for performance
    - Font weights: 400 (body), 500 (medium emphasis), 700 (bold/headings), italic 400
    - font-display: swap ensures text remains visible during font load
    - Theme color meta tags set to #1c1210 (warm base)
    - Title updated to "Loom"
    - iOS app title updated to "Loom"
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. `grep -c "<alpha-value>" tailwind.config.js` returns >= 20 (all color refs fixed)
2. `grep -c "\.dark" src/index.css` returns 0 (no .dark blocks remain)
3. `grep "JetBrains" index.html` returns the font link
4. `grep "JetBrains Mono" src/index.css` returns the body font-family
5. `grep "status-connected" tailwind.config.js` returns the status color config
6. `grep "10 27.3% 8.6%" src/index.css` returns the background variable (warm base)
7. `npm run build` succeeds (no CSS parsing errors, Tailwind processes correctly)
</verification>

<success_criteria>
- The CSS variable system defines the complete warm earthy palette as bare HSL channels
- Tailwind's alpha-value contract is fixed — opacity modifiers will work when used in components
- JetBrains Mono loads on page visit and applies to all text at 13px base size
- Scrollbars are warm-tinted muted gold
- Status colors are available as Tailwind utilities (bg-status-connected, etc.)
- The build succeeds with no CSS or Tailwind errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-01-SUMMARY.md`
</output>
