---
phase: 14-toast-overlay-system
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/sidebar/view/subcomponents/SidebarModals.tsx
  - src/components/git-panel/view/modals/ConfirmActionModal.tsx
  - src/components/git-panel/view/modals/NewBranchModal.tsx
  - src/components/sidebar/view/modals/VersionUpgradeModal.tsx
  - src/components/chat/view/subcomponents/ImageLightbox.tsx
  - src/components/file-tree/view/ImageViewer.tsx
  - src/components/LoginModal.jsx
  - src/components/code-editor/view/CodeEditor.tsx
  - src/components/code-editor/view/subcomponents/CodeEditorLoadingState.tsx
  - src/components/settings/view/Settings.tsx
  - src/components/settings/view/modals/CodexMcpFormModal.tsx
  - src/components/settings/view/modals/ClaudeMcpFormModal.tsx
  - src/components/CreateTaskModal.jsx
  - src/components/TaskDetail.jsx
  - src/components/TaskMasterSetupWizard.jsx
  - src/components/NextTaskBanner.jsx
  - src/components/PRDEditor.jsx
  - src/components/ProjectCreationWizard.jsx
autonomous: true
requirements: [TOST-04, TOST-05]

must_haves:
  truths:
    - All modal/overlay components render via OverlayPortal (not directly into their parent DOM tree)
    - All modal z-index values use the formal --z-modal or --z-critical scale
    - Modals use bg-black/60 backdrop-blur-sm for focus isolation (not glassmorphic -- per user decision)
    - No hardcoded z-[9999], z-[100], z-[110], z-[200], z-[300] remain in modal components
  artifacts:
    - All 18 modal files updated with OverlayPortal + z-index migration
  key_links:
    - OverlayPortal from Plan 01 wraps all modal content
    - Z-index scale from Plan 01 replaces all ad-hoc values
---

<objective>
Migrate all existing modal and overlay components to use OverlayPortal rendering and the formal z-index scale.

Purpose: Eliminate stacking context conflicts by ensuring all overlays render outside the React component tree, and standardize backdrop styling.
Output: All modals/overlays use portal rendering and formal z-index tiers.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-toast-overlay-system/14-RESEARCH.md
@src/components/ui/overlay-portal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate core modals to OverlayPortal + z-index scale</name>
  <files>src/components/sidebar/view/subcomponents/SidebarModals.tsx, src/components/git-panel/view/modals/ConfirmActionModal.tsx, src/components/git-panel/view/modals/NewBranchModal.tsx, src/components/sidebar/view/modals/VersionUpgradeModal.tsx, src/components/chat/view/subcomponents/ImageLightbox.tsx, src/components/file-tree/view/ImageViewer.tsx, src/components/LoginModal.jsx, src/components/settings/view/Settings.tsx, src/components/settings/view/modals/CodexMcpFormModal.tsx, src/components/settings/view/modals/ClaudeMcpFormModal.tsx</files>
  <action>
For each modal component, apply this migration pattern:

**A. SidebarModals.tsx** (already uses createPortal to document.body):
- Change `ReactDOM.createPortal(..., document.body)` calls to use `OverlayPortal` wrapper.
- Import `{ OverlayPortal }` from `'../../../ui/overlay-portal'` (adjust path as needed).
- Replace `ReactDOM.createPortal(<Component />, document.body)` with `<OverlayPortal><Component /></OverlayPortal>`.
- Remove the `ReactDOM` import if no longer needed.
- The delete confirmation modals already use `z-50` and `bg-black/60 backdrop-blur-sm` -- these are correct, just ensure they reference `z-[var(--z-modal)]`.

**B. ConfirmActionModal.tsx** (no portal currently):
- Import `{ OverlayPortal }` from `'../../../ui/overlay-portal'` (path: `../../ui/overlay-portal` relative to git-panel/view/modals/).
- Wrap the entire return JSX in `<OverlayPortal>...</OverlayPortal>`.
- Replace `z-50` with `z-[var(--z-modal)]`.
- Backdrop is already `bg-black/60 backdrop-blur-sm` -- correct, keep as-is.

**C. NewBranchModal.tsx** (no portal currently):
- Same pattern as ConfirmActionModal: wrap in OverlayPortal, replace `z-50` with `z-[var(--z-modal)]`.
- Backdrop is already `bg-black/60 backdrop-blur-sm` -- keep as-is.

**D. VersionUpgradeModal.tsx** (no portal currently):
- Wrap return in `<OverlayPortal>...</OverlayPortal>`.
- Replace `z-50` with `z-[var(--z-modal)]`.
- Standardize backdrop: change `bg-black/50 backdrop-blur-sm` to `bg-black/60 backdrop-blur-sm` for consistency.

**E. ImageLightbox.tsx** (no portal currently):
- Wrap return in `<OverlayPortal>...</OverlayPortal>`.
- Replace `z-50` with `z-[var(--z-modal)]`.
- Keep `bg-black/80 backdrop-blur-sm` (image lightbox needs darker backdrop for contrast).

**F. ImageViewer.tsx** (no portal currently):
- Wrap return in `<OverlayPortal>...</OverlayPortal>`.
- Replace `z-50` with `z-[var(--z-modal)]`.
- Change `bg-black bg-opacity-50` to `bg-black/60 backdrop-blur-sm` for consistency.

**G. LoginModal.jsx** (no portal, z-[9999]):
- Wrap return in `<OverlayPortal>...</OverlayPortal>`.
- Replace `z-[9999]` with `z-[var(--z-critical)]` (login modal IS critical -- must always be on top).
- Change `bg-black bg-opacity-50` to `bg-black/60 backdrop-blur-sm`.

**H. Settings.tsx** (no portal, z-[9999]):
- Wrap return in `<OverlayPortal>...</OverlayPortal>`.
- Replace `z-[9999]` with `z-[var(--z-modal)]` (settings is a regular modal, not critical).
- Keep `bg-background/95` backdrop (settings uses solid bg, which is fine for focus isolation).

**I. CodexMcpFormModal.tsx** (no portal, z-[110]):
- Wrap return in `<OverlayPortal>...</OverlayPortal>`.
- Replace `z-[110]` with `z-[var(--z-modal)]` (portal rendering eliminates need for higher z-index over parent Settings).
- Standardize backdrop to `bg-black/60 backdrop-blur-sm`.

**J. ClaudeMcpFormModal.tsx** (no portal, z-[110]):
- Same as CodexMcpFormModal: wrap in OverlayPortal, `z-[110]` → `z-[var(--z-modal)]`, standardize backdrop.

**Import path guidance:** The OverlayPortal is at `src/components/ui/overlay-portal.tsx`. Adjust relative imports based on each file's location in the tree.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>All 10 modal components use OverlayPortal. No z-[9999], z-[100], z-[110] remain except LoginModal which uses z-[var(--z-critical)]. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining modals and fullscreen overlays</name>
  <files>src/components/code-editor/view/CodeEditor.tsx, src/components/code-editor/view/subcomponents/CodeEditorLoadingState.tsx, src/components/CreateTaskModal.jsx, src/components/TaskDetail.jsx, src/components/TaskMasterSetupWizard.jsx, src/components/NextTaskBanner.jsx, src/components/PRDEditor.jsx, src/components/ProjectCreationWizard.jsx</files>
  <action>
Continue the portal migration for remaining modal/overlay components:

**A. CodeEditor.tsx** (fullscreen mode, z-[9999]):
- The fullscreen modal rendering uses a dynamic className with `z-[9999]`.
- Wrap the fullscreen container in `<OverlayPortal>...</OverlayPortal>` when in fullscreen/modal mode.
- Replace `z-[9999]` with `z-[var(--z-modal)]`.
- Keep the non-fullscreen (inline) rendering unchanged -- only the fullscreen overlay gets portal treatment.

**B. CodeEditorLoadingState.tsx** (z-[9999]):
- Wrap the `fixed inset-0` loading overlay in `<OverlayPortal>`.
- Replace `z-[9999]` with `z-[var(--z-modal)]`.

**C. CreateTaskModal.jsx** (z-50, no portal):
- Wrap return in `<OverlayPortal>...</OverlayPortal>`.
- Replace `z-50` with `z-[var(--z-modal)]`.
- Backdrop already uses `bg-black/50 backdrop-blur-sm` -- standardize to `bg-black/60`.

**D. TaskDetail.jsx** (z-[100], no portal):
- Wrap return in `<OverlayPortal>...</OverlayPortal>`.
- Replace `z-[100]` with `z-[var(--z-modal)]`.
- Standardize backdrop from `bg-black/50` to `bg-black/60 backdrop-blur-sm`.

**E. TaskMasterSetupWizard.jsx** (z-[100], no portal):
- Wrap return in `<OverlayPortal>...</OverlayPortal>`.
- Replace `z-[100]` with `z-[var(--z-modal)]`.
- Standardize backdrop from `bg-black/50` to `bg-black/60 backdrop-blur-sm`.

**F. NextTaskBanner.jsx** (multiple modals, z-50):
- This file has multiple modal overlays (confirm modals at lines ~271, ~376, ~569, ~581).
- Wrap each `fixed inset-0` modal container in `<OverlayPortal>`.
- Replace all `z-50` on modal containers with `z-[var(--z-modal)]`.
- Standardize backdrops to `bg-black/60 backdrop-blur-sm`.

**G. PRDEditor.jsx** (z-[200], z-[300]):
- Has multiple overlay layers: main editor (z-[200]) and nested confirm dialog (z-[300]).
- Wrap both in `<OverlayPortal>`.
- Replace `z-[200]` with `z-[var(--z-modal)]`.
- Replace `z-[300]` with `z-[var(--z-modal)]` (portal rendering eliminates need for stacking -- both are portaled independently).
- Standardize backdrops.

**H. ProjectCreationWizard.jsx** (z-[60], z-[70]):
- Main wizard at z-[60], nested confirm at z-[70].
- Wrap main wizard in `<OverlayPortal>`.
- Replace `z-[60]` with `z-[var(--z-modal)]`.
- The nested confirm dialog at z-[70] -- also wrap in `<OverlayPortal>` and use `z-[var(--z-modal)]`.
- Backdrops already use `backdrop-blur-sm` -- standardize opacity to `bg-black/60`.
- **Also apply dropdown glassmorphism** to the API key dropdown (~line 404): change `bg-surface-raised` to `bg-surface-raised/80 backdrop-blur-[16px] backdrop-saturate-[1.4]`. This avoids a file ownership conflict with Plan 03 (which handles other dropdowns).

After all changes, run typecheck to verify.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>All 8 remaining modal/overlay components use OverlayPortal. No hardcoded z-[60], z-[70], z-[100], z-[200], z-[300], z-[9999] remain anywhere in src/ (except z-[var(--z-critical)] on LoginModal and z-[var(--z-modal)] everywhere else). TypeScript compiles.</done>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` passes
- [ ] `npm run build` succeeds
- [ ] Grep for `z-\[9999\]` in src/ returns ZERO matches (all migrated to --z-critical or --z-modal)
- [ ] Grep for `z-\[100\]` in src/ returns ZERO matches
- [ ] Grep for `z-\[110\]` in src/ returns ZERO matches
- [ ] Grep for `z-\[200\]` in src/ returns ZERO matches
- [ ] Grep for `z-\[300\]` in src/ returns ZERO matches
- [ ] Grep for `OverlayPortal` finds imports in all migrated modal files
- [ ] All modals render above content without stacking context conflicts
</verification>

<success_criteria>
Every modal and overlay component in the codebase renders via OverlayPortal to #overlay-root. All z-index values use the formal CSS variable scale. No hardcoded magic z-index numbers remain on overlay elements.
</success_criteria>

<output>
After completion, create `.planning/phases/14-toast-overlay-system/14-02-SUMMARY.md`
</output>
