---
phase: 47-spring-physics-glass-surfaces
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - src/src/components/ui/dialog.tsx
  - src/src/components/ui/alert-dialog.tsx
  - src/src/components/command-palette/command-palette.css
autonomous: true
requirements: [GLASS-01, GLASS-02, GLASS-03, GLASS-04]

must_haves:
  truths:
    - "Settings modal overlay shows frosted glass effect -- background content is blurred and visible through the surface"
    - "Command palette overlay shows frosted glass effect with design token blur and saturate values"
    - "Delete/alert confirmation dialog overlay shows frosted glass effect matching settings modal"
    - "Glass effects are disabled when prefers-reduced-motion is active (handled by global override)"
  artifacts:
    - path: "src/src/components/ui/dialog.tsx"
      provides: "Frosted glass on DialogOverlay (backdrop-blur + saturate + semi-transparent bg)"
      contains: "backdrop-blur"
    - path: "src/src/components/ui/alert-dialog.tsx"
      provides: "Frosted glass on AlertDialogOverlay"
      contains: "backdrop-blur"
    - path: "src/src/components/command-palette/command-palette.css"
      provides: "Glass tokens on command palette overlay"
      contains: "glass-blur"
  key_links:
    - from: "src/src/components/ui/dialog.tsx"
      to: "src/src/styles/tokens.css"
      via: "Glass tokens --glass-blur and --glass-saturate"
      pattern: "glass-blur.*glass-saturate"
    - from: "src/src/styles/base.css"
      to: "all glass surfaces"
      via: "Global prefers-reduced-motion override kills transition-duration (backdrop-filter is static, unaffected)"
      pattern: "prefers-reduced-motion"
---

<objective>
Add frosted glass effect to all overlay surfaces: settings modal, command palette, and delete/alert confirmation dialogs. Replace opaque `bg-black/50` backgrounds with semi-transparent backgrounds plus `backdrop-filter: blur() saturate()`.

Purpose: Overlay surfaces gain depth perception -- users can see blurred content beneath the overlay, making the spatial hierarchy tangible rather than the current flat black veil.
Output: 3 files modified with glass effects. Zero new dependencies. GLASS-04 (reduced motion) requires no additional work -- the global base.css override already handles all transitions, and backdrop-filter is a static property that doesn't need motion gating.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-spring-physics-glass-surfaces/47-RESEARCH.md
@.planning/phases/47-spring-physics-glass-surfaces/47-01-SUMMARY.md

<interfaces>
<!-- Glass tokens from tokens.css (lines 163-168) -->
--glass-blur: 16px;
--glass-saturate: 1.4;
--glass-bg-opacity: 0.7;

<!-- Current DialogOverlay className (after Plan 01 spring changes) -->
"fixed inset-0 z-[var(--z-overlay)] bg-black/50 data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:animate-in data-[state=open]:fade-in-0"

<!-- Current AlertDialogOverlay className (after Plan 01 spring changes) -->
"fixed inset-0 z-[var(--z-overlay)] bg-black/50 data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:animate-in data-[state=open]:fade-in-0"

<!-- Current command-palette.css [cmdk-overlay] (after Plan 01 spring changes) -->
background: var(--backdrop-overlay);
backdrop-filter: blur(8px);
/* Plan 01 already changed animation timing to spring-gentle */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frosted glass on Dialog and AlertDialog overlays</name>
  <files>
    src/src/components/ui/dialog.tsx,
    src/src/components/ui/alert-dialog.tsx
  </files>
  <action>
**DialogOverlay (GLASS-01):**
In `dialog.tsx` `DialogOverlay` className, replace `bg-black/50` with frosted glass classes:
- Replace `bg-black/50` with `bg-[oklch(0_0_0/0.35)]` -- reduce opacity from 0.5 to 0.35 so background content bleeds through the blur
- Add `backdrop-blur-[var(--glass-blur)]` -- Tailwind arbitrary value syntax maps to `backdrop-filter: blur(16px)` and auto-generates the `-webkit-backdrop-filter` prefix
- Add `backdrop-saturate-[var(--glass-saturate)]` -- boost color saturation of blurred content for the glass effect

The final overlay className should be:
```
"fixed inset-0 z-[var(--z-overlay)] bg-[oklch(0_0_0/0.35)] backdrop-blur-[var(--glass-blur)] backdrop-saturate-[var(--glass-saturate)] data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:animate-in data-[state=open]:fade-in-0"
```

Do NOT animate the backdrop-filter values -- they are set statically. The overlay's fade-in animation handles the visual entrance; the blur is constant from the moment the overlay is visible.

**AlertDialogOverlay (GLASS-03):**
In `alert-dialog.tsx` `AlertDialogOverlay` className, apply the identical glass treatment:
- Replace `bg-black/50` with `bg-[oklch(0_0_0/0.35)]`
- Add `backdrop-blur-[var(--glass-blur)]`
- Add `backdrop-saturate-[var(--glass-saturate)]`

Both overlays are in the same visual family and must use identical glass parameters for consistency.

**GLASS-04 (reduced motion):**
No additional work needed. The global `prefers-reduced-motion` override in `base.css` (lines 72-81) sets `transition-duration: 0.01ms !important` and `animation-duration: 0.01ms !important` on all elements. `backdrop-filter` is a static visual property, not a transition/animation, so it is NOT affected by the reduced motion override. The blur effect remains visible even with reduced motion (this is correct behavior -- reduced motion gates motion, not visual effects). Verify this by confirming `backdrop-filter` does not appear in any `transition-property` or `@keyframes` rule.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>DialogOverlay and AlertDialogOverlay show frosted glass with backdrop-blur and backdrop-saturate using design tokens. Background content visible through semi-transparent blur.</done>
</task>

<task type="auto">
  <name>Task 2: Frosted glass on command palette overlay</name>
  <files>
    src/src/components/command-palette/command-palette.css
  </files>
  <action>
**Command Palette Overlay (GLASS-02):**
In `command-palette.css`, the `[cmdk-overlay]` rule currently has:
```css
background: var(--backdrop-overlay);
backdrop-filter: blur(8px);
```

Upgrade to use glass design tokens:
- Replace `background: var(--backdrop-overlay)` with `background: oklch(0 0 0 / 0.35)` -- match the Dialog/AlertDialog opacity for visual consistency across all overlays
- Replace `backdrop-filter: blur(8px)` with `backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-saturate))` -- use design tokens instead of hardcoded 8px, add saturate
- Add `-webkit-backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-saturate))` -- Safari prefix. Tailwind handles this automatically for utility classes, but raw CSS in component stylesheets needs both.

The `[cmdk-overlay]` rule should end up as:
```css
[cmdk-overlay] {
  position: fixed;
  inset: 0;
  z-index: var(--z-critical);
  background: oklch(0 0 0 / 0.35);
  backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-saturate));
  -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-saturate));
  animation: cmdk-overlay-in var(--duration-spring-gentle) var(--ease-spring-gentle);
}
```

Note: The animation timing was already changed to spring-gentle in Plan 01. This task only modifies the background and backdrop-filter properties.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>Command palette overlay uses glass tokens (--glass-blur, --glass-saturate) with Safari prefix. Visual consistency with Dialog/AlertDialog overlays.</done>
</task>

</tasks>

<verification>
1. All three overlay surfaces (Dialog, AlertDialog, command palette) use identical glass parameters: `oklch(0 0 0 / 0.35)` background, `var(--glass-blur)` blur, `var(--glass-saturate)` saturate
2. Tailwind's `backdrop-blur-*` and `backdrop-saturate-*` auto-generate `-webkit-` prefix for Dialog/AlertDialog (TSX). Command palette CSS has explicit `-webkit-backdrop-filter`.
3. `backdrop-filter` does not appear in any transition or animation -- it is always static
4. Full test suite passes: `cd /home/swd/loom/src && npx vitest run`
5. TypeScript compiles: `cd /home/swd/loom/src && npx tsc --noEmit`
</verification>

<success_criteria>
- Settings modal overlay shows frosted glass (blurred background visible through semi-transparent dark tint)
- Command palette overlay shows frosted glass with design token values (not hardcoded 8px)
- Alert/delete dialog overlay shows frosted glass matching settings modal
- All overlays use identical glass parameters (oklch 0.35 opacity, --glass-blur, --glass-saturate)
- backdrop-filter is never animated (static property only)
- Glass effect remains visible even with prefers-reduced-motion (correct -- it's not motion)
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/47-spring-physics-glass-surfaces/47-02-SUMMARY.md`
</output>
