---
phase: 47-spring-physics-glass-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/components/ui/dialog.tsx
  - src/src/components/ui/alert-dialog.tsx
  - src/src/styles/index.css
  - src/src/components/chat/tools/tool-card-shell.css
  - src/src/components/chat/tools/ToolCallGroup.css
  - src/src/components/command-palette/command-palette.css
  - src/src/components/chat/view/scroll-pill.css
autonomous: true
requirements: [SPRING-01, SPRING-02, SPRING-03, SPRING-04, SPRING-05]

must_haves:
  truths:
    - "Settings modal, delete dialogs, and alert dialogs open with visible spring overshoot (not linear or ease-out)"
    - "Sidebar expand/collapse animates with spring easing via grid-template-columns transition"
    - "Tool card and tool group expand/collapse use snappy spring easing with true overshoot"
    - "Command palette opens with spring easing on both overlay and content"
    - "Scroll-to-bottom pill entrance uses bouncy spring easing"
  artifacts:
    - path: "src/src/components/ui/dialog.tsx"
      provides: "Spring easing override on DialogContent via --tw-ease and --tw-animation-duration"
      contains: "ease-spring-gentle"
    - path: "src/src/components/ui/alert-dialog.tsx"
      provides: "Spring easing override on AlertDialogContent"
      contains: "ease-spring-gentle"
    - path: "src/src/styles/index.css"
      provides: "Grid column transition on app-shell for sidebar spring"
      contains: "grid-template-columns"
    - path: "src/src/components/chat/tools/tool-card-shell.css"
      provides: "Snappy spring on tool card expand/collapse"
      contains: "ease-spring-snappy"
    - path: "src/src/components/chat/tools/ToolCallGroup.css"
      provides: "Snappy spring on tool group accordion"
      contains: "ease-spring-snappy"
    - path: "src/src/components/command-palette/command-palette.css"
      provides: "Gentle spring on command palette open"
      contains: "ease-spring-gentle"
    - path: "src/src/components/chat/view/scroll-pill.css"
      provides: "Bouncy spring on scroll pill entrance"
      contains: "ease-spring-bouncy"
  key_links:
    - from: "src/src/components/ui/dialog.tsx"
      to: "src/src/styles/tokens.css"
      via: "CSS variable --ease-spring-gentle overriding --tw-ease"
      pattern: "--tw-ease.*--ease-spring-gentle"
    - from: "src/src/styles/index.css"
      to: "src/src/components/app-shell/AppShell.tsx"
      via: "data-testid=app-shell selector for grid transition"
      pattern: "data-testid.*app-shell"
---

<objective>
Apply CSS linear() spring easing to all key interactions: modal open, sidebar expand/collapse, tool card/group expand, command palette open, scroll pill entrance.

Purpose: Replace generic ease-out/cubic-bezier timing with true spring physics (overshoot + settle) so interactions feel physically grounded rather than flat. The spring linear() tokens were generated in Phase 44 and are ready to consume.
Output: 7 files modified with spring easing token swaps. Zero new dependencies, zero JavaScript changes -- pure CSS timing function replacements.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-spring-physics-glass-surfaces/47-RESEARCH.md
@.planning/phases/44-foundation/44-02-SUMMARY.md

<interfaces>
<!-- Spring easing tokens from tokens.css (lines 84-94) -->
--ease-spring-gentle: linear(0, 0.014, ... 64 points ...);
--duration-spring-gentle: 1000ms;
--ease-spring-snappy: linear(0, 0.024, ... 64 points ...);
--duration-spring-snappy: 833ms;
--ease-spring-bouncy: linear(0, 0.029, ... 64 points ...);
--duration-spring-bouncy: 1167ms;

<!-- tw-animate-css override mechanism -->
/* animate-in reads --tw-ease and --tw-animation-duration CSS variables */
/* Override on element to inject spring timing into existing enter keyframes */

<!-- Sidebar state CSS (index.css lines 95-112) -->
[data-sidebar-state="expanded"] { --sidebar-width: var(--sidebar-expanded-width); }
[data-sidebar-state="collapsed-hidden"] { --sidebar-width: 0px; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spring easing on modals, sidebar, and command palette</name>
  <files>
    src/src/components/ui/dialog.tsx,
    src/src/components/ui/alert-dialog.tsx,
    src/src/styles/index.css,
    src/src/components/command-palette/command-palette.css
  </files>
  <action>
**Dialog + AlertDialog (SPRING-01):**
In `dialog.tsx` `DialogContent` className, add Tailwind v4 arbitrary property overrides for tw-animate-css spring timing on the OPEN state only:
- Add `"[--tw-ease:var(--ease-spring-gentle)]"` and `"[--tw-animation-duration:var(--duration-spring-gentle)]"` to the className string
- These override the default `ease` and `.15s` that tw-animate-css uses for `animate-in`
- Do NOT modify `data-[state=closed]` animation classes -- exit stays instant per Phase 46-02 decision
- The existing `zoom-in-95` + `fade-in-0` keyframes stay unchanged -- the spring linear() easing adds visible overshoot to the scale animation automatically

In `alert-dialog.tsx` `AlertDialogContent` className, apply the identical `--tw-ease` and `--tw-animation-duration` overrides.

**Sidebar (SPRING-02):**
In `index.css`, after the sidebar state rules (after line 112), add a transition rule on `[data-testid="app-shell"]`:
```css
[data-testid="app-shell"] {
  transition: grid-template-columns var(--duration-spring-gentle) var(--ease-spring-gentle);
}
```
The sidebar grid child already has `overflow-hidden` (class on the wrapping div in AppShell.tsx line 50), so content won't spill during the transition. The sidebar component unmounts its content when collapsed -- the grid column smoothly animates width while the expand button appears at the left edge.

Do NOT add a separate `@media (prefers-reduced-motion)` rule -- the global override in base.css (lines 72-81) already sets `transition-duration: 0.01ms !important` on all elements.

**Command Palette (SPRING-04):**
In `command-palette.css`, replace the animation timing on `[cmdk-overlay]` and `[cmdk-root]`:
- `[cmdk-overlay]`: Change `var(--duration-normal) var(--ease-out)` to `var(--duration-spring-gentle) var(--ease-spring-gentle)`
- `[cmdk-root]`: Change `var(--duration-normal) var(--ease-out)` to `var(--duration-spring-gentle) var(--ease-spring-gentle)`
- The existing `@keyframes cmdk-content-in` (scale 0.95 + translateY -10px) stays unchanged -- spring easing adds overshoot to the existing keyframes
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>Dialog, AlertDialog, sidebar, and command palette all use spring easing tokens. No test regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Spring easing on tool cards, tool groups, and scroll pill</name>
  <files>
    src/src/components/chat/tools/tool-card-shell.css,
    src/src/components/chat/tools/ToolCallGroup.css,
    src/src/components/chat/view/scroll-pill.css
  </files>
  <action>
**Tool Card (SPRING-03):**
In `tool-card-shell.css` line 88, replace:
- `var(--duration-spring) var(--ease-spring)` with `var(--duration-spring-snappy) var(--ease-spring-snappy)`
This swaps the old cubic-bezier approximation for true linear() spring overshoot. The `grid-template-rows` expand/collapse transition pattern is unchanged.

**Tool Group (SPRING-03):**
In `ToolCallGroup.css` line 95, replace:
- `var(--duration-spring) var(--ease-spring)` with `var(--duration-spring-snappy) var(--ease-spring-snappy)`
Same token swap as tool card -- both are expand/collapse surfaces in the same visual family.

**Scroll Pill (SPRING-05):**
In `scroll-pill.css` lines 15-16, replace:
- `transition-duration: var(--duration-normal)` with `transition-duration: var(--duration-spring-bouncy)`
- `transition-timing-function: var(--ease-spring)` with `transition-timing-function: var(--ease-spring-bouncy)`
The bouncy profile is used here because the scroll pill is a playful micro-interaction that benefits from more pronounced overshoot.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>Tool card, tool group, and scroll pill all use profile-specific spring easing tokens (snappy for expand/collapse, bouncy for pill).</done>
</task>

</tasks>

<verification>
1. All spring token references use matched pairs (--ease-spring-X with --duration-spring-X) -- never mix profiles
2. No spring easing on exit/close animations -- enter/open only (Phase 46-02 decision)
3. Full test suite passes: `cd /home/swd/loom/src && npx vitest run`
4. TypeScript compiles clean: `cd /home/swd/loom/src && npx tsc --noEmit`
</verification>

<success_criteria>
- Dialog, AlertDialog, command palette, and sidebar use `--ease-spring-gentle` with `--duration-spring-gentle`
- Tool card and tool group use `--ease-spring-snappy` with `--duration-spring-snappy`
- Scroll pill uses `--ease-spring-bouncy` with `--duration-spring-bouncy`
- Zero new runtime dependencies
- Zero test regressions
- No exit animations modified (spring on open/enter only)
</success_criteria>

<output>
After completion, create `.planning/phases/47-spring-physics-glass-surfaces/47-01-SUMMARY.md`
</output>
