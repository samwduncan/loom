---
phase: 46-interactive-state-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/styles/base.css
  - src/src/components/ui/dialog.tsx
  - src/src/components/content-area/view/TabBar.tsx
  - src/src/components/terminal/TerminalHeader.tsx
  - src/src/components/sidebar/SessionItem.tsx
  - src/src/components/sidebar/BulkActionBar.tsx
  - src/src/components/sidebar/QuickSettingsPanel.tsx
  - src/src/components/sidebar/ProjectHeader.tsx
  - src/src/components/chat/view/CollapsibleMessage.tsx
  - src/src/components/chat/view/CodeBlock.tsx
  - src/src/components/chat/view/TokenUsage.tsx
  - src/src/components/editor/EditorTabs.tsx
  - src/src/components/sidebar/sidebar.css
  - src/src/components/file-tree/styles/file-tree.css
  - src/src/components/git/git-panel.css
  - src/src/components/chat/tools/tool-chip.css
  - src/src/components/chat/tools/tool-card-shell.css
  - src/src/components/chat/composer/composer.css
  - src/src/components/terminal/styles/terminal.css
autonomous: true
requirements:
  - INTER-01
  - INTER-02
  - INTER-03

must_haves:
  truths:
    - "Hovering any custom button, card, list item, or tab shows a consistent hover response using design tokens"
    - "Tabbing through the application shows a uniform focus ring on every focusable element"
    - "All disabled interactive elements have reduced opacity (0.5), no hover effect, and pointer-events-none"
  artifacts:
    - path: "src/src/styles/base.css"
      provides: "Canonical focus-ring and interactive-disabled CSS utility classes"
      contains: ".focus-ring:focus-visible"
    - path: "src/src/components/ui/dialog.tsx"
      provides: "Fixed close button using focus-visible instead of focus"
      contains: "focus-visible:ring"
    - path: "src/src/components/file-tree/styles/file-tree.css"
      provides: "OKLCH token hover instead of HSL fallback"
      contains: "surface-active"
    - path: "src/src/components/git/git-panel.css"
      provides: "Normalized hover, focus, disabled patterns for git buttons"
      contains: "focus-visible"
  key_links:
    - from: "src/src/styles/base.css"
      to: "all custom CSS component files"
      via: "focus-ring and interactive-disabled utility classes"
      pattern: "\\.focus-ring:focus-visible"
    - from: "shadcn button.tsx pattern"
      to: "all custom TSX components"
      via: "focus-visible:ring-2 focus-visible:ring-ring Tailwind classes"
      pattern: "focus-visible:ring-2"
---

<objective>
Standardize hover, focus, and disabled states across all custom interactive elements in the application.

Purpose: Every clickable, focusable, and disableable element should behave as if one designer touched every surface. shadcn components are already consistent -- this plan propagates those patterns to all custom components.

Output: ~18 files updated with canonical hover backgrounds, focus-visible rings, and normalized disabled states.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-interactive-state-consistency/46-RESEARCH.md

<interfaces>
<!-- Canonical patterns from shadcn to propagate -->

From src/src/components/ui/button.tsx (shadcn canonical):
- Focus: `focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50`
- Disabled: `disabled:pointer-events-none disabled:opacity-50`

From src/src/components/sidebar/NewChatButton.tsx (existing good pattern):
- Focus: `focus-visible:ring-2 focus-visible:ring-ring`

From src/src/styles/tokens.css (design tokens):
- `--surface-active: oklch(1 0 0 / 0.05)` — neutral list-item hover
- `--accent-primary-muted: oklch(0.63 0.14 20 / 0.15)` — primary action hover
- `--surface-raised: oklch(0.23 0.008 32)` — card/row hover
- `--accent-primary: oklch(0.63 0.14 20)` — focus ring color
- `--duration-fast: 100ms` — hover transition duration
- `--ease-out: cubic-bezier(0.16, 1, 0.3, 1)` — transition easing

Two canonical hover patterns:
1. Surface raise: `hover:bg-surface-raised/50` (list items, icon buttons, secondary actions)
2. Accent tint: `hover:bg-primary-muted` (primary/accent-colored actions)

Canonical focus ring (Tailwind): `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring`
Canonical focus ring (CSS): `.component:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--accent-primary); }`
Canonical disabled (Tailwind): `disabled:opacity-50 disabled:pointer-events-none`
Canonical disabled (CSS): `:disabled { opacity: 0.5; pointer-events: none; }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS utility classes + CSS-file component sweep (hover, focus, disabled)</name>
  <files>
    src/src/styles/base.css,
    src/src/components/sidebar/sidebar.css,
    src/src/components/file-tree/styles/file-tree.css,
    src/src/components/git/git-panel.css,
    src/src/components/chat/tools/tool-chip.css,
    src/src/components/chat/tools/tool-card-shell.css,
    src/src/components/chat/composer/composer.css,
    src/src/components/terminal/styles/terminal.css
  </files>
  <action>
    **Step 1: Add canonical CSS utility classes to base.css.**
    Append these after the `.skeleton-shimmer` block:

    ```css
    /* ─── Interactive State: Focus Ring (INTER-02) ─────────────────
     * Canonical focus ring for CSS-styled custom components.
     * Matches shadcn focus-visible:ring-ring pattern.
     */
    .focus-ring:focus-visible {
      outline: none;
      box-shadow: 0 0 0 2px var(--accent-primary);
    }

    /* ─── Interactive State: Disabled (INTER-03) ──────────────────
     * Canonical disabled state for CSS-styled custom components.
     * Matches shadcn disabled:opacity-50 disabled:pointer-events-none.
     */
    .interactive-disabled:disabled,
    .interactive-disabled[aria-disabled="true"] {
      opacity: 0.5;
      pointer-events: none;
    }
    ```

    **Step 2: Sweep CSS files.** For each file, apply these changes:

    **sidebar.css (SessionItem):**
    - Change SessionItem hover from `color-mix(in oklch, var(--text-muted) 5%, var(--surface-raised))` to `var(--surface-active)`. Keep existing transition.
    - Add `.session-item:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--accent-primary); }` if not already present.

    **file-tree.css (FileNode):**
    - Replace hover background `var(--color-surface-hover, hsl(var(--muted) / 0.5))` with `var(--surface-active)`. Remove the HSL fallback entirely.
    - Add `.file-node:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--accent-primary); outline-offset: -2px; }` (inset to hug content, not full row width).

    **git-panel.css:**
    - Normalize `.git-batch-btn:hover` from `bg-surface-overlay` to `var(--surface-raised)` to match other git button hovers.
    - Add `:focus-visible` rules to `.git-action-btn`, `.git-batch-btn`, `.git-branch-trigger`, `.git-branch-item`, `.git-generate-btn`, `.git-commit-btn` — all using `outline: none; box-shadow: 0 0 0 2px var(--accent-primary);`.
    - Fix disabled state: `.git-generate-btn:disabled`, `.git-commit-btn:disabled`, `.git-branch-create-confirm:disabled` — add `pointer-events: none;` alongside existing `opacity: 0.5; cursor: not-allowed;`. Remove `cursor: not-allowed` since `pointer-events: none` makes it irrelevant (shadcn pattern).
    - Ensure all `:hover` rules on buttons use `:hover:not(:disabled)` to prevent hover styles on disabled elements.

    **tool-chip.css:**
    - Add `.tool-chip:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--accent-primary); }`.
    - Keep existing border-color hover (appropriate for chips).

    **tool-card-shell.css:**
    - Add `.tool-card-header:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--accent-primary); }` for the clickable header.
    - Keep existing border-color hover.

    **composer.css:**
    - Change `.mention-chip-remove:hover { opacity: 1 }` to `.mention-chip-remove:hover { background-color: var(--surface-active); }` — opacity-only hover is inconsistent.
    - Add focus-visible to `.mention-picker-item:focus-visible` and `.slash-picker-item:focus-visible` (if items exist as distinct selectors).

    **terminal.css (TerminalHeader buttons):**
    - If terminal buttons have `disabled:opacity-40` or `opacity: 0.4` in CSS, change to `opacity: 0.5` for consistency.
    - Add focus-visible rules for terminal action buttons.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    All CSS-styled custom components have: (1) token-based hover backgrounds (no HSL fallbacks, no ad-hoc opacity), (2) focus-visible box-shadow rings using --accent-primary, (3) normalized disabled opacity at 0.5 with pointer-events:none. No existing tests break.
  </done>
</task>

<task type="auto">
  <name>Task 2: TSX component sweep (hover, focus, disabled via Tailwind classes)</name>
  <files>
    src/src/components/ui/dialog.tsx,
    src/src/components/content-area/view/TabBar.tsx,
    src/src/components/terminal/TerminalHeader.tsx,
    src/src/components/sidebar/BulkActionBar.tsx,
    src/src/components/sidebar/QuickSettingsPanel.tsx,
    src/src/components/sidebar/ProjectHeader.tsx,
    src/src/components/chat/view/CollapsibleMessage.tsx,
    src/src/components/chat/view/CodeBlock.tsx,
    src/src/components/chat/view/TokenUsage.tsx,
    src/src/components/editor/EditorTabs.tsx
  </files>
  <action>
    Sweep through all TSX components that use inline Tailwind classes for interactive elements. Apply canonical patterns:

    **Canonical focus ring (add to every interactive element missing it):**
    `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring`

    **Canonical disabled (normalize any deviations):**
    `disabled:opacity-50 disabled:pointer-events-none`

    **Per-component specifics:**

    **dialog.tsx (line 70):**
    - Change `focus:ring-2 focus:ring-ring focus:ring-offset-2 focus:outline-hidden` to `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` on the close button. This is the only shadcn file we modify -- it has a bug (`focus:` instead of `focus-visible:`).

    **TabBar.tsx:**
    - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` to each tab `<button>` className.
    - Keep existing `hover:text-foreground hover:bg-surface-raised/50` (already consistent).

    **TerminalHeader.tsx:**
    - Change any `hover:bg-[var(--surface-overlay)]` to `hover:bg-surface-raised` on terminal buttons (Restart, Disconnect).
    - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring`.
    - If `disabled:opacity-40` exists, change to `disabled:opacity-50`.

    **BulkActionBar.tsx:**
    - Fix delete button: normalize `hover:bg-[color-mix(...)]` to `hover:bg-destructive/80` or `hover:bg-status-error/20`.
    - Fix cancel button: add `hover:bg-surface-raised/50` (currently only has `hover:text-foreground`, missing background hover).
    - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` to both buttons.

    **QuickSettingsPanel.tsx:**
    - Add `hover:bg-surface-raised/50` to trigger button (currently only `hover:text-foreground`).
    - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring`.

    **ProjectHeader.tsx:**
    - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` to the expand/collapse button.

    **CollapsibleMessage.tsx:**
    - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` to the expand/collapse button.
    - Keep existing `hover:bg-surface-raised` (already consistent).

    **CodeBlock.tsx:**
    - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` to the copy button.
    - Keep existing `hover:bg-surface-raised`.

    **TokenUsage.tsx:**
    - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` to the expand/collapse button.

    **EditorTabs.tsx:**
    - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` to each tab button and close button.

    **Important:** Do NOT modify any shadcn UI component file other than dialog.tsx. All other shadcn components already have correct focus/disabled patterns.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    Every custom TSX interactive element has: (1) focus-visible ring classes (not focus:), (2) normalized disabled opacity at 0.5, (3) background-based hover (not opacity-only hover). Dialog close button uses focus-visible instead of focus. All existing tests pass.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/swd/loom/src && npx vitest run` — full test suite passes
2. `cd /home/swd/loom/src && npx tsc --noEmit` — no type errors
3. `cd /home/swd/loom/src && grep -rn "focus:ring" --include="*.tsx" src/ | grep -v "focus-visible"` — should return zero results (no bare focus:ring remaining)
4. `cd /home/swd/loom/src && grep -rn "opacity-40\|opacity: 0.4" --include="*.css" --include="*.tsx" src/ | grep -i "disabled"` — should return zero results (no 0.4 disabled opacity)
5. `cd /home/swd/loom/src && grep -rn "hsl(var" --include="*.css" src/` — should return zero results (no HSL fallbacks in hover states)
</verification>

<success_criteria>
- Zero bare `focus:ring` (only `focus-visible:ring`) across all TSX files
- Zero disabled opacity-40 values (all normalized to 0.5)
- Zero HSL fallbacks in CSS hover backgrounds
- All custom buttons, list items, and tabs have focus-visible rings
- All disabled buttons use `pointer-events: none` + `opacity: 0.5`
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/46-interactive-state-consistency/46-01-SUMMARY.md`
</output>
