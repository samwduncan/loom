---
phase: 45-loading-error-empty-states
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - src/src/components/file-tree/FileTree.tsx
  - src/src/components/git/ChangesView.tsx
  - src/src/components/git/HistoryView.tsx
  - src/src/components/sidebar/SessionList.tsx
  - src/src/components/command-palette/CommandPalette.tsx
  - src/src/components/command-palette/CommandPalette.test.tsx
  - src/src/components/command-palette/command-palette.css
  - src/src/components/chat/view/ChatView.tsx
  - src/src/components/chat/view/SearchBar.tsx
autonomous: true
requirements: [EMPTY-01, EMPTY-02, EMPTY-03, EMPTY-04]

must_haves:
  truths:
    - "File tree shows a designed empty state with icon, heading, and guidance when no files match the filter"
    - "File tree shows a designed empty state when no project is selected (or panel is visibly non-void)"
    - "Git Changes view shows a designed empty state with icon and description when there are no changes"
    - "Git History view shows a designed empty state with icon and description when there are no commits"
    - "Session list shows a designed empty state with icon, heading, guidance, and new chat button when no sessions exist"
    - "Session list search shows a designed empty state with icon and guidance when no sessions match the search query"
    - "Command palette shows a designed empty state with icon and guidance when no results match"
    - "ChatView search bar shows a designed empty state when search is active but no messages match"
  artifacts:
    - path: "src/src/components/file-tree/FileTree.tsx"
      provides: "FileTree empty state using EmptyState component"
      contains: "EmptyState"
    - path: "src/src/components/git/ChangesView.tsx"
      provides: "ChangesView empty state using EmptyState component"
      contains: "EmptyState"
    - path: "src/src/components/git/HistoryView.tsx"
      provides: "HistoryView empty state using EmptyState component"
      contains: "EmptyState"
    - path: "src/src/components/sidebar/SessionList.tsx"
      provides: "SessionList empty + search-empty states using EmptyState component"
      contains: "EmptyState"
    - path: "src/src/components/command-palette/CommandPalette.tsx"
      provides: "CommandPalette empty state using EmptyState component"
      contains: "EmptyState"
    - path: "src/src/components/chat/view/ChatView.tsx"
      provides: "ChatView search no-matches empty state using EmptyState component"
      contains: "EmptyState"
  key_links:
    - from: "src/src/components/file-tree/FileTree.tsx"
      to: "src/src/components/shared/EmptyState.tsx"
      via: "import and render"
      pattern: "import.*EmptyState.*from.*shared/EmptyState"
    - from: "src/src/components/git/ChangesView.tsx"
      to: "src/src/components/shared/EmptyState.tsx"
      via: "import and render"
      pattern: "import.*EmptyState.*from.*shared/EmptyState"
    - from: "src/src/components/sidebar/SessionList.tsx"
      to: "src/src/components/shared/EmptyState.tsx"
      via: "import and render"
      pattern: "import.*EmptyState.*from.*shared/EmptyState"
    - from: "src/src/components/chat/view/ChatView.tsx"
      to: "src/src/components/shared/EmptyState.tsx"
      via: "import and render for search no-matches"
      pattern: "import.*EmptyState.*from.*shared/EmptyState"
---

<objective>
Replace all bare-text empty states with designed EmptyState components featuring icons, headings, and contextual guidance across every data surface in the app.

Purpose: Users should never see a lonely "No changes" or "No results found" text string. Every empty surface communicates what the state means and what the user can do next, using the EmptyState primitive created in Plan 01.

Output: Eight surfaces upgraded from bare text to designed empty states: FileTree filter + no-project, ChangesView, HistoryView, SessionList (no sessions + search empty), CommandPalette, ChatView search bar.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-loading-error-empty-states/45-RESEARCH.md
@.planning/phases/45-loading-error-empty-states/45-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- these MUST exist before this plan runs -->

From src/src/components/shared/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  icon: React.ReactNode;
  heading: string;
  description?: string;
  action?: React.ReactNode;
  className?: string;
}
export function EmptyState({ icon, heading, description, action, className }: EmptyStateProps): JSX.Element;
```

<!-- Existing surfaces being modified -->

From src/src/components/file-tree/FileTree.tsx (line 98-101):
```typescript
// Current: bare text
<p className="px-2 py-3 text-center text-xs text-muted-foreground">
  No files match filter
</p>
```

From src/src/components/file-tree/FileTreePanel.tsx (line 51-53):
```typescript
// FileTreePanel always renders -- uses mount-once pattern.
// useFileTree receives projectName from useProjectContext.
// When no project is selected, projectName is empty and useFileTree
// returns empty tree with fetchState 'idle' or 'success' + empty array.
// FileTree.tsx receives this empty tree as props.
export const FileTreePanel = function FileTreePanel({ className }: FileTreePanelProps) {
  const { projectName } = useProjectContext();
  const { tree, fetchState, retry, projectRoot } = useFileTree(projectName);
```

From src/src/components/git/ChangesView.tsx (line 130-135):
```typescript
// Current: bare text in git-empty-state div
<div className="git-empty-state">
  <p className="git-empty-message">No changes</p>
</div>
```

From src/src/components/git/HistoryView.tsx (line 85-91):
```typescript
// Current: bare text in git-empty-state div
<div className="git-empty-state">
  <p className="git-empty-message">No commits yet</p>
</div>
```

From src/src/components/sidebar/SessionList.tsx (lines 191-198, 206-209):
```typescript
// No sessions:
<div className="flex flex-col items-center justify-center flex-1 px-4 py-8">
  <p className="text-muted text-[length:var(--text-body)] mb-3">No conversations yet</p>
  <NewChatButton />
</div>

// No search results:
<div className="px-3 py-8 text-center text-[length:var(--text-body)] text-muted">
  No matching sessions
</div>
```

From src/src/components/command-palette/CommandPalette.tsx (line 72):
```typescript
<Command.Empty>No results found</Command.Empty>
```

From src/src/components/chat/view/ChatView.tsx (lines 128-129, 222-228):
```typescript
// Search filter -- when active, displayMessages may be empty
const displayMessages = search.isOpen ? search.filterMessages(messages) : messages;

// SearchBar is rendered when search.isOpen
{search.isOpen && (
  <SearchBar
    query={search.query}
    onQueryChange={search.setQuery}
    onClose={search.close}
    resultCount={displayMessages.length}
  />
)}
// MessageList receives displayMessages -- when empty during search, renders nothing
```

From src/src/components/chat/view/SearchBar.tsx:
```typescript
// Shows result count but no empty state when 0 matches
interface SearchBarProps {
  query: string;
  onQueryChange: (q: string) => void;
  onClose: () => void;
  resultCount: number;
}
```

Lucide icons already available in the project:
```typescript
import { FolderOpen, GitBranch, Check, MessageSquare, Search } from 'lucide-react';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade FileTree and Git panel empty states</name>
  <files>
    src/src/components/file-tree/FileTree.tsx,
    src/src/components/git/ChangesView.tsx,
    src/src/components/git/HistoryView.tsx
  </files>
  <action>
    Import `EmptyState` from `@/components/shared/EmptyState` and relevant lucide icons in each file.

    1. **FileTree.tsx** (EMPTY-01) -- Two empty states:

       a. **No files match filter** (lines 98-101): Replace the bare `<p>No files match filter</p>` with:
       ```
       <EmptyState
         icon={<Search className="size-8" />}
         heading="No files match filter"
         description="Try adjusting your search terms"
       />
       ```
       Use `Search` icon from lucide-react.

       b. **No project selected**: FileTree receives `tree` and `fetchState` as props from FileTreePanel. When no project is selected, `tree` will be empty and `fetchState` will be `'idle'` or `'success'` with an empty array. Check: if `fetchState === 'success'` and `tree.length === 0` and there is no active filter, render a no-project empty state:
       ```
       <EmptyState
         icon={<FolderOpen className="size-8" />}
         heading="No project selected"
         description="Select a project from the sidebar to browse files"
       />
       ```
       Use `FolderOpen` icon from lucide-react. This goes in the success branch (after the loading/error checks), before the filter check. The render priority becomes: loading > error > no-tree-data-and-no-filter (no project) > has-filter-results > no-filter-results.

       **Verification step**: Before implementing, read `FileTree.tsx` to confirm props and confirm what `fetchState` value is used when tree is empty with no project. If FileTree already renders something acceptable (like a blank area because the panel itself is never shown), document that in the summary. If it renders a void, add the EmptyState.

    2. **ChangesView.tsx** (EMPTY-02 -- changes) -- Replace the bare `<div className="git-empty-state"><p>No changes</p></div>` (lines 130-135) with:
       ```
       <EmptyState
         icon={<Check className="size-8" />}
         heading="No changes"
         description="Working tree is clean"
         className="h-full"
       />
       ```
       Use `Check` icon (checkmark) from lucide-react -- communicates "all good, nothing to commit." Add `className="h-full"` to center vertically in the panel (matching the old `git-empty-state` centering).

    3. **HistoryView.tsx** (EMPTY-02 -- history) -- Replace the bare `<div className="git-empty-state"><p>No commits yet</p></div>` (lines 85-91) with:
       ```
       <EmptyState
         icon={<GitCommit className="size-8" />}
         heading="No commits yet"
         description="Make your first commit to see history here"
         className="h-full"
       />
       ```
       Use `GitCommit` icon from lucide-react (represents commit graph). Add `className="h-full"` for vertical centering.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/file-tree/FileTree.test.tsx src/src/components/git/ChangesView.test.tsx src/src/components/git/HistoryView.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    FileTree filter-empty shows EmptyState with Search icon and guidance. FileTree no-project shows EmptyState with FolderOpen icon and guidance (or documented as non-void if panel is hidden/disabled). ChangesView shows EmptyState with Check icon and "Working tree is clean" description. HistoryView shows EmptyState with GitCommit icon and guidance. All three existing test files pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade SessionList, CommandPalette, and ChatView search empty states</name>
  <files>
    src/src/components/sidebar/SessionList.tsx,
    src/src/components/command-palette/CommandPalette.tsx,
    src/src/components/command-palette/CommandPalette.test.tsx,
    src/src/components/command-palette/command-palette.css,
    src/src/components/chat/view/ChatView.tsx,
    src/src/components/chat/view/SearchBar.tsx
  </files>
  <action>
    Import `EmptyState` from `@/components/shared/EmptyState` and relevant lucide icons in each file.

    1. **SessionList.tsx** (EMPTY-03) -- Two empty states to upgrade:

       a. **No sessions** (lines 191-198): Replace with:
       ```
       <EmptyState
         icon={<MessageSquare className="size-8" />}
         heading="No conversations yet"
         description="Start a new conversation to get going"
         action={<NewChatButton />}
         className="flex-1"
       />
       ```
       The `NewChatButton` is already imported and used. Pass it as the `action` slot. Keep `className="flex-1"` so it stretches to fill the sidebar vertically.

       b. **No search results** (lines 206-209): Replace with:
       ```
       <EmptyState
         icon={<Search className="size-8" />}
         heading="No matching sessions"
         description="Try different search terms"
       />
       ```
       Import `Search` from lucide-react. Wrap in a container with appropriate padding (`px-3 py-4`).

    2. **CommandPalette.tsx** (EMPTY-04 -- command palette) -- Replace the bare `<Command.Empty>No results found</Command.Empty>` (line 72) with a styled empty state inside Command.Empty:
       ```
       <Command.Empty>
         <EmptyState
           icon={<Search className="size-8" />}
           heading="No results found"
           description="Try a different search term or command"
         />
       </Command.Empty>
       ```
       The cmdk `Command.Empty` component handles the show/hide logic (only visible when no items match). We just improve what it renders. Import `Search` from lucide-react. May need a small CSS adjustment in `command-palette.css` to ensure the empty state has enough vertical padding and the icon/text colors work against the command palette background -- check the existing `[cmdk-empty]` styles and adjust if needed (add `padding: var(--space-6) var(--space-4)` if not already present).

    3. **CommandPalette.test.tsx** (EMPTY-04 test coverage) -- Add a test case that verifies the empty state renders when no results match. The test file already exists with mocks for `useCommandSearch`. Add a test:
       ```typescript
       it('renders designed empty state when no results match search', async () => {
         useUIStore.setState({ commandPaletteOpen: true });
         renderPalette();
         const input = screen.getByPlaceholderText('Type a command or search...');
         await userEvent.type(input, 'xyznonexistent');
         // cmdk Command.Empty shows when no items match
         expect(screen.getByText('No results found')).toBeInTheDocument();
         expect(screen.getByText('Try a different search term or command')).toBeInTheDocument();
       });
       ```
       Import `userEvent` from `@testing-library/user-event` at the top of the test file.

    4. **ChatView.tsx** (EMPTY-04 -- chat search) -- Add an empty state when search is active, the user has typed a query, and no messages match. Between the SearchBar and the MessageList/skeleton conditional, add a check:
       ```
       {search.isOpen && search.debouncedQuery && displayMessages.length === 0 && !isLoadingMessages && (
         <div className="flex-1 flex items-center justify-center">
           <EmptyState
             icon={<SearchIcon className="size-8" />}
             heading="No matching messages"
             description="Try different search terms"
           />
         </div>
       )}
       ```
       Import `EmptyState` from `@/components/shared/EmptyState`. Use the `Search` icon that is already imported in ChatView.tsx (aliased if needed to avoid naming conflict -- check existing imports). Render this INSTEAD of the MessageList when the condition is true. The cleanest approach: wrap the existing `!hasSession ? ... : isLoadingMessages ? ... : <MessageList>` ternary to first check if search-empty, so the MessageList doesn't render an empty list:
       ```
       {search.isOpen && search.debouncedQuery && displayMessages.length === 0 && !isLoadingMessages ? (
         <div className="flex-1 flex items-center justify-center">
           <EmptyState icon={<Search className="size-8" />} heading="No matching messages" description="Try different search terms" />
         </div>
       ) : !hasSession ? (
         <ChatEmptyState ... />
       ) : isLoadingMessages && messages.length === 0 ? (
         <MessageListSkeleton />
       ) : (
         <MessageList ... />
       )}
       ```
       This ensures the user sees a designed empty state instead of an empty MessageList when their search matches nothing.

    5. **SearchBar.tsx** -- No structural changes needed. The SearchBar already displays `resultCount` which will show "0 matches" when no results match, and the ChatView empty state above provides the visual feedback. No modification required to SearchBar itself.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/sidebar/SessionList.test.tsx src/src/components/command-palette/CommandPalette.test.tsx src/src/components/chat/ --reporter=verbose</automated>
  </verify>
  <done>
    SessionList no-sessions state shows EmptyState with MessageSquare icon, heading, description, and NewChatButton action. SessionList search-empty shows EmptyState with Search icon and guidance. CommandPalette empty state shows EmptyState with Search icon and guidance inside Command.Empty, with test coverage in CommandPalette.test.tsx. ChatView search shows EmptyState with Search icon and "No matching messages" when search is active and no messages match. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. Every empty state renders an icon, heading, and guidance text -- no bare `<p>` text strings
2. Grep for `"No changes"` outside of test files -- should only appear inside EmptyState heading prop
3. Grep for `"No commits yet"` outside of test files -- should only appear inside EmptyState heading prop
4. Grep for `"No results found"` outside of test files -- should only appear inside EmptyState heading prop
5. ChatView search with a query that matches no messages shows EmptyState, not an empty MessageList
6. CommandPalette.test.tsx has test for designed empty state
7. Full test suite green: `cd src && npx vitest run`
8. Visual check (if desired): Each empty surface shows centered icon + text layout, not bare text
</verification>

<success_criteria>
- EMPTY-01: FileTree filter-empty state renders EmptyState with icon and guidance. FileTree no-project state is non-void (EmptyState added or documented as already handled).
- EMPTY-02: ChangesView and HistoryView render EmptyState with appropriate icons and descriptions
- EMPTY-03: SessionList no-sessions state renders EmptyState with icon, guidance, and NewChatButton action; search-empty renders EmptyState with Search icon
- EMPTY-04: CommandPalette renders EmptyState with icon and guidance inside Command.Empty (with test). ChatView search renders EmptyState when no messages match the query.
- Zero bare-text empty states remain in any data surface
</success_criteria>

<output>
After completion, create `.planning/phases/45-loading-error-empty-states/45-02-SUMMARY.md`
</output>
