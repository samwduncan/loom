---
phase: 45-loading-error-empty-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/styles/base.css
  - src/src/components/sidebar/sidebar.css
  - src/src/components/shared/Skeleton.tsx
  - src/src/components/shared/Skeleton.test.tsx
  - src/src/components/shared/InlineError.tsx
  - src/src/components/shared/InlineError.test.tsx
  - src/src/components/shared/EmptyState.tsx
  - src/src/components/shared/EmptyState.test.tsx
  - src/src/components/git/GitPanelSkeleton.tsx
  - src/src/components/git/git-panel.css
  - src/src/components/git/HistoryView.tsx
  - src/src/components/settings/SettingsTabSkeleton.tsx
  - src/src/components/file-tree/FileTree.tsx
  - src/src/components/content-area/view/ContentArea.tsx
  - src/src/hooks/useMultiProjectSessions.ts
  - src/src/components/sidebar/SessionList.tsx
autonomous: true
requirements: [LOAD-01, LOAD-02, LOAD-03]

must_haves:
  truths:
    - "Every skeleton in the app uses directional shimmer (skeleton-shimmer class), not animate-pulse or skeleton-pulse"
    - "Every component that fetches data has an error state with a visible retry button that triggers refetch"
    - "Terminal Suspense fallback renders a skeleton component matching terminal layout, not a text string"
  artifacts:
    - path: "src/src/components/shared/Skeleton.tsx"
      provides: "Reusable shimmer skeleton primitive"
      exports: ["Skeleton"]
    - path: "src/src/components/shared/InlineError.tsx"
      provides: "Fetch-error component with retry button"
      exports: ["InlineError"]
    - path: "src/src/components/shared/EmptyState.tsx"
      provides: "Reusable empty state layout component"
      exports: ["EmptyState"]
    - path: "src/src/styles/base.css"
      provides: "Global skeleton-shimmer class and @keyframes shimmer"
      contains: "skeleton-shimmer"
  key_links:
    - from: "src/src/components/git/GitPanelSkeleton.tsx"
      to: "skeleton-shimmer class"
      via: "CSS class application"
      pattern: "skeleton-shimmer"
    - from: "src/src/components/sidebar/SessionList.tsx"
      to: "InlineError"
      via: "retry button wired to refetch"
      pattern: "InlineError.*onRetry"
    - from: "src/src/components/content-area/view/ContentArea.tsx"
      to: "Skeleton"
      via: "TerminalSkeleton uses Skeleton component"
      pattern: "Skeleton"
---

<objective>
Create shared UI primitives (Skeleton, EmptyState, InlineError) and normalize all loading/error states across the app to use directional shimmer and functional retry buttons.

Purpose: Eliminate the three inconsistent skeleton animation approaches (skeleton-shimmer, animate-pulse, skeleton-pulse) and ensure every async surface has a proper error-with-retry state. This is the foundation that Plan 02 builds on for empty states.

Output: Three shared primitives in components/shared/, shimmer CSS moved to base.css, all existing skeletons normalized, all error states wired with retry.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-loading-error-empty-states/45-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From src/src/utils/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From src/src/components/ui/button.tsx:
```typescript
// shadcn Button -- already installed, use variant="outline" size="xs" for retry buttons
export const Button: React.FC<ButtonProps>;
```

From sidebar.css (lines 17-32 -- will be MOVED to base.css):
```css
@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}
.skeleton-shimmer {
  background: linear-gradient(90deg, var(--surface-raised) 25%, var(--surface-overlay) 50%, var(--surface-raised) 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s ease-in-out infinite;
  border-radius: var(--space-1);
}
```

From src/src/hooks/useMultiProjectSessions.ts (line 185):
```typescript
// Currently does NOT expose refetch -- must add it
return { projectGroups, isLoading, error, expandedProjects, toggleProject };
```

From src/src/hooks/useGitCommits.ts:
```typescript
export function useGitCommits(projectName: string): {
  commits: GitCommit[] | null;
  loading: boolean;
  error: string | null;
  refetch: () => void;  // Already has refetch
};
```

From src/src/components/sidebar/SessionListSkeleton.tsx:
```typescript
// Already uses skeleton-shimmer -- this is the reference implementation
export function SessionListSkeleton(): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared primitives and move shimmer CSS to base.css</name>
  <files>
    src/src/styles/base.css,
    src/src/components/sidebar/sidebar.css,
    src/src/components/shared/Skeleton.tsx,
    src/src/components/shared/Skeleton.test.tsx,
    src/src/components/shared/InlineError.tsx,
    src/src/components/shared/InlineError.test.tsx,
    src/src/components/shared/EmptyState.tsx,
    src/src/components/shared/EmptyState.test.tsx
  </files>
  <behavior>
    - Skeleton renders a div with className containing 'skeleton-shimmer'
    - Skeleton with variant='circle' includes 'rounded-full'
    - Skeleton with variant='block' includes 'rounded-md'
    - Skeleton sets aria-hidden="true"
    - Skeleton merges custom className via cn()
    - InlineError renders the error message text
    - InlineError renders an AlertCircle icon
    - InlineError renders a retry button when onRetry is provided
    - InlineError does NOT render a retry button when onRetry is omitted
    - InlineError retry button calls onRetry when clicked
    - EmptyState renders icon, heading, and optional description
    - EmptyState renders optional action slot
    - EmptyState does not render description when not provided
    - EmptyState does not render action when not provided
  </behavior>
  <action>
    1. Move `@keyframes shimmer` and `.skeleton-shimmer` from `sidebar.css` (lines 17-32) to `base.css` -- place after the reduced-motion media query block (after line 81). Remove those 16 lines from sidebar.css. The sidebar.css still imports fine because base.css is loaded globally.

    2. Create `src/src/components/shared/Skeleton.tsx`:
       - Props: `className?: string`, `variant?: 'line' | 'circle' | 'block'` (default 'line')
       - Renders `<div className={cn('skeleton-shimmer', variant === 'circle' && 'rounded-full', variant === 'block' && 'rounded-md', className)} aria-hidden="true" />`
       - No CSS import needed -- skeleton-shimmer is now global in base.css

    3. Create `src/src/components/shared/InlineError.tsx`:
       - Props: `message: string`, `onRetry?: () => void`, `className?: string`
       - Import `AlertCircle` from lucide-react, `Button` from `@/components/ui/button`, `cn` from `@/utils/cn`
       - Renders centered flex column: AlertCircle icon (size-5, text-destructive), message text (text-sm text-muted), optional retry Button (variant="outline" size="xs")
       - Wrapper div gets `role="alert"`

    4. Create `src/src/components/shared/EmptyState.tsx`:
       - Props: `icon: React.ReactNode`, `heading: string`, `description?: string`, `action?: React.ReactNode`, `className?: string`
       - Renders centered flex column: icon wrapper (text-muted), heading (text-sm font-medium text-[var(--text-secondary)]), optional description (mt-1 text-xs text-muted), optional action
       - Use design token CSS vars, not raw Tailwind colors

    5. Write tests for all three components covering the behaviors listed above. Use React Testing Library with screen.getByRole, getByText, queryByRole patterns.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/shared/Skeleton.test.tsx src/src/components/shared/InlineError.test.tsx src/src/components/shared/EmptyState.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    Three shared primitives (Skeleton, EmptyState, InlineError) exist with passing tests. Shimmer CSS lives in base.css globally. sidebar.css no longer defines shimmer keyframes or skeleton-shimmer class. SessionListSkeleton (which already uses skeleton-shimmer) still works because the class is now global.
  </done>
</task>

<task type="auto">
  <name>Task 2: Normalize all skeletons to shimmer and wire retry to all error states</name>
  <files>
    src/src/components/git/GitPanelSkeleton.tsx,
    src/src/components/git/git-panel.css,
    src/src/components/git/HistoryView.tsx,
    src/src/components/settings/SettingsTabSkeleton.tsx,
    src/src/components/file-tree/FileTree.tsx,
    src/src/components/content-area/view/ContentArea.tsx,
    src/src/hooks/useMultiProjectSessions.ts,
    src/src/components/sidebar/SessionList.tsx
  </files>
  <action>
    **Skeleton normalization (LOAD-01):**

    1. `GitPanelSkeleton.tsx`: Replace all `animate-pulse rounded bg-surface-raised` with `skeleton-shimmer`. Remove bg-surface-raised (shimmer handles its own background). Keep layout structure, just swap animation class. Add `role="status" aria-label="Loading git panel"` to wrapper.

    2. `git-panel.css`: Remove `@keyframes skeleton-pulse` (lines 441-444) and the `.git-skeleton-line` animation reference (line 430: `animation: skeleton-pulse 1.5s ease-in-out infinite`). Replace with `skeleton-shimmer` class usage. Update `.git-skeleton-line` to remove the animation property since the class will be applied via JSX. In `HistoryView.tsx`, add `skeleton-shimmer` class to the skeleton line divs alongside `git-skeleton-line`.

    3. `SettingsTabSkeleton.tsx`: Replace all `animate-pulse rounded-md bg-surface-raised` with `skeleton-shimmer`. Add `role="status" aria-label="Loading settings"` to wrapper.

    4. `FileTree.tsx` (lines 53-67): Replace `animate-pulse rounded-sm bg-muted` with `skeleton-shimmer`. Add `role="status" aria-label="Loading file tree"` to the skeleton wrapper div.

    5. `ContentArea.tsx` (lines 38-44): Replace the `TerminalSkeleton` text string with a proper skeleton matching terminal layout. Render 8-10 lines of varying-width `Skeleton` components (import from `@/components/shared/Skeleton`) inside a container with `p-3 space-y-2`. First line shorter (20% width) to mimic a prompt, subsequent lines 60-90% width. Add `role="status" aria-label="Loading terminal"`. (LOAD-03)

    **Error state retry wiring (LOAD-02):**

    6. `useMultiProjectSessions.ts`: Expose `fetchProjects` as `refetch` in the return value. Update the return type interface to include `refetch: () => void`. Change line 185 to: `return { projectGroups, isLoading, error, expandedProjects, toggleProject, refetch: fetchProjects };`

    7. `SessionList.tsx` (lines 182-188): Replace the bare error text div with `<InlineError message="Failed to load sessions" onRetry={refetch} />`. Destructure `refetch` from `useMultiProjectSessions()` alongside existing destructured values.

    8. `HistoryView.tsx` (lines 77-83): Replace the bare error text in git-empty-state div with `<InlineError message={error} onRetry={refetch} />`. The `refetch` is already available from `useGitCommits`.

    Verify: Run full test suite to confirm no regressions. Visually confirm no `animate-pulse` or `skeleton-pulse` references remain in skeleton components.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    All skeleton components use skeleton-shimmer (directional sweep) -- zero instances of animate-pulse or skeleton-pulse in any skeleton component. Terminal Suspense fallback renders a proper skeleton layout (not text string). SessionList error state and HistoryView error state both have InlineError with functional retry buttons wired to refetch callbacks. Full test suite passes.
  </done>
</task>

</tasks>

<verification>
1. Grep for `animate-pulse` in skeleton components -- must return zero matches (only shadcn UI internals may still use it)
2. Grep for `skeleton-pulse` -- must return zero matches anywhere
3. Grep for `"Loading terminal..."` or `"Loading editor..."` text strings in ContentArea -- must return zero matches
4. All skeleton containers have `role="status"` and `aria-label`
5. SessionList error state renders InlineError with retry button
6. HistoryView error state renders InlineError with retry button
7. Full test suite green: `cd src && npx vitest run`
</verification>

<success_criteria>
- LOAD-01: Every skeleton in the app uses the `skeleton-shimmer` directional sweep class. No `animate-pulse` in any skeleton component, no `skeleton-pulse` keyframe.
- LOAD-02: SessionList and HistoryView error states have InlineError with functional retry buttons. FileTree and GitPanel already have retry -- unchanged.
- LOAD-03: TerminalSkeleton in ContentArea renders Skeleton components matching terminal layout, not a text string.
- Three shared primitives (Skeleton, EmptyState, InlineError) exist with comprehensive tests.
</success_criteria>

<output>
After completion, create `.planning/phases/45-loading-error-empty-states/45-01-SUMMARY.md`
</output>
