---
phase: 23-file-tree-file-store
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/src/components/file-tree/FileNode.tsx
  - src/src/components/file-tree/FileTreeSearch.tsx
  - src/src/components/file-tree/FileTree.tsx
  - src/src/components/file-tree/FileTreePanel.tsx
  - src/src/components/content-area/view/ContentArea.tsx
  - src/src/components/file-tree/FileNode.test.tsx
  - src/src/components/file-tree/FileTree.test.tsx
autonomous: true
requirements:
  - FT-02
  - FT-06
  - FT-07
  - FT-08
  - FT-12
  - FT-13
  - FT-15

must_haves:
  truths:
    - "File tree renders hierarchical directory structure with visual indentation per nesting level"
    - "Clicking a file calls openFile on the store and highlights the active file"
    - "Typing in search input filters visible files/folders by case-insensitive match"
    - "Loading skeleton shown while fetch in progress; error state with retry button on failure"
    - "Dotfiles and hidden directories are filtered from the tree by default"
    - "Files tab in ContentArea shows FileTreePanel instead of PanelPlaceholder"
  artifacts:
    - path: "src/src/components/file-tree/FileNode.tsx"
      provides: "Recursive memoized tree node with indentation, icons, click handlers"
      exports: ["FileNode"]
    - path: "src/src/components/file-tree/FileTree.tsx"
      provides: "Tree container with loading/error/search states"
      exports: ["FileTree"]
    - path: "src/src/components/file-tree/FileTreeSearch.tsx"
      provides: "Search input for filtering tree"
      exports: ["FileTreeSearch"]
  key_links:
    - from: "src/src/components/file-tree/FileNode.tsx"
      to: "src/src/stores/file.ts"
      via: "useFileStore selectors (expandedDirs, activeFilePath, toggleDir, openFile)"
      pattern: "useFileStore"
    - from: "src/src/components/file-tree/FileTree.tsx"
      to: "src/src/hooks/useFileTree.ts"
      via: "useFileTree hook for data"
      pattern: "useFileTree"
    - from: "src/src/components/content-area/view/ContentArea.tsx"
      to: "src/src/components/file-tree/FileTreePanel.tsx"
      via: "import replacing PanelPlaceholder"
      pattern: "FileTreePanel"
---

<objective>
Build the recursive file tree UI with search filtering, loading/error states, and wire it into the Files tab.

Purpose: This is the core visual component -- the recursive tree that users interact with. It consumes the store and hook from Plan 01 and replaces the PanelPlaceholder in ContentArea.
Output: Fully rendered file tree with expand/collapse, active file highlighting, search filtering, loading skeleton, error retry, and hidden-file filtering. Files tab shows real content.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-file-tree-file-store/23-RESEARCH.md
@.planning/phases/23-file-tree-file-store/23-01-SUMMARY.md

@src/src/stores/file.ts
@src/src/types/file.ts
@src/src/hooks/useFileTree.ts
@src/src/components/file-tree/FileIcon.tsx
@src/src/components/file-tree/FileTreePanel.tsx
@src/src/components/file-tree/file-tree.css
@src/src/components/content-area/view/ContentArea.tsx
@src/src/components/ui/scroll-area.tsx
@src/src/components/ui/input.tsx

<interfaces>
<!-- From Plan 01 outputs -->

From src/src/types/file.ts (after Plan 01):
```typescript
export interface FileTreeNode {
  name: string;
  path: string;
  type: 'file' | 'directory';
  size: number;
  modified: string | null;
  children?: FileTreeNode[];
}

export interface FileStore = FileState & FileActions;
// FileState: expandedDirs, selectedPath, openTabs, activeFilePath
// FileActions: toggleDir, selectPath, openFile, closeFile, setDirty, setActiveFile, reset
```

From src/src/hooks/useFileTree.ts:
```typescript
export function useFileTree(projectName: string): {
  tree: FileTreeNode[];
  fetchState: 'idle' | 'loading' | 'error' | 'success';
  retry: () => void;
  projectRoot: string | null;
};
```

From src/src/components/file-tree/FileIcon.tsx:
```typescript
export function FileIcon({ name, isDirectory, isExpanded?, className? }): JSX.Element;
export function getFileIcon(name: string, isDirectory: boolean, isExpanded: boolean): React.ElementType;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FileNode recursive component + FileTreeSearch</name>
  <files>src/src/components/file-tree/FileNode.tsx, src/src/components/file-tree/FileTreeSearch.tsx, src/src/components/file-tree/FileNode.test.tsx</files>
  <behavior>
    - FileNode at depth 0 has no left padding; depth 2 has ~24px padding
    - Directory node shows chevron (ChevronRight rotated when expanded); file node has no chevron
    - Clicking a directory calls toggleDir(path) on the file store
    - Clicking a file calls openFile(path) on the file store
    - Active file node has file-node-active CSS class
    - Expanded directory renders children recursively
    - Collapsed directory does not render children
    - FileTreeSearch renders an input with placeholder "Filter files..."
    - FileTreeSearch calls onChange callback with current value on each keystroke
  </behavior>
  <action>
1. Create `src/src/components/file-tree/FileNode.tsx`:
   - Props: `{ node: FileTreeNode; depth: number; filter?: string }`
   - Wrap in `React.memo` for performance (prevent full-tree re-renders)
   - Use GRANULAR store selectors inside the component (not at parent level):
     - `const isExpanded = useFileStore(s => s.expandedDirs.includes(node.path))`
     - `const isActive = useFileStore(s => s.activeFilePath === node.path)`
     - `const toggleDir = useFileStore(s => s.toggleDir)`
     - `const openFile = useFileStore(s => s.openFile)`
   - Render a button/div row with:
     - Indentation via CSS custom property: `style={{ '--depth': depth } as React.CSSProperties}` and `file-node` CSS class
     - Chevron: `ChevronRight` from lucide-react, rotated 90deg when expanded via `transform rotate-90 transition-transform duration-100`
     - FileIcon component for the file/folder icon
     - File/folder name as text
   - Click handler: directory -> toggleDir(path), file -> openFile(path)
   - Active file: add `file-node-active` class when `isActive` is true
   - If directory and expanded, recursively render children as `<FileNode>` with depth+1
   - If `filter` prop is set, only render nodes whose name includes the filter (case-insensitive). For directories, render if any descendant matches OR the directory name itself matches. This requires a recursive `matchesFilter` helper.
   - Import `file-tree.css` at the top of this file

2. Create `src/src/components/file-tree/FileTreeSearch.tsx`:
   - Props: `{ value: string; onChange: (value: string) => void }`
   - Render shadcn `Input` with:
     - placeholder="Filter files..."
     - Search icon (lucide `Search`) as visual prefix (absolute positioned inside)
     - `data-testid="file-tree-search"`
   - Call onChange on every keystroke

3. Write tests in `src/src/components/file-tree/FileNode.test.tsx`:
   - Mock useFileStore with vi.mock or use the actual store (reset between tests)
   - Test: file node renders name and correct icon
   - Test: directory node renders chevron icon
   - Test: clicking file calls openFile with correct path
   - Test: clicking directory calls toggleDir with correct path
   - Test: active file has active CSS class
   - Test: expanded directory renders children
   - Test: collapsed directory does not render children
   - Test: indentation increases with depth (check style attribute)
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/file-tree/FileNode.test.tsx --reporter=verbose</automated>
  </verify>
  <done>FileNode renders with indentation, icons, expand/collapse, active highlighting, and filter support. FileTreeSearch renders filter input. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FileTree container + wire into ContentArea</name>
  <files>src/src/components/file-tree/FileTree.tsx, src/src/components/file-tree/FileTree.test.tsx, src/src/components/file-tree/FileTreePanel.tsx, src/src/components/content-area/view/ContentArea.tsx</files>
  <behavior>
    - FileTree shows loading skeleton (animate-pulse blocks) when fetchState is 'loading'
    - FileTree shows error message with Retry button when fetchState is 'error'
    - FileTree renders FileNode for each top-level entry when fetchState is 'success'
    - Dotfiles (names starting with '.') are filtered out by default
    - Search input filters visible nodes by substring match
    - Empty search result shows "No files match filter"
    - ContentArea renders FileTreePanel instead of PanelPlaceholder for files tab
    - Refresh button in tree header triggers retry
  </behavior>
  <action>
1. Create `src/src/components/file-tree/FileTree.tsx`:
   - Uses `useProjectContext` to get projectName, then `useFileTree(projectName)` for data
   - State: `filter: string` (controlled by FileTreeSearch)
   - State: `showHidden: boolean` (default false) -- filters nodes whose name starts with '.'
   - Render logic based on fetchState:
     - `'idle'` or `'loading'`: render loading skeleton -- 8-10 rows of `animate-pulse` divs with varying widths (similar to SettingsTabSkeleton pattern). Alternate between "folder-like" (wider) and "file-like" (narrower) blocks with increasing indent to simulate tree structure
     - `'error'`: render error message "Failed to load file tree" with a `Button` (shadcn) "Retry" that calls retry()
     - `'success'`: filter tree (remove dotfiles if !showHidden), then render FileNode for each top-level entry at depth=0 with filter prop
   - Wrap tree nodes in shadcn `ScrollArea` for consistent scrollbar styling
   - If filter is active and no nodes match, show "No files match filter" text
   - Header area at top: "Files" label + RefreshCw icon button that calls retry()

2. Update `src/src/components/file-tree/FileTreePanel.tsx`:
   - Replace the placeholder "File tree loading..." in the left sidebar with `<FileTree />`
   - Keep the right side editor placeholder as-is ("Select a file to view")
   - Import FileTreeSearch into the header area above FileTree

3. Update `src/src/components/content-area/view/ContentArea.tsx`:
   - Import `FileTreePanel` from `@/components/file-tree/FileTreePanel`
   - Replace `<PanelPlaceholder name="Files" icon={FolderTree} />` with `<FileTreePanel />`
   - Remove the FolderTree import from lucide-react if no longer used

4. Write tests in `src/src/components/file-tree/FileTree.test.tsx`:
   - Mock useFileTree hook and useProjectContext
   - Test: loading state shows skeleton elements
   - Test: error state shows error message and retry button
   - Test: retry button calls retry function
   - Test: success state renders tree nodes
   - Test: dotfiles are hidden by default (node named ".env" not rendered)
   - Test: search input filters visible nodes
   - Test: empty filter result shows "No files match filter"
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/file-tree/FileTree.test.tsx --reporter=verbose</automated>
  </verify>
  <done>FileTree renders loading/error/success states correctly. Dotfiles hidden by default. Search filtering works. ContentArea shows FileTreePanel for Files tab. All tests pass including existing suite.</done>
</task>

</tasks>

<verification>
cd /home/swd/loom/src && npx vitest run --reporter=verbose
</verification>

<success_criteria>
- FileNode renders recursive tree with correct indentation at each depth level
- Clicking files calls openFile, clicking directories calls toggleDir
- Active file is visually highlighted
- Loading skeleton shown during fetch, error state with retry on failure
- Dotfiles hidden by default
- Search input filters tree nodes by substring match
- Files tab in ContentArea shows real FileTreePanel (no more PanelPlaceholder)
- All new and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-file-tree-file-store/23-02-SUMMARY.md`
</output>
