---
phase: 23-file-tree-file-store
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/src/components/file-tree/FileTreeContextMenu.tsx
  - src/src/components/file-tree/ImagePreview.tsx
  - src/src/components/file-tree/FileNode.tsx
  - src/src/components/command-palette/groups/FileGroup.tsx
  - src/src/components/file-tree/FileTreeContextMenu.test.tsx
  - src/src/components/file-tree/ImagePreview.test.tsx
autonomous: true
requirements:
  - FT-09
  - FT-10
  - FT-11

user_setup: []

must_haves:
  truths:
    - "Right-clicking a file shows context menu with Copy Path, Copy Relative Path, Open in Editor, Open Containing Folder in Terminal"
    - "Right-clicking a directory shows context menu with Copy Path, Expand All, Collapse All"
    - "Clicking an image file opens a lightbox preview dialog instead of setting it as active editor file"
    - "Selecting a file in the command palette opens it in the file tree (calls openFile on store)"
  artifacts:
    - path: "src/src/components/file-tree/FileTreeContextMenu.tsx"
      provides: "Context menu wrappers for files and directories"
      exports: ["FileContextMenu", "DirContextMenu"]
    - path: "src/src/components/file-tree/ImagePreview.tsx"
      provides: "Lightbox dialog for image file preview"
      exports: ["ImagePreview"]
  key_links:
    - from: "src/src/components/file-tree/FileTreeContextMenu.tsx"
      to: "navigator.clipboard"
      via: "Copy Path / Copy Relative Path actions"
      pattern: "navigator.clipboard.writeText"
    - from: "src/src/components/file-tree/ImagePreview.tsx"
      to: "shadcn Dialog"
      via: "Dialog wrapper for image display"
      pattern: "Dialog"
    - from: "src/src/components/command-palette/groups/FileGroup.tsx"
      to: "src/src/stores/file.ts"
      via: "openFile store action"
      pattern: "useFileStore.*openFile"
---

<objective>
Add right-click context menus, image lightbox preview, and wire the command palette FileGroup to actually open files.

Purpose: Layer interactive features onto the file tree -- context menus for file operations, image preview for non-text files, and complete the command palette -> file opening circuit that was stubbed in Phase 22.
Output: Context menus on right-click, image lightbox on image file click, and command palette file selection wired to file store.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-file-tree-file-store/23-RESEARCH.md
@.planning/phases/23-file-tree-file-store/23-01-SUMMARY.md

@src/src/stores/file.ts
@src/src/types/file.ts
@src/src/components/file-tree/FileNode.tsx
@src/src/components/command-palette/groups/FileGroup.tsx
@src/src/components/ui/dialog.tsx

<interfaces>
<!-- From Plan 01 outputs -->

From src/src/stores/file.ts:
```typescript
export const useFileStore = create<FileStore>()((set) => ({
  // State
  expandedDirs: string[];
  activeFilePath: string | null;
  // Actions
  toggleDir: (path: string) => void;
  openFile: (path: string) => void;
  // ... other actions
}));
```

From src/src/components/command-palette/groups/FileGroup.tsx:
```typescript
export interface FileGroupProps {
  files: FileEntry[];
  onClose: () => void;
}
// Currently calls setActiveTab('files') but does NOT call openFile on file store
```

Image extensions to detect:
```typescript
const IMAGE_EXTENSIONS = new Set(['.png', '.jpg', '.jpeg', '.gif', '.svg', '.webp', '.ico']);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install shadcn context-menu + build context menus + image lightbox</name>
  <files>src/src/components/file-tree/FileTreeContextMenu.tsx, src/src/components/file-tree/ImagePreview.tsx, src/src/components/file-tree/FileTreeContextMenu.test.tsx, src/src/components/file-tree/ImagePreview.test.tsx</files>
  <behavior>
    - FileContextMenu wraps children in ContextMenu trigger; right-click shows: Copy Path, Copy Relative Path, Open in Editor, Open Containing Folder in Terminal
    - DirContextMenu wraps children in ContextMenu trigger; right-click shows: Copy Path, Expand All, Collapse All
    - Copy Path copies absolute path to clipboard via navigator.clipboard.writeText
    - Copy Relative Path strips projectRoot prefix and copies
    - Open in Editor calls openFile on file store
    - Open Containing Folder in Terminal calls setActiveTab('shell') (actual cd deferred to Phase 25)
    - Expand All recursively adds all descendant directories to expandedDirs
    - Collapse All recursively removes all descendant directories from expandedDirs
    - ImagePreview renders a Dialog with the image at natural size, file name as title, close button
    - ImagePreview is controlled (open/onOpenChange props)
  </behavior>
  <action>
1. Install shadcn context-menu:
   ```bash
   cd /home/swd/loom/src && npx shadcn@latest add context-menu --yes
   ```

2. Create `src/src/components/file-tree/FileTreeContextMenu.tsx`:
   - Import shadcn ContextMenu, ContextMenuTrigger, ContextMenuContent, ContextMenuItem
   - Export `FileContextMenu` component:
     - Props: `{ children, filePath, projectRoot: string | null }`
     - Menu items:
       - "Copy Path" -- `navigator.clipboard.writeText(filePath)`, show toast.success('Copied to clipboard')
       - "Copy Relative Path" -- strip projectRoot prefix (if known) then copy, show toast
       - "Open in Editor" -- call `useFileStore.getState().openFile(filePath)` (yes, getState is fine here since it's a menu action callback, not a render selector)
       - "Open Containing Folder in Terminal" -- call `useUIStore.getState().setActiveTab('shell')`, add `// TODO: Phase 25 -- send cd command to terminal`
     - Use lucide-react icons for each item: Copy, FileText, Terminal
   - Export `DirContextMenu` component:
     - Props: `{ children, dirPath, node: FileTreeNode, projectRoot: string | null }`
     - Menu items:
       - "Copy Path" -- same as file
       - "Expand All" -- recursively collect all descendant directory paths from node.children, then use file store to add them all to expandedDirs. Implement via a helper: `getAllDirPaths(node: FileTreeNode): string[]` that walks children recursively collecting directories. Then call set on the store.
       - "Collapse All" -- same recursive collection, then filter all those paths out of expandedDirs
     - For Expand All / Collapse All, add `expandDirs` and `collapseDirs` helper actions. Since we can't easily batch from outside the store, use `useFileStore.setState()` with a function: `useFileStore.setState(s => ({ expandedDirs: [...new Set([...s.expandedDirs, ...paths])] }))` for expand, and filter for collapse.

3. Create `src/src/components/file-tree/ImagePreview.tsx`:
   - Props: `{ filePath: string; fileName: string; open: boolean; onOpenChange: (open: boolean) => void; projectName: string }`
   - Render shadcn `Dialog` with `DialogContent`:
     - DialogTitle: fileName
     - Image: `<img>` tag with `src` pointing to backend file content endpoint. The backend serves file content at `GET /api/projects/:projectName/files/content?path=<encodedPath>`. Use `src={/api/projects/${encodeURIComponent(projectName)}/files/content?path=${encodeURIComponent(filePath)}}` with auth header (use the token from localStorage if needed, or rely on cookie). Actually, check the backend -- if it requires auth header, use an img with onError fallback. Simpler: just set src directly since the backend may not support raw image serving via that endpoint. Alternative: show the absolute path and a message "Image preview requires backend file content endpoint" as a graceful degradation. The safest approach: construct the URL and attempt to load; show error message if it fails.
     - Max dimensions: `max-w-[80vw] max-h-[70vh] object-contain`
     - DialogClose button
   - Export `isImageFile(name: string): boolean` helper (used by FileNode to decide between openFile and ImagePreview)

4. Write tests in `src/src/components/file-tree/FileTreeContextMenu.test.tsx`:
   - Test: FileContextMenu renders children and shows menu items on context menu trigger
   - Test: Copy Path calls navigator.clipboard.writeText with absolute path
   - Test: Copy Relative Path strips project root
   - Test: DirContextMenu shows Expand All and Collapse All items
   - Mock navigator.clipboard.writeText with vi.fn()

5. Write tests in `src/src/components/file-tree/ImagePreview.test.tsx`:
   - Test: renders dialog with image when open=true
   - Test: shows file name as title
   - Test: isImageFile returns true for .png, .jpg, .svg, .webp
   - Test: isImageFile returns false for .ts, .json, .md
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/file-tree/FileTreeContextMenu.test.tsx src/src/components/file-tree/ImagePreview.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Context menus appear on right-click with correct actions. Image lightbox opens for image files. Clipboard copy works. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire context menus into FileNode + wire FileGroup in command palette</name>
  <files>src/src/components/file-tree/FileNode.tsx, src/src/components/command-palette/groups/FileGroup.tsx</files>
  <action>
1. Update `src/src/components/file-tree/FileNode.tsx`:
   - Wrap each file node row in `<FileContextMenu filePath={node.path} projectRoot={projectRoot}>` (pass projectRoot as prop)
   - Wrap each directory node row in `<DirContextMenu dirPath={node.path} node={node} projectRoot={projectRoot}>`
   - Add `projectRoot` to FileNode props
   - For image files: instead of calling `openFile(path)` on click, open the ImagePreview dialog. Add local state `const [imagePreviewOpen, setImagePreviewOpen] = useState(false)`. Detect with `isImageFile(node.name)`. Render `<ImagePreview>` conditionally when imagePreviewOpen is true.
   - Get projectName from useProjectContext (or pass as prop from parent)

2. Update `src/src/components/command-palette/groups/FileGroup.tsx`:
   - Import `useFileStore` from `@/stores/file`
   - In handleSelect: after `setActiveTab('files')`, also call `openFile(file.path)` from the file store
   - Remove the underscore prefix from `_file` parameter (no longer unused)

3. Run full test suite to verify no regressions.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Right-click context menus work on file tree nodes. Image files open lightbox instead of editor. Command palette file selection opens files in the tree (calls openFile). Full test suite passes.</done>
</task>

</tasks>

<verification>
cd /home/swd/loom/src && npx vitest run --reporter=verbose
</verification>

<success_criteria>
- Right-click on file shows 4 context menu items (Copy Path, Copy Relative Path, Open in Editor, Open in Terminal)
- Right-click on directory shows 3 context menu items (Copy Path, Expand All, Collapse All)
- Copy Path copies absolute path to clipboard
- Image files (.png, .jpg, .gif, .svg, .webp) open in lightbox dialog instead of editor
- Command palette file selection calls openFile on file store and switches to Files tab
- shadcn context-menu installed and usable
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-file-tree-file-store/23-03-SUMMARY.md`
</output>
