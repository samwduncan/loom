---
phase: 05-chat-message-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/chat/hooks/useShikiHighlighter.ts
  - src/components/chat/view/subcomponents/CodeBlock.tsx
  - src/components/chat/view/subcomponents/Markdown.tsx
  - vite.config.js
  - package.json
autonomous: true
requirements: [CHAT-01, CHAT-02]

must_haves:
  truths:
    - "Code blocks render with VS Code Dark+ syntax highlighting via Shiki with warm-tinted colors"
    - "Code block header shows language label (left) and copy button (right) with success feedback"
    - "During streaming, code blocks fall back to raw monospace text until the closing fence arrives"
    - "Long code blocks (50+ lines) truncate at ~25 lines with a Show more button"
    - "Soft word wrap on long lines prevents horizontal scrolling"
  artifacts:
    - path: "src/components/chat/hooks/useShikiHighlighter.ts"
      provides: "Singleton Shiki highlighter with warm Dark+ color replacements"
      exports: ["getHighlighter", "highlightCode"]
    - path: "src/components/chat/view/subcomponents/CodeBlock.tsx"
      provides: "Shiki-powered code block with header bar, copy button, truncation, streaming fallback"
      exports: ["ShikiCodeBlock"]
    - path: "src/components/chat/view/subcomponents/Markdown.tsx"
      provides: "Markdown renderer using ShikiCodeBlock instead of react-syntax-highlighter"
    - path: "vite.config.js"
      provides: "Shiki isolated in its own vendor chunk"
      contains: "vendor-shiki"
  key_links:
    - from: "src/components/chat/view/subcomponents/Markdown.tsx"
      to: "src/components/chat/view/subcomponents/CodeBlock.tsx"
      via: "react-markdown components.code"
      pattern: "ShikiCodeBlock"
    - from: "src/components/chat/view/subcomponents/CodeBlock.tsx"
      to: "src/components/chat/hooks/useShikiHighlighter.ts"
      via: "highlightCode async call"
      pattern: "highlightCode"
---

<objective>
Install Shiki v4 and build the warm-tinted VS Code Dark+ code block experience, replacing react-syntax-highlighter throughout the Markdown renderer.

Purpose: CHAT-01 and CHAT-02 deliver the single biggest visual quality improvement in Phase 5 -- syntax highlighting with VS Code-identical token parity and a warm Loom aesthetic. This is foundational infrastructure consumed by every AI response containing code.

Output: ShikiCodeBlock component, singleton highlighter module, updated Markdown.tsx, Vite chunk config.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chat-message-architecture/05-CONTEXT.md
@.planning/phases/05-chat-message-architecture/05-RESEARCH.md

@src/components/chat/view/subcomponents/Markdown.tsx
@src/components/chat/view/subcomponents/MessageComponent.tsx
@src/utils/clipboard.ts
@vite.config.js

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/chat/view/subcomponents/Markdown.tsx:
```typescript
type MarkdownProps = {
  children: React.ReactNode;
  className?: string;
};
// CodeBlock is the component passed to react-markdown's components.code
// Currently uses react-syntax-highlighter (Prism) with oneDark theme
// Language extracted from className via /language-(\w+)/ regex
```

From src/utils/clipboard.ts:
```typescript
export function copyTextToClipboard(text: string): Promise<boolean>;
```

From vite.config.js:
```javascript
// Existing manualChunks in build.rollupOptions.output:
manualChunks: {
  'vendor-react': ['react', 'react-dom', 'react-router-dom'],
  'vendor-codemirror': [/* codemirror packages */],
  'vendor-xterm': [/* xterm packages */],
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Shiki and create singleton highlighter with warm Dark+ theme</name>
  <files>package.json, src/components/chat/hooks/useShikiHighlighter.ts, vite.config.js</files>
  <action>
1. Run `npm install shiki@^4.0.1`

2. Create `src/components/chat/hooks/useShikiHighlighter.ts`:
   - Module-level `highlighterPromise: Promise<Highlighter> | null = null` singleton pattern
   - Import from `shiki/bundle/web` (NOT the full `shiki` package -- web bundle is 3.8MB vs 6.4MB)
   - Pre-load common languages: javascript, typescript, python, json, html, css, bash, markdown, jsx, tsx, rust, go, yaml, toml, sql, ruby, java, c, cpp, shell, diff, dockerfile, graphql
   - Theme: `dark-plus` with `colorReplacements` for warm tinting
   - Color replacement map (exact Dark+ hex -> warm Loom equivalents):
     - `#1E1E1E` -> `#1c1210` (background -> Loom base)
     - `#D4D4D4` -> `#f5e6d3` (foreground -> Loom cream)
     - `#264F78` -> `#3d2e25` (selection -> warm selection)
     - `#808080` -> `#c4a882` (comments -> muted gold)
     - `#569CD6` -> `#6bacce` (keywords -> slightly warmer blue)
     - `#4EC9B0` -> `#5cc0a0` (types -> slightly warmer teal)
     - `#CE9178` -> `#d4a574` (strings -> Loom amber)
     - `#DCDCAA` -> `#e0d0a0` (functions -> warmer yellow)
     - `#9CDCFE` -> `#a0ccde` (variables -> warmer light blue)
     - `#C586C0` -> `#c48aab` (control flow -> warmer mauve)
     - `#6A9955` -> `#7a9960` (comments green -> warmer green)
     - `#B5CEA8` -> `#c0c8a0` (numbers -> warmer green)
   - Export `getHighlighter(): Promise<Highlighter>` -- creates or returns the singleton promise
   - Export `highlightCode(code: string, lang: string): Promise<string>` -- calls `codeToHtml` with the warm Dark+ theme, falls back to `text` for unknown languages
   - At implementation time, extract theme JSON via `highlighter.getTheme('dark-plus')` to verify hex values and log any unmapped colors in development

3. Add `'vendor-shiki': ['shiki']` to `vite.config.js` manualChunks object to isolate Shiki in its own chunk

WARNING: Do NOT create a highlighter instance per render. The module-level promise pattern ensures WASM + grammars load exactly once.
  </action>
  <verify>
    <automated>cd /home/swd/loom && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Shiki installed, singleton highlighter module exports getHighlighter and highlightCode, Vite chunks include vendor-shiki, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Build ShikiCodeBlock component and wire into Markdown.tsx</name>
  <files>src/components/chat/view/subcomponents/CodeBlock.tsx, src/components/chat/view/subcomponents/Markdown.tsx</files>
  <action>
1. Create `src/components/chat/view/subcomponents/CodeBlock.tsx`:
   - Props: `{ code: string, language: string, isStreaming?: boolean, filename?: string }`
   - Header bar div: language name (left, uppercase small text), filename if available (center), copy button (right)
   - Copy button: Use existing `copyTextToClipboard` utility, show checkmark for 2 seconds on success (same pattern as existing Markdown.tsx copy button but cleaner)
   - Use lucide-react icons: `Copy`, `Check` for copy button states
   - Streaming fallback: when `isStreaming` is true OR Shiki HTML hasn't resolved yet, render raw `<pre><code>` with monospace styling in Loom warm background (`bg-[#1c1210]`), `text-[#f5e6d3]`, `whitespace-pre-wrap` for soft word wrap
   - Completed blocks: call `highlightCode(code, language)` via useEffect, set state to HTML string, render via `dangerouslySetInnerHTML` wrapped in a div with warm background
   - Add `word-wrap: break-word` and `white-space: pre-wrap` CSS override on the Shiki output container to enforce soft word wrap (no horizontal scroll)
   - No line numbers per user decision
   - Long code block truncation: if code has >50 lines and block is not streaming, show first ~25 lines with a "Show N more lines" button that reveals the full content. Use simple state toggle, not animation.
   - Inline code (when `inline` prop is true or detected): keep the existing simple styled `<code>` span (warm bg, warm text, monospace, rounded) -- do NOT run Shiki on inline code

2. Update `src/components/chat/view/subcomponents/Markdown.tsx`:
   - Remove `import { Prism as SyntaxHighlighter }` and `import { oneDark }`
   - Import `ShikiCodeBlock` from `./CodeBlock`
   - Replace the `CodeBlock` component: for block-level code, render `<ShikiCodeBlock code={raw} language={language} />`. For inline code, keep the existing `<code>` span styling (warm tinted per discretion: `bg-gray-800/60 text-gray-100 border-gray-700`)
   - The `isStreaming` prop should be determined by checking if the code block appears incomplete (no closing fence detected in the raw content). A pragmatic heuristic: the Markdown component doesn't know streaming state directly, so default `isStreaming={false}` for now -- Phase 5 Plan 05-05 will wire streaming awareness.
   - Remove `react-syntax-highlighter` from package.json dependencies and run `npm install` to clean up

3. Verify the inline code heuristic remains: `inline || node.type === 'inlineCode' || !looksMultiline` gates inline rendering. Only multi-line fenced blocks go through ShikiCodeBlock.
  </action>
  <verify>
    <automated>cd /home/swd/loom && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Code blocks render via Shiki with warm Dark+ theme. Header shows language + copy button. Long blocks truncate at 25 lines. Inline code uses simple styled spans. react-syntax-highlighter removed from dependencies. Build passes.</done>
  <depth>
    - Streaming state: raw monospace fallback while Shiki promise resolves, highlighted HTML once resolved
    - Empty code block: renders as empty pre with warm background (no crash)
    - Unknown language: falls back to `text` highlighting (no error)
    - Very long single line: wraps via white-space pre-wrap, no horizontal scrollbar
    - Copy button states: idle (copy icon), success (check icon for 2s), back to idle
    - Header layout: language left, filename center (if present), copy button right -- all in a slim bar above the code
    - Truncation UX: "Show 47 more lines" button text reflects actual hidden line count
  </depth>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. Shiki appears in vendor-shiki chunk in build output
3. `react-syntax-highlighter` no longer in node_modules after npm install (or at minimum removed from package.json dependencies)
4. `grep -r "react-syntax-highlighter" src/` returns zero matches
</verification>

<success_criteria>
- Code blocks use Shiki v4 with warm-tinted VS Code Dark+ theme
- Language label and copy button visible in code block header
- Long code blocks truncate at ~25 lines with expand button
- No horizontal scroll on long lines (soft wrap)
- Inline code remains simple styled spans
- react-syntax-highlighter fully removed
- Build succeeds, Shiki isolated in vendor chunk
</success_criteria>

<output>
After completion, create `.planning/phases/05-chat-message-architecture/05-01-SUMMARY.md`
</output>
