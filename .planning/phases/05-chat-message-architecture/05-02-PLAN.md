---
phase: 05-chat-message-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/chat/view/subcomponents/ThinkingDisclosure.tsx
  - src/components/chat/view/subcomponents/ActivityIndicator.tsx
  - src/components/chat/view/subcomponents/AssistantThinkingIndicator.tsx
  - src/components/chat/view/subcomponents/MessageComponent.tsx
autonomous: true
requirements: [CHAT-08]

must_haves:
  truths:
    - "Completed thinking blocks render as a Claude.ai-style disclosure triangle with muted 'Thinking' text, collapsed by default"
    - "Expanding a thinking disclosure reveals the full reasoning text with muted styling"
    - "Activity indicator shows Claude CLI-style blinking amber text with rotating status phrases"
    - "Activity indicator replaces the old pulsing dots AssistantThinkingIndicator"
    - "Real-time thinking content is toggleable via per-block eye icon (off by default)"
  artifacts:
    - path: "src/components/chat/view/subcomponents/ThinkingDisclosure.tsx"
      provides: "Claude.ai-style collapsible thinking block with disclosure triangle"
      exports: ["ThinkingDisclosure"]
    - path: "src/components/chat/view/subcomponents/ActivityIndicator.tsx"
      provides: "Claude CLI-style rotating phrase indicator"
      exports: ["ActivityIndicator"]
  key_links:
    - from: "src/components/chat/view/subcomponents/MessageComponent.tsx"
      to: "src/components/chat/view/subcomponents/ThinkingDisclosure.tsx"
      via: "import and render for isThinking messages and reasoning blocks"
      pattern: "ThinkingDisclosure"
    - from: "src/components/chat/view/subcomponents/AssistantThinkingIndicator.tsx"
      to: "src/components/chat/view/subcomponents/ActivityIndicator.tsx"
      via: "AssistantThinkingIndicator replaced to render ActivityIndicator"
      pattern: "ActivityIndicator"
---

<objective>
Build the thinking block disclosure and Claude CLI-style activity indicator, replacing the basic pulsing dots with engaging rotating status phrases.

Purpose: CHAT-08 transforms two key interaction points -- the "AI is working" indicator and the expandable thinking content. The activity indicator with rotating phrases gives users feedback that the AI is actively processing (like Claude Code CLI), while the thinking disclosure follows the Claude.ai pattern for clean expandable reasoning blocks.

Output: ThinkingDisclosure component, ActivityIndicator component, updated MessageComponent thinking rendering, replaced AssistantThinkingIndicator.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chat-message-architecture/05-CONTEXT.md
@.planning/phases/05-chat-message-architecture/05-RESEARCH.md

@src/components/chat/view/subcomponents/AssistantThinkingIndicator.tsx
@src/components/chat/view/subcomponents/MessageComponent.tsx
@src/components/chat/view/subcomponents/Markdown.tsx
@src/components/chat/types/types.ts

<interfaces>
<!-- Key types the executor needs -->

From src/components/chat/types/types.ts:
```typescript
export interface ChatMessage {
  type: string;
  content?: string;
  timestamp: string | number | Date;
  reasoning?: string;
  isThinking?: boolean;
  isStreaming?: boolean;
  // ... other fields
}
```

From src/components/chat/view/subcomponents/MessageComponent.tsx:
```typescript
// showThinking prop controls thinking visibility
// message.isThinking = dedicated thinking message type
// message.reasoning = reasoning content attached to a regular assistant message
// Two places where thinking is rendered:
// 1. isThinking messages: <details> with "Thinking..." summary + Markdown content
// 2. reasoning on assistant messages: <details> with "Thinking..." summary + raw text
```

From src/components/chat/view/subcomponents/AssistantThinkingIndicator.tsx:
```typescript
// Used in ChatMessagesPane when isLoading is true
// Shows provider logo + pulsing dots + "Thinking..." text
// Props: { selectedProvider: SessionProvider }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ThinkingDisclosure and ActivityIndicator components</name>
  <files>src/components/chat/view/subcomponents/ThinkingDisclosure.tsx, src/components/chat/view/subcomponents/ActivityIndicator.tsx</files>
  <action>
1. Create `src/components/chat/view/subcomponents/ThinkingDisclosure.tsx`:
   - Props: `{ content: string, isStreaming?: boolean, showByDefault?: boolean }`
   - Claude.ai-style design: NOT using native `<details>` (snaps open/close). Use controlled React state + CSS grid `0fr/1fr` trick for smooth height animation.
   - Collapsed state: disclosure triangle (chevron-right SVG, rotates 90deg when expanded) + "Thinking" text in muted gray (`text-gray-500`)
   - Expanded state: muted content area with left border (`border-l-2 border-gray-600`), slightly indented, gray text (`text-gray-400`), renders content as plain text with `whitespace-pre-wrap` (NOT Markdown -- thinking content is raw reasoning)
   - CSS animation: `.collapsible-content { display: grid; grid-template-rows: 0fr; transition: grid-template-rows 200ms ease-out; }` with `.expanded { grid-template-rows: 1fr; }` and inner div `overflow: hidden`
   - Per-block eye/toggle icon: small eye icon (lucide-react `Eye`/`EyeOff`) at the right edge of the header that toggles real-time thinking visibility. When `isStreaming` is true AND eye toggle is on, content shows live. When eye toggle is off (default), content is hidden during streaming.
   - Once streaming completes (`isStreaming` becomes false), the block auto-collapses to the disclosure state regardless of eye toggle state
   - If `showByDefault` is true (from global settings), start expanded

2. Create `src/components/chat/view/subcomponents/ActivityIndicator.tsx`:
   - Props: `{ provider: SessionProvider }`
   - Claude CLI-style design: blinking amber/red text with rotating status phrases
   - Phrase array (rotate every 3 seconds):
     - "Thinking...", "Reading files...", "Analyzing code...", "Working on it...",
     - "Processing...", "Searching...", "Considering options...", "Crafting response...",
     - "Reviewing context...", "Almost there...", "Reasoning...", "Evaluating..."
   - Text styling: `text-amber-500 font-mono text-sm` with CSS `animation: blink 1s step-start infinite` (blinking, not pulsing -- per user wanting "Claude CLI-style blinking amber/red text")
   - Add `@keyframes blink { 50% { opacity: 0; } }` as a Tailwind arbitrary animation or inline style
   - Provider logo to the left (reuse `SessionProviderLogo` like existing indicator)
   - No pulsing dots -- this completely replaces that pattern
  </action>
  <verify>
    <automated>cd /home/swd/loom && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>ThinkingDisclosure renders with smooth CSS grid animation, disclosure triangle, muted styling. ActivityIndicator shows blinking amber text with rotating phrases. Both components build successfully.</done>
  <depth>
    - Empty thinking content: disclosure header visible but no expand (or shows "No reasoning available")
    - Very long thinking content: scrollable within expanded area (max-height with overflow-y-auto)
    - Streaming thinking: eye toggle shows/hides real-time content. Content updates live when visible.
    - Phrase rotation: starts at random index to avoid all indicators showing "Thinking..." simultaneously
    - Blink timing: 1s step-start matches terminal cursor blink rate
    - Accessibility: disclosure button has aria-expanded, aria-controls linking to content panel
  </depth>
</task>

<task type="auto">
  <name>Task 2: Wire ThinkingDisclosure into MessageComponent and replace AssistantThinkingIndicator</name>
  <files>src/components/chat/view/subcomponents/MessageComponent.tsx, src/components/chat/view/subcomponents/AssistantThinkingIndicator.tsx</files>
  <action>
1. Update `src/components/chat/view/subcomponents/MessageComponent.tsx`:
   - Import `ThinkingDisclosure` from `./ThinkingDisclosure`
   - Replace the `isThinking` branch (lines ~397-413): instead of native `<details>` with emoji, render `<ThinkingDisclosure content={message.content || ''} isStreaming={message.isStreaming} showByDefault={showThinking} />`
   - Replace the `reasoning` branch (lines ~417-428): instead of native `<details>`, render `<ThinkingDisclosure content={message.reasoning} isStreaming={false} showByDefault={showThinking} />` above the regular content
   - Keep the `shouldHideThinkingMessage` guard (line ~91) that hides thinking messages when `showThinking` is false. But now the ThinkingDisclosure handles its own collapse state, so the guard should only fully hide when `showThinking` is false AND `message.isThinking` is true (dedicated thinking messages). For messages with `reasoning` field, always show the message content but let ThinkingDisclosure handle the reasoning visibility.

2. Replace `src/components/chat/view/subcomponents/AssistantThinkingIndicator.tsx`:
   - Import `ActivityIndicator` from `./ActivityIndicator`
   - Rewrite the component body to render `ActivityIndicator` instead of the pulsing dots
   - Keep the same export name and props interface so `ChatMessagesPane` import doesn't change
   - The layout wrapper stays (w-full, provider logo header) but the indicator content changes from dots to rotating phrases

3. Ensure `ChatMessagesPane.tsx` needs NO changes -- it already renders `<AssistantThinkingIndicator>` when `isLoading` is true, and the file export/import paths remain the same.
  </action>
  <verify>
    <automated>cd /home/swd/loom && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Thinking messages render via ThinkingDisclosure with smooth disclosure animation. AssistantThinkingIndicator now shows rotating phrases with blinking amber text. Build passes. No native details elements used for thinking content.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. `grep -r '<details' src/components/chat/view/subcomponents/MessageComponent.tsx` returns zero matches (no native details for thinking)
3. `grep -r 'animate-pulse' src/components/chat/view/subcomponents/AssistantThinkingIndicator.tsx` returns zero matches (no pulsing dots)
4. `grep -r 'ThinkingDisclosure' src/components/chat/view/subcomponents/MessageComponent.tsx` returns matches (properly wired)
</verification>

<success_criteria>
- Thinking blocks use Claude.ai-style disclosure with smooth CSS grid animation
- Activity indicator shows blinking amber text with rotating status phrases
- Per-block eye toggle controls real-time thinking visibility
- Thinking blocks auto-collapse when streaming completes
- No native `<details>` elements used for thinking content
- Old pulsing dots pattern fully removed
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-chat-message-architecture/05-02-SUMMARY.md`
</output>
