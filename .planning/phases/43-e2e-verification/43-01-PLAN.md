---
phase: 43-e2e-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/e2e/permission-banner.spec.ts
  - src/e2e/token-usage.spec.ts
  - src/e2e/image-attachment.spec.ts
  - src/e2e/export-conversation.spec.ts
  - src/e2e/message-retry.spec.ts
autonomous: true
requirements:
  - E2E-01
  - E2E-02
  - E2E-03
  - E2E-04
  - E2E-05

must_haves:
  truths:
    - "Permission banner appears during a tool call and responds to Y/N keyboard shortcuts"
    - "Token usage footer shows input/output token counts and cost after a completed turn"
    - "Image pasted or dropped into composer is sent and rendered in the conversation"
    - "Export conversation produces a downloadable file with message content"
    - "Retry button on error re-sends the last user message"
  artifacts:
    - path: "src/e2e/permission-banner.spec.ts"
      provides: "E2E test for permission banners with countdown and keyboard shortcuts"
    - path: "src/e2e/token-usage.spec.ts"
      provides: "E2E test for token usage display after completed turn"
    - path: "src/e2e/image-attachment.spec.ts"
      provides: "E2E test for image paste/drop into composer"
    - path: "src/e2e/export-conversation.spec.ts"
      provides: "E2E test for conversation export download"
    - path: "src/e2e/message-retry.spec.ts"
      provides: "E2E test for message retry on error"
  key_links:
    - from: "src/e2e/permission-banner.spec.ts"
      to: "src/src/components/chat/tools/PermissionBanner.tsx"
      via: "Playwright assertions on [data-testid] elements"
      pattern: "locator.*permission"
    - from: "src/e2e/token-usage.spec.ts"
      to: "src/src/components/chat/view/TokenUsage.tsx"
      via: "Playwright assertions on token display"
      pattern: "locator.*token"
---

<objective>
Write and run Playwright E2E tests verifying all chat-related features: permission banners, token usage, image attachments, conversation export, and message retry.

Purpose: Verify that features built across M1-M4 work correctly with a real backend under daily-driver conditions.
Output: 5 Playwright spec files that pass against the real backend.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/playwright.config.ts
@src/e2e/streaming.spec.ts
@src/e2e/new-chat.spec.ts

<interfaces>
<!-- Existing E2E test pattern from streaming.spec.ts -->
<!-- Tests use real backend (port 5555) + Vite dev (port 5184) -->
<!-- Serial mode within describe blocks, test.slow() for API calls -->
<!-- WS connection pattern: page.waitForEvent('websocket', { predicate: ws => ws.url().includes('/ws?token=') }) -->
<!-- Composer input: page.locator('[aria-label="Message input"]') -->
<!-- Send button: page.locator('[aria-label="Send message"]') -->
<!-- Streaming complete: expect(page.locator('[data-testid="streaming-cursor"]')).toBeHidden() -->

From src/src/components/chat/tools/PermissionBanner.tsx:
- Component renders when pendingPermission is non-null in stream store
- Shows tool name, input preview, countdown ring (55s)
- Y key = allow, N key = deny
- data-testid not yet added — test may need to select by text/role

From src/src/components/chat/view/TokenUsage.tsx:
- Renders below assistant messages when metadata has inputTokens/outputTokens/cost
- Compact one-liner by default, expandable on click
- Uses MessageMetadata type with inputTokens, outputTokens, cost fields

From src/src/components/chat/composer/useImageAttachments.ts:
- Handles paste (ClipboardEvent) and drop (DragEvent) for images
- Converts to base64 data URLs, stores in attachments array
- Images sent as content blocks in the message

From src/src/lib/export-conversation.ts:
- exportConversation() builds markdown from messages
- Triggered from ChatView via a button/menu action

From src/src/components/chat/view/ErrorMessage.tsx:
- handleRetry re-sends lastUserMessage via sendMessage
- Retry button disabled when !canRetry (no last message or disconnected)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write permission banner, token usage, and image attachment E2E specs</name>
  <files>src/e2e/permission-banner.spec.ts, src/e2e/token-usage.spec.ts, src/e2e/image-attachment.spec.ts</files>
  <action>
Create three Playwright spec files following the existing pattern in e2e/streaming.spec.ts:

**permission-banner.spec.ts** (E2E-01):
- Send a message like "Run this bash command: echo hello" to trigger a tool call requiring permission
- Wait for the permission banner to appear (look for text containing "Allow" and "Deny", or the countdown ring, or a data-testid if one exists — inspect PermissionBanner.tsx for selectors)
- Verify the banner shows the tool name ("Bash") and a preview of the command
- Verify countdown timer is visible and counting down
- Press 'y' key and verify the banner dismisses and the tool executes
- Second test: trigger another permission, press 'n' key, verify it's denied
- Mark test.slow() — real API calls needed
- If PermissionBanner.tsx lacks data-testid attributes, add them: `data-testid="permission-banner"`, `data-testid="permission-allow"`, `data-testid="permission-deny"`, `data-testid="permission-countdown"`

**token-usage.spec.ts** (E2E-02):
- Send a simple message ("Say: hello") and wait for streaming to complete
- Look for token usage footer below the assistant message — check for text matching the compact format pattern (e.g., "X in / Y out" or "$")
- Verify the footer contains numeric token counts (not "null" or "undefined")
- Click the footer to expand and verify detailed breakdown is visible
- Use data-testid selectors if available, or add them to TokenUsage.tsx: `data-testid="token-usage"`, `data-testid="token-usage-expanded"`

**image-attachment.spec.ts** (E2E-03):
- Navigate to /chat, wait for composer
- Create a small test PNG buffer (1x1 pixel) using Buffer or a base64-encoded minimal PNG
- Simulate paste event with the image data using page.evaluate to dispatch a ClipboardEvent with a DataTransfer containing a File blob
- Verify an image preview/thumbnail appears in the composer attachment area
- Send the message and verify the image content block appears in the conversation
- Mark test.slow() — real API call with image

For any component that lacks data-testid attributes needed for reliable selection, add them to the production component. Keep additions minimal — just the data-testid attribute on the outermost element and key interactive elements.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx playwright test e2e/permission-banner.spec.ts e2e/token-usage.spec.ts e2e/image-attachment.spec.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <done>All three spec files exist and their tests pass against the real backend. Permission banner shows with countdown and responds to Y/N keys, token usage footer shows real numbers, image paste sends and displays.</done>
</task>

<task type="auto">
  <name>Task 2: Write export conversation and message retry E2E specs</name>
  <files>src/e2e/export-conversation.spec.ts, src/e2e/message-retry.spec.ts</files>
  <action>
Create two Playwright spec files:

**export-conversation.spec.ts** (E2E-04):
- Navigate to /chat, send a message, wait for response to complete
- Find and click the export/download button (inspect ChatView.tsx for the export trigger — likely a button in a menu or toolbar)
- Use Playwright's download event listener: `const [download] = await Promise.all([page.waitForEvent('download'), clickExportButton])`
- Verify the download has a non-empty suggested filename
- Read the download content and verify it contains the sent message text
- If the export button is in a dropdown menu, open the menu first

**message-retry.spec.ts** (E2E-05):
- This test needs an error state. Strategy: send a message to trigger a response, then verify the retry mechanism exists.
- Option A (preferred): Find the retry button on any ErrorMessage component. Check if there's a way to force an error — e.g., disconnect WebSocket mid-stream, or the ErrorMessage renders on certain conditions.
- Option B (if error is hard to trigger): Write a unit-level E2E that verifies the retry button is wired correctly by checking ErrorMessage.tsx renders with the right props. But since this is E2E, prefer triggering a real error.
- Practical approach: Send a message that will likely cause an error (e.g., extremely long input that exceeds limits, or a message to a non-existent session). If the error state is hard to trigger reliably in E2E, document why and write the test to verify the retry UI exists and is wired (check the component renders, button has correct aria label, etc.).
- Look at ErrorMessage.tsx for data-testid or aria-label on the retry button. Add `data-testid="retry-button"` if missing.

Run all 5 chat E2E specs together after writing to verify no conflicts.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx playwright test e2e/export-conversation.spec.ts e2e/message-retry.spec.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <done>Export spec downloads a valid file containing message content. Retry spec verifies the retry mechanism works (either via triggered error or structural verification of the retry button wiring).</done>
</task>

</tasks>

<verification>
Run all chat E2E specs: `cd /home/swd/loom/src && npx playwright test e2e/permission-banner.spec.ts e2e/token-usage.spec.ts e2e/image-attachment.spec.ts e2e/export-conversation.spec.ts e2e/message-retry.spec.ts --reporter=list`
</verification>

<success_criteria>
- 5 new Playwright spec files exist in src/e2e/
- All specs pass against the real backend (port 5555)
- Permission banner responds to Y/N keyboard shortcuts
- Token usage shows real numeric data
- Image paste/drop sends and displays
- Export produces downloadable file with content
- Retry mechanism verified
</success_criteria>

<output>
After completion, create `.planning/phases/43-e2e-verification/43-01-SUMMARY.md`
</output>
