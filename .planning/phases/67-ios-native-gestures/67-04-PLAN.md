---
phase: 67-ios-native-gestures
plan: 04
type: execute
wave: 3
depends_on: ["67-01", "67-02", "67-03"]
files_modified:
  - src/src/components/app-shell/AppShell.tsx
  - src/src/components/sidebar/Sidebar.tsx
autonomous: false
requirements: [GESTURE-04, GESTURE-05, GESTURE-08]

must_haves:
  truths:
    - "App returns from background and WebSocket reconnects within 2 seconds"
    - "Sidebar edge swipe (left edge pan) still opens sidebar on mobile"
    - "Haptic feedback fires on all 7 gesture events defined in grammar"
    - "All gestures work together without conflicts"
  artifacts:
    - path: "src/src/components/app-shell/AppShell.tsx"
      provides: "Root-level useAppLifecycle() hook mount point"
      contains: "useAppLifecycle"
  key_links:
    - from: "src/src/components/app-shell/AppShell.tsx"
      to: "src/src/hooks/useAppLifecycle.ts"
      via: "Hook call in AppShell component"
      pattern: "useAppLifecycle"
---

<objective>
Wire app lifecycle hook into AppShell, verify sidebar edge swipe (GESTURE-04), and validate all gestures work together on real device.

Purpose: This final plan connects the foreground-reconnect hook, verifies the existing edge swipe wasn't broken by new gesture code, and provides a human checkpoint for real-device validation of all 10 GESTURE requirements.

Output: AppShell mounts useAppLifecycle. All Phase 67 features integrated and validated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-ios-native-gestures/67-CONTEXT.md
@.planning/phases/67-ios-native-gestures/67-UI-SPEC.md
@.planning/phases/67-ios-native-gestures/67-01-SUMMARY.md
@.planning/phases/67-ios-native-gestures/67-02-SUMMARY.md
@.planning/phases/67-ios-native-gestures/67-03-SUMMARY.md

<interfaces>
From src/src/hooks/useAppLifecycle.ts (Plan 01 output):
```typescript
export function useAppLifecycle(): void;
// Mount in root component. On web, this is a no-op.
// On native: listens for foreground return, reconnects WS if disconnected.
```

From src/src/components/app-shell/AppShell.tsx:
```typescript
export const AppShell = memo(function AppShell() {
  const sidebarOpen = useUIStore((state) => state.sidebarOpen);
  return (
    <>
      <SkipLink />
      <div data-testid="app-shell" ...>
        <ConnectionBanner />
        <div className="overflow-hidden min-w-0">
          {/* Sidebar + ContentArea */}
        </div>
      </div>
    </>
  );
});
```

From src/src/components/sidebar/Sidebar.tsx (lines 42-101):
```typescript
// Existing swipe-to-close gesture using raw touch handlers
// touchStart -> touchMove -> touchEnd on the sidebar div
// This code must NOT be modified (per D-02)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount useAppLifecycle in AppShell and add hapticEvent to sidebar toggle</name>
  <files>
    src/src/components/app-shell/AppShell.tsx,
    src/src/components/sidebar/Sidebar.tsx
  </files>
  <read_first>
    src/src/components/app-shell/AppShell.tsx,
    src/src/components/sidebar/Sidebar.tsx,
    src/src/hooks/useAppLifecycle.ts,
    src/src/lib/haptics.ts
  </read_first>
  <action>
**Step 1: Modify `AppShell.tsx`** -- Mount useAppLifecycle hook (per D-18)

Add import and call at the top of the AppShell component body:

```typescript
import { useAppLifecycle } from '@/hooks/useAppLifecycle';

export const AppShell = memo(function AppShell() {
  const sidebarOpen = useUIStore((state) => state.sidebarOpen);

  // GESTURE-08: Reconnect WebSocket on foreground return (native only)
  useAppLifecycle();

  return (
    // ... existing JSX unchanged
  );
});
```

This is a one-line addition. The hook is a no-op on web (returns early if !IS_NATIVE), so there are zero side effects for desktop users.

**Step 2: Add haptic feedback to sidebar toggle** (per D-27 - GESTURE-05)

In `Sidebar.tsx`, find the toggle function that opens/closes the sidebar. Add `hapticEvent('sidebarToggle')` call. Read the file first to find the exact toggle mechanism.

The sidebar toggle is typically triggered by `useUIStore((s) => s.toggleSidebar)` or similar. Find where the sidebar state changes and add the haptic call at that point.

Look for the mobile hamburger button or the swipe-to-close handler's completion callback (the point where `setSidebarOpen(false)` is called at the END of the swipe). Add `hapticEvent('sidebarToggle')` at that state transition point. Do NOT modify the raw touch event handler logic itself (per D-02) -- only add the haptic call where the final state change occurs.

Import: `import { hapticEvent } from '@/lib/haptics';`

Find the sidebar toggle button (likely a button with onClick that calls toggleSidebar or setSidebarOpen). Add haptic to that onClick handler.

If the sidebar toggle is called from multiple places, the cleanest approach is to add the haptic call in the Sidebar component's effect that responds to `sidebarOpen` state change, or directly in the button onClick handler.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `AppShell.tsx` imports `useAppLifecycle` from `@/hooks/useAppLifecycle`
    - `AppShell.tsx` calls `useAppLifecycle()` inside the component body
    - `Sidebar.tsx` imports `hapticEvent` from `@/lib/haptics`
    - `Sidebar.tsx` calls `hapticEvent('sidebarToggle')` on sidebar toggle action
    - Existing swipe-to-close touch handlers in Sidebar.tsx are NOT modified (per D-02)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>useAppLifecycle mounted in AppShell for foreground reconnect. Sidebar toggle fires haptic feedback. Existing sidebar swipe-to-close code untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-device validation of all Phase 67 gestures</name>
  <files>none</files>
  <action>Human verifies all 10 GESTURE requirements on real iPhone 16 Pro Max and desktop web.</action>
  <what-built>
    Complete Phase 67 iOS-native gesture system:
    - Swipe-to-delete on session items (left swipe reveals red Delete button)
    - Pull-to-refresh on session list (pull down for spinner + refresh)
    - Long-press context menus on session items (Copy ID, Rename, Pin, Export, Delete)
    - Long-press context menus on messages (Copy Text, Retry, Share)
    - App lifecycle reconnect (background to foreground reconnects WebSocket)
    - Haptic feedback on 7 gesture events
    - Native action sheet for delete confirmation (iOS)
    - Native share sheet for message/session sharing (iOS)
    - Native clipboard for copy operations (iOS)
    - Sidebar edge swipe preserved (GESTURE-04)
  </what-built>
  <how-to-verify>
    **On iPhone 16 Pro Max (native app via Capacitor):**

    1. **Swipe-to-delete (GESTURE-01):** Swipe left on any session item. Red Delete button should appear at 80px. Tap Delete. Native action sheet slides up from bottom with "Delete Session" title. Confirm. Session removed.

    2. **Pull-to-refresh (GESTURE-02):** Scroll to top of session list. Pull down. Spinner appears with proportional opacity. Pull past 60px and release. Spinner rotates, sessions refresh. Release below 60px -- snaps back, no fetch.

    3. **Long-press session (GESTURE-03):** Long-press a session item (~500ms). Context menu appears with: Copy ID, Rename, Pin to Top, Export, Delete. Test each action.

    4. **Sidebar edge swipe (GESTURE-04):** From chat view, swipe from left edge. Sidebar should open. Swipe sidebar closed. Verify no conflicts with session list gestures.

    5. **Haptics (GESTURE-05):** During all above gestures, feel for haptic feedback:
       - Swipe reveal: light thud
       - Delete confirm: warning pulse
       - Context menu open: medium thud
       - Pull-to-refresh complete: success pulse
       - Share: light thud
       - Session select (tap): selection click
       - Sidebar toggle: light thud

    6. **Long-press message (GESTURE-06):** Long-press a user message. Context menu: Copy Text, Share. Long-press an assistant message. Context menu: Copy Text, Retry, Share. Test Copy Text (should show "Copied to clipboard" toast). Test Share (should open iOS share sheet).

    7. **App lifecycle (GESTURE-08):** Open app, verify WebSocket connected. Switch to another app (background Loom). Wait 5 seconds. Switch back. WebSocket should reconnect within 2 seconds (connection banner should not persist).

    8. **Native share (GESTURE-09):** From message context menu, tap Share. iOS UIActivityViewController should appear with AirDrop, Messages, Mail, Notes options.

    9. **Native action sheet (GESTURE-10):** Swipe-to-delete and confirm. The confirmation should be a native iOS bottom sheet (not a web modal).

    **On desktop web (http://100.86.4.57:5184):**

    10. **Right-click context menus work:** Right-click a session item. Same menu appears. Right-click a message. Same menu. No swipe gestures visible.

    11. **Delete uses Radix AlertDialog:** Delete a session via right-click > Delete. Web-style Radix AlertDialog appears (not native action sheet).

    12. **No regressions:** Navigate between sessions, send messages, verify streaming works, check sidebar collapse/expand.
  </how-to-verify>
  <verify>Manual device testing -- see how-to-verify above</verify>
  <done>All 10 GESTURE requirements pass on real device and desktop web with no regressions.</done>
  <resume-signal>Type "approved" or describe issues for each numbered item above</resume-signal>
</task>

</tasks>

<verification>
- `cd /home/swd/loom/src && npx vitest run --reporter=verbose` -- full test suite green
- `cd /home/swd/loom/src && npx tsc --noEmit` -- TypeScript compiles clean
- All 10 GESTURE requirements verified on real device
- No regressions in desktop web behavior
</verification>

<success_criteria>
All Phase 67 gestures work on real iPhone 16 Pro Max: swipe-to-delete, pull-to-refresh, long-press context menus, haptic feedback, app lifecycle reconnect, native share/action sheets. Desktop web continues to work with right-click context menus and Radix dialogs. No gesture conflicts between swipe, long-press, and scroll.
</success_criteria>

<output>
After completion, create `.planning/phases/67-ios-native-gestures/67-04-SUMMARY.md`
</output>
