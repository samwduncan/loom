---
phase: 20-content-layout-tab-system
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/src/components/content-area/view/ContentArea.tsx
  - src/src/components/content-area/view/ContentArea.test.tsx
  - src/src/components/content-area/view/TabBar.tsx
  - src/src/components/content-area/view/TabBar.test.tsx
  - src/src/components/content-area/view/PanelPlaceholder.tsx
  - src/src/components/content-area/hooks/useTabKeyboardShortcuts.ts
  - src/src/components/content-area/hooks/useTabKeyboardShortcuts.test.ts
  - src/src/components/app-shell/AppShell.tsx
  - src/src/App.tsx
autonomous: true
requirements: [LAY-01, LAY-02, LAY-03, LAY-04, LAY-05, LAY-06, LAY-08]

must_haves:
  truths:
    - "User sees a tab bar with Chat, Files, Shell, Git tabs"
    - "Clicking a tab shows that panel and hides others"
    - "All four panels remain mounted in the DOM at all times"
    - "Cmd+1/2/3/4 switches between tabs"
    - "Tab bar is hidden on mobile (<768px)"
    - "A crash in one panel does not take down other panels"
  artifacts:
    - path: "src/src/components/content-area/view/ContentArea.tsx"
      provides: "Mount-once container with CSS show/hide for all panels"
      min_lines: 30
    - path: "src/src/components/content-area/view/TabBar.tsx"
      provides: "Horizontal tab bar with icons, labels, active indicator"
      min_lines: 30
    - path: "src/src/components/content-area/hooks/useTabKeyboardShortcuts.ts"
      provides: "Global Cmd+1/2/3/4 keyboard shortcut handler"
      min_lines: 15
    - path: "src/src/components/content-area/view/PanelPlaceholder.tsx"
      provides: "Placeholder stubs for Files, Shell, Git panels"
      min_lines: 10
    - path: "src/src/components/app-shell/AppShell.tsx"
      provides: "Updated shell replacing Outlet with ContentArea"
      contains: "ContentArea"
    - path: "src/src/App.tsx"
      provides: "Updated routing -- removes /dashboard, /settings routes"
      contains: "ContentArea"
  key_links:
    - from: "src/src/components/content-area/view/ContentArea.tsx"
      to: "src/src/stores/ui.ts"
      via: "useUIStore activeTab selector"
      pattern: "useUIStore.*activeTab"
    - from: "src/src/components/content-area/view/TabBar.tsx"
      to: "src/src/stores/ui.ts"
      via: "setActiveTab action"
      pattern: "setActiveTab"
    - from: "src/src/components/content-area/hooks/useTabKeyboardShortcuts.ts"
      to: "src/src/stores/ui.ts"
      via: "getState().setActiveTab"
      pattern: "useUIStore\\.getState\\(\\)\\.setActiveTab"
    - from: "src/src/components/app-shell/AppShell.tsx"
      to: "src/src/components/content-area/view/ContentArea.tsx"
      via: "direct render (replaces Outlet)"
      pattern: "<ContentArea"
    - from: "src/src/components/content-area/view/ContentArea.tsx"
      to: "src/src/components/chat/view/ChatView.tsx"
      via: "Routes/Route block for URL params"
      pattern: "Route.*ChatView"
---

<objective>
Build the tab bar, content area with CSS show/hide panel switching, keyboard shortcuts, and wire into AppShell.

Purpose: Transform Loom from a single-panel chat app into a multi-panel workspace. After this plan, users can switch between Chat, Files, Shell, and Git tabs while Chat preserves its URL routing and all panels survive tab switches.
Output: Working tab bar, CSS show/hide content area, keyboard shortcuts, placeholder panels for Files/Shell/Git.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-content-layout-tab-system/20-RESEARCH.md
@.planning/phases/20-content-layout-tab-system/20-01-SUMMARY.md

<interfaces>
<!-- Types and contracts from Plan 01 that this plan consumes -->

From src/src/types/ui.ts (updated by Plan 01):
```typescript
export type TabId = 'chat' | 'files' | 'shell' | 'git';
```

From src/src/stores/ui.ts:
```typescript
// useUIStore selectors:
// state.activeTab: TabId
// state.setActiveTab: (tab: TabId) => void
// useUIStore.getState().setActiveTab(tab) -- for non-React contexts
```

From src/src/components/shared/ErrorBoundary.tsx:
```typescript
export function PanelErrorBoundary({ children, panelName, onResetData }: PanelErrorBoundaryProps)
// panelName: string -- identifies the panel for error logging
```

From src/src/components/chat/view/ChatView.tsx:
```typescript
// Uses useParams<{ sessionId: string }>() from react-router-dom
// MUST be rendered inside a <Route> element to access URL params
```

From src/src/components/app-shell/AppShell.tsx (CURRENT -- will be modified):
```typescript
// Currently uses <Outlet /> for content -- will be replaced with <ContentArea />
// Grid layout: sidebar | content | artifact(0px)
```

From src/src/App.tsx (CURRENT -- will be modified):
```typescript
// Currently has /dashboard and /settings routes -- both removed
// /chat/:sessionId? route MUST survive (ChatView URL params)
// /dev/* routes stay untouched
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TabBar + PanelPlaceholder + keyboard shortcuts hook + tests</name>
  <files>
    src/src/components/content-area/view/TabBar.tsx,
    src/src/components/content-area/view/TabBar.test.tsx,
    src/src/components/content-area/view/PanelPlaceholder.tsx,
    src/src/components/content-area/hooks/useTabKeyboardShortcuts.ts,
    src/src/components/content-area/hooks/useTabKeyboardShortcuts.test.ts
  </files>
  <behavior>
    - Test (TabBar): Renders 4 tab buttons with correct labels (Chat, Files, Shell, Git)
    - Test (TabBar): Each tab button has role="tab" and correct aria-selected state
    - Test (TabBar): Clicking a tab calls setActiveTab with correct TabId
    - Test (TabBar): Active tab has visual differentiation (check className includes active-state class)
    - Test (TabBar): Tab bar has role="tablist" with aria-label
    - Test (keyboard): Cmd+1 fires setActiveTab('chat'), Cmd+2 fires setActiveTab('files'), etc.
    - Test (keyboard): Non-meta key presses (plain '1') do NOT trigger tab switch
    - Test (keyboard): Event with target inside [data-terminal] does NOT trigger tab switch
  </behavior>
  <action>
    1. Create `src/src/components/content-area/view/PanelPlaceholder.tsx`:
       - Takes `name: string` and `icon: React.ComponentType<{ className?: string }>` props
       - Renders centered flex column with large faded icon and "{name} panel coming soon" text
       - Uses design token classes (text-muted, opacity-30, etc.)

    2. Create `src/src/components/content-area/view/TabBar.tsx`:
       - Define TABS config array: `[{ id: 'chat', label: 'Chat', icon: MessageSquare, shortcut: '1' }, ...]`
       - Icons from lucide-react: MessageSquare (Chat), FolderTree (Files), Terminal (Shell), GitBranch (Git)
       - Render `<nav role="tablist" aria-label="Workspace panels">` with `hidden md:flex` for mobile hiding (LAY-06)
       - Each tab: `<button role="tab" aria-selected={active} aria-controls={panel-${id}}>` with onClick calling setActiveTab
       - Active state: `text-foreground bg-surface-raised` vs inactive `text-muted hover:text-foreground hover:bg-surface-raised/50`
       - Show keyboard shortcut hint `<kbd>` with Cmd symbol on lg+ screens
       - Tab bar height: `h-10` with `border-b border-border/8 bg-surface-base`
       - Use `cn()` for all className composition

    3. Create `src/src/components/content-area/hooks/useTabKeyboardShortcuts.ts`:
       - `useEffect` with `document.addEventListener('keydown', handler)`
       - Handler: check `e.metaKey || e.ctrlKey`, map e.key '1'-'4' to TabId
       - CRITICAL: Check `document.activeElement?.closest('[data-terminal]')` -- if true, return early (don't steal terminal shortcuts)
       - Also check `document.activeElement?.closest('[data-codemirror]')` for future-proofing (CodeMirror in Phase 24)
       - Call `useUIStore.getState().setActiveTab(tab)` and `e.preventDefault()`
       - Cleanup: remove event listener in useEffect return

    4. Create test files:
       - `TabBar.test.tsx`: Render TabBar with a mock activeTab, verify 4 buttons, aria attributes, click handlers. Mock useUIStore with vi.mock.
       - `useTabKeyboardShortcuts.test.ts`: Use renderHook, dispatch KeyboardEvent on document, verify setActiveTab called. Test terminal escape hatch by adding element with data-terminal attribute.

    Run: `cd /home/swd/loom/src && npx vitest run src/components/content-area/ -x`
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/components/content-area/ -x</automated>
  </verify>
  <done>TabBar renders 4 tabs with correct icons, labels, ARIA attributes. Keyboard shortcuts work for Cmd+1-4. Terminal escape hatch prevents shortcut theft. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContentArea with CSS show/hide + AppShell/App.tsx rewiring + tests</name>
  <files>
    src/src/components/content-area/view/ContentArea.tsx,
    src/src/components/content-area/view/ContentArea.test.tsx,
    src/src/components/app-shell/AppShell.tsx,
    src/src/App.tsx
  </files>
  <behavior>
    - Test (ContentArea): All four panel containers are in the DOM simultaneously
    - Test (ContentArea): Active panel has no 'hidden' class; inactive panels have 'hidden' class
    - Test (ContentArea): Each panel is wrapped in PanelErrorBoundary
    - Test (ContentArea): Switching activeTab changes which panel is visible
    - Test (ContentArea): ChatView is rendered inside a Route for URL param access
  </behavior>
  <action>
    1. Create `src/src/components/content-area/view/ContentArea.tsx`:
       - Import TabBar, useTabKeyboardShortcuts, ChatView, PanelPlaceholder, PanelErrorBoundary
       - Import lucide icons for placeholders: FolderTree, Terminal, GitBranch
       - Call `useTabKeyboardShortcuts()` at top of component
       - Read `activeTab` from `useUIStore((s) => s.activeTab)`
       - Render layout: TabBar at top, then a container div filling remaining height
       - Inside container, render 4 panel divs side by side (all always mounted):
         - Each div: `className={activeTab === tabId ? 'h-full' : 'hidden'}` with `id={panel-${tabId}}` and `role="tabpanel"`
         - Chat panel: Wrap in `<Routes><Route path="/chat/:sessionId?" element={<ChatView />} /><Route path="*" element={<ChatView />} /></Routes>` to preserve URL params. Use a nested Routes block (not the app-level one).
         - Files panel: `<PanelPlaceholder name="Files" icon={FolderTree} />`
         - Shell panel: `<PanelPlaceholder name="Shell" icon={Terminal} />`
         - Git panel: `<PanelPlaceholder name="Git" icon={GitBranch} />`
       - Each panel wrapped in its own `<PanelErrorBoundary panelName={tabId}>`

    2. Update `src/src/components/app-shell/AppShell.tsx`:
       - Remove `import { Outlet } from 'react-router-dom'`
       - Import `ContentArea` from `@/components/content-area/view/ContentArea`
       - Replace `<Outlet />` with `<ContentArea />`
       - Keep the existing PanelErrorBoundary wrapper around the main content area

    3. Update `src/src/App.tsx`:
       - Remove DashboardPlaceholder and SettingsPlaceholder functions
       - Remove PlaceholderView import
       - Remove `/dashboard` and `/settings` routes from the Route tree
       - Remove the `/chat/:sessionId?` child route from AppShell's layout route (ContentArea handles chat routing internally now)
       - Keep `<Route element={<AppShell />}>` as the layout route with just `<Route index element={<Navigate to="/chat" replace />} />` and a catch-all `<Route path="*" element={null} />` (ContentArea handles content)
       - Keep `/dev/tokens` and `/dev/proof-of-life` routes outside AppShell unchanged
       - Keep AppRoutes export for testing

    4. Create `src/src/components/content-area/view/ContentArea.test.tsx`:
       - Render ContentArea inside MemoryRouter
       - Mock useUIStore to control activeTab
       - Verify all 4 panel containers exist in DOM (mount-once)
       - Verify only active panel is visible (no 'hidden' class), others have 'hidden'
       - Verify PanelErrorBoundary wraps each panel (check by panelName prop or error boundary behavior)
       - Switch activeTab to 'files', verify chat panel gets 'hidden' and files panel becomes visible

    Run: `cd /home/swd/loom/src && npx vitest run --reporter=verbose`
    Also: `cd /home/swd/loom/src && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>ContentArea renders all 4 panels simultaneously with CSS show/hide. AppShell uses ContentArea instead of Outlet. App.tsx routes simplified (no dashboard/settings routes). ChatView still reads sessionId from URL params. All tests pass. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
cd /home/swd/loom/src && npx vitest run --reporter=verbose
cd /home/swd/loom/src && npx tsc --noEmit
# Manual: Resize browser to <768px -- tab bar should be hidden, only Chat visible
</verification>

<success_criteria>
- Tab bar visible with 4 tabs (Chat, Files, Shell, Git) with icons and labels
- Clicking a tab switches the visible panel; others remain in DOM but hidden
- Cmd+1/2/3/4 switches tabs (does not fire when terminal has focus)
- Tab bar hidden on viewports <768px
- Each panel wrapped in PanelErrorBoundary
- ChatView still reads sessionId from URL params
- /dashboard and /settings routes removed
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/20-content-layout-tab-system/20-02-SUMMARY.md`
</output>
