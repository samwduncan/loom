---
phase: 11-hardcoded-color-sweep
plan: 05
type: execute
wave: 4
depends_on: ["11-02", "11-03", "11-04"]
files_modified: []
autonomous: false
requirements:
  - COLR-01
  - COLR-02
  - COLR-03

must_haves:
  truths:
    - "COLR-01 grep returns zero matches: no bg-[#, text-[#, border-[# in any src/ file"
    - "COLR-02 grep returns zero matches: no bg-gray-, text-gray-, border-gray-, bg-slate-, text-slate-, border-slate-, bg-zinc-, bg-neutral- in any src/ file"
    - "Every visible surface renders the charcoal+rose palette with no warm brown, blue, or gray artifacts"
    - "Build succeeds with no Tailwind or CSS errors"
  artifacts: []
  key_links: []
---

<objective>
Run the definitive COLR-03 verification audit, fix any stragglers missed by Plans 02-04, and verify the build. This is the gate plan — Phase 11 is not complete until both COLR-01 and COLR-02 verification greps return zero matches.

Purpose: Ensures 100% palette consistency. The sweep plans (02, 03, 04) may miss edge cases — files not in the initial audit, dynamically constructed classes, or files touched by concurrent development. This plan catches everything.
Output: Clean audit with zero hardcoded color references. Build verification. Phase complete.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hardcoded-color-sweep/11-CONTEXT.md
@.planning/phases/11-hardcoded-color-sweep/11-02-SUMMARY.md
@.planning/phases/11-hardcoded-color-sweep/11-03-SUMMARY.md
@.planning/phases/11-hardcoded-color-sweep/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run COLR-03 full codebase audit and fix stragglers</name>
  <files>(any files with remaining matches)</files>
  <action>
Run the two definitive verification grep commands from the project root:

**COLR-01 audit (hardcoded hex in Tailwind classes):**
```bash
grep -rn 'bg-\[#\|text-\[#\|border-\[#\|fill-\[#\|ring-\[#' src/ \
  --include="*.tsx" --include="*.ts" --include="*.jsx" --include="*.js"
```

**COLR-02 audit (generic gray/slate/zinc/neutral):**
```bash
grep -rn 'bg-gray-\|text-gray-\|border-gray-\|bg-slate-\|text-slate-\|border-slate-\|bg-zinc-\|text-zinc-\|bg-neutral-\|text-neutral-\|border-zinc-\|border-neutral-' src/ \
  --include="*.tsx" --include="*.ts" --include="*.jsx" --include="*.js"
```

**If any matches are found:**
1. Identify the file and line
2. Apply the same mapping tables used in Plans 02-04
3. Fix each remaining instance
4. Re-run the audit to confirm zero

**Also check for any remaining bg-white dead code:**
```bash
grep -rn 'bg-white\b' src/ --include="*.tsx" --include="*.ts" --include="*.jsx" --include="*.js"
```
Fix any remaining instances.

**Known acceptable exceptions:**
- fileIcons.ts colored families (text-amber-500, text-blue-400, etc.) — these are NOT gray/slate/zinc so they pass COLR-02
- CSS files (src/index.css) — :root token definitions are not Tailwind utility classes
- If any instance is in a code string (e.g., a constant that stores a class name for dynamic use), map it the same way

**Build verification:**
```bash
npm run build
```
Must succeed. A broken Tailwind class reference may cause silent CSS omission but won't fail the build — visual verification is needed for that.

After the audit passes, run `npm run typecheck` if available to catch any TypeScript issues introduced by editing.
  </action>
  <verify>
    <automated>cd /home/swd/loom && COLR01=$(grep -r 'bg-\[#\|text-\[#\|border-\[#\|fill-\[#\|ring-\[#' src/ --include="*.tsx" --include="*.ts" --include="*.jsx" --include="*.js" | wc -l) && COLR02=$(grep -r 'bg-gray-\|text-gray-\|border-gray-\|bg-slate-\|text-slate-\|border-slate-\|bg-zinc-\|text-zinc-\|bg-neutral-\|text-neutral-\|border-zinc-\|border-neutral-' src/ --include="*.tsx" --include="*.ts" --include="*.jsx" --include="*.js" | wc -l) && echo "COLR-01: $COLR01 matches, COLR-02: $COLR02 matches" && test "$COLR01" -eq 0 && test "$COLR02" -eq 0 && echo "PASS: COLR-03 gate passed" || echo "FAIL: COLR-03 gate failed"</automated>
  </verify>
  <done>Both COLR-01 and COLR-02 verification greps return exactly zero matches across all src/ files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete hardcoded color sweep across the entire codebase. All 75+ files with generic color utilities have been migrated to semantic Tailwind tokens. The charcoal+rose palette should now render consistently across every surface — chat messages, tool cards, code blocks, diffs, status messages, shell, settings, code editor, sidebar, file tree, task manager, and all modals/dialogs.</what-built>
  <how-to-verify>
    1. Open http://100.86.4.57:5173 in your browser
    2. Check the main chat view — messages, code blocks, thinking disclosures should be charcoal+rose
    3. Open a tool call result — tool cards should use charcoal surfaces, not gray
    4. Check the sidebar — session items and project list should be charcoal
    5. Open settings — all tabs should be charcoal+rose with no gray/blue/white artifacts
    6. Check the shell/terminal wrapper — should be charcoal surface-base, not gray-900
    7. If TaskMaster is accessible, check task list, task detail, and creation wizard — should be charcoal+rose with rose buttons (not blue)
    8. Look for ANY remaining warm brown, bright blue, or generic gray artifacts — these are regressions
    9. Check hover states — should step up one surface tier (slightly lighter charcoal)
    10. Check focus states — inputs should show dusty rose focus glow ring
  </how-to-verify>
  <resume-signal>Type "approved" if all surfaces render charcoal+rose consistently, or describe any visual issues found</resume-signal>
</task>

</tasks>

<verification>
1. COLR-01 grep: `grep -r 'bg-\[#\|text-\[#\|border-\[#\|fill-\[#\|ring-\[#' src/ --include="*.tsx" --include="*.ts" --include="*.jsx" --include="*.js"` — zero matches
2. COLR-02 grep: `grep -r 'bg-gray-\|text-gray-\|border-gray-\|bg-slate-\|text-slate-\|border-slate-\|bg-zinc-\|text-zinc-\|bg-neutral-\|text-neutral-\|border-zinc-\|border-neutral-' src/ --include="*.tsx" --include="*.ts" --include="*.jsx" --include="*.js"` — zero matches
3. `npm run build` — succeeds
4. Human visual verification — all surfaces render charcoal+rose palette consistently
</verification>

<success_criteria>
- COLR-01: Zero hardcoded hex values in Tailwind arbitrary classes across entire src/
- COLR-02: Zero generic gray/slate/zinc/neutral Tailwind utilities across entire src/
- COLR-03: Both audits pass — 100% palette consistency confirmed by grep + human visual check
- Build succeeds with no errors
- Human approves visual consistency across all app surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/11-hardcoded-color-sweep/11-05-SUMMARY.md`
</output>
