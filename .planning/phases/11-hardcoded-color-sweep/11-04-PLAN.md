---
phase: 11-hardcoded-color-sweep
plan: 04
type: execute
wave: 3
depends_on: ["11-01"]
files_modified:
  - src/components/TaskList.jsx
  - src/components/ProjectCreationWizard.jsx
  - src/components/NextTaskBanner.jsx
  - src/components/QuickSettingsPanel.jsx
  - src/components/TaskDetail.jsx
  - src/components/TaskMasterSetupWizard.jsx
  - src/components/PRDEditor.jsx
  - src/components/TaskCard.jsx
  - src/components/LoginModal.jsx
  - src/components/CreateTaskModal.jsx
  - src/components/TodoList.jsx
  - src/components/TaskMasterStatus.jsx
  - src/components/Onboarding.jsx
  - src/components/TaskIndicator.jsx
  - src/components/Tooltip.jsx
  - src/components/GeminiStatus.jsx
autonomous: true
requirements:
  - COLR-02
  - COLR-03

must_haves:
  truths:
    - "Zero gray/slate/zinc/neutral Tailwind utilities remain in any JSX component file"
    - "Zero blue/red/green/amber generic Tailwind utilities remain in TaskMaster JSX files — all mapped to semantic status tokens or primary/destructive"
    - "All light-mode dead code removed from dual-class patterns in JSX files"
    - "TaskMaster UI renders with charcoal+rose palette — no blue/gray/white artifacts"
  artifacts:
    - path: "src/components/TaskList.jsx"
      provides: "Task list with semantic token styling"
      contains: "surface-"
    - path: "src/components/ProjectCreationWizard.jsx"
      provides: "PRD wizard with semantic tokens"
      contains: "surface-"
  key_links:
    - from: "src/components/*.jsx"
      to: "tailwind.config.js colors"
      via: "Tailwind utility classes"
      pattern: "surface-base|surface-raised|primary|status-"
---

<objective>
Replace all generic color utilities (gray/slate/zinc + blue/red/green/amber families) in TaskMaster JSX files and remaining root-level JSX components. These ~16 files contain ~600+ gray instances and ~400+ colored-family instances, mostly in dual light+dark class pairs.

Purpose: The TaskMaster UI is a major panel within Loom. Leaving it unstyled would create a jarring visual seam against the charcoal+rose main interface. This plan completes the gray sweep.
Output: All JSX files use semantic Tailwind tokens. Zero generic color utilities remaining in the entire src/ tree (modulo intentional file-icon colors in fileIcons.ts).
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hardcoded-color-sweep/11-CONTEXT.md
@.planning/phases/11-hardcoded-color-sweep/11-RESEARCH.md
@.planning/phases/11-hardcoded-color-sweep/11-01-SUMMARY.md

@src/index.css
@tailwind.config.js

<interfaces>
<!-- Same core mapping tables, plus TaskMaster-specific patterns -->

GRAY UTILITY MAPPING:
| Old | Replacement |
|-----|------------|
| bg-gray-950 / bg-gray-900 / bg-slate-900 | bg-surface-base |
| bg-gray-800 / bg-slate-800 | bg-surface-raised |
| bg-gray-700 / bg-slate-700 | bg-surface-elevated |
| bg-gray-600 / bg-gray-500 | bg-muted |
| text-gray-100 / text-white | text-foreground |
| text-gray-200 / text-gray-300 | text-foreground-secondary |
| text-gray-400 / text-gray-500 | text-muted-foreground |
| border-gray-700 / border-slate-700 / border-gray-600 | border-border/10 |

DEAD CODE (DELETE) -- Critical for JSX files:
| Pattern | Reason |
|---------|--------|
| bg-white, bg-gray-50, bg-gray-100, bg-gray-200 | Light-mode dead code (especially prevalent in JSX) |
| text-gray-600, text-gray-700, text-gray-800, text-gray-900 | Light-mode dead code |
| border-gray-200, border-gray-300, border-gray-400 | Light-mode dead code |

TASKMASTER BLUE BUTTONS:
| Old | Replacement | Rationale |
|-----|------------|-----------|
| bg-blue-600 hover:bg-blue-700 | bg-primary hover:bg-primary/90 | Task manager uses Loom's accent (dusty rose) |
| bg-blue-500 | bg-primary | Primary action |
| text-blue-400 / text-blue-500 | text-status-info | Informational text/links |
| bg-blue-500/10 / bg-blue-900/50 | bg-status-info/10 | Info backgrounds |
| border-blue-500 | border-primary | Primary border accent |
| ring-blue-500 | ring-primary | Focus rings |

TASKMASTER STATUS COLORS:
| Old | Replacement |
|-----|------------|
| bg-red-500/10, text-red-400, border-red-500 | bg-status-error/10, text-status-error, border-status-error |
| bg-green-500/10, text-green-400, border-green-500 | bg-status-connected/10, text-status-connected, border-status-connected |
| bg-amber-500/10, text-amber-400, border-amber-500 | bg-status-reconnecting/10, text-status-reconnecting, border-status-reconnecting |
| bg-purple-500/10, text-purple-400, border-purple-500 | bg-primary/10, text-primary, border-primary |

DUAL-CLASS REMOVAL PATTERN (very common in JSX):
```jsx
// BEFORE (TaskMaster files have paired light+dark without dark: prefix):
className="bg-white bg-gray-900 border border-gray-200 border-gray-700 text-gray-900 text-gray-100"

// AFTER (delete light, map dark):
className="bg-surface-base border border-border/10 text-foreground"
```

The light-mode class counts in TaskMaster files are massive (50+ bg-white in TaskList.jsx alone). DELETE all of them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep top-7 TaskMaster JSX files (highest instance counts)</name>
  <files>
    src/components/TaskList.jsx,
    src/components/ProjectCreationWizard.jsx,
    src/components/NextTaskBanner.jsx,
    src/components/QuickSettingsPanel.jsx,
    src/components/TaskDetail.jsx,
    src/components/TaskMasterSetupWizard.jsx,
    src/components/PRDEditor.jsx
  </files>
  <action>
Replace all generic color utilities in the 7 heaviest TaskMaster JSX files. These files are dense — combined ~380 gray instances + ~280 colored-family instances + ~150 light-mode dead classes.

**Work each file systematically:**

1. **TaskList.jsx** (86 gray + 53 blue/red/green + 51 white dead code = ~190 changes)
   - This is the single largest file. Work methodically through every className.
   - Task status badges: Map blue/green/red/amber to status-* tokens
   - List backgrounds: bg-gray-900 → bg-surface-base, bg-gray-800 → bg-surface-raised
   - All bg-white instances: DELETE (dead code)
   - All text-gray-900 instances: DELETE (dead code)
   - Button styles: bg-blue-600 → bg-primary

2. **ProjectCreationWizard.jsx** (65 gray + 27 blue/red/green + 22 white dead code)
   - Wizard steps, form fields, buttons
   - PRD generation buttons: blue → primary (rose)
   - Step indicators: Map to surface tiers

3. **NextTaskBanner.jsx** (50 gray + 29 blue/red/green + 24 white dead code)
   - Banner background to surface-raised
   - Status indicators to status-* tokens

4. **QuickSettingsPanel.jsx** (48 gray + 10 blue/red/green + 10 white dead code)
   - Panel background to surface-raised
   - Toggle states: green active → status-connected, gray inactive → muted

5. **TaskDetail.jsx** (44 gray + 12 blue/red/green + 6 white dead code)
   - Detail view backgrounds and text
   - Status badges to status-* tokens

6. **TaskMasterSetupWizard.jsx** (36 gray + 37 blue/red/green + 14 white dead code)
   - Setup wizard UI — heavy on blue action buttons → primary (rose)

7. **PRDEditor.jsx** (34 gray + 0 color + 22 white dead code)
   - Editor surfaces and borders

**CRITICAL for all files:** Do not just find-replace gray-900→surface-base blindly. Read each className string, identify the full set of classes (light + dark pairs), delete light, map dark. Check hover states step up one tier.
  </action>
  <verify>
    <automated>grep -r 'bg-gray-\|text-gray-\|border-gray-\|bg-slate-\|text-slate-\|bg-white\b\|bg-blue-\|text-blue-\|border-blue-\|bg-red-\|text-red-\|bg-green-\|text-green-\|bg-amber-\|text-amber-' src/components/TaskList.jsx src/components/ProjectCreationWizard.jsx src/components/NextTaskBanner.jsx src/components/QuickSettingsPanel.jsx src/components/TaskDetail.jsx src/components/TaskMasterSetupWizard.jsx src/components/PRDEditor.jsx | wc -l | xargs -I{} test {} -eq 0 && echo "PASS: zero generic colors in top-7 JSX" || echo "FAIL"</automated>
  </verify>
  <done>Top 7 TaskMaster JSX files have zero gray/slate/zinc/blue/red/green/amber generic utilities. All dead light-mode code removed. All surfaces use semantic charcoal+rose tokens.</done>
  <depth>
    - Dead code volume: ~150+ bg-white/text-gray-900 instances removed across 7 files
    - Blue button migration: All bg-blue-* CTAs become bg-primary (dusty rose)
    - Status consistency: Error=red→status-error, success=green→status-connected, warning=amber→status-reconnecting, info=blue→status-info
    - Hover correctness: Each hover state verified as one tier above its resting surface
    - Opacity preservation: /N suffixes maintained on all replacements
  </depth>
</task>

<task type="auto">
  <name>Task 2: Sweep remaining JSX files and miscellaneous components</name>
  <files>
    src/components/TaskCard.jsx,
    src/components/LoginModal.jsx,
    src/components/CreateTaskModal.jsx,
    src/components/TodoList.jsx,
    src/components/TaskMasterStatus.jsx,
    src/components/Onboarding.jsx,
    src/components/TaskIndicator.jsx,
    src/components/Tooltip.jsx,
    src/components/GeminiStatus.jsx
  </files>
  <action>
Replace all generic color utilities in the remaining 9 JSX component files. These are smaller files (~140 gray+color instances total).

**File-by-file:**

1. **TaskCard.jsx** (17 gray + 25 blue/red/green) — Task card component with status-colored borders and badges
2. **LoginModal.jsx** (12 gray) — Login form backgrounds and inputs
3. **CreateTaskModal.jsx** (10 gray + 11 blue/red/green) — Task creation modal
4. **TodoList.jsx** (6 gray) — Simple todo list
5. **TaskMasterStatus.jsx** (5 gray) — Status indicator
6. **Onboarding.jsx** (3 gray + 18 blue/red/green) — Onboarding wizard, heavy blue buttons → primary
7. **TaskIndicator.jsx** (2 gray) — Small indicator component
8. **Tooltip.jsx** (1 gray) — Tooltip styling
9. **GeminiStatus.jsx** (2 gray) — Gemini status dot

Apply the same mapping tables and dual-class removal pattern. For smaller files, the entire component can likely be scanned in one pass.

**Special attention for TaskCard.jsx and CreateTaskModal.jsx:** These have heavy blue/red/green usage for task status styling. Map consistently to status-* tokens.

**Onboarding.jsx:** 18 colored instances — mostly blue action buttons and green success states. Map to primary and status-connected.

After completing all files, run the full COLR-02 verification grep across ALL of src/ to identify any files missed by this plan or Plan 02/03.
  </action>
  <verify>
    <automated>grep -r 'bg-gray-\|text-gray-\|border-gray-\|bg-slate-\|text-slate-\|bg-white\b' src/components/TaskCard.jsx src/components/LoginModal.jsx src/components/CreateTaskModal.jsx src/components/TodoList.jsx src/components/TaskMasterStatus.jsx src/components/Onboarding.jsx src/components/TaskIndicator.jsx src/components/Tooltip.jsx src/components/GeminiStatus.jsx 2>/dev/null | wc -l | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 9 remaining JSX files have zero generic color utilities. TaskMaster UI renders entirely with semantic charcoal+rose tokens.</done>
</task>

</tasks>

<verification>
1. `grep -r 'bg-gray-\|text-gray-\|border-gray-\|bg-slate-\|text-slate-\|border-slate-\|bg-zinc-\|text-zinc-\|bg-neutral-\|text-neutral-' src/ --include="*.jsx" | wc -l` — zero matches
2. `grep -r 'bg-blue-\|text-blue-\|bg-red-\|text-red-\|bg-green-\|text-green-\|bg-amber-\|text-amber-' src/ --include="*.jsx" | wc -l` — zero matches (all JSX files migrated)
3. `npm run build` — succeeds
</verification>

<success_criteria>
- All 16 JSX component files converted to semantic tokens
- Zero gray/slate/zinc/neutral utilities in JSX files
- Zero generic blue/red/green/amber in JSX files (all mapped to status-* or primary)
- All light-mode dead code removed
- TaskMaster panels render with charcoal+rose palette
</success_criteria>

<output>
After completion, create `.planning/phases/11-hardcoded-color-sweep/11-04-SUMMARY.md`
</output>
