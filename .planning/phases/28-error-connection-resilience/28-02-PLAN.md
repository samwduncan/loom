---
phase: 28-error-connection-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/hooks/useNavigateAwayGuard.ts
  - src/src/hooks/useNavigateAwayGuard.test.ts
  - src/src/components/chat/view/ChatView.tsx
  - src/src/lib/websocket-client.ts
  - src/src/lib/websocket-client.test.ts
autonomous: true
requirements:
  - ERR-03
  - ERR-04

must_haves:
  truths:
    - "WebSocket auto-reconnects with exponential backoff after disconnection"
    - "After reconnection, user can continue working without page refresh"
    - "Closing the tab during active streaming triggers a browser confirmation dialog"
    - "Navigate-away guard does NOT trigger when not streaming"
  artifacts:
    - path: "src/src/hooks/useNavigateAwayGuard.ts"
      provides: "beforeunload guard tied to streaming state"
      min_lines: 15
    - path: "src/src/hooks/useNavigateAwayGuard.test.ts"
      provides: "Tests for navigate-away guard"
      min_lines: 30
    - path: "src/src/lib/websocket-client.test.ts"
      provides: "Tests for reconnection behavior"
      min_lines: 40
  key_links:
    - from: "src/src/hooks/useNavigateAwayGuard.ts"
      to: "useStreamStore"
      via: "selector subscription"
      pattern: "useStreamStore.*isStreaming"
    - from: "src/src/components/chat/view/ChatView.tsx"
      to: "useNavigateAwayGuard"
      via: "hook call"
      pattern: "useNavigateAwayGuard"
    - from: "src/src/lib/websocket-client.ts"
      to: "WebSocket"
      via: "scheduleReconnect on abnormal close"
      pattern: "scheduleReconnect"
---

<objective>
Harden WebSocket reconnection behavior and add navigate-away protection during streaming.

Purpose: ERR-03 requires reliable auto-reconnect (already partially implemented -- needs test coverage and post-reconnect state sync). ERR-04 prevents accidental data loss by warning before tab close during active streaming.
Output: useNavigateAwayGuard hook, hardened websocket-client with tests, ChatView wired with guard
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/src/lib/websocket-client.ts:
```typescript
export class WebSocketClient {
  connect(token: string): void;
  disconnect(): void;
  send(msg: ClientMessage): boolean;
  getState(): ConnectionState;
  // Already has: scheduleReconnect() with exponential backoff (1s * 2^attempts, max 30s)
  // Already has: handleOpen() sends 'get-active-sessions' on reconnect
  // Already has: handleClose() calls scheduleReconnect when code !== 1000
}
export const wsClient: WebSocketClient;
```

From src/src/stores/stream.ts:
```typescript
export const useStreamStore = create<StreamState>()(...)
// Key selector: state.isStreaming
```

From src/src/components/chat/view/ChatView.tsx:
```typescript
// Main chat view component -- useNavigateAwayGuard() should be called here
// since this is where streaming happens
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useNavigateAwayGuard hook with tests</name>
  <files>
    src/src/hooks/useNavigateAwayGuard.ts,
    src/src/hooks/useNavigateAwayGuard.test.ts
  </files>
  <behavior>
    - When isStreaming is true, beforeunload event handler is registered that sets event.returnValue (triggers browser confirm dialog)
    - When isStreaming is false, no beforeunload handler is registered
    - When isStreaming transitions from true to false, handler is removed (cleanup)
    - Hook accepts no arguments, returns nothing
  </behavior>
  <action>
    Create useNavigateAwayGuard.ts:
    - Import useEffect from react, useStreamStore from stores/stream
    - Subscribe to `useStreamStore(s => s.isStreaming)`
    - In useEffect: if isStreaming, add beforeunload listener that sets `event.returnValue = 'You have an active streaming session. Are you sure you want to leave?'` and returns the same string (cross-browser compat)
    - Return cleanup that removes the listener
    - Dependency array: [isStreaming]
    - Named export only, no default export

    Create useNavigateAwayGuard.test.ts:
    - Mock useStreamStore with vitest
    - Test 1: When isStreaming=false, no beforeunload listener added
    - Test 2: When isStreaming=true, beforeunload listener added, event.returnValue is set
    - Test 3: When isStreaming transitions true->false, listener removed
    - Use renderHook from @testing-library/react
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/hooks/useNavigateAwayGuard.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Hook registers beforeunload only during active streaming
    - Hook cleans up listener when streaming stops or component unmounts
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebSocket reconnect tests and ChatView wiring</name>
  <files>
    src/src/lib/websocket-client.test.ts,
    src/src/lib/websocket-client.ts,
    src/src/components/chat/view/ChatView.tsx
  </files>
  <behavior>
    - WebSocketClient.scheduleReconnect uses exponential backoff: delay = min(1000 * 2^attempts, 30000)
    - After reconnect success (handleOpen), reconnectAttempts resets to 0
    - handleOpen sends 'get-active-sessions' to sync state
    - WebSocketClient cleans up old WS handlers before creating new connection in reconnect()
  </behavior>
  <action>
    Create/update websocket-client.test.ts:
    - Mock WebSocket class globally
    - Test: scheduleReconnect calculates correct delays (1s, 2s, 4s, 8s, 16s, 30s cap)
    - Test: successful reconnect resets attempts to 0
    - Test: handleOpen sends get-active-sessions message
    - Test: disconnect clears reconnect timer
    - Test: clean close (code 1000) does NOT trigger reconnect

    Update websocket-client.ts (minor hardening):
    - In reconnect(), null out old ws event handlers before creating new WebSocket (prevent ghost handlers on stale reference if GC is slow): add `if (this.ws) { this.ws.onopen = null; ... }` before creating new WS
    - This is a small defensive fix -- the current code works but could theoretically have a race

    Update ChatView.tsx:
    - Import useNavigateAwayGuard from '@/hooks/useNavigateAwayGuard'
    - Call useNavigateAwayGuard() at the top of the component (no args, no return value)
    - Single line addition

    Constitution: Named exports (2.2), no default export, selector-only store access (4.2).
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/lib/websocket-client.test.ts src/src/hooks/useNavigateAwayGuard.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - WebSocket reconnection behavior has test coverage for backoff timing, reset, and state sync
    - Old WS handlers cleaned up before reconnect attempt
    - ChatView calls useNavigateAwayGuard, protecting against accidental tab close during streaming
    - TypeScript compiles, all tests pass
  </done>
</task>

</tasks>

<verification>
- `cd /home/swd/loom/src && npx vitest run --reporter=verbose` -- all tests pass
- `cd /home/swd/loom/src && npx tsc --noEmit` -- no type errors
- `cd /home/swd/loom/src && npx eslint src/src/hooks/useNavigateAwayGuard.ts src/src/lib/websocket-client.ts` -- no lint errors
</verification>

<success_criteria>
- WebSocket reconnects automatically with exponential backoff (1s, 2s, 4s... capped at 30s) -- covered by tests
- After reconnection, get-active-sessions is sent to re-sync -- covered by tests
- Browser shows confirmation dialog when closing tab during active streaming
- No confirmation dialog when not streaming
- All new and existing tests pass, TypeScript compiles, ESLint clean
</success_criteria>

<output>
After completion, create `.planning/phases/28-error-connection-resilience/28-02-SUMMARY.md`
</output>
