---
phase: 17-streaming-status
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/chat/view/subcomponents/PreTokenIndicator.tsx
  - src/components/chat/view/subcomponents/ThinkingShimmer.tsx
  - src/components/chat/view/subcomponents/ReconnectSkeletons.tsx
  - src/components/chat/styles/aurora-shimmer.css
autonomous: true
requirements:
  - STRM-04
  - STRM-05
  - STRM-06

must_haves:
  truths:
    - "Before the first token arrives, an atmospheric aurora glow field appears -- not just a bar but diffused light shining down"
    - "Thinking... text with rainbow gradient sits above the aurora field, visually connected but separate elements"
    - "When streaming content arrives, the aurora fades upward and dissolves -- no layout jump, no grid squish"
    - "Reconnect skeleton placeholders match the atmospheric aurora treatment"
    - "Extended thinking transitions into collapsed thinking block when complete"
  artifacts:
    - path: "src/components/chat/view/subcomponents/PreTokenIndicator.tsx"
      provides: "Atmospheric aurora glow field with fade-up exit"
      contains: "aurora-atmosphere"
    - path: "src/components/chat/view/subcomponents/ThinkingShimmer.tsx"
      provides: "Thinking text above aurora, transitions to collapsed block"
      contains: "Thinking"
    - path: "src/components/chat/view/subcomponents/ReconnectSkeletons.tsx"
      provides: "Aurora-matched skeleton placeholders"
      contains: "aurora-atmosphere"
    - path: "src/components/chat/styles/aurora-shimmer.css"
      provides: "Atmospheric aurora CSS with fade-up dissolve keyframes"
      contains: "aurora-atmosphere"
  key_links:
    - from: "src/components/chat/view/subcomponents/PreTokenIndicator.tsx"
      to: "aurora-shimmer.css"
      via: "CSS class references"
      pattern: "aurora-atmosphere"
    - from: "src/components/chat/view/subcomponents/ReconnectSkeletons.tsx"
      to: "aurora-shimmer.css"
      via: "CSS class references"
      pattern: "aurora-atmosphere"
---

<objective>
Upgrade the pre-token indicator to an atmospheric aurora glow field, wire thinking indicator transitions, and unify reconnect skeletons with the same aurora treatment.

Purpose: The aurora atmosphere is the signature visual of Loom's streaming experience -- soft, animated light that signals AI activity without being mechanical.
Output: Three upgraded indicator components sharing a consistent atmospheric aurora CSS system.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-streaming-status/17-CONTEXT.md
@.planning/phases/17-streaming-status/17-RESEARCH.md

@src/components/chat/view/subcomponents/PreTokenIndicator.tsx
@src/components/chat/view/subcomponents/ThinkingShimmer.tsx
@src/components/chat/view/subcomponents/ReconnectSkeletons.tsx
@src/components/chat/styles/aurora-shimmer.css

<interfaces>
From src/components/chat/view/subcomponents/PreTokenIndicator.tsx:
```typescript
interface PreTokenIndicatorProps {
  isVisible: boolean;
  // Uses grid 1fr/0fr collapse for exit animation currently
}
```

From src/components/chat/view/subcomponents/ThinkingShimmer.tsx:
```typescript
interface ThinkingShimmerProps {
  isThinking: boolean;
  // Currently standalone "Thinking..." with aurora text gradient
}
```

From src/components/chat/view/subcomponents/ReconnectSkeletons.tsx:
```typescript
interface ReconnectSkeletonsProps {
  count: number;
  isLoading: boolean;
  // Aurora aura bars with fade-out on load complete
}
```

From aurora-shimmer.css:
- Uses @property --aurora-angle for compositor-thread rotation
- Existing .aurora-aura class with gradient and blur
- aurora-rotate keyframe already defined
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create atmospheric aurora CSS and upgrade PreTokenIndicator</name>
  <files>src/components/chat/styles/aurora-shimmer.css, src/components/chat/view/subcomponents/PreTokenIndicator.tsx</files>
  <action>
**aurora-shimmer.css -- add atmospheric classes:**
- Add `.aurora-atmosphere` class: `position: relative; height: 48px; overflow: hidden;` (taller than current 16px bar for diffused feel).
- Add `::before` pseudo-element: the primary glow layer. Uses existing `--aurora-angle` property for rotation. `filter: blur(20px); opacity: 0.3;` with the existing aurora gradient.
- Add `::after` pseudo-element: secondary glow layer. Same gradient but `filter: blur(8px); opacity: 0.15; animation-delay: -1s;` for layered depth.
- Both pseudo-elements animate with `aurora-rotate 4s linear infinite`. Add a subtle `aurora-pulse` keyframe that oscillates opacity from 0.8 to 1.0 over 2s ease-in-out.
- Add `.aurora-atmosphere-exit` class: `animation: aurora-dissolve-up 300ms ease-out forwards;`
- Add `@keyframes aurora-dissolve-up`: from `{ opacity: 1; transform: translateY(0); }` to `{ opacity: 0; transform: translateY(-16px); }`.
- Keep all existing classes intact (other components may use them).

**PreTokenIndicator.tsx -- atmospheric upgrade:**
- Replace the current grid-based collapse with the atmospheric aurora approach.
- When `isVisible` becomes true: render a div with `aurora-atmosphere` class. No grid wrapper needed.
- When `isVisible` becomes false: add `aurora-atmosphere-exit` class. After 300ms animation completes, unmount.
- Remove the grid `1fr -> 0fr` exit pattern entirely (per research: mixing grid collapse with fade-up creates weird squish-then-fade).
- The indicator should feel like "aurora lights shining down onto where the AI response will appear" -- atmospheric, not a loading bar.
  </action>
  <verify>
    <automated>cd /home/swd/loom && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>PreTokenIndicator renders atmospheric aurora glow field. Exit is fade-up dissolve, not grid collapse. Aurora CSS classes added to aurora-shimmer.css.</done>
  <depth>
    - Aurora layers use compositor-thread animation via @property --aurora-angle (no main thread competition with streaming)
    - Exit animation timing: 300ms matches the fade-up dissolve keyframe duration
    - No layout jump: the aurora div has fixed height, content pushes in below/through it as it fades up
    - Existing aurora-aura classes preserved for backward compatibility
  </depth>
</task>

<task type="auto">
  <name>Task 2: Upgrade ThinkingShimmer transition + ReconnectSkeletons aurora match</name>
  <files>src/components/chat/view/subcomponents/ThinkingShimmer.tsx, src/components/chat/view/subcomponents/ReconnectSkeletons.tsx</files>
  <action>
**ThinkingShimmer.tsx:**
- "Thinking..." text with rainbow gradient sits ABOVE the aurora atmosphere field -- two separate elements but visually connected per user decision.
- The ThinkingShimmer component renders the "Thinking..." text. Ensure it is positioned to appear above (before in DOM order) the PreTokenIndicator aurora field. These are sibling elements in ChatMessagesPane -- verify the render order places ThinkingShimmer first.
- When extended thinking completes (isThinking goes false), the text should fade out. The transition to collapsed thinking disclosure block is handled by the existing thinking content rendering in the message list -- ThinkingShimmer just needs to cleanly exit.
- Keep the existing aurora text gradient on the "Thinking..." text (rainbow shimmer effect).
- Ensure the exit animation is a simple opacity fade (200-300ms) that doesn't compete with the PreTokenIndicator's fade-up.

**ReconnectSkeletons.tsx:**
- Replace the current aurora-aura bar styling with the new `.aurora-atmosphere` treatment to match PreTokenIndicator.
- Each skeleton bar should use the atmospheric aurora glow instead of the previous thinner bar style.
- Keep the staggered entry animation (each bar slightly delayed).
- When `isLoading` becomes false, use the same `aurora-atmosphere-exit` fade-up dissolve as PreTokenIndicator for consistency.
- The count of skeleton bars stays configurable via props.
  </action>
  <verify>
    <automated>cd /home/swd/loom && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ThinkingShimmer renders above aurora field with clean exit. ReconnectSkeletons use atmospheric aurora matching PreTokenIndicator. All three indicator components share consistent visual language.</done>
  <depth>
    - ThinkingShimmer + PreTokenIndicator render order in ChatMessagesPane: Thinking text first, aurora field below
    - ReconnectSkeletons: stagger delay preserved across bars
    - Both exit animations use aurora-dissolve-up for visual consistency
    - No z-index conflicts between ThinkingShimmer text and PreTokenIndicator glow
  </depth>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- aurora-shimmer.css contains `.aurora-atmosphere`, `.aurora-atmosphere-exit`, `aurora-dissolve-up` keyframe
- PreTokenIndicator uses aurora-atmosphere class, not grid collapse
- ThinkingShimmer has clean fade-out exit
- ReconnectSkeletons reference aurora-atmosphere classes
</verification>

<success_criteria>
All three indicator components (pre-token, thinking, reconnect) share the atmospheric aurora visual language. Pre-token aurora fades up and dissolves when content arrives. Thinking text sits above aurora field. Reconnect skeletons match the same treatment.
</success_criteria>

<output>
After completion, create `.planning/phases/17-streaming-status/17-02-SUMMARY.md`
</output>
