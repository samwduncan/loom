---
phase: 17-streaming-status
plan: 04
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-03
files_modified:
  - src/components/chat/view/subcomponents/ErrorBanner.tsx
  - src/components/chat/view/subcomponents/ChatMessagesPane.tsx
  - src/components/chat/view/subcomponents/ToolActionCard.tsx
  - src/components/chat/hooks/useChatRealtimeHandlers.ts
autonomous: true
requirements:
  - STRM-08

must_haves:
  truths:
    - "Permanent errors (process crash, exit code) display as persistent inline banners with red accent after the last message"
    - "Error banners are visually distinct from toasts and regular messages"
    - "Error banners are dismissible with an X button"
  artifacts:
    - path: "src/components/chat/view/subcomponents/ErrorBanner.tsx"
      provides: "Inline persistent error banner component"
      exports: ["default"]
      contains: "destructive"
    - path: "src/components/chat/view/subcomponents/ChatMessagesPane.tsx"
      provides: "ErrorBanner rendered inline after last message"
      contains: "ErrorBanner"
  key_links:
    - from: "src/components/chat/view/subcomponents/ChatMessagesPane.tsx"
      to: "ErrorBanner"
      via: "Rendered conditionally after message list"
      pattern: "ErrorBanner"
    - from: "src/components/chat/view/subcomponents/ErrorBanner.tsx"
      to: "CSS --destructive token"
      via: "Background/border color"
      pattern: "destructive"
---

<objective>
Create persistent inline error banners for permanent errors (process crash, exit code) and add pulsing border glow to the active tool card during streaming.

Purpose: Permanent errors need persistent, prominent display -- not transient toasts that disappear. Active tool cards need visual feedback showing which tool is currently running.
Output: New ErrorBanner component integrated into ChatMessagesPane, active tool card border glow.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-streaming-status/17-CONTEXT.md
@.planning/phases/17-streaming-status/17-RESEARCH.md
@.planning/phases/17-streaming-status/17-01-SUMMARY.md
@.planning/phases/17-streaming-status/17-03-SUMMARY.md

@src/components/chat/view/subcomponents/ChatMessagesPane.tsx
@src/components/chat/view/subcomponents/ToolActionCard.tsx
@src/components/chat/hooks/useChatRealtimeHandlers.ts

<interfaces>
From ChatMessagesPane.tsx:
```typescript
interface ChatMessagesPaneProps {
  chatMessages: ChatMessage[];
  isLoading: boolean;
  // ... many other props
}
// ErrorBanner should render after the message list, before the scroll container ends
```

From useChatRealtimeHandlers.ts:
```typescript
// Error detection points:
// - exitCode in session events
// - error type messages
// - process crash signals
```

From ToolActionCard.tsx:
```typescript
// Phase 15 tool card component with pill-styled state-driven borders
// Has status/state prop indicating running/complete/error
// Add pulsing border glow when status === 'running'
```

From index.css design tokens:
```css
--destructive: 0 50% 50%;            /* #bf4040 -- muted red */
--destructive-foreground: 0 10% 88%;  /* #e4dbd9 -- warm white */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ErrorBanner component + integrate into ChatMessagesPane + tool card glow</name>
  <files>src/components/chat/view/subcomponents/ErrorBanner.tsx, src/components/chat/view/subcomponents/ChatMessagesPane.tsx, src/components/chat/view/subcomponents/ToolActionCard.tsx, src/components/chat/hooks/useChatRealtimeHandlers.ts</files>
  <action>
**ErrorBanner.tsx -- new component:**
- Props: `{ message: string; type: 'crash' | 'exit' | 'error'; onDismiss: () => void; }`.
- Renders an inline banner with:
  - `bg-destructive/10` (muted red at 10% opacity) background.
  - `border-l-2 border-destructive` left accent border (matching the persistent/important visual language).
  - An error icon (lucide-react `AlertTriangle` or `XCircle`) in destructive color.
  - Error message text in `text-foreground` (not destructive -- readability first).
  - Dismiss X button on the right (lucide-react `X` icon), calls `onDismiss`.
- The banner should feel persistent and important -- NOT like a toast. Full width within the message rail, rounded corners, padding matching message density.
- Entry animation: subtle `opacity 0->1 + translateY(4px)->0` over 200ms.

**ChatMessagesPane.tsx -- error banner placement:**
- Add state: `const [permanentError, setPermanentError] = useState<{ message: string; type: string } | null>(null);`
- Detect permanent errors from the chat messages or session state:
  - Check if the last message or session indicates a process crash or non-zero exit code.
  - The error state should be set when `useChatRealtimeHandlers` processes an error event (exit code, crash). This may require threading a callback or adding the error to a shared state.
  - Look for existing error handling patterns: session error states, exit codes in tool results.
- Render `<ErrorBanner />` at the bottom of the message list (after the last message, before the scroll container ends) when `permanentError` is set.
- `onDismiss` clears `permanentError` state.
- The banner renders INSIDE the scroll container so it scrolls with messages -- it is part of the conversation, not a floating overlay.

**ToolActionCard.tsx -- active tool card pulsing border:**
- In the existing ToolActionCard component (from Phase 15), add a pulsing border glow when the tool is in "running" state.
- Use `box-shadow: 0 0 0 1px hsl(var(--primary) / 0.3)` with a `pulse` animation that oscillates the opacity between 0.15 and 0.4 over 1.5s.
- Per user decision: NO spinner icon, just border glow using existing Phase 15 pill styling.
- Add the conditional glow class when `status === 'running'` or equivalent state.
  </action>
  <verify>
    <automated>cd /home/swd/loom && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ErrorBanner renders inline after last message for permanent errors. Dismissible with X. Active tool cards show pulsing border glow during running state. No spinner icons added.</done>
  <depth>
    - ErrorBanner: entry animation subtle (200ms opacity + translateY)
    - ErrorBanner: red accent is muted (10% bg, 2px left border) -- not screaming
    - ErrorBanner: text is foreground color for readability, only icon/border/bg use destructive
    - Dismiss: clicking X removes banner, does not reappear for same error
    - Tool card glow: uses box-shadow not border (avoids layout shift)
    - Tool card glow: animation is CSS-only, compositor thread, no JS
    - Error detection: handles both exit code errors and process crash signals
  </depth>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- ErrorBanner.tsx exists with dismiss functionality
- ChatMessagesPane renders ErrorBanner conditionally
- Active tool cards have pulsing border glow class when running
- No spinner icons added to tool cards (border glow only per user decision)
</verification>

<success_criteria>
Permanent errors show as persistent inline red-accented banners after the last message. Banners are dismissible. Active tool cards pulse with border glow during execution.
</success_criteria>

<output>
After completion, create `.planning/phases/17-streaming-status/17-04-SUMMARY.md`
</output>
