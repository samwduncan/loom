---
phase: 06-chat-message-polish
plan: 05
type: execute
wave: 3
depends_on:
  - "06-04"
files_modified:
  - .planning/phases/06-chat-message-polish/06-05-SUMMARY.md
autonomous: false
requirements:
  - CHAT-09
  - CHAT-10
  - CHAT-11
  - CHAT-12
  - CHAT-13
  - CHAT-14
  - CHAT-15
  - CHAT-16

must_haves:
  truths:
    - "All 8 Phase 6 requirements verified visually in a running application"
    - "No regressions in Phase 5 functionality (Shiki, TurnBlock, ToolActionCard, ThinkingDisclosure)"
    - "Build passes, TypeScript compiles, no console errors"
  artifacts:
    - path: ".planning/phases/06-chat-message-polish/06-05-SUMMARY.md"
      provides: "Visual verification results and phase completion summary"
  key_links: []
---

<objective>
Verify all Phase 6 changes end-to-end: build the app, run the dev server, and visually confirm each requirement. Verify no regressions from Phase 5.

Purpose: Phase 6 introduces significant visual and layout changes across the chat interface. This checkpoint ensures all changes work together correctly and no Phase 5 functionality was broken.

Output: Verification summary documenting pass/fail for each requirement.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chat-message-polish/06-CONTEXT.md
@.planning/phases/06-chat-message-polish/06-01-SUMMARY.md
@.planning/phases/06-chat-message-polish/06-02-SUMMARY.md
@.planning/phases/06-chat-message-polish/06-03-SUMMARY.md
@.planning/phases/06-chat-message-polish/06-04-SUMMARY.md
@.planning/phases/06-chat-message-polish/06-06-SUMMARY.md (optional -- 06-06 is a soft prerequisite, may not exist at verification time)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and automated checks</name>
  <files>n/a -- read-only verification task</files>
  <action>
1. Run the full build and type check:
   ```bash
   cd /home/swd/loom && npm run build 2>&1
   npx tsc --noEmit 2>&1
   ```

2. Grep audit for regressions:
   ```bash
   # Verify no blue-600 in user messages
   grep -n "blue-600" src/components/chat/view/subcomponents/MessageComponent.tsx || echo "PASS: No blue-600"

   # Verify react-diff-viewer-continued used
   grep -r "react-diff-viewer-continued" src/ || echo "FAIL: No diff viewer import"

   # Verify auto-collapse removed (no streaming-triggered collapse)
   grep -n "prevStreamingTurnId" src/components/chat/view/subcomponents/ChatMessagesPane.tsx || echo "PASS: No auto-collapse refs"

   # Verify TurnUsageFooter integrated
   grep -n "TurnUsageFooter" src/components/chat/view/subcomponents/TurnBlock.tsx || echo "FAIL: No usage footer"

   # Verify SystemStatusMessage component exists
   ls src/components/chat/view/subcomponents/SystemStatusMessage.tsx || echo "FAIL: No SystemStatusMessage"

   # Verify pricing utility exists
   ls src/components/chat/utils/pricing.ts || echo "FAIL: No pricing utility"

   # Verify max-width centered layout
   grep -n "max-w-\[720px\]" src/components/chat/view/subcomponents/ChatMessagesPane.tsx || echo "FAIL: No max-width"

   # Verify no space-y in TurnBlock collapsible content
   grep -n "space-y" src/components/chat/view/subcomponents/TurnBlock.tsx | grep -v "^[[:space:]]*\/\/" || echo "PASS: No space-y gaps"

   # Verify PermissionRequestsBanner has distinct border
   grep -n "border-l" src/components/chat/view/subcomponents/PermissionRequestsBanner.tsx || echo "FAIL: No left border accent"

   # Verify Shiki renderContent in DiffViewer
   grep -n "renderContent" src/components/chat/tools/components/DiffViewer.tsx || echo "FAIL: No Shiki renderContent"

   # Verify ShikiDiffLine exists
   ls src/components/chat/tools/components/ShikiDiffLine.tsx || echo "FAIL: No ShikiDiffLine"

   # Verify IntersectionObserver in ChatMessagesPane
   grep -n "IntersectionObserver" src/components/chat/view/subcomponents/ChatMessagesPane.tsx || echo "FAIL: No IntersectionObserver"

   # Verify session cumulative in ChatInputControls
   grep -n "formatTokenCount" src/components/chat/view/subcomponents/ChatInputControls.tsx || echo "FAIL: No session cumulative"

   # Verify ImageLightbox exists
   ls src/components/chat/view/subcomponents/ImageLightbox.tsx || echo "FAIL: No ImageLightbox"

   # Verify interactive prompt copper accent (soft: only if 06-06 completed before verification)
   grep -n "b87333" src/components/chat/view/subcomponents/MessageComponent.tsx || echo "SOFT-SKIP: No copper accent -- 06-06 may not have completed yet (not a hard dependency)"
   ```

3. Record results in the summary.
  </action>
  <verify>
    <automated>cd /home/swd/loom && npm run build 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Build and typecheck pass. All automated grep checks pass. No regressions detected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 6 requirements</name>
  <action>Human verifies all 8 Phase 6 requirements visually in running application.</action>
  <verify>Human approval received</verify>
  <done>All 8 requirements verified, no regressions</done>
  <what-built>
Phase 6 Chat Message Polish — complete chat message experience:
1. **Diff rendering** (CHAT-09): react-diff-viewer-continued with warm theme, unified view, 3 context lines, Shiki syntax highlighting via renderContent
2. **User messages** (CHAT-10, CHAT-11): Warm amber tint, hover-reveal copy button outside message, image lightbox on click
3. **Permission banners** (CHAT-12): Warm amber theme with distinct left border accent, Waiting/Approved/Denied status states
4. **Usage summaries** (CHAT-13): Per-turn token count + cost at bottom of each AI turn, session cumulative under composer
5. **System status messages** (CHAT-14): 3-tier inline rendering (info/warning/error)
6. **Layout** (CHAT-15): Full-width blocks, max-width ~720px centered
7. **Continuous flow** (CHAT-16): Thin dividers between AI turn sub-elements, no gaps
8. **Smart collapse**: IntersectionObserver-based collapse on scroll-away, re-expand on revisit, no auto-collapse on stream
9. **Interactive prompts**: Warm copper card restyle, option buttons with pulse waiting indicator (websocket response if backend feasible)
  </what-built>
  <how-to-verify>
Start the dev server:
```bash
cd /home/swd/loom && npm run dev
```

Open in browser: http://100.86.4.57:5173

**Verify each requirement:**

1. **CHAT-09 (Diffs + Shiki):** Send a message asking Claude to edit a file. Verify the diff shows green additions, red removals, gray context lines, clickable file path header, line numbers, warm earthy colors (not blue-gray), AND syntax highlighting within diff lines (Shiki).

2. **CHAT-10 (User message tint):** Check that your sent messages have a warm amber background tint (not blue). The messages should use rounded-lg corners.

3. **CHAT-11 (User copy button):** Hover over a sent message. A small copy icon should appear to the right of the message. Click it — verify it copies the full message text and shows a check icon briefly.

4. **Image lightbox:** If a user message has an attached image, click the image thumbnail. A dark overlay should open with the full-size image. Close via click-outside, X button, or Escape.

5. **CHAT-12 (Permission banners):** Trigger a permission request (e.g., ask Claude to run a command). Verify the banner has warm amber background, distinct left border accent, tool name in bold, approve/deny buttons, "Waiting for response" pulse indicator. After clicking approve/deny, verify the status briefly shows "Approved"/"Denied" before the banner is removed.

6. **CHAT-13 (Usage summaries):** After an AI response completes, verify token count and cost appear at the bottom of the turn (always visible, not collapsed). Also verify the session cumulative token count appears near the TokenUsagePie under the composer.

7. **CHAT-14 (System status):** Check that any system messages (session started, errors) render as muted inline elements with appropriate tier coloring.

8. **CHAT-15 (Layout):** Verify messages use full-width blocks centered at ~720px max-width. No left/right bubble layout.

9. **CHAT-16 (Continuous flow):** Verify AI turns have thin divider lines between sub-elements (tool calls, thinking, prose). No large whitespace gaps between elements inside a turn.

10. **IntersectionObserver collapse:** Scroll down past several completed turns. After scrolling away, verify turns collapse. Scroll back up to a collapsed turn — it should re-expand automatically. Verify the currently streaming turn never collapses.

11. **Interactive prompts (soft -- only if 06-06 completed):** If an interactive prompt appears AND 06-06 has been executed, verify it uses warm copper card styling (#b87333 accent), smaller option buttons, and a pulse waiting indicator. If 06-06 has not yet completed, skip this check -- it is not a hard dependency for phase verification.

**Verify no regressions:**
- Code blocks still use Shiki syntax highlighting
- Turn collapse/expand still works manually (click turn header)
- Tool action cards still display correctly
- Thinking disclosures still animate smoothly
  </how-to-verify>
  <resume-signal>Type "approved" with any notes, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Build passes: `npm run build` exits 0
- TypeScript compiles: `npx tsc --noEmit` exits 0
- All grep audits pass (detailed in Task 1)
- Human visual verification of all 8 requirements
</verification>

<success_criteria>
- All 8 Phase 6 requirements (CHAT-09 through CHAT-16) verified working
- No regressions in Phase 5 functionality
- Build and typecheck pass
- Human approval received
</success_criteria>

<output>
After completion, create `.planning/phases/06-chat-message-polish/06-05-SUMMARY.md`
</output>
