---
phase: 65-touch-target-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/components/chat/styles/thinking-disclosure.css
  - src/src/components/chat/tools/tool-card-shell.css
  - src/src/components/sidebar/ProjectHeader.tsx
  - src/src/components/chat/view/ChatEmptyState.tsx
  - src/src/components/chat/view/LiveSessionBanner.tsx
  - src/src/components/sidebar/BulkActionBar.tsx
  - src/src/components/sidebar/SearchInput.tsx
  - src/src/components/sidebar/sidebar.css
  - src/e2e/touch-targets.spec.ts
autonomous: true
requirements: [TOUCH-01, TOUCH-02, TOUCH-03, TOUCH-04, TOUCH-05, TOUCH-06]

must_haves:
  truths:
    - "ThinkingDisclosure trigger measures 44px+ height at 375px viewport"
    - "ToolCardShell header measures 44px+ height at 375px viewport"
    - "ProjectHeader button measures 44px+ height at 375px viewport"
    - "ChatEmptyState template buttons measure 44px+ height at 375px viewport"
    - "LiveSessionBanner Detach button measures 44px+ height at 375px viewport"
    - "All sidebar interactive elements (BulkActionBar, SearchInput, Sidebar buttons) measure 44px+ on mobile"
    - "All TOUCH-01-06 targets revert to compact natural sizing on desktop (>= 768px)"
  artifacts:
    - path: "src/src/components/chat/styles/thinking-disclosure.css"
      provides: "44px mobile min-height for thinking trigger + focus ring"
      contains: "min-height: 44px"
    - path: "src/src/components/chat/tools/tool-card-shell.css"
      provides: "44px mobile min-height for tool card header + standardized focus ring"
      contains: "min-height: 44px"
    - path: "src/src/components/sidebar/ProjectHeader.tsx"
      provides: "44px mobile min-height with desktop revert"
      contains: "min-h-[44px] md:min-h-0"
    - path: "src/src/components/chat/view/ChatEmptyState.tsx"
      provides: "44px mobile min-height on template buttons"
      contains: "min-h-[44px] md:min-h-0"
    - path: "src/src/components/chat/view/LiveSessionBanner.tsx"
      provides: "44px mobile min-height on detach button"
      contains: "min-h-[44px] md:min-h-0"
    - path: "src/src/components/sidebar/BulkActionBar.tsx"
      provides: "44px mobile min-height on action buttons"
      contains: "min-h-[44px]"
    - path: "src/src/components/sidebar/SearchInput.tsx"
      provides: "44px mobile min-height on input and clear button"
      contains: "min-h-[44px]"
    - path: "src/e2e/touch-targets.spec.ts"
      provides: "Playwright regression test for touch target compliance"
      contains: "toBeGreaterThanOrEqual(44)"
  key_links:
    - from: "src/e2e/touch-targets.spec.ts"
      to: "all TOUCH-01-06 components"
      via: "Playwright getBoundingClientRect at 375px viewport"
      pattern: "boundingBox.*height.*44"
---

<objective>
Fix all 6 touch target sizing violations (TOUCH-01 through TOUCH-06) so every interactive element meets Apple's 44px minimum on mobile, add focus rings to the fixed components, and add a Playwright regression test to prevent future violations.

Purpose: Users currently mis-tap ThinkingDisclosure (~20px), ToolCardShell header (~32px), ChatEmptyState templates (~32px), LiveSessionBanner detach (~24px), and several sidebar elements. Fixing these eliminates the most common tap frustration on iPhone.

Output: 9 modified component files + 1 new Playwright test file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-touch-target-compliance/65-CONTEXT.md
@.planning/phases/65-touch-target-compliance/65-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs -->

From src/src/components/sidebar/SessionItem.tsx (REFERENCE PATTERN for touch targets):
```tsx
// Line 161 -- THE established 44px touch target pattern
'min-h-[44px] md:min-h-0'
```

From src/src/components/ui/button.tsx (REFERENCE PATTERN for focus rings):
```tsx
// Line 8 -- shadcn focus ring standard
'focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50'
```

From src/src/components/chat/tools/tool-chip.css (REFERENCE PATTERN for CSS media query touch targets):
```css
/* Lines 184-188 */
@media (max-width: 767px) {
  .tool-chip { min-height: 44px; }
}
```

CSS focus ring equivalent for CSS files:
```css
.selector:focus-visible {
  outline: none;
  box-shadow: 0 0 0 3px oklch(from var(--ring) l c h / 0.5);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 5 named TOUCH violations (TOUCH-01 through TOUCH-05) with focus rings</name>
  <files>
    src/src/components/chat/styles/thinking-disclosure.css,
    src/src/components/chat/tools/tool-card-shell.css,
    src/src/components/sidebar/ProjectHeader.tsx,
    src/src/components/chat/view/ChatEmptyState.tsx,
    src/src/components/chat/view/LiveSessionBanner.tsx
  </files>
  <read_first>
    src/src/components/chat/styles/thinking-disclosure.css,
    src/src/components/chat/tools/tool-card-shell.css,
    src/src/components/sidebar/ProjectHeader.tsx,
    src/src/components/chat/view/ChatEmptyState.tsx,
    src/src/components/chat/view/LiveSessionBanner.tsx,
    src/src/components/sidebar/SessionItem.tsx,
    src/src/components/ui/button.tsx
  </read_first>
  <action>
Fix 5 specific touch target violations. Each fix follows the established pattern. NO double-padding (use min-height OR padding, not both). Per D-01 all components visually grow. Per D-02 use `min-h-[44px]` (Tailwind) or `min-height: 44px` (CSS). Per D-03 every Tailwind `min-h-[44px]` pairs with `md:min-h-0`.

**TOUCH-01: ThinkingDisclosure (thinking-disclosure.css)**
Add a mobile media query block at the end of the file (before the reduced-motion block):
```css
/* ─── Mobile Touch Targets ──────────────────────────────────────────── */

@media (max-width: 767px) {
  .thinking-disclosure-trigger {
    min-height: 44px;
  }
}
```
Also add a focus-visible rule for the trigger (it currently has NONE):
```css
.thinking-disclosure-trigger:focus-visible {
  outline: none;
  box-shadow: 0 0 0 3px oklch(from var(--ring) l c h / 0.5);
  border-radius: 4px;
}
```
Place the focus-visible rule right after the existing `.thinking-disclosure-trigger` block (after line 31).

**TOUCH-02: ToolCardShell (tool-card-shell.css)**
Add a mobile media query block (before the reduced-motion block):
```css
@media (max-width: 767px) {
  .tool-card-shell-header {
    min-height: 44px;
  }
}
```
Also fix the existing non-standard focus ring on `.tool-card-shell-header:focus-visible` (line 33-36). Change from:
```css
box-shadow: 0 0 0 2px var(--accent-primary);
```
to:
```css
box-shadow: 0 0 0 3px oklch(from var(--ring) l c h / 0.5);
```
Per D-06, this standardizes on the shadcn ring pattern.

**TOUCH-03: ProjectHeader (ProjectHeader.tsx)**
Add `min-h-[44px] md:min-h-0` to the button's className. Also add focus ring classes since this component uses `session-item-hover` which has a non-standard focus ring in sidebar.css. Add `focus-visible:outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50` to the button className. The button element (line 36) should have its className updated:
```tsx
className={cn(
  'w-full flex items-center gap-2 px-3 py-2',
  'min-h-[44px] md:min-h-0',
  'text-left font-medium text-foreground',
  'transition-[background-color] duration-[var(--duration-fast)]',
  'focus-visible:outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50',
  'session-item-hover',
  isCurrentProject && 'border-l-2 border-l-[var(--accent-primary)]',
)}
```

**TOUCH-04: ChatEmptyState (ChatEmptyState.tsx)**
Add `min-h-[44px] md:min-h-0` and focus ring to each template button. Update the button className (line 55-59):
```tsx
className={cn(
  'rounded-lg bg-surface-raised px-3 py-1.5 text-sm text-secondary',
  'min-h-[44px] md:min-h-0 flex items-center justify-center',
  'hover:bg-surface-overlay transition-colors',
  'border border-border/10',
  'focus-visible:outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50',
)}
```
Note: add `flex items-center justify-center` so text is vertically centered within the taller 44px button on mobile.

**TOUCH-05: LiveSessionBanner (LiveSessionBanner.tsx)**
Add `min-h-[44px] md:min-h-0` and focus ring to the Detach button. Update the button className (line 46-51):
```tsx
className={cn(
  'px-2 py-0.5 rounded text-xs',
  'min-h-[44px] md:min-h-0 flex items-center',
  'bg-[var(--live-banner-btn-bg)] hover:bg-[var(--live-banner-btn-bg-hover)]',
  'text-[var(--live-banner-btn-text)] hover:text-[var(--live-banner-btn-text-hover)]',
  'transition-colors duration-[var(--duration-fast)]',
  'focus-visible:outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50',
)}
```
The banner will grow taller on mobile to accommodate the 44px button -- this is expected and acceptable per D-01.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - thinking-disclosure.css contains `@media (max-width: 767px)` with `min-height: 44px` for `.thinking-disclosure-trigger`
    - thinking-disclosure.css contains `.thinking-disclosure-trigger:focus-visible` with `box-shadow: 0 0 0 3px oklch`
    - tool-card-shell.css contains `@media (max-width: 767px)` with `min-height: 44px` for `.tool-card-shell-header`
    - tool-card-shell.css `.tool-card-shell-header:focus-visible` contains `box-shadow: 0 0 0 3px oklch(from var(--ring) l c h / 0.5)` (not `2px var(--accent-primary)`)
    - ProjectHeader.tsx contains `min-h-[44px] md:min-h-0` AND `focus-visible:ring-[3px] focus-visible:ring-ring/50`
    - ChatEmptyState.tsx template button contains `min-h-[44px] md:min-h-0` AND `focus-visible:ring-[3px] focus-visible:ring-ring/50`
    - LiveSessionBanner.tsx detach button contains `min-h-[44px] md:min-h-0` AND `focus-visible:ring-[3px] focus-visible:ring-ring/50`
    - No component has BOTH extra padding AND min-height (double-padding trap)
  </acceptance_criteria>
  <done>All 5 named TOUCH violations fixed to 44px mobile min-height with desktop revert, standardized focus rings added to each</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar audit (TOUCH-06) -- fix remaining non-compliant sidebar elements</name>
  <files>
    src/src/components/sidebar/BulkActionBar.tsx,
    src/src/components/sidebar/SearchInput.tsx,
    src/src/components/sidebar/sidebar.css
  </files>
  <read_first>
    src/src/components/sidebar/BulkActionBar.tsx,
    src/src/components/sidebar/SearchInput.tsx,
    src/src/components/sidebar/sidebar.css,
    src/src/components/sidebar/SessionItem.tsx,
    src/src/components/sidebar/Sidebar.tsx
  </read_first>
  <action>
Audit and fix all remaining non-compliant sidebar interactive elements per D-08/D-09. SessionItem already compliant. ProjectHeader covered in Task 1. NewChatButton, QuickSettingsPanel, and Sidebar hamburger/close/settings buttons already have `min-h-[44px]` (confirmed by research grep). Three elements remain:

**BulkActionBar.tsx -- Delete and Cancel buttons**
Both buttons currently use `p-1.5` (~28px). Add `min-h-[44px] min-w-[44px] md:min-h-0 md:min-w-0` to each (icon-only buttons need both dimensions). Also upgrade focus ring from `ring-2 ring-ring` to `ring-[3px] ring-ring/50` per D-05/D-06.

Delete button (line 35-41) className becomes:
```tsx
className={cn(
  'p-1.5 rounded-md text-[var(--status-error)]',
  'min-h-[44px] min-w-[44px] md:min-h-0 md:min-w-0 flex items-center justify-center',
  'hover:bg-destructive/20',
  'focus-visible:outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50',
  'transition-colors duration-[var(--duration-fast)]',
)}
```

Cancel button (line 48-52) className becomes:
```tsx
className={cn(
  'p-1.5 rounded-md text-muted hover:text-foreground hover:bg-surface-raised/50',
  'min-h-[44px] min-w-[44px] md:min-h-0 md:min-w-0 flex items-center justify-center',
  'focus-visible:outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50',
  'transition-colors duration-[var(--duration-fast)]',
)}
```

**SearchInput.tsx -- Input field and Clear button**
Input (line 41-47): Add `min-h-[44px] md:min-h-0` and upgrade focus ring. className becomes:
```tsx
className={cn(
  'w-full bg-surface-base border border-border rounded-md',
  'pl-7 pr-7 py-1.5 text-base md:text-sm',
  'min-h-[44px] md:min-h-0',
  'text-foreground placeholder:text-muted',
  'outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50',
  'transition-colors duration-[var(--duration-fast)]',
)}
```

Clear button (line 54-57): Add `min-h-[44px] min-w-[44px] md:min-h-0 md:min-w-0` and upgrade ring. The button is absolutely positioned, so ensure it doesn't overlap text by adjusting right positioning if needed. className becomes:
```tsx
className={cn(
  'absolute right-0 top-0 h-full min-w-[44px] md:min-w-0 flex items-center justify-center',
  'text-muted hover:text-foreground',
  'focus-visible:outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50 rounded-sm',
  'transition-colors duration-[var(--duration-fast)]',
)}
```
Note: changed from `right-2` to `right-0 top-0 h-full` to make the full-height clear button tap target work naturally with the absolutely-positioned layout. The icon stays centered via flex. On desktop, `md:min-w-0` reverts it.

**sidebar.css -- Fix non-standard focus ring on `.session-item-hover:focus-visible`**
Line 12-15: Change from:
```css
box-shadow: 0 0 0 2px var(--accent-primary);
```
to:
```css
box-shadow: 0 0 0 3px oklch(from var(--ring) l c h / 0.5);
```
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BulkActionBar.tsx delete button contains `min-h-[44px] min-w-[44px] md:min-h-0 md:min-w-0`
    - BulkActionBar.tsx cancel button contains `min-h-[44px] min-w-[44px] md:min-h-0 md:min-w-0`
    - BulkActionBar.tsx both buttons contain `focus-visible:ring-[3px] focus-visible:ring-ring/50` (NOT `ring-2 ring-ring`)
    - SearchInput.tsx input contains `min-h-[44px] md:min-h-0` and `focus-visible:ring-[3px] focus-visible:ring-ring/50`
    - SearchInput.tsx clear button contains `min-w-[44px] md:min-w-0`
    - sidebar.css `.session-item-hover:focus-visible` contains `box-shadow: 0 0 0 3px oklch(from var(--ring) l c h / 0.5)` (NOT `2px var(--accent-primary)`)
    - No sidebar interactive element at mobile viewport has height < 44px
  </acceptance_criteria>
  <done>All sidebar interactive elements audited and fixed to 44px mobile min-height with standardized focus rings</done>
</task>

<task type="auto">
  <name>Task 3: Playwright touch target regression test (D-11/D-12)</name>
  <files>src/e2e/touch-targets.spec.ts</files>
  <read_first>
    src/e2e/helpers.ts,
    src/playwright.config.ts,
    src/e2e/thinking.spec.ts,
    src/e2e/tool-calls.spec.ts
  </read_first>
  <action>
Create a Playwright e2e test that validates all interactive elements meet 44px height on mobile viewport per D-11/D-12.

Create `src/e2e/touch-targets.spec.ts`:

The test should:
1. Set viewport to `{ width: 375, height: 812 }` (iPhone SE/standard)
2. Navigate to `/chat` using the `setupChat` helper from `./helpers`
3. Test the empty state page first (ChatEmptyState template buttons visible)
4. Query all visible interactive elements: `button, a[href], [role="button"], input, select, textarea`
5. For each visible element, assert `boundingBox().height >= 44`
6. Collect all violations (element text/label + actual height) and report them in the assertion message
7. Use a batch assertion approach: collect all violations first, then `expect(violations).toHaveLength(0)` with a descriptive failure message listing each violation

Important implementation details:
- Import `setupChat` from `./helpers`
- Use `test.describe('Touch Target Compliance')` as the outer block
- The test must set mobile viewport with `page.setViewportSize({ width: 375, height: 812 })` BEFORE navigation
- Filter out elements that are hidden (`isVisible()` check) or have zero dimensions
- Skip elements inside `[data-testid="sidebar"]` if the sidebar is collapsed on mobile (it's a drawer)
- To test sidebar elements, add a second test that opens the sidebar first (click hamburger button `[aria-label="Open sidebar"]`)
- Include `aria-label` OR `textContent` in violation messages for debugging
- The test file should NOT require any backend interaction (just DOM measurement on the rendered page)

Test structure:
```typescript
test.describe('Touch Target Compliance', () => {
  test('chat view interactive elements meet 44px minimum on mobile', async ({ page }) => {
    // viewport, navigate, measure all visible buttons/inputs
  });

  test('sidebar interactive elements meet 44px minimum on mobile', async ({ page }) => {
    // viewport, navigate, open sidebar, measure all visible buttons/inputs in sidebar
  });
});
```
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx playwright test e2e/touch-targets.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/e2e/touch-targets.spec.ts` exists
    - File contains `page.setViewportSize({ width: 375, height: 812 })`
    - File contains `toBeGreaterThanOrEqual(44)` or equivalent height assertion
    - File contains `import` from `./helpers`
    - File contains at least 2 test cases (chat view + sidebar)
    - Tests pass when run with `npx playwright test e2e/touch-targets.spec.ts`
  </acceptance_criteria>
  <done>Playwright regression test validates no interactive element is shorter than 44px at mobile viewport, test passes green</done>
</task>

</tasks>

<verification>
1. `cd /home/swd/loom/src && npx vitest run --reporter=verbose 2>&1 | tail -5` -- existing tests still pass
2. `cd /home/swd/loom/src && npx playwright test e2e/touch-targets.spec.ts --reporter=list` -- touch target test passes
3. `grep -r 'min-h-\[44px\]' src/src/components/chat/view/ChatEmptyState.tsx src/src/components/chat/view/LiveSessionBanner.tsx src/src/components/sidebar/ProjectHeader.tsx src/src/components/sidebar/BulkActionBar.tsx src/src/components/sidebar/SearchInput.tsx` -- all 5 TSX files contain the pattern
4. `grep -c 'min-height: 44px' src/src/components/chat/styles/thinking-disclosure.css src/src/components/chat/tools/tool-card-shell.css` -- both CSS files contain 44px
5. No `ring-2` remains in any file modified by this plan (all upgraded to `ring-[3px]`)
</verification>

<success_criteria>
- All 6 TOUCH requirements (01-06) have fixes verified by grep for `44px` pattern
- Playwright touch-targets.spec.ts passes green at 375px mobile viewport
- Desktop layout unaffected (every `min-h-[44px]` paired with `md:min-h-0` or scoped to `@media (max-width: 767px)`)
- Existing Vitest tests still pass
- Focus rings on all 6 modified components use the shadcn `ring-[3px] ring-ring/50` standard
</success_criteria>

<output>
After completion, create `.planning/phases/65-touch-target-compliance/65-01-SUMMARY.md`
</output>
