---
phase: 16-sidebar-global-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/view/Settings.tsx
  - src/components/sidebar/view/subcomponents/SidebarModals.tsx
  - src/components/sidebar/view/modals/VersionUpgradeModal.tsx
  - src/components/sidebar/view/subcomponents/SidebarFooter.tsx
  - src/components/settings/view/SettingsMainTabs.tsx
autonomous: true
requirements: [SIDE-02, SIDE-04]

must_haves:
  truths:
    - Settings panel uses charcoal+rose palette with no blue-600 or gray artifacts
    - All modal/dialog backdrops use glassmorphic blur (backdrop-blur-xl with bg-background/60)
    - Confirmation dialogs use design system tokens instead of hardcoded red/blue colors
    - Save button and settings icon use dusty rose primary color
  artifacts:
    - src/components/settings/view/Settings.tsx (charcoal+rose restyle)
    - src/components/sidebar/view/subcomponents/SidebarModals.tsx (glassmorphic backdrops)
  key_links:
    - Settings backdrop uses bg-background/60 backdrop-blur-xl pattern
    - Modal confirmation dialogs use bg-background/60 backdrop-blur-xl pattern
---

<objective>
Restyle settings panel and all modals/dialogs with charcoal+rose palette and glassmorphic blur backdrops.

Purpose: Eliminate all blue-600, gray, and white color artifacts from settings and dialogs, replacing with the design system's charcoal+rose tokens and consistent glassmorphic backdrop treatment.
Output: Settings, confirmation dialogs, and version modal fully matching the charcoal+rose palette.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-sidebar-global-polish/16-RESEARCH.md
@.planning/phases/16-sidebar-global-polish/16-CONTEXT.md
@src/components/settings/view/Settings.tsx
@src/components/sidebar/view/subcomponents/SidebarModals.tsx
@src/components/sidebar/view/modals/VersionUpgradeModal.tsx
@src/components/sidebar/view/subcomponents/SidebarFooter.tsx
@src/components/settings/view/SettingsMainTabs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Settings panel with charcoal+rose palette</name>
  <files>src/components/settings/view/Settings.tsx, src/components/settings/view/SettingsMainTabs.tsx</files>
  <action>
**Settings.tsx** — Apply charcoal+rose palette:

1. Modal backdrop: Change `bg-background/95` to `bg-background/60 backdrop-blur-xl` for the glassmorphic effect. The outer fixed div should be: `fixed inset-0 flex items-center justify-center z-[var(--z-modal)] md:p-4 bg-background/60 backdrop-blur-xl`

2. Settings icon: Replace `text-blue-600` on the SettingsIcon with `text-primary`. This is in the header div.

3. Save button: Replace `bg-blue-600 hover:bg-blue-700` with `bg-primary hover:bg-primary/90`. This is the "Save Settings" button in the footer.

4. Save success feedback: Replace `text-green-600 text-green-400` with `text-emerald-500` (or keep as-is since status feedback green is acceptable — but ensure it's a single class, not light+dark double declaration). Actually, replace with a single token: use `text-status-success` if it exists in the design system, otherwise use `text-emerald-400` as a single class.

5. Save error feedback: Replace `text-red-600 text-red-400` with `text-destructive` (single token).

6. Inner container: Keep `bg-background border border-border` which already uses tokens. This is correct.

**SettingsMainTabs.tsx** — Check for any blue-600 references. Read the file first to see current state. Replace any hardcoded blue colors with `text-primary` or `border-primary` as appropriate.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
- Settings backdrop is glassmorphic (backdrop-blur-xl + bg-background/60)
- Settings icon uses text-primary (dusty rose) instead of text-blue-600
- Save button uses bg-primary instead of bg-blue-600
- No blue-600 or blue-700 references remain in Settings.tsx
- Status messages use single-class color tokens
  </done>
  <depth>
  - Save button disabled state: ensure `disabled:opacity-50` still works with bg-primary
  - The Cancel button uses `variant="outline"` which already inherits from design system — no changes needed
  - Modal inner container uses `bg-background` (not bg-card or bg-surface-*) for maximum contrast with blur backdrop
  - Footer border uses `border-border` token — already correct
  </depth>
</task>

<task type="auto">
  <name>Task 2: Glassmorphic backdrops and palette cleanup for modals and footer</name>
  <files>src/components/sidebar/view/subcomponents/SidebarModals.tsx, src/components/sidebar/view/modals/VersionUpgradeModal.tsx, src/components/sidebar/view/subcomponents/SidebarFooter.tsx</files>
  <action>
**SidebarModals.tsx** — Update both confirmation dialogs:

1. Delete project confirmation backdrop: Replace `bg-black/60 backdrop-blur-sm` with `bg-background/60 backdrop-blur-xl`

2. Delete project icon background: Replace `bg-red-100 bg-red-900/30` (double light+dark declaration) with `bg-destructive/10`

3. Delete project icon color: Replace `text-red-600 text-red-400` with `text-destructive`

4. Warning box: Replace `bg-red-50 bg-red-900/20 border border-red-200 border-red-800` with `bg-destructive/5 border border-destructive/20`

5. Warning text: Replace `text-red-700 text-red-300` with `text-destructive` and `text-red-600 text-red-400` with `text-destructive/80`

6. Footer background: Replace `bg-muted/30` with `bg-surface-raised` if available, or keep `bg-muted/30` as it uses a token

7. Delete button: Replace `bg-red-600 hover:bg-red-700 text-white` with `bg-destructive hover:bg-destructive/90 text-destructive-foreground`

8. Apply identical changes to the session delete confirmation dialog (same structure, same replacements)

**VersionUpgradeModal.tsx** — Check backdrop and color usage:
- Backdrop already uses `bg-black/60 backdrop-blur-sm` — replace with `bg-background/60 backdrop-blur-xl`
- The modal already uses design tokens (bg-surface-raised, text-foreground, border-border/10) — these are correct
- Check for any remaining blue color references and replace with primary or status-info tokens as appropriate

**SidebarFooter.tsx** — Clean up blue artifact colors:

1. Desktop update banner: Replace `hover:bg-blue-50/80 hover:bg-blue-900/15` with `hover:bg-primary/10`
2. Update icon: Replace `text-blue-500 text-blue-400` with `text-primary`
3. Update dot: Replace `bg-blue-500` with `bg-primary`
4. Update text: Replace `text-blue-600 text-blue-300` with `text-primary` and `text-blue-500/70 text-blue-400/60` with `text-primary/60`

5. Mobile update banner: Same replacements — replace `bg-blue-50/80 bg-blue-900/15 border border-blue-200/60 border-blue-700/40` with `bg-primary/10 border border-primary/20`
6. Mobile update icon/text: Same color replacements as desktop

Note: The SidebarCollapsed.tsx also has blue-500 references on the update indicator — but that file is handled in Plan 16-03. Only modify files listed in this plan.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
- Both confirmation dialogs use bg-background/60 backdrop-blur-xl backdrops
- Red/blue hardcoded colors replaced with design system tokens (destructive, primary)
- No double light+dark color class patterns remain in modified files
- SidebarFooter update banner uses primary (rose) instead of blue
- VersionUpgradeModal backdrop matches glassmorphic pattern
  </done>
  <depth>
  - Destructive button variant: ensure hover state provides sufficient visual feedback
  - Warning boxes: bg-destructive/5 should be subtle enough to not overwhelm
  - Icon treatment in confirmation dialogs: rounded-full bg with destructive icon is consistent pattern
  - "This action cannot be undone" text stays as text-muted-foreground (neutral warning)
  - Version modal already uses good design tokens — minimal changes needed beyond backdrop
  </depth>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- No `blue-600`, `blue-700`, `blue-500` references remain in modified files
- No double light+dark color patterns (e.g., `bg-red-100 bg-red-900/30`) remain in modified files
- Settings panel backdrop is blurred and translucent
- Confirmation dialog backdrops are blurred and translucent
- Save button and settings icon are dusty rose colored
</verification>

<success_criteria>
- SIDE-02: Settings panel fully matches charcoal + rose theme with no gray/blue/white artifacts
- SIDE-04: All modals and dialogs use charcoal palette with glassmorphic blur backgrounds
</success_criteria>

<output>
After completion, create `.planning/phases/16-sidebar-global-polish/16-02-SUMMARY.md`
</output>
