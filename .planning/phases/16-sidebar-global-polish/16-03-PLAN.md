---
phase: 16-sidebar-global-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MobileNav.jsx
  - src/components/sidebar/view/subcomponents/SidebarCollapsed.tsx
  - src/components/sidebar/view/subcomponents/SidebarHeader.tsx
autonomous: true
requirements: [SIDE-03, SIDE-07]

must_haves:
  truths:
    - Mobile navigation uses rose pill behind active tab instead of current primary/8 highlight
    - Sidebar collapsed strip includes new-session icon button alongside existing expand/settings/update buttons
    - Collapsed sidebar styling uses consistent charcoal+rose tokens with no blue artifacts
  artifacts:
    - src/components/MobileNav.jsx (rose pill active state)
    - src/components/sidebar/view/subcomponents/SidebarCollapsed.tsx (expanded icon strip)
  key_links:
    - SidebarCollapsed needs onNewSession callback prop
    - MobileNav active pill uses bg-primary/15 for rose tint
---

<objective>
Polish mobile navigation with rose palette and expand the collapsed sidebar icon strip with new-session functionality.

Purpose: Ensure mobile nav and collapsed sidebar match the charcoal+rose design system with functional improvements to the collapsed strip.
Output: Mobile nav with rose active pill, collapsed sidebar with new-session button and blue artifact cleanup.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-sidebar-global-polish/16-RESEARCH.md
@.planning/phases/16-sidebar-global-polish/16-CONTEXT.md
@src/components/MobileNav.jsx
@src/components/sidebar/view/subcomponents/SidebarCollapsed.tsx
@src/components/sidebar/view/Sidebar.tsx
@src/components/sidebar/view/subcomponents/SidebarHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rose pill active state for mobile nav</name>
  <files>src/components/MobileNav.jsx</files>
  <action>
Update the MobileNav active tab indicator to use a more visible rose pill:

1. Active pill background: Replace `bg-primary/8 bg-primary/12` (double light+dark declaration) with a single `bg-primary/15` class. This gives the rose tint more presence while keeping it as a subtle pill.

2. Active text color: Currently uses `text-primary` which is already correct (dusty rose).

3. Verify the `nav-glass` class is already applied to the container — it is (`nav-glass mobile-nav-float`). The nav-glass class already uses Phase 14 charcoal glassmorphic tokens (`--nav-glass-bg`, `--nav-glass-blur`), so no backdrop changes needed.

4. Remove the double class pattern: The current code has `bg-primary/8 bg-primary/12` as a space-separated string where presumably one is for light and one for dark. Since this is a dark-only app, use the single `bg-primary/15` value.

5. Overall, this is a minimal change — MobileNav is already well-styled from Phase 14. The main fix is the double-class artifact and slightly increased rose pill opacity.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
- Active tab shows rose pill with bg-primary/15 (single class, no double declaration)
- Inactive tabs remain text-muted-foreground
- nav-glass glassmorphic backdrop unchanged (already correct)
- No blue or double light+dark color patterns remain
  </done>
  <depth>
  - Touch feedback: `active:scale-95` transition already present — keep it
  - Safe area: bottom padding already uses `env(safe-area-inset-bottom)` — preserve it
  - Icon scaling: active icons scale up to w-5 h-5 — keep this polish
  - Tab labels: active labels at full opacity, inactive at 60% — keep this differentiation
  </depth>
</task>

<task type="auto">
  <name>Task 2: Expand collapsed sidebar strip and clean up blue artifacts</name>
  <files>src/components/sidebar/view/subcomponents/SidebarCollapsed.tsx, src/components/sidebar/view/Sidebar.tsx, src/components/sidebar/view/subcomponents/SidebarHeader.tsx</files>
  <action>
**SidebarCollapsed.tsx** — Add new-session button and clean blue artifacts:

1. Add `onNewSession` prop to `SidebarCollapsedProps`: `onNewSession: () => void`

2. Import `Plus` from lucide-react (alongside existing Settings, Sparkles, PanelLeftOpen)

3. Add a new-session button between the expand button and the nav-divider:
```tsx
{/* New session button */}
<button
  onClick={onNewSession}
  className="w-8 h-8 rounded-lg flex items-center justify-center hover:bg-accent/80 transition-colors group"
  aria-label="New session"
  title="New session"
>
  <Plus className="w-4 h-4 text-muted-foreground group-hover:text-foreground transition-colors" />
</button>
```

4. Update indicator: Replace `text-blue-500` on the Sparkles icon with `text-primary`. Replace `bg-blue-500` on the pulse dot with `bg-primary`.

5. Final icon order (top to bottom): Expand, New Session, divider, Settings, Update (if available)

**Sidebar.tsx** — Pass `onNewSession` to SidebarCollapsed:

The `SidebarCollapsed` component is rendered conditionally when `isSidebarCollapsed` is true. It currently receives `onExpand`, `onShowSettings`, `updateAvailable`, and `onShowVersionModal`.

Add `onNewSession` prop. The value should trigger creating a new session on the selected project. Looking at the existing code, `onNewSession` is passed down from SidebarProps and used in SidebarProjectSessions. In collapsed mode, we need to call `onNewSession(selectedProject)` if a project is selected, or show settings if no project is selected.

Pass it as: `onNewSession={() => { if (projects.length > 0) { const target = selectedProject || projects[0]; onNewSession(target); } }}`

**SidebarHeader.tsx** — Clean up any remaining blue artifacts:

Read the file and check for hardcoded blue colors. Currently the header uses `bg-primary/90` and `text-primary-foreground` on the create project button (mobile), which is already using design tokens. The refresh button uses `text-muted-foreground` which is correct.

If any blue references are found, replace with appropriate design tokens. If none found, no changes needed to this file — remove it from the plan scope.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
- SidebarCollapsed has new-session Plus button between expand and divider
- Update indicator uses primary (rose) instead of blue-500
- onNewSession prop wired through from Sidebar.tsx
- No blue-500 references remain in SidebarCollapsed.tsx
- TypeScript compiles without errors
  </done>
  <depth>
  - New session in collapsed mode: creates session on selected project, falls back to first project
  - Button sizing: all collapsed strip buttons are w-8 h-8 for consistent tap targets
  - Hover states: all buttons use hover:bg-accent/80 for consistent interaction
  - Update pulse dot: bg-primary matches the rose palette
  - Icon order provides logical grouping: navigation (expand + new) above divider, utility (settings + update) below
  </depth>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- Mobile nav active tab shows rose pill (bg-primary/15)
- Collapsed sidebar strip has 4 icons: expand, new session, settings, update
- No blue-500 or double light+dark color patterns in modified files
- New session from collapsed strip creates a session correctly
</verification>

<success_criteria>
- SIDE-03: Mobile navigation matches charcoal + rose theme at 375px+ viewports
- SIDE-07: Sidebar collapses to icon-only strip with functional new-session button
</success_criteria>

<output>
After completion, create `.planning/phases/16-sidebar-global-polish/16-03-SUMMARY.md`
</output>
