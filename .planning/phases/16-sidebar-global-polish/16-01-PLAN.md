---
phase: 16-sidebar-global-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/dateUtils.ts
  - src/components/sidebar/view/subcomponents/SidebarProjectSessions.tsx
  - src/components/sidebar/view/subcomponents/SidebarSessionItem.tsx
autonomous: true
requirements: [SIDE-01, SIDE-05, SIDE-06]

must_haves:
  truths:
    - Sessions are visually grouped by time categories (Today, Yesterday, Last 7 Days, Older) with collapsible headers
    - Active session shows 2px dusty rose left border with subtle accent background instead of green dot
    - Session rows are single-line compact density (~28-30px) with session name + timestamp on one line
  artifacts:
    - src/utils/dateUtils.ts (with getTimeCategory and groupSessionsByTime functions)
    - src/components/sidebar/view/subcomponents/SidebarProjectSessions.tsx (with time-grouped rendering)
    - src/components/sidebar/view/subcomponents/SidebarSessionItem.tsx (with compact density and rose active indicator)
  key_links:
    - groupSessionsByTime utility consumed by SidebarProjectSessions
    - SidebarSessionItem active state uses border-primary instead of green-500
---

<objective>
Implement time-based session grouping, compact sidebar density, and dusty rose active session indicator.

Purpose: Transform the flat session list into a familiar time-grouped pattern (like ChatGPT/Claude.ai) with significantly denser rows and the charcoal+rose active indicator.
Output: Updated session list with collapsible time groups, compact rows, and rose left-border active state.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-sidebar-global-polish/16-RESEARCH.md
@.planning/phases/16-sidebar-global-polish/16-CONTEXT.md
@src/utils/dateUtils.ts
@src/components/sidebar/view/subcomponents/SidebarProjectSessions.tsx
@src/components/sidebar/view/subcomponents/SidebarSessionItem.tsx
@src/components/sidebar/utils/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add time-grouping utility and compact session items</name>
  <files>src/utils/dateUtils.ts, src/components/sidebar/view/subcomponents/SidebarSessionItem.tsx</files>
  <action>
**dateUtils.ts** — Add two new exports:

1. `getTimeCategory(dateString: string, now: Date): TimeCategory` — Returns 'Today' | 'Yesterday' | 'Last 7 Days' | 'Older'. Calculate midnight boundaries for today, yesterday, and 7 days ago. Fall back to 'Older' for invalid dates.

2. `groupSessionsByTime<T>(sessions: T[], now: Date, getDate: (item: T) => string): Map<TimeCategory, T[]>` — Groups sessions into ordered time categories. Accept a `getDate` accessor function so it works with any session shape. Use the `createSessionViewModel` pattern's `sessionTime` field via the accessor. Remove empty groups. Maintain the insertion order: Today, Yesterday, Last 7 Days, Older.

Export the `TimeCategory` type.

**SidebarSessionItem.tsx** — Restyle for compact density and rose active indicator:

Desktop variant (the `hidden md:block` div):
- Remove the green dot indicator entirely (the `sessionView.isActive` div with `bg-green-500 rounded-full animate-pulse`)
- Replace the `Button variant="ghost"` with a plain div using: `px-2 py-1 rounded-md cursor-pointer hover:bg-accent/50 transition-colors duration-150`
- When `isSelected`: add `border-l-2 border-primary bg-accent/10` classes
- Single-line layout: flex row with provider logo (w-3 h-3), session name (text-xs truncate flex-1), timestamp (text-[10px] text-muted-foreground ml-auto). Remove the second line with Clock icon. Use `formatTimeAgo` directly in the flex row.
- Target height: ~28-30px per row (achieved with py-1 and text-xs)
- Keep hover action buttons (edit/delete) — they appear on group:hover as before, positioned absolute right

Mobile variant (the `md:hidden` div):
- Keep the card-style layout (larger tap targets per user decision)
- Replace green dot and green border indicators with: when `isSelected`, use `border-l-2 border-primary bg-accent/10` on the card
- Remove `bg-green-500`, `border-green-500/30`, `bg-green-50/5`, `bg-green-900/5` references
- When `isSelected && sessionView.isActive`: use `border-l-2 border-primary bg-accent/15` (slightly more tint)
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
- dateUtils.ts exports getTimeCategory and groupSessionsByTime with TimeCategory type
- Desktop session items are single-line, ~28-30px tall, no green indicators
- Selected sessions show 2px dusty rose left border + subtle accent bg
- Mobile session items keep card style but use rose indicators instead of green
  </done>
  <depth>
  - Edge case: sessions with invalid/null dates fall into "Older" group
  - Edge case: all sessions in one category — only that group header shows
  - Active (streaming) sessions: no green dot, rely on the rose border for selection indication
  - Hover state on desktop: bg-accent/50 provides subtle feedback
  - Provider logo remains visible as small inline icon at start of row
  </depth>
</task>

<task type="auto">
  <name>Task 2: Integrate time-grouped rendering in SidebarProjectSessions</name>
  <files>src/components/sidebar/view/subcomponents/SidebarProjectSessions.tsx</files>
  <action>
Replace the flat `sessions.map()` with time-grouped rendering:

1. Import `groupSessionsByTime`, `TimeCategory` from `../../../../utils/dateUtils`
2. Import `ChevronRight` from lucide-react (in addition to existing ChevronDown)
3. Add local state: `const [collapsedGroups, setCollapsedGroups] = useState<Set<string>>(new Set())`
4. Add toggle function: `const toggleGroup = (category: string) => setCollapsedGroups(prev => { const next = new Set(prev); if (next.has(category)) next.delete(category); else next.add(category); return next; })`

5. Compute grouped sessions: `const groupedSessions = useMemo(() => groupSessionsByTime(sessions, currentTime, (s) => s.lastActivity || s.createdAt || ''), [sessions, currentTime])`

Note: Check the actual session object shape — `SessionWithProvider` type may use different field names. Read the `createSessionViewModel` utility to find the correct date field. The accessor function should use whatever field `sessionView.sessionTime` resolves from.

6. Replace the existing `sessions.map((session) => ...)` block with:
```tsx
{Array.from(groupedSessions.entries()).map(([category, groupSessions]) => {
  const isCollapsed = collapsedGroups.has(category);
  return (
    <div key={category}>
      {/* Group header */}
      <button
        onClick={() => toggleGroup(category)}
        className="flex items-center gap-1.5 px-2 py-1 w-full text-left group/header"
      >
        <ChevronRight className={cn(
          'w-3 h-3 text-muted-foreground/60 transition-transform duration-150',
          !isCollapsed && 'rotate-90'
        )} />
        <span className="text-[11px] font-medium text-muted-foreground/70 uppercase tracking-wider">
          {category}
        </span>
        <span className="text-[10px] text-muted-foreground/40 ml-auto tabular-nums">
          {groupSessions.length}
        </span>
      </button>
      {/* Group sessions */}
      {!isCollapsed && groupSessions.map((session) => (
        <SidebarSessionItem key={session.id} ... />
      ))}
    </div>
  );
})}
```

7. Keep the "Show more sessions" button and "New Session" button OUTSIDE the time groups — they remain at the bottom of the session list.

8. Import `cn` from `../../../../lib/utils` and `useMemo`, `useState` from 'react'.

9. Both mobile and desktop should see the grouped sessions since SidebarSessionItem handles its own mobile/desktop rendering internally.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
- Sessions render under collapsible time-category headers (Today, Yesterday, Last 7 Days, Older)
- Empty categories are not shown
- Clicking a group header collapses/expands that group
- Group headers show category name and session count
- "New Session" and "Show more" buttons remain at bottom, outside groups
- TypeScript compiles without errors
  </done>
  <depth>
  - Collapsed state persists across re-renders (stored in component state)
  - When currentTime updates (every 60s), groups recalculate but collapsed state persists
  - If all sessions are in one group, the single header still shows (for consistency)
  - Group header chevron animates rotation on toggle
  - Count uses tabular-nums for stable width when numbers change
  </depth>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- Sessions in sidebar are grouped by time categories
- Active session has rose left border, no green dot
- Session rows are compact single-line on desktop
- Mobile retains card style with rose indicators
- Group headers are collapsible with chevron + count
</verification>

<success_criteria>
- SIDE-01: Sidebar uses tighter spacing with compact session rows
- SIDE-05: Sessions grouped by Today, Yesterday, Last 7 Days, Older
- SIDE-06: Active session shows 2px dusty rose left border with bg-accent tint
</success_criteria>

<output>
After completion, create `.planning/phases/16-sidebar-global-polish/16-01-SUMMARY.md`
</output>
