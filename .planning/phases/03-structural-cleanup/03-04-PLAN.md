---
phase: 03-structural-cleanup
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx
  - src/components/chat/view/subcomponents/ProviderDropdown.tsx
  - src/components/chat/view/subcomponents/ComposerProviderPicker.tsx
  - src/components/chat/view/ChatInterface.tsx
  - src/components/chat/view/subcomponents/ChatComposer.tsx
  - src/components/chat/hooks/useChatProviderState.ts
autonomous: true
requirements:
  - FORK-03

must_haves:
  truths:
    - "New sessions auto-start with Claude as the default provider (no provider selection gate)"
    - "A dropdown in the chat header shows 'Provider (Active Model)' and allows switching between Claude, Codex, and Gemini"
    - "A compact provider logo icon near the send button opens a mini-picker for provider switching"
    - "Each provider independently remembers its last-used model across sessions"
    - "A one-time welcome screen appears on first-ever session, mentioning other providers are available via header dropdown"
    - "After welcome dismissal, new sessions go straight to chat with Claude"
    - "Provider switching mid-session keeps conversation history and changes API for subsequent messages"
    - "npm run build succeeds with zero errors"
  artifacts:
    - path: "src/components/chat/view/subcomponents/ProviderDropdown.tsx"
      provides: "Header provider dropdown component"
      exports: ["ProviderDropdown"]
    - path: "src/components/chat/view/subcomponents/ComposerProviderPicker.tsx"
      provides: "Composer area mini-picker component"
      exports: ["ComposerProviderPicker"]
    - path: "src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx"
      provides: "Repurposed as one-time welcome screen"
  key_links:
    - from: "ProviderDropdown.tsx"
      to: "useChatProviderState.ts"
      via: "provider state and setProvider callback"
      pattern: "setProvider|provider"
    - from: "ComposerProviderPicker.tsx"
      to: "useChatProviderState.ts"
      via: "same provider state (shared with dropdown)"
      pattern: "setProvider|provider"
    - from: "ProviderSelectionEmptyState.tsx"
      to: "localStorage"
      via: "loom-welcomed flag check"
      pattern: "loom-welcomed"
    - from: "ChatInterface.tsx"
      to: "ProviderDropdown.tsx"
      via: "rendered in chat header area"
      pattern: "ProviderDropdown"
    - from: "ChatComposer.tsx"
      to: "ComposerProviderPicker.tsx"
      via: "rendered near send button"
      pattern: "ComposerProviderPicker"
---

<objective>
Build provider UX: default-to-Claude flow, header dropdown, composer mini-picker, and one-time welcome screen.

Purpose: Replace the old 4-provider selection gate with a streamlined UX where Claude is the default, switching is available via header dropdown and composer picker, each provider remembers its last model, and a one-time welcome screen introduces the app on first use. Three providers remain: Claude, Codex, Gemini (per user decision to keep Codex).

Output: ProviderDropdown.tsx, ComposerProviderPicker.tsx (new components); ProviderSelectionEmptyState.tsx repurposed as welcome screen; ChatInterface.tsx and ChatComposer.tsx wired with new provider UX.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structural-cleanup/03-CONTEXT.md
@.planning/phases/03-structural-cleanup/03-RESEARCH.md
@.planning/phases/03-structural-cleanup/03-03-SUMMARY.md

<interfaces>
<!-- After Cursor removal (plan 03-03), these are the key interfaces -->

From src/types/app.ts:
```typescript
export type SessionProvider = 'claude' | 'codex' | 'gemini';
```

From src/components/chat/hooks/useChatProviderState.ts (post-Cursor-removal):
```typescript
// Returns provider state with per-provider model memory
// Already has: provider, setProvider, claudeModel, codexModel, geminiModel
// Already persists to localStorage independently per model
// Default provider is 'claude' via localStorage fallback
```

From src/components/llm-logo-provider/SessionProviderLogo.tsx:
```typescript
// Renders provider logo SVG for given SessionProvider
// Already handles claude, codex, gemini (cursor removed in 03-03)
```

From shared/modelConstants.js:
```javascript
export const CLAUDE_MODELS = [...];  // Array of { id, name } model objects
export const CODEX_MODELS = [...];
export const GEMINI_MODELS = [...];
```

User decisions (LOCKED):
- Header dropdown: "Provider (Active Model)" format, e.g. "Claude (Sonnet 4)"
- Composer picker: Compact logo icon near send button, click opens mini-picker
- Welcome screen: First-time only, mentions other providers, after dismiss → straight to chat
- Per-provider model memory: Each provider remembers last-used model independently
- All three providers with equal visual treatment
- Claude's discretion: dropdown styling, welcome copy, mid-session behavior, "first-time" persistence
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProviderDropdown and ComposerProviderPicker components</name>
  <files>
    src/components/chat/view/subcomponents/ProviderDropdown.tsx (CREATE)
    src/components/chat/view/subcomponents/ComposerProviderPicker.tsx (CREATE)
    src/components/chat/view/ChatInterface.tsx
    src/components/chat/view/subcomponents/ChatComposer.tsx
  </files>
  <action>
    **1. Create ProviderDropdown.tsx:**

    A compact dropdown in the chat header showing the current provider and model, with a dropdown to switch.

    Props interface:
    ```typescript
    interface ProviderDropdownProps {
      provider: SessionProvider;
      modelLabel: string;  // human-readable current model name
      onProviderChange: (provider: SessionProvider) => void;
      providers: Array<{ id: SessionProvider; name: string; model: string }>;
    }
    ```

    Implementation:
    - Display: `SessionProviderLogo` (small, ~16px) + "Claude (Sonnet 4)" text + ChevronDown icon
    - Click toggles open/closed state
    - Dropdown panel: Lists all 3 providers with their logo + "Provider (Model)" text
    - Current provider has a Check icon or highlight
    - Click-outside closes the dropdown (useRef + useEffect click handler)
    - On provider select: call `onProviderChange`, close dropdown
    - Styling: Tailwind classes matching warm dark theme — bg-surface, text-primary, border-border, hover states
    - Use `lucide-react` for ChevronDown and Check icons (already in project)
    - Compact: the trigger button should be small enough for a header bar (h-8, text-sm)

    **2. Create ComposerProviderPicker.tsx:**

    A compact provider logo icon near the send button that opens a mini-picker popup.

    Props interface:
    ```typescript
    interface ComposerProviderPickerProps {
      provider: SessionProvider;
      onProviderChange: (provider: SessionProvider) => void;
    }
    ```

    Implementation:
    - Display: Just a `SessionProviderLogo` icon (~20px) with subtle border/bg
    - Click toggles a small popup above/below the icon
    - Popup shows 3 provider logo buttons in a row (or small grid)
    - Each button: provider logo + provider name below
    - Current provider has subtle highlight
    - Click-outside closes popup
    - Minimal visual footprint — should not clutter the composer area

    **3. Wire ProviderDropdown into ChatInterface.tsx:**
    - Import ProviderDropdown
    - Place it in the chat header area (look for the header section of ChatInterface)
    - Connect to provider state from `useChatProviderState`:
      - `provider` → current provider
      - `modelLabel` → derive from current model name for the active provider
      - `onProviderChange` → `setProvider` callback
      - `providers` → build from CLAUDE_MODELS/CODEX_MODELS/GEMINI_MODELS, using the remembered model for each

    **4. Wire ComposerProviderPicker into ChatComposer.tsx:**
    - Import ComposerProviderPicker
    - Place it near the send button (before or after, visually compact)
    - Connect to the same provider state
    - Ensure both the dropdown and picker update the same state (they share `useChatProviderState`)

    **Mid-session switching behavior (Claude's discretion):** Keep it simple — switching provider mid-session changes the API endpoint for subsequent messages but keeps the conversation history visible. No new session creation. This matches the existing code behavior.
  </action>
  <verify>
    <automated>test -f src/components/chat/view/subcomponents/ProviderDropdown.tsx && test -f src/components/chat/view/subcomponents/ComposerProviderPicker.tsx && npm run typecheck 2>&1 | tail -10</automated>
    Both files must exist. TypeScript check must pass.
  </verify>
  <done>ProviderDropdown renders in chat header showing "Provider (Model)" with working dropdown to switch. ComposerProviderPicker renders near send button with logo icon and mini-picker popup. Both connected to shared provider state. TypeScript passes.</done>
  <depth>
    - Dropdown open/closed state: Toggle on click, close on click-outside, close on Escape key
    - Empty state: If model list for a provider is empty, still show provider name (graceful)
    - Keyboard: Escape closes dropdown/picker
    - Visual: Current provider highlighted in dropdown list (Check icon or bg tint)
    - Integration: Both ProviderDropdown and ComposerProviderPicker update the same shared state — changing in one reflects immediately in the other
    - Responsive: Dropdown trigger text can truncate on narrow viewports (model name hidden on sm, shown on md+)
  </depth>
</task>

<task type="auto">
  <name>Task 2: Repurpose ProviderSelectionEmptyState as first-time welcome screen</name>
  <files>
    src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx
    src/components/chat/hooks/useChatProviderState.ts
  </files>
  <action>
    **1. Refactor ProviderSelectionEmptyState.tsx:**

    The current component shows a 4-provider grid (now 3 after Cursor removal) for every new session. Repurpose it as a one-time welcome screen:

    - Add a `localStorage.getItem('loom-welcomed')` check at the top
    - If `loom-welcomed` is `'true'`, return `null` (or the normal empty chat state — a minimal "Start a new conversation" prompt)
    - If `loom-welcomed` is NOT set (first-time user), show the welcome screen:

    Welcome screen content (Claude's discretion on copy):
    - Heading: "Welcome to Loom"
    - Subheading: "Your AI-powered development companion"
    - Brief description: "Loom defaults to Claude for a great out-of-the-box experience. You can switch between Claude, Codex, and Gemini anytime using the provider dropdown in the header."
    - Show the 3 provider logos with names (equal visual treatment, not a selection grid — informational only)
    - A "Start chatting" button that:
      a. Sets `localStorage.setItem('loom-welcomed', 'true')`
      b. Focuses the chat textarea
      c. Dismisses the welcome screen

    - After dismissal, the component returns null (or a minimal empty state) for all future new sessions
    - New sessions go straight to the chat composer with Claude active (this is already the default from useChatProviderState)

    **2. Verify default-to-Claude flow in useChatProviderState.ts:**

    - Confirm that the hook already defaults to `'claude'` when no provider is set in localStorage
    - If not, set the default to `'claude'`
    - Per-provider model memory should already work (claudeModel, codexModel, geminiModel are persisted independently). Verify this is intact after Cursor removal.

    **3. Styling:**
    - Welcome screen: Centered, warm palette, matches the app's dark theme
    - Provider logos: Use existing SessionProviderLogo component
    - Button: Warm accent color (amber/copper), not too large
    - Overall feel: Inviting but minimal — this is a one-time screen, not an onboarding wizard

    **4. Build and verify:**
    - `npm run build` must succeed
    - Manually verify the welcome flow logic: first visit shows welcome, after dismiss it's gone forever
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -n "loom-welcomed" src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx | head -5</automated>
    Build must succeed. The loom-welcomed localStorage check must appear in the file.
  </verify>
  <done>ProviderSelectionEmptyState repurposed: first-time users see welcome screen with provider info and "Start chatting" button. After dismissal, localStorage flag prevents it from showing again. New sessions default to Claude. Per-provider model memory intact. npm run build succeeds.</done>
  <depth>
    - First-time flow: Welcome screen → "Start chatting" → localStorage flag set → textarea focused → user starts typing
    - Return-user flow: No welcome screen, straight to empty chat with Claude
    - Welcome screen shows all 3 providers with equal treatment (logos + names, not a grid selector)
    - Button focus: After dismiss, focus moves to chat textarea for immediate typing
    - Edge case: If localStorage is cleared, welcome screen shows again (acceptable)
    - No session creation on dismiss — user is in the empty state, ready to type
  </depth>
</task>

</tasks>

<verification>
1. `npm run build` — succeeds
2. `test -f src/components/chat/view/subcomponents/ProviderDropdown.tsx` — exists
3. `test -f src/components/chat/view/subcomponents/ComposerProviderPicker.tsx` — exists
4. `grep "loom-welcomed" src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx` — has localStorage check
5. `grep "ProviderDropdown" src/components/chat/view/ChatInterface.tsx` — wired into header
6. `grep "ComposerProviderPicker" src/components/chat/view/subcomponents/ChatComposer.tsx` — wired into composer
</verification>

<success_criteria>
- ProviderDropdown shows "Provider (Active Model)" in header, switches between 3 providers
- ComposerProviderPicker shows logo icon near send button, opens mini-picker
- Both update shared provider state — changing one reflects in the other
- Welcome screen appears once on first-ever session, mentions other providers
- After dismiss, new sessions go straight to chat with Claude
- Per-provider model memory works (each provider remembers its last model)
- Provider switching mid-session keeps history, changes API
- npm run build succeeds
- FORK-03 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-structural-cleanup/03-04-SUMMARY.md`
</output>
