---
phase: 03-structural-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - server/cursor-cli.js
  - server/routes/cursor.js
  - server/index.js
  - server/projects.js
  - server/routes/cli-auth.js
  - server/routes/commands.js
  - server/routes/agent.js
  - server/gemini-cli.js
  - src/components/llm-logo-provider/CursorLogo.tsx
  - src/components/llm-logo-provider/SessionProviderLogo.tsx
  - src/types/app.ts
  - src/components/chat/hooks/useChatProviderState.ts
  - src/components/chat/hooks/useChatComposerState.ts
  - src/components/chat/hooks/useChatRealtimeHandlers.ts
  - src/components/chat/hooks/useChatSessionState.ts
  - src/components/chat/hooks/useFileMentions.tsx
  - src/components/chat/hooks/useSlashCommands.ts
  - src/components/chat/view/ChatInterface.tsx
  - src/components/chat/view/subcomponents/ChatComposer.tsx
  - src/components/chat/view/subcomponents/ChatMessagesPane.tsx
  - src/components/chat/view/subcomponents/CommandMenu.tsx
  - src/components/chat/view/subcomponents/MessageComponent.tsx
  - src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx
  - src/components/chat/view/subcomponents/AssistantThinkingIndicator.tsx
  - src/components/chat/view/subcomponents/PermissionRequestsBanner.tsx
  - src/components/chat/tools/components/CollapsibleDisplay.tsx
  - src/components/chat/tools/components/CollapsibleSection.tsx
  - src/components/chat/tools/components/DiffViewer.tsx
  - src/components/chat/tools/components/InteractiveRenderers/AskUserQuestionPanel.tsx
  - src/components/chat/tools/components/SubagentContainer.tsx
  - src/components/settings/constants/constants.ts
  - src/components/settings/types/types.ts
  - src/components/settings/hooks/useSettingsController.ts
  - src/components/settings/view/Settings.tsx
  - src/components/settings/view/tabs/agents-settings/AgentListItem.tsx
  - src/components/settings/view/tabs/agents-settings/AgentsSettingsTab.tsx
  - src/components/settings/view/tabs/agents-settings/sections/AgentCategoryContentSection.tsx
  - src/components/settings/view/tabs/agents-settings/sections/AgentSelectorSection.tsx
  - src/components/settings/view/tabs/agents-settings/sections/content/AccountContent.tsx
  - src/components/settings/view/tabs/agents-settings/sections/content/McpServersContent.tsx
  - src/components/settings/view/tabs/agents-settings/sections/content/PermissionsContent.tsx
  - src/components/settings/view/tabs/agents-settings/types.ts
  - src/components/settings/view/tabs/tasks-settings/TasksSettingsTab.tsx
  - src/components/sidebar/types/types.ts
  - src/components/sidebar/utils/utils.ts
  - src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx
  - src/components/sidebar/view/subcomponents/SidebarProjectSessions.tsx
  - src/components/sidebar/view/subcomponents/SidebarSessionItem.tsx
  - src/hooks/useProjectsState.ts
  - shared/modelConstants.js
  - src/components/code-editor/hooks/useEditorSidebar.ts
  - src/components/code-editor/utils/editorStyles.ts
  - src/components/code-editor/view/EditorSidebar.tsx
  - src/components/shell/constants/constants.ts
  - src/components/shell/utils/auth.ts
  - src/components/shell/view/subcomponents/ShellHeader.tsx
  - src/components/QuickSettingsPanel.jsx
  - src/components/ErrorBoundary.jsx
  - src/components/LoginModal.jsx
  - src/components/Onboarding.jsx
  - src/components/NextTaskBanner.jsx
  - src/components/TaskCard.jsx
  - src/components/TaskDetail.jsx
  - src/components/TaskList.jsx
  - src/components/TaskMasterSetupWizard.jsx
  - src/components/mic-button/view/MicButtonView.tsx
  - src/components/file-tree/view/FileTreeNode.tsx
  - src/components/git-panel/view/changes/ChangesView.tsx
  - src/components/git-panel/view/changes/CommitComposer.tsx
  - src/components/git-panel/view/changes/FileChangeItem.tsx
  - src/components/git-panel/view/history/CommitHistoryItem.tsx
  - src/components/git-panel/view/modals/NewBranchModal.tsx
  - src/components/main-content/view/subcomponents/MainContentTitle.tsx
  - src/components/ui/input.tsx
  - src/components/sidebar/view/modals/VersionUpgradeModal.tsx
autonomous: true
requirements:
  - FORK-02

must_haves:
  truths:
    - "No Cursor-specific code, routes, or UI elements exist in the codebase"
    - "SessionProvider type is 'claude' | 'codex' | 'gemini' (no 'cursor')"
    - "Provider dropdown/lists show Claude, Codex, and Gemini only (no Cursor)"
    - "cursorSessions property does not exist on Project interface"
    - "CURSOR_MODELS is not exported from shared/modelConstants.js"
    - "WebSocket handler has no cursor-command, cursor-resume, or cursor-abort message types"
    - "npm run build succeeds with zero errors"
  artifacts:
    - path: "server/cursor-cli.js"
      provides: "DELETED - must not exist"
    - path: "server/routes/cursor.js"
      provides: "DELETED - must not exist"
    - path: "src/components/llm-logo-provider/CursorLogo.tsx"
      provides: "DELETED - must not exist"
    - path: "src/types/app.ts"
      provides: "SessionProvider = 'claude' | 'codex' | 'gemini'"
      contains: "'claude' | 'codex' | 'gemini'"
    - path: "shared/modelConstants.js"
      provides: "CLAUDE_MODELS, CODEX_MODELS, GEMINI_MODELS only"
  key_links:
    - from: "server/index.js"
      to: "server/cursor-cli.js"
      via: "REMOVED - no cursor import, no cursor route mounting"
      pattern: "cursor-cli|cursor.js"
    - from: "src/components/llm-logo-provider/SessionProviderLogo.tsx"
      to: "CursorLogo.tsx"
      via: "REMOVED - no cursor branch in provider logo switch"
      pattern: "CursorLogo|cursor"
---

<objective>
Remove all Cursor CLI backend code from both server and client.

Purpose: Eliminate all Cursor-specific infrastructure (server processes, routes, WebSocket handlers, types, UI elements) while preserving Claude, Codex, and Gemini providers intact. This is a subtractive cleanup — delete Cursor files and remove Cursor branches from shared code.

Output: 3 files deleted (server/cursor-cli.js, server/routes/cursor.js, CursorLogo.tsx). ~65 files modified to remove Cursor references. SessionProvider type reduced to 3 members. Clean build.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structural-cleanup/03-RESEARCH.md
@.planning/phases/03-structural-cleanup/03-01-SUMMARY.md
@.planning/phases/03-structural-cleanup/03-02-SUMMARY.md

<interfaces>
<!-- Key types that change -->

From src/types/app.ts (BEFORE):
```typescript
export type SessionProvider = 'claude' | 'cursor' | 'codex' | 'gemini';

export interface Project {
  cursorSessions?: ProjectSession[];
  // ... other fields stay
}
```

From src/types/app.ts (AFTER):
```typescript
export type SessionProvider = 'claude' | 'codex' | 'gemini';

export interface Project {
  // cursorSessions removed
  // ... other fields stay
}
```

From shared/modelConstants.js (REMOVE):
```javascript
export const CURSOR_MODELS = [...]  // DELETE this export entirely
```

From src/components/settings/constants/constants.ts (REMOVE cursor from):
```typescript
export const AGENT_PROVIDERS = ['claude', 'cursor', 'codex', 'gemini']
// → ['claude', 'codex', 'gemini']
```

From src/components/settings/types/types.ts (REMOVE):
```typescript
// Remove 'cursor' from AgentProvider type
// Remove CursorPermissionsState interface
// Remove cursor from SettingsStoragePayload
```

From src/components/sidebar/types/types.ts (REMOVE):
```typescript
// Remove isCursorSession from SessionViewModel
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Cursor server files and strip Cursor from all server code</name>
  <files>
    server/cursor-cli.js (DELETE)
    server/routes/cursor.js (DELETE)
    server/index.js
    server/projects.js
    server/routes/cli-auth.js
    server/routes/commands.js
    server/routes/agent.js
    server/gemini-cli.js
    server/routes/git.js
  </files>
  <action>
    1. **Delete Cursor server files:**
       - `rm server/cursor-cli.js` (275 lines — Cursor CLI process spawner)
       - `rm server/routes/cursor.js` (794 lines — Cursor REST API routes)

    2. **Strip Cursor from server/index.js (~30 references):**
       This is a 2,017-line file. Search systematically for ALL cursor patterns:
       - Remove `require('./cursor-cli')` or `import` of cursor-cli
       - Remove cursor routes mounting (e.g., `app.use('/api/cursor', cursorRoutes)`)
       - Remove WebSocket message handlers: `cursor-command`, `cursor-resume`, `cursor-abort`
       - Remove cursor provider branches in switch/if statements
       - Remove `spawnCursor`, `abortCursorSession`, `isCursorSessionActive`, `getActiveCursorSessions` and any functions that reference them
       - Keep all claude/codex/gemini branches intact
       - Search patterns to grep: `'cursor'`, `cursor-`, `Cursor`, `spawnCursor`, `abortCursor`, `cursorSession`

    3. **Strip Cursor from server/projects.js (~12 references):**
       - Remove `getCursorSessions()` function
       - Remove `cursorSessions` from project data loading/aggregation
       - Remove cursor project directory handling
       - Keep claude/codex/gemini session loading intact

    4. **Strip Cursor from server/routes/cli-auth.js (2 references):**
       - Remove `/cursor/status` route
       - Remove cursor-agent spawn reference

    5. **Strip Cursor from server/routes/commands.js (3 references):**
       - Remove CURSOR_MODELS usage/import
       - Remove cursor provider branch in model list command

    6. **Strip Cursor from server/routes/agent.js:**
       - Remove any cursor provider branch

    7. **Strip Cursor from server/gemini-cli.js:**
       - Remove any cursor comparison/reference (minor)

    8. **Strip Cursor from server/routes/git.js:**
       - Remove any cursor reference (minor)

    For each file: search for ALL occurrences of 'cursor' (case-insensitive), evaluate each, remove cursor-specific code while keeping claude/codex/gemini code intact.
  </action>
  <verify>
    <automated>test ! -f server/cursor-cli.js && test ! -f server/routes/cursor.js && echo "deleted" && grep -rn -i "cursor" server/ --include="*.js" 2>/dev/null | grep -v "node_modules" | grep -v ".git" | wc -l</automated>
    Deleted files must not exist. Grep count should be 0 (or very close — some non-cursor uses of the word "cursor" like CSS cursor property may remain).
  </verify>
  <done>server/cursor-cli.js and server/routes/cursor.js deleted. All cursor-specific code removed from server/index.js, server/projects.js, and all server route files. Only claude/codex/gemini provider paths remain in server code.</done>
</task>

<task type="auto">
  <name>Task 2: Delete CursorLogo and strip Cursor from all frontend code</name>
  <files>
    src/components/llm-logo-provider/CursorLogo.tsx (DELETE)
    src/types/app.ts
    src/components/llm-logo-provider/SessionProviderLogo.tsx
    src/components/chat/hooks/useChatProviderState.ts
    src/components/chat/hooks/useChatComposerState.ts
    src/components/chat/hooks/useChatRealtimeHandlers.ts
    src/components/chat/hooks/useChatSessionState.ts
    src/components/chat/hooks/useFileMentions.tsx
    src/components/chat/hooks/useSlashCommands.ts
    src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx
    src/components/settings/constants/constants.ts
    src/components/settings/types/types.ts
    src/components/settings/hooks/useSettingsController.ts
    src/components/sidebar/types/types.ts
    src/components/sidebar/utils/utils.ts
    src/hooks/useProjectsState.ts
    shared/modelConstants.js
    (Plus ~40 additional files with minor cursor string references)
  </files>
  <action>
    **Phase A — Core type/interface changes (do these first):**

    1. **src/types/app.ts:**
       - Change `SessionProvider = 'claude' | 'cursor' | 'codex' | 'gemini'` → `'claude' | 'codex' | 'gemini'`
       - Remove `cursorSessions?: ProjectSession[]` from `Project` interface

    2. **shared/modelConstants.js:**
       - Remove the entire `CURSOR_MODELS` export array
       - Keep CLAUDE_MODELS, CODEX_MODELS, GEMINI_MODELS

    3. **src/components/settings/types/types.ts:**
       - Remove `'cursor'` from `AgentProvider` type
       - Remove `CursorPermissionsState` interface (if exists)
       - Remove cursor-related fields from `SettingsStoragePayload`

    4. **src/components/settings/constants/constants.ts:**
       - Remove `'cursor'` from `AGENT_PROVIDERS` array
       - Remove `DEFAULT_CURSOR_PERMISSIONS` (if exists)
       - Remove cursor from `AUTH_STATUS_ENDPOINTS`

    5. **src/components/sidebar/types/types.ts:**
       - Remove `isCursorSession` from `SessionViewModel`

    **Phase B — Delete CursorLogo and update provider logo:**

    6. **Delete `src/components/llm-logo-provider/CursorLogo.tsx`**

    7. **src/components/llm-logo-provider/SessionProviderLogo.tsx:**
       - Remove `CursorLogo` import
       - Remove cursor case from provider switch/conditional

    **Phase C — Hook and state cleanup:**

    8. **src/components/chat/hooks/useChatProviderState.ts:**
       - Remove `cursorModel` state variable and setter
       - Remove cursor config fetch logic
       - Remove `CURSOR_MODELS` import
       - Keep claude, codex, gemini model state intact

    9. **src/hooks/useProjectsState.ts:**
       - Remove `cursorSessions` from project comparison and session aggregation

    10. **src/components/settings/hooks/useSettingsController.ts:**
        - Remove cursor-related state and settings

    11. **src/components/sidebar/utils/utils.ts:**
        - Remove cursor session view model logic

    **Phase D — Component cleanup (remaining ~50 files):**

    12. For each file in the frontend that references 'cursor' (use the list from research: ~62 frontend files total beyond those already handled):
        - Search for string `'cursor'` in type unions, arrays, switch cases, conditionals
        - Remove the cursor branch/entry
        - Keep claude/codex/gemini branches intact
        - For files that ONLY have 'cursor' in a comment or CSS cursor property, skip them

    Run after all changes: `npm run typecheck` to catch any remaining broken references. Fix all TypeScript errors.

    **Phase E — Build verification:**

    13. Run `npm run build` — must succeed
    14. Run `grep -rni "cursor" src/ --include="*.tsx" --include="*.ts" --include="*.jsx" | grep -v "node_modules" | grep -vi "css\|style\|pointer\|text\|not-allowed\|move\|grab\|resize\|default\|auto\|cursor:"` — should return 0 meaningful Cursor-provider references
  </action>
  <verify>
    <automated>test ! -f src/components/llm-logo-provider/CursorLogo.tsx && npm run build 2>&1 | tail -5 && echo "---" && grep -rni "'cursor'" src/types/app.ts shared/modelConstants.js src/components/settings/constants/constants.ts 2>/dev/null | wc -l</automated>
    CursorLogo must not exist. Build must succeed. Grep count must be 0 (no 'cursor' string in key type/constant files).
  </verify>
  <done>CursorLogo.tsx deleted. SessionProvider type is 'claude' | 'codex' | 'gemini'. cursorSessions removed from Project interface. CURSOR_MODELS removed from modelConstants. All cursor branches removed from hooks, settings, sidebar, and all other frontend components. npm run build succeeds.</done>
</task>

</tasks>

<verification>
1. `test ! -f server/cursor-cli.js && test ! -f server/routes/cursor.js && test ! -f src/components/llm-logo-provider/CursorLogo.tsx` — all 3 deleted
2. `grep "'cursor'" src/types/app.ts` — returns 0 matches
3. `grep "CURSOR_MODELS" shared/modelConstants.js` — returns 0 matches
4. `grep "'cursor'" src/components/settings/constants/constants.ts` — returns 0 matches
5. `npm run build` — succeeds
6. `grep -rni "cursorSessions\|CursorLogo\|CURSOR_MODELS\|cursor-cli\|cursor-command\|cursor-resume\|cursor-abort" src/ server/ shared/ --include="*.ts" --include="*.tsx" --include="*.jsx" --include="*.js" | grep -v node_modules` — returns 0 matches
</verification>

<success_criteria>
- server/cursor-cli.js, server/routes/cursor.js, CursorLogo.tsx all deleted
- SessionProvider type = 'claude' | 'codex' | 'gemini'
- Project interface has no cursorSessions
- CURSOR_MODELS removed from modelConstants
- All cursor WebSocket handlers removed from server/index.js
- All cursor branches removed from frontend switch/if statements
- npm run build succeeds with zero errors
- FORK-02 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-structural-cleanup/03-03-SUMMARY.md`
</output>
