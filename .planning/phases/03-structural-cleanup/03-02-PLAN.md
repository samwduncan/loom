---
phase: 03-structural-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/view/Settings.tsx
  - src/components/settings/view/SettingsMainTabs.tsx
  - src/components/settings/view/modals/ClaudeMcpFormModal.tsx
  - src/components/settings/view/modals/CodexMcpFormModal.tsx
  - src/components/settings/view/tabs/agents-settings/AgentListItem.tsx
  - src/components/settings/view/tabs/agents-settings/sections/AgentCategoryTabsSection.tsx
  - src/components/settings/view/tabs/agents-settings/sections/content/AccountContent.tsx
  - src/components/settings/view/tabs/agents-settings/sections/content/McpServersContent.tsx
  - src/components/settings/view/tabs/agents-settings/sections/content/PermissionsContent.tsx
  - src/components/settings/view/tabs/AppearanceSettingsTab.tsx
  - src/components/settings/view/tabs/api-settings/CredentialsSettingsTab.tsx
  - src/components/settings/view/tabs/api-settings/sections/ApiKeysSection.tsx
  - src/components/settings/view/tabs/api-settings/sections/GithubCredentialsSection.tsx
  - src/components/settings/view/tabs/api-settings/sections/NewApiKeyAlert.tsx
  - src/components/settings/view/tabs/api-settings/sections/VersionInfoSection.tsx
  - src/components/settings/view/tabs/git-settings/GitSettingsTab.tsx
  - src/components/settings/view/tabs/tasks-settings/TasksSettingsTab.tsx
  - src/components/QuickSettingsPanel.jsx
  - src/components/sidebar/view/Sidebar.tsx
  - src/components/sidebar/view/modals/VersionUpgradeModal.tsx
autonomous: true
requirements:
  - FORK-01

must_haves:
  truths:
    - "No useTranslation import or t() call remains in any settings or sidebar component"
    - "Zero react-i18next imports exist anywhere in the entire codebase"
    - "npm run build succeeds with zero i18n-related errors"
  artifacts:
    - path: "src/components/settings/"
      provides: "All settings components use English string literals"
    - path: "src/components/sidebar/"
      provides: "All sidebar components use English string literals"
    - path: "src/components/QuickSettingsPanel.jsx"
      provides: "English string literals, no i18n"
  key_links:
    - from: "entire codebase"
      to: "react-i18next"
      via: "REMOVED - zero imports remain"
      pattern: "from 'react-i18next'"
---

<objective>
Strip i18n from the remaining 20 settings/sidebar components and verify zero i18n references remain codebase-wide.

Purpose: Complete the i18n removal started in plan 03-01. After this plan, zero useTranslation, t(), or react-i18next imports exist anywhere in the codebase. The i18n infrastructure was already deleted in 03-01 — this plan handles the remaining component-level cleanup and does a final codebase-wide verification.

Output: 20 component files with English string literals; zero i18n references codebase-wide; clean build.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structural-cleanup/03-RESEARCH.md
@.planning/phases/03-structural-cleanup/03-01-SUMMARY.md

Source of truth for English replacement strings:
NOTE: src/i18n/locales/en/ was deleted in plan 03-01. The executor MUST read the locale files BEFORE plan 03-01 deletes them, OR use the 03-01-SUMMARY.md which should document the approach. Alternative: the locale content is captured in the RESEARCH.md code examples.

IMPORTANT: If running in parallel with plan 03-01 (same wave), this plan must read the en/*.json locale files at the START before 03-01 deletes them. Read all 7 locale files first, then perform replacements.

Locale files to read FIRST (before they are deleted by 03-01):
@src/i18n/locales/en/settings.json (primary — most settings strings)
@src/i18n/locales/en/common.json (used by AppContent, VersionUpgradeModal)
@src/i18n/locales/en/sidebar.json (used by Sidebar)
@src/i18n/locales/en/tasks.json (used by TasksSettingsTab)

<interfaces>
<!-- i18n replacement pattern (same as 03-01) -->

Simple string:
  BEFORE: import { useTranslation } from 'react-i18next';
          const { t } = useTranslation('settings');
          {t('appearance.theme')}
  AFTER:  {"Theme"}
  (Remove both the import and const { t } lines)

Interpolated string:
  BEFORE: t('key.path', { count: n })
  AFTER:  `${n} items` (look up en/*.json, replace {{count}} with ${n})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip useTranslation from 20 settings/sidebar components</name>
  <files>
    src/components/settings/view/Settings.tsx
    src/components/settings/view/SettingsMainTabs.tsx
    src/components/settings/view/modals/ClaudeMcpFormModal.tsx
    src/components/settings/view/modals/CodexMcpFormModal.tsx
    src/components/settings/view/tabs/agents-settings/AgentListItem.tsx
    src/components/settings/view/tabs/agents-settings/sections/AgentCategoryTabsSection.tsx
    src/components/settings/view/tabs/agents-settings/sections/content/AccountContent.tsx
    src/components/settings/view/tabs/agents-settings/sections/content/McpServersContent.tsx
    src/components/settings/view/tabs/agents-settings/sections/content/PermissionsContent.tsx
    src/components/settings/view/tabs/AppearanceSettingsTab.tsx
    src/components/settings/view/tabs/api-settings/CredentialsSettingsTab.tsx
    src/components/settings/view/tabs/api-settings/sections/ApiKeysSection.tsx
    src/components/settings/view/tabs/api-settings/sections/GithubCredentialsSection.tsx
    src/components/settings/view/tabs/api-settings/sections/NewApiKeyAlert.tsx
    src/components/settings/view/tabs/api-settings/sections/VersionInfoSection.tsx
    src/components/settings/view/tabs/git-settings/GitSettingsTab.tsx
    src/components/settings/view/tabs/tasks-settings/TasksSettingsTab.tsx
    src/components/QuickSettingsPanel.jsx
    src/components/sidebar/view/Sidebar.tsx
    src/components/sidebar/view/modals/VersionUpgradeModal.tsx
  </files>
  <action>
    FIRST: Read all English locale JSON files to have replacement strings available:
    - src/i18n/locales/en/settings.json
    - src/i18n/locales/en/common.json
    - src/i18n/locales/en/sidebar.json
    - src/i18n/locales/en/tasks.json
    (If these files were already deleted by 03-01 running in parallel, use the strings from 03-RESEARCH.md code examples or reconstruct from component context.)

    For each of the 20 files:

    1. Read the file and identify:
       - The `import { useTranslation } from 'react-i18next'` line
       - The `const { t } = useTranslation('namespace')` line
       - All `t('key.path')` and `t('key.path', { var: value })` calls

    2. For each `t()` call:
       - Identify the namespace (from useTranslation arg, or 'common' if no arg, or prefixed like 'sidebar:key')
       - Look up the key in the corresponding en/*.json
       - Replace with English string literal
       - Convert `{{var}}` interpolation to `${var}` template literals

    3. Remove the useTranslation import and const { t } destructuring
    4. If `i18n` object is also imported/used, remove that too

    Special cases:
    - **Sidebar.tsx**: Uses `useTranslation(['sidebar', 'common'])` with namespace prefixes like `t('sidebar:key')` and `t('common:key')`. Look up each in the correct namespace.
    - **AppearanceSettingsTab.tsx**: LanguageSelector import/usage was already removed in 03-01. Just handle the remaining useTranslation calls.
    - **QuickSettingsPanel.jsx**: LanguageSelector import/usage was already removed in 03-01. Just handle the remaining useTranslation calls.
    - **Settings components with deeply nested keys**: settings.json has deep nesting like `agents.permissions.title`. Follow the full path.

    Process files in groups of 5 for manageability.
  </action>
  <verify>
    <automated>grep -rn "useTranslation\|from 'react-i18next'" src/components/settings/ src/components/sidebar/ src/components/QuickSettingsPanel.jsx 2>/dev/null | wc -l</automated>
    Result must be 0.
  </verify>
  <done>All 20 settings/sidebar component files have English string literals instead of t() calls. No useTranslation or react-i18next import remains in any of these files.</done>
</task>

<task type="auto">
  <name>Task 2: Verify zero i18n references codebase-wide and build</name>
  <files>
    (verification task — reads all src/ files, modifies any stragglers)
  </files>
  <action>
    1. **Codebase-wide i18n audit:**
       Run: `grep -rn "useTranslation\|from 'react-i18next'\|from 'i18next'\|i18next-browser-languagedetector\|I18nextProvider" src/ --include="*.tsx" --include="*.ts" --include="*.jsx"`

       If ANY matches remain, fix them:
       - Remove the import
       - Replace any remaining t() calls with English strings (use context to determine the correct string)

       Also check:
       - `src/components/sidebar/hooks/useSidebarController.ts` — may reference i18n
       - `src/components/sidebar/utils/utils.ts` — may reference i18n
       - `src/components/sidebar/view/subcomponents/*.tsx` — may have i18n imports via barrel exports
       - `src/utils/dateUtils.ts` — may import i18n for locale-aware formatting

    2. **Check for orphaned i18n type references:**
       Run: `grep -rn "TFunction\|i18n\.\|\.changeLanguage\|\.language" src/ --include="*.tsx" --include="*.ts" --include="*.jsx"`
       Remove any remaining i18n API usage.

    3. **Build verification:**
       Run: `npm run build`
       Must succeed with zero errors.

       If build fails:
       - TypeScript errors about missing 'react-i18next' module → a file still imports it, fix it
       - TypeScript errors about missing 'src/i18n/' → a file still imports i18n config, fix it
       - Any other error → diagnose and fix

    4. **Final count verification:**
       Run: `grep -rn "react-i18next" src/ | wc -l` — must be 0
       Run: `grep -rn "useTranslation" src/ | wc -l` — must be 0
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && echo "---" && grep -rn "react-i18next\|useTranslation\|I18nextProvider" src/ --include="*.tsx" --include="*.ts" --include="*.jsx" 2>/dev/null | wc -l</automated>
    Build must succeed. Grep count must be 0.
  </verify>
  <done>Zero i18n references remain anywhere in src/. npm run build succeeds. The i18n system is completely eliminated from the codebase.</done>
</task>

</tasks>

<verification>
1. `grep -rn "useTranslation\|from 'react-i18next'\|I18nextProvider\|i18next" src/ --include="*.tsx" --include="*.ts" --include="*.jsx"` — returns 0 matches
2. `npm run build` — succeeds with zero errors
3. `grep "react-i18next\|i18next" package.json` — returns 0 matches
</verification>

<success_criteria>
- All 20 settings/sidebar component files use English string literals
- Zero useTranslation, react-i18next, or i18next references remain in entire src/ directory
- npm run build succeeds
- Complete i18n removal verified codebase-wide (FORK-01 fully satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/03-structural-cleanup/03-02-SUMMARY.md`
</output>
