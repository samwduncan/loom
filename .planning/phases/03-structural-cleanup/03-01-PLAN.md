---
phase: 03-structural-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/app/AppContent.tsx
  - src/components/chat/view/ChatInterface.tsx
  - src/components/chat/view/subcomponents/ChatComposer.tsx
  - src/components/chat/view/subcomponents/ChatInputControls.tsx
  - src/components/chat/view/subcomponents/ChatMessagesPane.tsx
  - src/components/chat/view/subcomponents/Markdown.tsx
  - src/components/chat/view/subcomponents/MessageComponent.tsx
  - src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx
  - src/components/chat/view/subcomponents/ThinkingModeSelector.tsx
  - src/components/code-editor/view/CodeEditor.tsx
  - src/components/file-tree/view/FileTree.tsx
  - src/components/file-tree/view/FileTreeBody.tsx
  - src/components/file-tree/view/FileTreeDetailedColumns.tsx
  - src/components/file-tree/view/FileTreeHeader.tsx
  - src/components/file-tree/view/FileTreeLoadingState.tsx
  - src/components/main-content/view/subcomponents/MainContentStateView.tsx
  - src/components/main-content/view/subcomponents/MainContentTabSwitcher.tsx
  - src/components/main-content/view/subcomponents/MainContentTitle.tsx
  - src/components/LoginForm.jsx
  - src/components/ProjectCreationWizard.jsx
  - src/components/TaskList.jsx
  - src/components/shell/view/Shell.tsx
  - src/App.tsx
  - src/main.jsx
  - src/components/LanguageSelector.jsx
  - src/i18n/
  - package.json
autonomous: true
requirements:
  - FORK-01

must_haves:
  truths:
    - "No useTranslation import or t() call remains in any chat, file-tree, code-editor, main-content, shell, or top-level component"
    - "I18nextProvider wrapper is removed from App.tsx"
    - "i18n config import is removed from main.jsx"
    - "LanguageSelector component is deleted"
    - "src/i18n/ directory is completely deleted"
    - "react-i18next, i18next, i18next-browser-languagedetector are removed from package.json dependencies"
    - "All replaced strings show correct English text (not translation keys or {{placeholders}})"
  artifacts:
    - path: "src/i18n/"
      provides: "DELETED - must not exist"
    - path: "src/components/LanguageSelector.jsx"
      provides: "DELETED - must not exist"
    - path: "package.json"
      provides: "No i18n packages in dependencies"
  key_links:
    - from: "src/App.tsx"
      to: "src/i18n/config.js"
      via: "REMOVED - no I18nextProvider, no i18n import"
      pattern: "I18nextProvider|i18n/config"
    - from: "src/main.jsx"
      to: "src/i18n/config.js"
      via: "REMOVED - no i18n import"
      pattern: "i18n/config"
---

<objective>
Strip i18n from 22 chat/core components and remove all i18n infrastructure.

Purpose: Eliminate i18n overhead from the first batch of components (chat, file-tree, code-editor, main-content, shell, top-level) and remove the entire i18n system (packages, locale files, config, I18nextProvider wrapper, LanguageSelector). After this plan, the i18n system itself is gone even though some settings/sidebar components still have useTranslation calls (cleaned in plan 03-02).

Output: 22 component files with English string literals replacing t() calls; deleted src/i18n/ directory; removed I18nextProvider from App.tsx; removed i18n import from main.jsx; deleted LanguageSelector.jsx; uninstalled i18n packages.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structural-cleanup/03-RESEARCH.md

Source of truth for English replacement strings:
@src/i18n/locales/en/chat.json
@src/i18n/locales/en/common.json
@src/i18n/locales/en/auth.json
@src/i18n/locales/en/codeEditor.json
@src/i18n/locales/en/sidebar.json
@src/i18n/locales/en/tasks.json

<interfaces>
<!-- i18n replacement pattern (from RESEARCH.md) -->

Simple string:
  BEFORE: import { useTranslation } from 'react-i18next';
          const { t } = useTranslation('chat');
          title={t('input.attachImages')}
  AFTER:  title="Attach images"
  (Remove both the import and const { t } lines)

Interpolated string:
  BEFORE: t('session.messages.showingOf', { shown, total })
  AFTER:  `Showing ${shown} of ${total} messages`
  (Look up en/*.json, replace {{var}} with ${var})

Multi-namespace:
  BEFORE: const { t } = useTranslation(['sidebar', 'common']);
          t('sidebar:key') or t('common:key')
  AFTER:  Look up in respective en/*.json file

No-namespace (default):
  BEFORE: const { t } = useTranslation();
          t('key.path')
  AFTER:  Look up in en/common.json (default namespace)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip useTranslation from 22 component files</name>
  <files>
    src/components/app/AppContent.tsx
    src/components/chat/view/ChatInterface.tsx
    src/components/chat/view/subcomponents/ChatComposer.tsx
    src/components/chat/view/subcomponents/ChatInputControls.tsx
    src/components/chat/view/subcomponents/ChatMessagesPane.tsx
    src/components/chat/view/subcomponents/Markdown.tsx
    src/components/chat/view/subcomponents/MessageComponent.tsx
    src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx
    src/components/chat/view/subcomponents/ThinkingModeSelector.tsx
    src/components/code-editor/view/CodeEditor.tsx
    src/components/file-tree/view/FileTree.tsx
    src/components/file-tree/view/FileTreeBody.tsx
    src/components/file-tree/view/FileTreeDetailedColumns.tsx
    src/components/file-tree/view/FileTreeHeader.tsx
    src/components/file-tree/view/FileTreeLoadingState.tsx
    src/components/main-content/view/subcomponents/MainContentStateView.tsx
    src/components/main-content/view/subcomponents/MainContentTabSwitcher.tsx
    src/components/main-content/view/subcomponents/MainContentTitle.tsx
    src/components/LoginForm.jsx
    src/components/ProjectCreationWizard.jsx
    src/components/TaskList.jsx
    src/components/shell/view/Shell.tsx
  </files>
  <action>
    For each of the 22 files listed above, perform the mechanical i18n replacement:

    1. Read the file and identify:
       - The `import { useTranslation } from 'react-i18next'` line
       - The `const { t } = useTranslation('namespace')` line (or destructured variants like `const { t, i18n }`)
       - All `t('key.path')` calls and `t('key.path', { var: value })` interpolated calls

    2. For each `t('key.path')` call:
       - Identify the namespace (from the useTranslation argument, or 'common' if no argument)
       - Read `src/i18n/locales/en/{namespace}.json`
       - Look up the key path (e.g., 'input.attachImages' → input → attachImages)
       - Replace the `t()` call with the English string literal
       - If the JSON value contains `{{variable}}` interpolation AND a second arg object is passed, convert to template literal: `{{count}}` → `${count}`

    3. Remove the `import { useTranslation } from 'react-i18next'` line
    4. Remove the `const { t } = useTranslation(...)` line
    5. If the component also imports `i18n` directly, remove that import too
    6. If removing useTranslation makes 'react-i18next' the only import from that line, remove the entire import statement

    IMPORTANT: For ProviderSelectionEmptyState.tsx, this file has cursor-related t() calls (cursor provider prompts). Replace ONLY the i18n calls now — Cursor content removal happens in plan 03-03. Keep the cursor string literal in place for now.

    Process files in alphabetical order. After each batch of ~5 files, verify no syntax errors by checking for balanced braces/parentheses.
  </action>
  <verify>
    <automated>grep -rn "useTranslation\|from 'react-i18next'" src/components/app/AppContent.tsx src/components/chat/ src/components/code-editor/ src/components/file-tree/ src/components/main-content/ src/components/LoginForm.jsx src/components/ProjectCreationWizard.jsx src/components/TaskList.jsx src/components/shell/ 2>/dev/null | wc -l</automated>
    Result must be 0.
  </verify>
  <done>All 22 component files have English string literals instead of t() calls. No useTranslation import remains in any of these files. Interpolated strings use template literals with the correct variable names.</done>
</task>

<task type="auto">
  <name>Task 2: Remove i18n infrastructure</name>
  <files>
    src/App.tsx
    src/main.jsx
    src/components/LanguageSelector.jsx
    src/i18n/ (entire directory)
    package.json
  </files>
  <action>
    1. **Remove I18nextProvider from App.tsx:**
       - Remove `import { I18nextProvider } from 'react-i18next'`
       - Remove `import i18n from './i18n/config.js'`
       - Remove the `<I18nextProvider i18n={i18n}>` wrapper and its closing tag
       - Keep all other providers/wrappers intact — just unwrap I18nextProvider

    2. **Remove i18n import from main.jsx:**
       - Remove the `import './i18n/config.js'` line
       - Remove the `// Initialize i18n` comment

    3. **Delete LanguageSelector.jsx:**
       - Delete `src/components/LanguageSelector.jsx`
       - Check its importers: `AppearanceSettingsTab.tsx` and `QuickSettingsPanel.jsx` both import it
       - In those files, remove the LanguageSelector import and its JSX usage (the language selector UI section)
       - NOTE: The settings/QuickSettings useTranslation calls themselves are handled in plan 03-02. Here, ONLY remove the LanguageSelector import and its usage element.

    4. **Delete the entire src/i18n/ directory:**
       - `rm -rf src/i18n/` — this removes config.js, languages.js, and all locales (en/, ko/, zh-CN/, ja/)

    5. **Uninstall i18n packages:**
       - Run: `npm uninstall react-i18next i18next i18next-browser-languagedetector`
       - Verify they are removed from package.json dependencies

    6. **Also check and clean:**
       - `src/components/file-tree/utils/fileTreeUtils.ts` — if it imports from i18n, remove that import
       - `src/utils/dateUtils.ts` — if it imports from i18n, remove that import and replace with direct English formatting
       - `src/components/sidebar/utils/utils.ts` — if it references i18n, remove
       - Any other file that imports from `src/i18n/` or `react-i18next` that is NOT in the settings/sidebar batch (plan 03-02)
  </action>
  <verify>
    <automated>test ! -d src/i18n && echo "i18n dir deleted" && grep -rn "i18next\|I18nextProvider\|LanguageSelector" src/App.tsx src/main.jsx 2>/dev/null | wc -l</automated>
    Directory must not exist. Grep count must be 0.
    Also run: npm run typecheck 2>&1 | head -30
    TypeScript errors about missing i18n modules are expected if settings/sidebar files still import react-i18next (addressed in plan 03-02). Errors about LanguageSelector or i18n/config should NOT appear.
  </verify>
  <done>I18nextProvider removed from App.tsx. i18n import removed from main.jsx. LanguageSelector.jsx deleted and its usage removed from importers. Entire src/i18n/ directory deleted. react-i18next, i18next, i18next-browser-languagedetector removed from package.json.</done>
</task>

</tasks>

<verification>
1. `grep -rn "useTranslation\|from 'react-i18next'" src/components/app/ src/components/chat/ src/components/code-editor/ src/components/file-tree/ src/components/main-content/ src/components/LoginForm.jsx src/components/ProjectCreationWizard.jsx src/components/TaskList.jsx src/components/shell/` — returns 0 matches
2. `test ! -d src/i18n` — directory does not exist
3. `test ! -f src/components/LanguageSelector.jsx` — file does not exist
4. `grep "react-i18next\|i18next" package.json` — returns 0 matches (packages removed)
5. `grep -rn "I18nextProvider" src/App.tsx` — returns 0 matches
</verification>

<success_criteria>
- 22 component files have English string literals replacing all t() calls
- No useTranslation or react-i18next import in any file covered by this plan
- src/i18n/ directory completely deleted
- LanguageSelector.jsx deleted, removed from AppearanceSettingsTab and QuickSettingsPanel
- I18nextProvider unwrapped from App.tsx
- i18n config import removed from main.jsx
- react-i18next, i18next, i18next-browser-languagedetector removed from package.json
</success_criteria>

<output>
After completion, create `.planning/phases/03-structural-cleanup/03-01-SUMMARY.md`
</output>
