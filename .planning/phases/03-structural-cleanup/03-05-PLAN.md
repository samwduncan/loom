---
phase: 03-structural-cleanup
plan: 05
type: execute
wave: 4
depends_on:
  - 03-04
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - FORK-01
  - FORK-02
  - FORK-03

must_haves:
  truths:
    - "npm run build succeeds with zero errors"
    - "Zero useTranslation or react-i18next references in codebase"
    - "Zero Cursor-specific code, routes, or UI elements in codebase"
    - "Provider dropdown in header shows Claude, Codex, Gemini with active model"
    - "Composer picker near send button allows provider switching"
    - "Welcome screen appears once for first-time users"
    - "ROADMAP success criteria updated to reflect Codex kept"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 3 success criteria reflecting Codex scope change"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FORK-03 description updated"
  key_links:
    - from: "Phase 3 plans"
      to: "ROADMAP success criteria"
      via: "All criteria met"
---

<objective>
Final verification of Phase 3: build check, audit, visual confirmation, and roadmap update.

Purpose: Confirm all three requirements (FORK-01, FORK-02, FORK-03) are fully satisfied with automated checks and a visual checkpoint. Update the ROADMAP.md success criteria to reflect the scope change (Codex kept instead of removed).

Output: Verified build, audit report, updated roadmap/requirements, visual confirmation from user.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structural-cleanup/03-01-SUMMARY.md
@.planning/phases/03-structural-cleanup/03-02-SUMMARY.md
@.planning/phases/03-structural-cleanup/03-03-SUMMARY.md
@.planning/phases/03-structural-cleanup/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full verification audit and update roadmap</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    **1. Build verification:**
    - Run `npm run build` — must succeed with zero errors
    - Run `npm run typecheck` (if separate from build) — must succeed

    **2. FORK-01 audit (i18n removed):**
    - `grep -rn "useTranslation\|from 'react-i18next'\|from 'i18next'\|I18nextProvider\|i18next-browser-languagedetector" src/ --include="*.tsx" --include="*.ts" --include="*.jsx"` — must return 0
    - `test ! -d src/i18n` — must pass (directory deleted)
    - `grep "react-i18next\|\"i18next\"\|i18next-browser-languagedetector" package.json` — must return 0
    - Count: previously 43 files with useTranslation → now 0

    **3. FORK-02 audit (Cursor removed):**
    - `test ! -f server/cursor-cli.js && test ! -f server/routes/cursor.js && test ! -f src/components/llm-logo-provider/CursorLogo.tsx` — all deleted
    - `grep -rn "cursorSessions\|CursorLogo\|CURSOR_MODELS\|cursor-cli\|cursor-command\|cursor-resume\|cursor-abort\|spawnCursor\|abortCursor" src/ server/ shared/ --include="*.ts" --include="*.tsx" --include="*.jsx" --include="*.js" | grep -v node_modules` — must return 0
    - `grep "'cursor'" src/types/app.ts` — must return 0
    - Provider type is `'claude' | 'codex' | 'gemini'`

    **4. FORK-03 audit (Provider UX):**
    - `test -f src/components/chat/view/subcomponents/ProviderDropdown.tsx` — exists
    - `test -f src/components/chat/view/subcomponents/ComposerProviderPicker.tsx` — exists
    - `grep "ProviderDropdown" src/components/chat/view/ChatInterface.tsx` — wired
    - `grep "ComposerProviderPicker" src/components/chat/view/subcomponents/ChatComposer.tsx` — wired
    - `grep "loom-welcomed" src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx` — welcome flag
    - Default provider is claude (check useChatProviderState)

    **5. Update ROADMAP.md Phase 3 success criteria:**
    Update the Phase 3 success criteria to reflect the scope change:
    - Criterion #2: Change from "provider dropdown shows Claude and Gemini only" to "provider dropdown shows Claude, Codex, and Gemini"
    - Criterion #3: Remove the line about removing `@openai/codex-sdk` from package.json (Codex is kept)
    - Add criterion about provider UX: "New sessions default to Claude, header dropdown and composer picker enable provider switching"
    - Add criterion about welcome screen: "First-time welcome screen appears once, then auto-dismisses"

    **6. Update REQUIREMENTS.md:**
    - Update FORK-03 description to reflect: "Codex KEPT. Three providers remain: Claude, Codex, Gemini. Provider UX features added (default-to-Claude, header dropdown, composer picker, welcome screen, per-provider model memory)."

    **7. Print summary of all audit results** for the checkpoint task.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && echo "=== i18n ===" && grep -rn "useTranslation" src/ --include="*.tsx" --include="*.ts" --include="*.jsx" 2>/dev/null | wc -l && echo "=== cursor ===" && grep -rn "CURSOR_MODELS\|CursorLogo\|cursor-cli" src/ server/ shared/ --include="*.ts" --include="*.tsx" --include="*.jsx" --include="*.js" 2>/dev/null | grep -v node_modules | wc -l && echo "=== provider UX ===" && test -f src/components/chat/view/subcomponents/ProviderDropdown.tsx && echo "dropdown exists" && test -f src/components/chat/view/subcomponents/ComposerProviderPicker.tsx && echo "picker exists"</automated>
    Build succeeds. i18n count = 0. cursor count = 0. Both new components exist.
  </verify>
  <done>All automated audits pass. ROADMAP.md and REQUIREMENTS.md updated to reflect Codex scope change. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Phase 3 changes</name>
  <files>(no files modified — visual verification only)</files>
  <action>
    Present the user with verification steps for visual confirmation of all Phase 3 changes.

    Phase 3 Structural Cleanup — complete:
    1. i18n removed from all 43 components (English string literals), packages uninstalled, locale files deleted
    2. Cursor removed — all server code, routes, WebSocket handlers, frontend types, UI elements deleted
    3. Provider UX — header dropdown showing "Provider (Model)", composer mini-picker near send button, one-time welcome screen, default-to-Claude, per-provider model memory
    4. Three providers remain: Claude, Codex, Gemini

    Start the dev server: `cd /home/swd/loom && npm run dev`
    Open in browser: http://100.86.4.57:5555 (or whatever port the dev server runs on)

    Check 1 — Welcome screen (clear localStorage first):
    1. Open browser DevTools → Application → Local Storage → clear `loom-welcomed` key
    2. Refresh the page
    3. Verify: A welcome screen appears mentioning Claude, Codex, and Gemini
    4. Click "Start chatting" → welcome disappears, textarea is focused
    5. Refresh again → welcome does NOT reappear (localStorage flag set)

    Check 2 — Provider dropdown:
    1. Look at the chat header bar
    2. Verify: A dropdown shows current provider and model (e.g., "Claude (Sonnet 4)")
    3. Click it → dropdown opens showing Claude, Codex, Gemini
    4. Switch to Gemini → dropdown updates, composer picker updates
    5. Switch back to Claude → previous Claude model is remembered

    Check 3 — Composer picker:
    1. Look near the send button in the composer area
    2. Verify: A small provider logo icon is visible
    3. Click it → mini-picker popup opens with 3 providers
    4. Switch provider → both dropdown and picker reflect the change

    Check 4 — No Cursor:
    1. Verify: No "Cursor" option appears in dropdown or picker
    2. Settings → verify no Cursor configuration sections

    Check 5 — English strings:
    1. Navigate through settings tabs
    2. Verify: All text is in English (no translation keys like "settings.appearance.theme" showing)
    3. Check file tree, chat interface, sidebar — all English
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
    Build must succeed. User visually confirms all 5 checks.
  </verify>
  <done>User approves visual verification. All checks pass: welcome screen works once, provider dropdown shows 3 providers, composer picker works, no Cursor anywhere, all English strings.</done>
</task>

</tasks>

<verification>
All verification handled by Task 1 automated audit and Task 2 visual checkpoint.
</verification>

<success_criteria>
- All automated audits pass (build, i18n=0, cursor=0, new components exist)
- Visual verification confirms: welcome screen works, provider dropdown works, composer picker works, no Cursor, all English strings
- ROADMAP.md updated with corrected success criteria
- REQUIREMENTS.md updated with corrected FORK-03 description
- User approves the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/03-structural-cleanup/03-05-SUMMARY.md`
</output>
