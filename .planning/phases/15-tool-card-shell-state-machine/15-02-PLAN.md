---
phase: 15-tool-card-shell-state-machine
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/src/components/chat/tools/ToolChip.tsx
  - src/src/components/chat/tools/ToolChip.test.tsx
  - src/src/components/chat/tools/tool-chip.css
  - src/src/lib/tool-registry.ts
autonomous: true
requirements: [TOOL-02, TOOL-06]

must_haves:
  truths:
    - "ToolChip displays elapsed time after tool name with middle dot separator"
    - "Error tool calls start force-expanded and show red chip border tint"
    - "Clicking ToolChip toggles ToolCardShell expansion with animated transition"
    - "Tools transitioning to rejected during streaming auto-expand via adjust-state-during-rendering"
    - "DefaultToolCard shows structured key-value rows instead of raw JSON"
    - "DefaultToolCard output renders in scrollable monospace container with max-height ~200px"
  artifacts:
    - path: "src/src/components/chat/tools/ToolChip.tsx"
      provides: "Refactored chip with elapsed time, error force-expand, ToolCardShell integration"
      exports: ["ToolChip", "ToolChipProps"]
    - path: "src/src/lib/tool-registry.ts"
      provides: "Upgraded DefaultToolCard with structured key-value layout"
      exports: ["registerTool", "getToolConfig", "ToolCardProps", "ToolConfig"]
  key_links:
    - from: "src/src/components/chat/tools/ToolChip.tsx"
      to: "src/src/components/chat/tools/ToolCardShell.tsx"
      via: "import ToolCardShell"
      pattern: "ToolCardShell"
    - from: "src/src/components/chat/tools/ToolChip.tsx"
      to: "src/src/hooks/useElapsedTime.ts"
      via: "import useElapsedTime"
      pattern: "useElapsedTime"
    - from: "src/src/components/chat/tools/ToolChip.tsx"
      to: "src/src/lib/tool-registry.ts"
      via: "import getToolConfig"
      pattern: "getToolConfig"
---

<objective>
Refactor ToolChip to integrate ToolCardShell for expansion, add elapsed time display on chips, implement error force-expand behavior, and upgrade DefaultToolCard from raw JSON to structured key-value layout.

Purpose: Wires the Plan 01 infrastructure into the existing component tree. After this, tool calls render with live timers, animated expand/collapse, error treatments, and readable card content.
Output: Updated ToolChip.tsx, tool-chip.css, tool-registry.ts + updated tests
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-tool-card-shell-state-machine/15-CONTEXT.md
@.planning/phases/15-tool-card-shell-state-machine/15-RESEARCH.md
@.planning/phases/15-tool-card-shell-state-machine/15-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/src/components/chat/tools/ToolCardShell.tsx (created in Plan 01):
```typescript
interface ToolCardShellProps {
  toolCall: ToolCallState;
  config: ToolConfig;
  isExpanded: boolean;
  children: React.ReactNode;
}
export const ToolCardShell: React.FC<ToolCardShellProps>;
```

From src/src/hooks/useElapsedTime.ts (created in Plan 01):
```typescript
export function useElapsedTime(startedAt: string | null, completedAt: string | null): string;
```

From src/src/lib/format-elapsed.ts (created in Plan 01):
```typescript
export function formatElapsed(ms: number): string;
```

From src/src/components/chat/tools/ToolChip.tsx (current, to be modified):
```typescript
export interface ToolChipProps {
  toolCall: ToolCallState;
}
export const ToolChip: React.FC<ToolChipProps>;
```

From src/src/lib/tool-registry.ts (current DefaultToolCard, to be upgraded):
```typescript
// Currently renders raw JSON.stringify in createElement('pre')
function DefaultToolCard(props: ToolCardProps) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ToolChip refactor -- elapsed time, error force-expand, ToolCardShell wiring</name>
  <files>src/src/components/chat/tools/ToolChip.tsx, src/src/components/chat/tools/ToolChip.test.tsx, src/src/components/chat/tools/tool-chip.css</files>
  <action>
    Update `src/src/components/chat/tools/ToolChip.tsx`:

    1. **Import additions:** Add `useElapsedTime` from `@/hooks/useElapsedTime`, `ToolCardShell` from `./ToolCardShell`

    2. **Error force-expand with adjust-state-during-rendering:**
       - Initialize `isExpanded` based on error: `useState(toolCall.isError && toolCall.status === 'rejected')`
       - Track `prevStatus` in state: `const [prevStatus, setPrevStatus] = useState(toolCall.status)`
       - In render body (before JSX): if `toolCall.status === 'rejected' && prevStatus !== 'rejected'`, call `setPrevStatus('rejected')` and `setIsExpanded(true)` (force-expand on transition to error). Otherwise if `prevStatus !== toolCall.status`, call `setPrevStatus(toolCall.status)`.
       - This is the same pattern used in ThinkingDisclosure for globalExpanded reset.

    3. **Elapsed time on chip:**
       - Call `useElapsedTime(toolCall.startedAt, toolCall.completedAt)`
       - Display after chip label with middle dot separator: `· {elapsed}`
       - Only show elapsed when status is `executing`, `resolved`, or `rejected` (not `invoked` since startedAt may not be meaningful yet). Actually show for all statuses with a startedAt -- the hook handles null.
       - Add `tool-chip-elapsed` class with `color: var(--text-muted)`, `font-variant-numeric: tabular-nums`

    4. **ToolCardShell integration:**
       - Replace `{isExpanded && <CardComponent ... />}` with `<ToolCardShell>` always rendered below the button
       - Pass `toolCall`, `config`, `isExpanded`, and `<CardComponent>` as children
       - ToolCardShell handles the animation (always mounted, uses CSS grid 0fr/1fr)

    5. **Error chip styling:**
       - Add `tool-chip--rejected` class variant in tool-chip.css: `border-color: oklch(from var(--status-error) l c h / 0.3)`
       - Already using `tool-chip--${toolCall.status}` class pattern, so this hooks in automatically

    Update `src/src/components/chat/tools/tool-chip.css`:
    - Add `.tool-chip--rejected` with subtle red border tint
    - Add `.tool-chip-elapsed` with `color: var(--text-muted)`, `font-variant-numeric: tabular-nums`, `font-size: 0.75rem`
    - Add `.tool-chip-separator` with `color: var(--text-muted)`, `margin: 0 2px`

    Update `src/src/components/chat/tools/ToolChip.test.tsx`:
    - Update expand/collapse tests: ToolCardShell is always mounted, check `data-expanded` attribute instead of `.tool-card` presence
    - Add test: error tool call starts expanded (`data-expanded="true"`)
    - Add test: elapsed time renders on chip (mock useElapsedTime if needed, or use vi.useFakeTimers)
    - Add test: tool transitioning to rejected auto-expands (rerender with changed status prop)
    - Keep existing tests working (status dot, icon, label, aria-expanded still on button)
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/components/chat/tools/ToolChip.test.tsx -x</automated>
  </verify>
  <done>ToolChip shows elapsed time with middle dot separator, error tools force-expand on first render and on transition to rejected, expansion uses ToolCardShell with animated CSS Grid transition, error chips have red border tint. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: DefaultToolCard structured key-value upgrade</name>
  <files>src/src/lib/tool-registry.ts</files>
  <action>
    Update `DefaultToolCard` in `src/src/lib/tool-registry.ts`:

    1. **Structured input display:**
       - Replace raw `JSON.stringify(input, null, 2)` with key-value rows
       - Use `Object.entries(input)` to iterate top-level keys
       - Each row: key label (bold, `color: var(--text-secondary)`) + value
       - String values: render directly as text
       - Non-string values (objects, arrays, numbers, booleans): `JSON.stringify(value, null, 2)` in monospace
       - No truncation on input (ToolCardShell body is scrollable)

    2. **Output display:**
       - Render in monospace `<pre>` with `white-space: pre-wrap`
       - No truncation (full error output is important per user decision)
       - Error class still applied via `isError` prop

    3. **Scrollable body:**
       - Wrap entire DefaultToolCard body in a container with `max-height: 200px`, `overflow-y: auto`
       - Use CSS classes, not inline styles. Add to tool-chip.css (or create minimal inline since this is createElement):
         - `.default-tool-card`: `max-height: 200px`, `overflow-y: auto`
         - `.default-tool-card-row`: `display: flex`, `gap: var(--space-2)`, `padding: 2px 0`
         - `.default-tool-card-key`: `color: var(--text-secondary)`, `font-weight: 500`, `white-space: nowrap`, `flex-shrink: 0`
         - `.default-tool-card-value`: `color: var(--text-primary)`, `font-family: var(--font-mono)`, `word-break: break-all`
         - `.default-tool-card-output`: same as existing `.tool-card-output` styling
         - `.default-tool-card-output--error`: same as existing error styling

    4. **Keep using createElement** (no JSX) since tool-registry.ts is a .ts file

    5. **Add the new CSS classes** to `tool-chip.css` (append to existing file) since that's where tool card styles already live. Do NOT create a separate CSS file for DefaultToolCard -- keep tool card styles co-located.

    Note: Per-tool cards (Phase 16) will replace DefaultToolCard registrations. ToolCardShell does NOT impose max-height on body -- that's DefaultToolCard's own responsibility. Other cards will have their own truncation rules.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>DefaultToolCard renders structured key-value rows for input, full output in scrollable monospace container, error output has red styling. Full test suite passes with no regressions.</done>
</task>

</tasks>

<verification>
cd /home/swd/loom/src && npx vitest run --reporter=verbose
</verification>

<success_criteria>
- ToolChip displays elapsed time with middle dot separator after chip label
- Error tool calls auto-expand on mount and on transition to rejected status
- ToolCardShell wraps card content with animated expand/collapse
- Error chips have subtle red border tint
- DefaultToolCard shows structured key-value input rows (not raw JSON)
- DefaultToolCard output is scrollable with max-height ~200px
- All existing tests pass, new tests cover error force-expand and elapsed time
</success_criteria>

<output>
After completion, create `.planning/phases/15-tool-card-shell-state-machine/15-02-SUMMARY.md`
</output>
