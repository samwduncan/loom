---
phase: 68-scaffolding-design
plan: 06
type: execute
wave: 2
depends_on: ["68-05"]
files_modified:
  - .planning/NATIVE-APP-SOUL.md
autonomous: false
requirements:
  - SCAFF-06

must_haves:
  truths:
    - "Official Native App Soul document exists as the visual authority for Phases 69-73"
    - "Document covers all 12 v3.0 screens with specific layout and motion specs"
    - "swd has approved the final Soul document"
  artifacts:
    - path: ".planning/NATIVE-APP-SOUL.md"
      provides: "Authoritative visual contract for native app"
      min_lines: 150
  key_links:
    - from: ".planning/NATIVE-APP-SOUL.md"
      to: ".planning/phases/68-scaffolding-design/68-UI-SPEC.md"
      via: "Supersedes baseline tokens where they conflict"
      pattern: "supersede"
---

<objective>
Claude formalizes Bard's approved creative direction into the official NATIVE-APP-SOUL.md -- the authoritative visual contract for all v3.0 phases (69-73). swd reviews and gives final approval.

Purpose: Per D-08, the Soul doc is "written specs (3-5 page markdown) + annotated reference screenshots." This is the design gate -- Phase 69 CANNOT start until this is approved. Per D-10, this is Plan 2 of the budgeted soul doc work.
Output: .planning/NATIVE-APP-SOUL.md approved by swd.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/PROJECT_SOUL.md
@.planning/ROADMAP.md
@.planning/phases/68-scaffolding-design/68-CONTEXT.md
@.planning/phases/68-scaffolding-design/68-UI-SPEC.md
@.planning/phases/68-scaffolding-design/NATIVE-APP-SOUL-DRAFT.md
@.planning/phases/68-scaffolding-design/68-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize Soul document from Bard's approved direction</name>
  <files>.planning/NATIVE-APP-SOUL.md</files>
  <read_first>
    .planning/PROJECT_SOUL.md,
    .planning/phases/68-scaffolding-design/NATIVE-APP-SOUL-DRAFT.md,
    .planning/phases/68-scaffolding-design/68-UI-SPEC.md,
    .planning/phases/68-scaffolding-design/68-CONTEXT.md
  </read_first>
  <action>
Formalize Bard's approved creative direction (from NATIVE-APP-SOUL-DRAFT.md and any swd revisions from Plan 05's checkpoint) into the official `.planning/NATIVE-APP-SOUL.md`.

The document structure (per D-08, 3-5 page markdown):

```markdown
# Loom Native App Soul

> The authoritative visual contract for Loom's React Native iOS app (v3.0 Phases 69-73).
> This document supersedes 68-UI-SPEC.md baseline values where they conflict.

## North Star

[1-2 paragraph vision statement synthesizing swd's D-12 vision + Bard's creative direction.
What makes Loom's native app feel alive vs static chat apps.]

## Design Principles

[3-5 principles derived from Bard's analysis. Each with a concrete example.
E.g., "Every interaction has a physical response" -> button press = spring + haptic]

## Elevation Layer

### Motion
[Spring physics configs for all interaction categories.
Table format: Interaction | Damping | Stiffness | Mass | Settle Time | Haptic]

### Depth
[Surface hierarchy (4 levels), shadow configs, glass/blur usage rules.
When to use which surface level. When blur is appropriate vs distracting.]

### Dynamic Color
[How color shifts based on context. Streaming state, error state, idle state.
Specific animation configs for color transitions.]

## Screen Specifications

[For each of the 12 screens from D-09:]

### Session List
[Layout, components, motion behaviors, color treatment.
Reference: how ChatGPT iOS / Claude iOS handle this screen.]

### Chat Thread
[Message density, spacing, bubble/flat decision, typing indicator, scroll behavior.]

### Composer
[Height, send button, placeholder, expand/collapse, keyboard avoidance.]

### Tool Cards
[Card design, status indicators, expand/collapse animation, grouped display.]

### Permission Request
[Inline UI above composer, approve/deny buttons, haptic feedback, urgency treatment.]

### Sidebar Drawer
[Width, session list layout, active indicator, swipe gesture area, background.]

### Settings
[Layout, section grouping, toggle style.]

### Share Sheet
[Native share integration, content formatting.]

### Notification UI
[Push notification layout, action buttons, deep link behavior.]

### Code Block Detail
[Syntax highlighting approach, copy button, scroll behavior.]

### Search
[Session search UI, real-time filtering, empty state.]

### Pinned Sessions
[Pin indicator, top-of-list treatment, pin/unpin gesture.]

## Typography Overrides
[If Bard's direction changes any UI-SPEC typography values, document here.
Otherwise: "Inherits from 68-UI-SPEC.md baseline."]

## Color Overrides
[If Bard's direction changes any UI-SPEC color values, document here.
Otherwise: "Inherits from 68-UI-SPEC.md baseline."]

## Anti-Patterns
[Explicit list: things Loom's native app must NEVER do.
From Bard's analysis + ChatGPT/Claude reference comparison.]

## Reference App Comparison
[Key measurements and observations from ChatGPT iOS and Claude iOS.
What Loom copies, what Loom improves upon, what Loom skips.]
```

Apply swd's revisions from Plan 05 checkpoint. If swd approved as-is, formalize without changes. If swd requested specific modifications, apply them.

The formalized document should be:
- Concrete (spring values, pixel sizes, color codes -- not vague principles)
- Actionable (an executor can build from this without asking "but what should it look like?")
- Referenced (cites ChatGPT/Claude iOS for each decision)
- Complete (all 12 screens, all 3 elevation layers)

Place at `.planning/NATIVE-APP-SOUL.md` (project root planning directory, NOT phase directory -- this is a living document for Phases 69-73).
  </action>
  <verify>
    <automated>cd /home/swd/loom && test -f .planning/NATIVE-APP-SOUL.md && wc -l .planning/NATIVE-APP-SOUL.md | awk '{print ($1 >= 150) ? "PASS: " $1 " lines" : "FAIL: only " $1 " lines"}' && grep -c "###" .planning/NATIVE-APP-SOUL.md | awk '{print ($1 >= 12) ? "PASS: " $1 " sections" : "FAIL: only " $1 " sections"}'</automated>
  </verify>
  <acceptance_criteria>
    - .planning/NATIVE-APP-SOUL.md exists at project root planning directory
    - Document is >= 150 lines
    - Contains >= 12 ### sections (one per screen + elevation layers + anti-patterns)
    - Contains "Motion" section with spring physics values (damping, stiffness in numbers)
    - Contains "Anti-Patterns" section with at least 5 items
    - Contains screen specs for: session list, chat thread, composer, tool cards, permission request, sidebar drawer, settings, share sheet, notification UI, code block detail, search, pinned sessions
    - Document header states it "supersedes 68-UI-SPEC.md baseline values where they conflict"
  </acceptance_criteria>
  <done>NATIVE-APP-SOUL.md formalized with all screen specs, elevation layer, and anti-patterns</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: swd approves official Native App Soul document</name>
  <what-built>
    The official NATIVE-APP-SOUL.md at `.planning/NATIVE-APP-SOUL.md` -- the visual authority for all v3.0 native app development. Formalized from Bard's creative direction (approved in Plan 05) with specific screen specs, spring physics configs, anti-patterns, and reference app comparisons.

    This is the Phase 68 design gate: Phase 69 CANNOT start until this document is approved.
  </what-built>
  <how-to-verify>
    1. Read `.planning/NATIVE-APP-SOUL.md`
    2. Check screen coverage: all 12 screens from D-09 have specs
    3. Check elevation: motion (spring values), depth (surface levels), color (context shifts) all defined
    4. Check anti-patterns: clear about what NOT to do
    5. Check that the vision matches D-12: "dynamic interfaces, things moving, nice animations, beautiful color palettes"
    6. Compare with ChatGPT iOS and Claude iOS -- does this document capture what makes them feel native?
    7. Ask yourself: "Could a developer build the session list screen from this spec alone?"
  </how-to-verify>
  <resume-signal>Type "approved" to lock the Soul document as the visual authority for v3.0, or describe changes needed</resume-signal>
  <files>N/A (checkpoint)</files>
  <action>The official NATIVE-APP-SOUL.md at `.planning/NATIVE-APP-SOUL.md` -- the visual authority for all v3.0 native app development. Formalized from Bard's creative direction (approved in Plan 05) with specific screen specs, spring physics configs, anti-patterns, and reference app comparisons.

    This is the Phase 68 design gate: Phase 69 CANNOT start until this document is approved.

    **How to verify:**
    1. Read `.planning/NATIVE-APP-SOUL.md`
    2. Check screen coverage: all 12 screens from D-09 have specs
    3. Check elevation: motion (spring values), depth (surface levels), color (context shifts) all defined
    4. Check anti-patterns: clear about what NOT to do
    5. Check that the vision matches D-12: "dynamic interfaces, things moving, nice animations, beautiful color palettes"
    6. Compare with ChatGPT iOS and Claude iOS -- does this document capture what makes them feel native?
    7. Ask yourself: "Could a developer build the session list screen from this spec alone?"</action>
  <verify>Human verification -- see how-to-verify steps above</verify>
  <done>Type "approved" to lock the Soul document as the visual authority for v3.0, or describe changes needed</done>
</task>

</tasks>

<verification>
- NATIVE-APP-SOUL.md exists with formalized specs
- All 12 v3.0 screens have specific layout/motion/color specs
- Elevation layer fully defined (motion + depth + color)
- swd has approved the document
</verification>

<success_criteria>
- Official NATIVE-APP-SOUL.md approved by swd
- Phase 69 design gate satisfied
- Document is concrete enough for executors to build from
</success_criteria>

<output>
After completion, create `.planning/phases/68-scaffolding-design/68-06-SUMMARY.md`
</output>
