---
phase: 68-scaffolding-design
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - SCAFF-05

user_setup:
  - service: apple-developer
    why: "iOS app distribution and push notification certificates"
    account_setup:
      - task: "Enroll in Apple Developer Program"
        location: "https://developer.apple.com/programs/enroll/"
        cost: "$99/year personal account"
        timeline: "24-48 hours for individual, up to 2 weeks for org"
    dashboard_config:
      - task: "Create APNs push notification key"
        location: "Apple Developer > Certificates, Identifiers & Profiles > Keys"
        details: "Create a new key with 'Apple Push Notifications service (APNs)' enabled. Download the .p8 key file. Note the Key ID and Team ID."
      - task: "Upload APNs key to Expo"
        location: "https://expo.dev > Project > Credentials > iOS > Push Notifications"
        details: "Upload the .p8 key file with Key ID and Team ID"

must_haves:
  truths:
    - "Apple Developer Program enrollment is active"
    - "APNs push notification key exists and is configured in Expo"
  artifacts: []
  key_links: []
---

<objective>
Complete Apple Developer Program enrollment and configure APNs push notification certificates for the Loom iOS app. This is gated by enrollment clearing (24-48 hours) and is Phase 68's last requirement per D-16.

Purpose: SCAFF-05 requires Apple Developer enrollment with APNs certs configured. Push notifications are used in Phase 72 for permission requests and session completion alerts.
Output: Active Apple Developer account, APNs key configured in Expo project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/68-scaffolding-design/68-CONTEXT.md
@.planning/phases/68-scaffolding-design/68-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify Apple Developer Program enrollment is active</name>
  <files>N/A (external service verification)</files>
  <action>
    Per D-15: Personal Apple Developer account ($99/year). Enrollment was initiated before Phase 68 code began.

    swd must verify enrollment has cleared:
    1. Go to https://developer.apple.com/account
    2. Check membership status shows "Active"
    3. If still "Enrollment Pending" -- wait and retry later. This plan can be deferred until enrollment clears while other Phase 68 plans proceed.
  </action>
  <verify>Human verification -- check Apple Developer account membership status at developer.apple.com/account</verify>
  <done>Apple Developer Program membership shows "Active" status</done>
  <resume-signal>Type "enrolled" when Apple Developer Program membership shows Active, or "pending" if still processing</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create APNs key and configure in Expo</name>
  <files>N/A (external service configuration)</files>
  <action>
    Once enrollment is active, create the push notification key:

    1. Go to https://developer.apple.com/account/resources/authkeys/list
    2. Click "+" to create a new key
    3. Name: "Loom Push Notifications"
    4. Check: "Apple Push Notifications service (APNs)"
    5. Click Continue, then Register
    6. IMPORTANT: Download the .p8 key file immediately (can only be downloaded once)
    7. Note the Key ID (shown on the key details page)
    8. Note your Team ID (visible at https://developer.apple.com/account > Membership details)

    Then configure in Expo:
    9. Go to https://expo.dev
    10. Navigate to the Loom project > Credentials > iOS
    11. Under Push Notifications, upload the .p8 key file
    12. Enter the Key ID and Team ID

    After configuration, verify by running (Claude will execute):
    ```bash
    cd mobile && eas credentials
    ```
    This should show the push notification key configured for iOS.
  </action>
  <verify>Run `cd mobile && eas credentials` and confirm push notification key appears for iOS</verify>
  <done>APNs .p8 key created, downloaded, and uploaded to Expo project credentials with Key ID and Team ID</done>
  <resume-signal>Type "configured" with the Key ID (not the key file contents) to confirm APNs is set up</resume-signal>
</task>

</tasks>

<verification>
- Apple Developer account shows Active membership
- APNs .p8 key created and downloaded
- Key uploaded to Expo project credentials
- `eas credentials` shows push notification key for iOS
</verification>

<success_criteria>
- Apple Developer Program enrollment active (SCAFF-05)
- APNs push notification key created and configured in Expo
- Phase 72 (push notifications) is unblocked from a certificate perspective
</success_criteria>

<output>
After completion, create `.planning/phases/68-scaffolding-design/68-07-SUMMARY.md`
</output>
