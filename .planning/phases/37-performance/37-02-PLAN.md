---
phase: 37-performance
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/src/components/chat/view/MessageContainer.test.tsx
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03, PERF-04]

must_haves:
  truths:
    - "Streaming with 200+ messages maintains 55+ FPS"
    - "content-visibility: auto is applied to finalized messages and verified via test"
    - "No memory growth trend across 10+ session switches"
    - "Initial page load completes under 2 seconds on dev server"
  artifacts:
    - path: "src/src/components/chat/view/MessageContainer.test.tsx"
      provides: "Unit test verifying content-visibility style application"
      contains: "contentVisibility"
  key_links:
    - from: "src/src/components/chat/view/MessageContainer.tsx"
      to: "contentVisibility style"
      via: "inline style object"
      pattern: "contentVisibility.*auto"
---

<objective>
Stress testing and performance verification for all PERF requirements.

Purpose: Verify that the optimizations from Plan 01 (chunk splitting, bounded cache, consolidated observer) hold under real workload conditions. Produce evidence for FPS, memory, load time, and content-visibility requirements. This is the measurement and validation plan.

Output: Updated MessageContainer test proving content-visibility, and checkpoint verification of FPS/memory/load time via Chrome DevTools.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-performance/37-RESEARCH.md
@.planning/phases/37-performance/37-01-SUMMARY.md

<interfaces>
From src/src/components/chat/view/MessageContainer.tsx:
```typescript
interface MessageContainerProps {
  children: ReactNode;
  role: MessageRole;
  isStreaming?: boolean;
}
// contentVisibilityStyle applied when !isStreaming
const contentVisibilityStyle: CSSProperties = {
  contentVisibility: 'auto',
  containIntrinsicHeight: 'auto 200px',
};
export function MessageContainer({ children, role, isStreaming }: MessageContainerProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Content-visibility verification test + stress test utility</name>
  <files>src/src/components/chat/view/MessageContainer.test.tsx</files>
  <behavior>
    - Test 1: Finalized (non-streaming) MessageContainer applies content-visibility: auto style
    - Test 2: Streaming MessageContainer does NOT apply content-visibility style
    - Test 3: contain-intrinsic-height is set to "auto 200px" on finalized messages
  </behavior>
  <action>
    1. Open or create `src/src/components/chat/view/MessageContainer.test.tsx`

    2. Add these test cases:
       - Render MessageContainer with role="assistant" and isStreaming=false (default). Assert the outer div has `style` containing `content-visibility: auto` and `contain-intrinsic-height: auto 200px`.
       - Render MessageContainer with role="assistant" and isStreaming=true. Assert the outer div does NOT have content-visibility in its style.
       - Render with role="user" and isStreaming=false. Assert content-visibility is applied (all non-streaming messages get it).

    3. Use `getByTestId('message-container')` since the component has `data-testid="message-container"`.

    4. For style assertions, use `element.style.contentVisibility` (jsdom should expose inline styles).

    Run: `cd src && npx vitest run src/src/components/chat/view/MessageContainer.test.tsx --reporter=verbose`
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/chat/view/MessageContainer.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - MessageContainer test verifies content-visibility: auto on finalized messages
    - Test verifies streaming messages do NOT get content-visibility
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Chrome DevTools performance verification</name>
  <what-built>
    Plan 01 optimized the bundle (vendor chunk splitting), bounded the shiki cache (500 entry LRU), and consolidated IntersectionObservers (single shared observer). Task 1 of this plan verified content-visibility. Now we need manual DevTools verification for FPS, memory, and load time.
  </what-built>
  <how-to-verify>
    **PERF-01: FPS Test (55+ FPS with 200+ messages)**
    1. Open http://100.86.4.57:5184 in Chrome
    2. Open DevTools > Performance tab
    3. Open or create a session with 200+ messages (use a long conversation or inject via store)
    4. Click Record, scroll through messages, then stop
    5. Check the FPS chart: should stay above 55 FPS during scroll
    6. If streaming is active, verify FPS stays above 55 during streaming with 200+ existing messages

    **PERF-02: content-visibility visual check**
    1. In the same 200+ message session, open DevTools > Elements tab
    2. Scroll to a message far from the viewport
    3. Inspect its container div -- should have `content-visibility: auto` in inline styles
    4. The actively streaming message should NOT have content-visibility

    **PERF-03: Memory leak test (10+ session switches)**
    1. Open DevTools > Memory tab
    2. Navigate to a session, wait for load
    3. Take Heap Snapshot #1
    4. Switch between 10+ different sessions (clicking sidebar items)
    5. Force GC (trash can icon in Memory tab)
    6. Take Heap Snapshot #2
    7. Compare: select Snapshot #2, change view to "Comparison"
    8. Sort by "Alloc. Size" -- look for growing Map entries, detached DOM nodes, EventListener accumulation
    9. Some growth is expected (session metadata) but there should be no unbounded trend

    **PERF-04: Load time test (under 2s)**
    1. Open DevTools > Network tab
    2. Check "Disable cache"
    3. Hard refresh (Ctrl+Shift+R)
    4. Check DOMContentLoaded time in the bottom bar -- should be under 2s
    5. Verify the vendor chunks load in parallel (not waterfall)

    **PERF-05: Bundle report review**
    1. Check that dist/bundle-report.html was generated (from Plan 01)
    2. Open it in a browser
    3. Verify the treemap shows vendor chunks separated from application code
    4. Note any remaining chunks over 50KB that could be further optimized
  </how-to-verify>
  <resume-signal>Type "approved" with any notes on the numbers observed, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `cd src && npx vitest run --reporter=verbose` -- full suite green
- Chrome DevTools evidence for PERF-01 (FPS), PERF-03 (memory), PERF-04 (load time)
- content-visibility verified both via test (automated) and DevTools (manual)
- Bundle report reviewed and recommendations documented
</verification>

<success_criteria>
- MessageContainer content-visibility test passes
- FPS stays above 55 with 200+ messages (DevTools verification)
- No memory growth trend across 10+ session switches (heap snapshot comparison)
- Page load under 2s with cache disabled
- Bundle report exists and has been reviewed
</success_criteria>

<output>
After completion, create `.planning/phases/37-performance/37-02-SUMMARY.md`
</output>
