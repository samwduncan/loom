---
phase: 70-chat-polish
plan: 06
type: execute
wave: 4
depends_on: [70-02, 70-03, 70-04, 70-05]
files_modified:
  - mobile/components/chat/MessageList.tsx
  - mobile/components/chat/MessageBubble.tsx
  - mobile/components/session/SessionList.tsx
  - mobile/components/composer/Composer.tsx
autonomous: false
requirements: [CHAT-05, CHAT-06, CHAT-07, CHAT-08, CHAT-12]

must_haves:
  truths:
    - "All rebuilt components render correctly on device"
    - "Message list scrolls at 60fps with 50+ messages"
    - "Keyboard avoidance syncs with iOS keyboard without jumps"
    - "Session list shows temporal groups correctly"
    - "Tool cards, thinking blocks, and code blocks render during streaming"
    - "Side-by-side with ChatGPT iOS shows comparable density and spacing"
  artifacts: []
  key_links: []
---

<objective>
Integration testing and visual verification of all Phase 70 work on device. Fix any visual inconsistencies, spacing issues, or interaction bugs found during testing.

Purpose: Per user feedback, device testing after every plan was requested. This final plan consolidates verification and polish across all 5 prior plans. The success criteria require side-by-side comparison with ChatGPT iOS.

Output: Phase 70 complete -- app visually matches reference apps in layout, density, and interaction quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/NATIVE-APP-SOUL.md
@.planning/phases/70-chat-polish/70-01-SUMMARY.md
@.planning/phases/70-chat-polish/70-02-SUMMARY.md
@.planning/phases/70-chat-polish/70-03-SUMMARY.md
@.planning/phases/70-chat-polish/70-04-SUMMARY.md
@.planning/phases/70-chat-polish/70-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration pass -- fix visual inconsistencies and wire remaining gaps</name>
  <files>mobile/components/chat/MessageList.tsx, mobile/components/chat/MessageBubble.tsx, mobile/components/session/SessionList.tsx, mobile/components/composer/Composer.tsx</files>
  <action>
  Run through the full app flow and fix any issues found:

  1. **Full flow verification** -- Build and run the app, testing this sequence:
     - Open app -> auth prompt -> enter token -> session list loads
     - Verify session list shows temporal groups (Today/Yesterday/etc.)
     - Tap a session -> chat screen loads with messages
     - Verify message layout: user bubbles right (accent), assistant flat left (avatar)
     - Send a new message -> verify composer contracts, user bubble appears
     - During streaming: verify tool cards appear, thinking blocks render, text streams
     - After streaming: verify thinking block auto-collapses
     - Open drawer -> verify search filters work
     - Long-press session -> verify Zeego context menu appears
     - Verify keyboard avoidance (tap composer, keyboard should push content up smoothly)
     - Verify empty state (new chat -> suggestion chips visible)

  2. **Spacing audit** -- Compare all spacing values against Soul doc:
     - Message turn spacing: 16pt between senders, 4pt grouped
     - Message padding: px-4 (16px) horizontal
     - Composer to keyboard: 0px (flush)
     - Tool card group spacing: 8px
     - Session item height: min 48px

  3. **Performance check** -- Add 50+ messages to a conversation and verify:
     - Scroll FPS stays at 60fps (no jank)
     - LegendList handles the load without visible issues
     - Streaming messages don't cause scroll jumps

  4. **Fix any compilation warnings** -- Clean up TypeScript warnings in modified files.

  5. **Verify deferred items are NOT implemented** -- Check that none of these snuck in:
     - No syntax highlighting (Phase 73)
     - No haptic feedback on interactions (Phase 71)
     - No spring physics on screen transitions (Phase 71)
     - No swipe-to-delete on sessions from context menu (swipe is existing, context menu is new)
     - No share sheet (Phase 73)

  6. **Verify bundle builds** -- Final `npx expo export --platform ios` check.
  </action>
  <verify>
    <automated>cd /home/swd/loom/mobile && npx expo export --platform ios 2>&1 | tail -5</automated>
  </verify>
  <done>
  - Full app flow works end-to-end
  - All spacing matches Soul doc values
  - 50+ messages scroll at 60fps
  - No Phase 71/73 features leak in
  - Bundle compiles clean
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device verification -- Phase 70 UI rebuild quality gate</name>
  <files>mobile/components/chat/MessageList.tsx</files>
  <action>
  Human tests the full Phase 70 UI rebuild on physical iPhone device.
  Claude prepares the app for testing: ensures dev build runs, no crashes on launch.
  </action>
  <verify>
    <automated>cd /home/swd/loom/mobile && npx expo export --platform ios 2>&1 | tail -5</automated>
  </verify>
  <done>
  - Human approves the UI rebuild on device
  - All 9 ROADMAP success criteria for Phase 70 verified on device
  </done>
  <what-built>
  Complete Phase 70 UI rebuild:
  - LegendList message list with bottom-alignment (no inverted FlatList)
  - react-native-keyboard-controller with translate-with-padding
  - Composer unified pill with auto-grow and 3-state send button
  - User messages in accent-colored bubbles, assistant flat with avatar
  - Session list with temporal grouping (Today/Yesterday/etc.)
  - Zeego context menus on sessions (Rename/Delete)
  - Empty state with 6 suggestion chips
  - Tool call cards with status dots and expand/collapse
  - Thinking blocks with disclosure toggle
  - Code blocks with copy button and language label
  </what-built>
  <how-to-verify>
  1. Open Loom on iPhone 16 Pro Max via Expo Dev Client
  2. **Session list:** Open drawer. Verify sessions grouped by Today/Yesterday/etc. (not by project). Long-press a session -- native context menu should appear with Rename and Delete.
  3. **Chat screen:** Open a session with messages.
     - User messages: right-aligned, accent-colored pill bubbles, white text
     - Assistant messages: left-aligned, no bubble, 32px avatar on first in group
     - Spacing: tight within sender groups (4pt), larger between senders (16pt)
  4. **Composer:** Tap the text input.
     - Keyboard should push content up smoothly (no jump, no gap)
     - Type a multi-line message -- input should grow, max 6 lines
     - Send button should appear with accent glow when text present
     - During streaming, send button should become a red stop button
  5. **Empty state:** Create new chat. Verify centered greeting + 6 suggestion chips in 2-column grid. Tap a chip -- it should send the prompt.
  6. **Streaming:** Send a message that triggers tool use.
     - Tool call cards should appear with status dots
     - Thinking blocks should auto-expand during thinking, collapse after
     - Code blocks should have language label and copy button
  7. **Performance:** Scroll through 50+ messages. Should feel smooth (60fps).
  8. **Side-by-side:** Open ChatGPT iOS next to Loom. Message density, spacing, and general layout should be comparable.
  </how-to-verify>
  <resume-signal>Type "approved" if the UI matches ChatGPT/Claude iOS patterns, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Human verification on iPhone 16 Pro Max
2. Side-by-side comparison with ChatGPT iOS
3. All 9 success criteria from ROADMAP Phase 70 checked
</verification>

<success_criteria>
All Phase 70 ROADMAP success criteria met:
1. Message list uses legend-list with alignItemsAtEnd + maintainScrollAtEnd. 50+ messages at 60fps.
2. Keyboard avoidance uses react-native-keyboard-controller (translate-with-padding). 120Hz sync.
3. Message layout matches reference: user in colored bubbles, assistant flat left with avatar.
4. Composer is unified pill with auto-grow TextInput (max 6 lines), attachment button outside.
5. Session list uses temporal grouping. Context menu via Zeego.
6. Empty state has centered greeting + suggestion chips in 2-column grid.
7. Tool call cards render with status indicators and expand/collapse.
8. Thinking blocks render with expand/collapse disclosure, code blocks in monospace.
9. Side-by-side with ChatGPT iOS: comparable density, spacing, responsiveness.
</success_criteria>

<output>
After completion, create `.planning/phases/70-chat-polish/70-06-SUMMARY.md`
</output>
