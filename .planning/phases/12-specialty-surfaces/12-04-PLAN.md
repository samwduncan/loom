---
phase: 12-specialty-surfaces
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/chat/view/subcomponents/Markdown.tsx
  - src/components/code-editor/view/subcomponents/markdown/MarkdownPreview.tsx
autonomous: true
requirements: [SURF-06]

must_haves:
  truths:
    - Blockquote left border uses rose accent color (--primary) in both chat and editor markdown
    - Chat inline links stay blue (text-status-info) -- unchanged
    - Editor/preview prose links use rose accent (text-primary) -- already correct
    - Heading hierarchy uses warm white foreground with font size/weight differentiation only
    - Inline code snippets use Catppuccin-tinted background matching code block background
  artifacts:
    - src/components/chat/view/subcomponents/Markdown.tsx (updated)
    - src/components/code-editor/view/subcomponents/markdown/MarkdownPreview.tsx (updated)
  key_links:
    - Both Markdown.tsx and MarkdownPreview.tsx define separate blockquote/heading components
---

<objective>
Update markdown prose styling in both chat messages and editor preview to use rose accent for blockquote borders, warm white heading hierarchy, and Catppuccin-tinted inline code backgrounds.

Purpose: Markdown prose content should feel native to the charcoal + rose palette, with consistent visual treatment across chat messages and the file editor's markdown preview.
Output: Polished markdown prose with rose accents, proper heading hierarchy, and consistent inline code styling.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-specialty-surfaces/12-CONTEXT.md
@.planning/phases/12-specialty-surfaces/12-RESEARCH.md
@.planning/phases/12-specialty-surfaces/12-01-SUMMARY.md
@src/components/chat/view/subcomponents/Markdown.tsx
@src/components/code-editor/view/subcomponents/markdown/MarkdownPreview.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update chat Markdown.tsx prose styling</name>
  <files>src/components/chat/view/subcomponents/Markdown.tsx</files>
  <action>
Update the staticComponents in Markdown.tsx for blockquote, inline code, and headings.

**Blockquote (line ~71-74):**
- Change `border-border/10` to `border-primary` to use rose accent for the left border
- Keep `pl-4 italic text-muted-foreground my-2` unchanged
- Result: `className="border-l-4 border-primary pl-4 italic text-muted-foreground my-2"`

**Links (line ~76-78):**
- NO CHANGE. Chat links stay `text-status-info` (blue). This is a locked decision.

**Inline code (inside makeCodeComponent, line ~106):**
- Change `bg-surface-raised/60` to `bg-[#191817]` to match Catppuccin-tinted code block background (catppuccinMocha.mantle)
- This creates visual consistency between inline `code` and fenced code blocks
- Keep `text-foreground border border-border/10` and other classes unchanged
- Result: `className="font-mono text-[0.9em] px-1.5 py-0.5 rounded-md bg-[#191817] text-foreground border border-border/10 whitespace-pre-wrap break-words"`

**Headings -- add custom heading components to staticComponents:**
Currently Markdown.tsx does NOT define custom h1-h4 components, so ReactMarkdown uses defaults. Add heading components to `staticComponents`:

```typescript
h1: ({ children }: { children?: React.ReactNode }) => (
  <h1 className="text-2xl font-bold text-foreground mt-6 mb-3 first:mt-0">{children}</h1>
),
h2: ({ children }: { children?: React.ReactNode }) => (
  <h2 className="text-xl font-semibold text-foreground mt-5 mb-2">{children}</h2>
),
h3: ({ children }: { children?: React.ReactNode }) => (
  <h3 className="text-lg font-semibold text-foreground mt-4 mb-2">{children}</h3>
),
h4: ({ children }: { children?: React.ReactNode }) => (
  <h4 className="text-base font-semibold text-foreground mt-3 mb-1">{children}</h4>
),
```

Per locked decision: heading hierarchy uses warm white foreground only (text-foreground). Hierarchy communicated through font size (2xl > xl > lg > base) and weight (bold > semibold). No color differentiation.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - Blockquote left border uses rose accent (border-primary)
    - Chat links remain blue (text-status-info) -- unchanged per locked decision
    - Inline code uses Catppuccin-tinted background (#191817) matching code blocks
    - h1-h4 use warm white foreground with size/weight hierarchy only
  </done>
  <depth>
    - Heading margin-top values decrease with heading level for proper visual hierarchy
    - first:mt-0 on h1 prevents extra top margin when heading is first element
    - Inline code border-border/10 provides subtle edge definition
    - The bg-[#191817] matches catppuccinMocha.mantle which is the code block background
  </depth>
</task>

<task type="auto">
  <name>Task 2: Update editor MarkdownPreview.tsx prose styling</name>
  <files>src/components/code-editor/view/subcomponents/markdown/MarkdownPreview.tsx</files>
  <action>
Update the markdownPreviewComponents in MarkdownPreview.tsx for blockquote and ensure consistency.

**Blockquote (line ~16):**
- Change `border-border/20` to `border-primary` to use rose accent for the left border
- Keep other classes unchanged
- Result: `className="border-l-4 border-primary pl-4 italic text-muted-foreground my-2"`

**Links (line ~20-22):**
- NO CHANGE. Editor preview links already use `text-primary` (rose). This is correct.

**Prose classes on the wrapper div in CodeEditorSurface.tsx (line ~31):**
- These use Tailwind Typography's prose modifiers. The existing `prose-a:text-primary` is correct.
- The `prose-headings:font-semibold` is correct for heading weight.
- Add `prose-headings:text-foreground` to ensure heading color is warm white (not default prose color).
  - Check if this is already present. If the current classes include heading color, verify it's `text-foreground`.
  - UPDATE the className on the prose wrapper div in CodeEditorSurface.tsx to include:
    `prose-headings:text-foreground` (if not already present)
    `prose-blockquote:border-primary` (Tailwind Typography modifier for blockquote border)

**Wait -- CodeEditorSurface.tsx is listed in Plan 03's files_modified. Let me reconsider.**

Actually, the prose wrapper div is in CodeEditorSurface.tsx which Plan 03 also modifies. However, Plan 03 only changes the `theme` prop on the CodeMirror component and the import. The prose wrapper div (line ~29-33) is in the markdown preview branch which Plan 03 does NOT touch.

So for this task, update CodeEditorSurface.tsx ONLY if the prose-headings:text-foreground class is missing. Read the file first to verify. The file path is: `src/components/code-editor/view/subcomponents/CodeEditorSurface.tsx`

If the prose wrapper div needs updating, add these Tailwind Typography modifiers to the className:
- `prose-headings:text-foreground` -- warm white headings
- `prose-blockquote:border-primary` -- rose blockquote border (supplements the custom component)
- `prose-code:bg-[#191817]` -- Catppuccin-tinted inline code background in prose context
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - MarkdownPreview blockquote uses rose accent border (border-primary)
    - Editor preview links stay rose (text-primary) -- already correct
    - Prose wrapper includes heading and code Tailwind Typography modifiers
    - Visual consistency between chat markdown and editor markdown preview
  </done>
  <depth>
    - Tailwind Typography prose-* modifiers apply to rendered markdown content
    - The custom blockquote component (markdownPreviewComponents) takes priority over prose-blockquote modifier, but having both ensures consistency
    - prose-code:bg-[#191817] matches the inline code background used in chat Markdown.tsx
    - Editor markdown preview already has prose-pre:bg-surface-raised for fenced code blocks
  </depth>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- In chat messages: blockquote left border is rose (#D4736C), not gray
- In chat messages: links are blue (text-status-info) -- unchanged
- In editor markdown preview: blockquote left border is rose
- In editor markdown preview: links are rose (text-primary) -- already correct
- Heading hierarchy: h1 is largest/boldest, h4 is smallest, all warm white
- Inline code: background matches fenced code block background (#191817)
- No heading color differentiation -- all use text-foreground
</verification>

<success_criteria>
- Blockquote borders use rose accent in both chat and editor contexts
- Heading hierarchy uses warm white + size/weight only
- Inline code background matches code block background (Catppuccin-tinted)
- Chat links stay blue, editor/preview links stay rose
- Consistent prose styling across both markdown rendering contexts
</success_criteria>

<output>
After completion, create `.planning/phases/12-specialty-surfaces/12-04-SUMMARY.md`
</output>
