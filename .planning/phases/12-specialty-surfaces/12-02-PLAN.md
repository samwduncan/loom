---
phase: 12-specialty-surfaces
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/chat/hooks/useShikiHighlighter.ts
  - src/components/chat/tools/components/DiffViewer.tsx
  - src/components/chat/view/subcomponents/CodeBlock.tsx
autonomous: true
requirements: [SURF-04, SURF-05]

must_haves:
  truths:
    - Shiki uses catppuccin-mocha bundled theme (not dark-plus)
    - Shiki code blocks have warm-shifted backgrounds matching app charcoal, not Catppuccin blue-tinted base
    - No warm brown/amber v1.0 color remnants in WARM_COLOR_REPLACEMENTS
    - Diff viewer backgrounds use charcoal palette, not v1.0 browns (#1c1210)
    - Diff added/removed colors use CSS custom properties --diff-added-bg / --diff-removed-bg
    - Token colors in code blocks and diff lines are identical (same Shiki theme)
  artifacts:
    - src/components/chat/hooks/useShikiHighlighter.ts (updated)
    - src/components/chat/tools/components/DiffViewer.tsx (updated)
    - src/components/chat/view/subcomponents/CodeBlock.tsx (updated)
  key_links:
    - useShikiHighlighter.ts provides highlighting for both CodeBlock and ShikiDiffLine
    - DiffViewer.tsx styles must match the Shiki highlighting colors
    - CodeBlock.tsx CSS overrides must match the new code block background
---

<objective>
Switch Shiki from dark-plus to the bundled catppuccin-mocha theme with warm-shifted backgrounds, and update the diff viewer's warmDiffStyles to charcoal + Catppuccin colors.

Purpose: Eliminate all v1.0 warm brown/amber color remnants from syntax highlighting and diff viewing, replacing with Catppuccin Mocha tokens on charcoal backgrounds.
Output: Unified syntax highlighting across code blocks and diff viewer using Catppuccin Mocha palette.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-specialty-surfaces/12-CONTEXT.md
@.planning/phases/12-specialty-surfaces/12-RESEARCH.md
@.planning/phases/12-specialty-surfaces/12-01-SUMMARY.md
@src/shared/catppuccin-mocha.ts
@src/components/chat/hooks/useShikiHighlighter.ts
@src/components/chat/tools/components/DiffViewer.tsx
@src/components/chat/tools/components/ShikiDiffLine.tsx
@src/components/chat/view/subcomponents/CodeBlock.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch Shiki to catppuccin-mocha theme</name>
  <files>src/components/chat/hooks/useShikiHighlighter.ts</files>
  <action>
Rewrite useShikiHighlighter.ts to use Shiki's bundled catppuccin-mocha theme instead of dark-plus with manual color replacements.

**Changes:**

1. Import shared constants:
   `import { shikiBaseReplacements } from '@/shared/catppuccin-mocha';`
   (Adjust import path as needed based on tsconfig/vite alias)

2. DELETE the entire `WARM_COLOR_REPLACEMENTS` constant (lines ~24-37). This is the v1.0 warm brown/amber mapping that maps dark-plus colors to earthy tones. It is completely replaced by the catppuccin-mocha theme + shikiBaseReplacements.

3. Update `createHighlighter` call:
   - Change `themes: ['dark-plus']` to `themes: ['catppuccin-mocha']`
   - Languages list stays unchanged

4. Update `highlightCode` function's `codeToHtml` call:
   - Change `theme: 'dark-plus'` to `theme: 'catppuccin-mocha'`
   - Change `colorReplacements: WARM_COLOR_REPLACEMENTS` to `colorReplacements: shikiBaseReplacements`
   (shikiBaseReplacements only remaps the 12 base/surface/text colors from Catppuccin's blue-tinted originals to warm charcoal equivalents. Token/accent colors stay pure Catppuccin.)

5. Update the fallback `<pre>` in the catch block:
   - Change `background:#1c1210` to use catppuccinMocha.mantle value (import catppuccinMocha)
   - Change `color:#f5e6d3` to use catppuccinMocha.text value
   - Import catppuccinMocha for these values

6. Keep the Highlighter interface, singleton promise pattern, langMap, and error handling unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Shiki uses catppuccin-mocha theme. WARM_COLOR_REPLACEMENTS deleted. colorReplacements only contains warm-shift for base/surface/text colors. No v1.0 warm brown/amber hex values remain anywhere in the file.</done>
  <depth>
    - Verify catppuccin-mocha is a valid bundled theme name in shiki/bundle/web (it is -- confirmed via Context7)
    - The Highlighter interface stays loose to avoid generic-mismatch errors
    - shikiBaseReplacements maps original Catppuccin hex -> warm-shifted hex (12 entries, not 12+ token colors)
    - Fallback pre styling matches the themed code block background
  </depth>
</task>

<task type="auto">
  <name>Task 2: Update diff viewer and code block styles for charcoal palette</name>
  <files>src/components/chat/tools/components/DiffViewer.tsx, src/components/chat/view/subcomponents/CodeBlock.tsx</files>
  <action>
**DiffViewer.tsx changes:**

1. Import shared constants:
   `import { catppuccinMocha } from '@/shared/catppuccin-mocha';`

2. Replace the entire `warmDiffStyles` object. Update ALL color values from v1.0 browns to charcoal + Catppuccin:

   ```
   variables.dark:
     diffViewerBackground: catppuccinMocha.base          // #1b1a19 (was #1c1210)
     diffViewerColor: catppuccinMocha.text               // #e4dbd9 (was #f5e6d3)
     addedBackground: 'hsl(140 35% 12%)'                 // Use --diff-added-bg value
     addedColor: catppuccinMocha.green                   // #a6e3a1 (was #b8dab8)
     removedBackground: 'hsl(0 35% 14%)'                 // Use --diff-removed-bg value
     removedColor: catppuccinMocha.red                   // #f38ba8 (was #dab8b8)
     changedBackground: catppuccinMocha.surface0         // #252423 (was #2a1f1a)
     wordAddedBackground: 'hsl(140 35% 20%)'             // Brighter green for word-level
     wordRemovedBackground: 'hsl(0 35% 22%)'             // Brighter red for word-level
     addedGutterBackground: 'hsl(140 35% 10%)'           // Darker green for gutter
     removedGutterBackground: 'hsl(0 35% 11%)'           // Darker red for gutter
     gutterBackground: catppuccinMocha.mantle            // #191817 (was #241a14)
     gutterBackgroundDark: catppuccinMocha.base          // #1b1a19 (was #1c1210)
     gutterColor: catppuccinMocha.subtext0               // #989494 (was #c4a882)
     addedGutterColor: catppuccinMocha.green             // #a6e3a1 (was #b8dab8)
     removedGutterColor: catppuccinMocha.red             // #f38ba8 (was #dab8b8)
     emptyLineBackground: catppuccinMocha.base           // #1b1a19 (was #1c1210)
     codeFoldBackground: catppuccinMocha.mantle          // #191817 (was #241a14)
     codeFoldGutterBackground: catppuccinMocha.base      // #1b1a19 (was #1c1210)
     codeFoldContentColor: catppuccinMocha.subtext0      // #989494 (was #c4a882)
     diffViewerTitleBackground: catppuccinMocha.mantle   // #191817 (was #241a14)
     diffViewerTitleColor: catppuccinMocha.subtext0      // #989494 (was #c4a882)
     diffViewerTitleBorderColor: catppuccinMocha.surface0 // #252423 (was #3d2e25)
     highlightBackground: catppuccinMocha.surface0       // #252423 (was #3d2e25)
     highlightGutterBackground: catppuccinMocha.surface0 // #252423 (was #2a1f1a)
   ```

3. Update contentText.fontFamily: Keep the existing JetBrains Mono family string.

4. Rename the constant from `warmDiffStyles` to `charcoalDiffStyles` to reflect the new palette. Update the reference in the component (the `useMemo` line).

**CodeBlock.tsx changes:**

5. Update the Shiki HTML override class in the highlighted div (around line 122):
   - The `[&_pre]:!bg-surface-base` may need updating. Since code block backgrounds should be "slightly darker than surface-base" per CONTEXT.md, change this to a custom value. Use a Tailwind arbitrary value: `[&_pre]:!bg-[#191817]` to match catppuccinMocha.mantle.
   - Alternatively, if there's a better approach, use `[&_pre]:!bg-[var(--code-block-bg)]` and define `--code-block-bg` in index.css. The simpler approach (direct mantle color) is fine for now.

6. Update the streaming/loading fallback `<pre>` (around line 113):
   - Change `bg-surface-base` to the same code block background. Use `bg-[#191817]` or define a Tailwind utility. This ensures streaming code blocks match highlighted code blocks.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - DiffViewer uses charcoal + Catppuccin colors (no #1c1210, #f5e6d3, #c4a882 browns remain)
    - Diff added/removed backgrounds use green/red tints matching CSS custom properties
    - Code block backgrounds are slightly darker than surface-base, visually distinct
    - warmDiffStyles renamed to charcoalDiffStyles
    - Token colors in diff lines and regular code blocks are identical (both use catppuccin-mocha Shiki theme)
  </done>
  <depth>
    - Diff word-level highlights (wordAddedBackground, wordRemovedBackground) should be slightly brighter than line-level to stand out
    - Gutter backgrounds should be slightly darker than diff line backgrounds
    - The contentText font family stays as-is (already uses JetBrains Mono)
    - ShikiDiffLine.tsx needs NO changes -- it calls highlightCode() which now uses catppuccin-mocha automatically
    - Verify no v1.0 hex values remain: search for #1c1210, #f5e6d3, #c4a882, #3d2e25, #d4a574, #e0d0a0
  </depth>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- Code blocks in chat show Catppuccin Mocha syntax colors (mauve keywords, green strings, blue functions)
- Code block backgrounds are slightly darker than surrounding UI surface
- Diff viewer shows charcoal base with green-tinted added lines and red-tinted removed lines
- No warm brown/amber colors (#1c1210, #f5e6d3, #c4a882) visible in any code or diff surface
- Syntax token colors are identical between regular code blocks and diff view lines
</verification>

<success_criteria>
- Shiki switched from dark-plus to catppuccin-mocha theme
- All v1.0 WARM_COLOR_REPLACEMENTS deleted
- Diff viewer uses charcoal + Catppuccin palette
- Code block and diff viewer backgrounds integrate seamlessly with app charcoal
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/12-specialty-surfaces/12-02-SUMMARY.md`
</output>
