---
phase: 12-specialty-surfaces
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/code-editor/themes/loom-dark.ts
  - src/components/code-editor/view/subcomponents/CodeEditorSurface.tsx
autonomous: true
requirements: [SURF-03]

must_haves:
  truths:
    - CodeMirror file viewer uses custom charcoal + rose dark theme
    - CodeMirror background matches app charcoal base
    - CodeMirror syntax token colors use Catppuccin Mocha palette (identical to Shiki)
    - CodeMirror gutters, selection, cursor use rose/charcoal palette
    - No oneDark theme artifacts remain
  artifacts:
    - src/components/code-editor/themes/loom-dark.ts (new)
    - src/components/code-editor/view/subcomponents/CodeEditorSurface.tsx (updated)
  key_links:
    - loom-dark.ts imports from catppuccin-mocha.ts shared constants
    - CodeEditorSurface.tsx imports loom-dark theme instead of oneDark
---

<objective>
Create a custom CodeMirror dark theme using EditorView.theme() and HighlightStyle.define() with Catppuccin Mocha syntax colors on the app's warm charcoal base, replacing the oneDark import.

Purpose: The file editor should match the app palette and use identical syntax colors as Shiki code blocks for visual consistency.
Output: Custom CodeMirror theme extension with full editor chrome styling and syntax highlighting.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-specialty-surfaces/12-CONTEXT.md
@.planning/phases/12-specialty-surfaces/12-RESEARCH.md
@.planning/phases/12-specialty-surfaces/12-01-SUMMARY.md
@src/shared/catppuccin-mocha.ts
@src/components/code-editor/view/subcomponents/CodeEditorSurface.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create custom CodeMirror Loom dark theme</name>
  <files>src/components/code-editor/themes/loom-dark.ts</files>
  <action>
Create `src/components/code-editor/themes/loom-dark.ts` -- a complete CodeMirror theme with both editor chrome styling and syntax highlighting.

**Imports:**
```typescript
import { EditorView } from '@codemirror/view';
import { HighlightStyle, syntaxHighlighting } from '@codemirror/language';
import { tags } from '@lezer/highlight';
import { catppuccinMocha, syntaxColors } from '../../shared/catppuccin-mocha'; // adjust path
```

Note: `@codemirror/language` and `@lezer/highlight` should already be installed as peer dependencies of the CodeMirror packages. If not, they need to be added. Check first.

**Part 1 -- Editor Chrome Theme** using `EditorView.theme({...}, { dark: true })`:

| Selector | Styles |
|----------|--------|
| `&` | backgroundColor: catppuccinMocha.base, color: catppuccinMocha.text |
| `.cm-content` | caretColor: catppuccinMocha.rosewater |
| `&.cm-focused .cm-cursor` | borderLeftColor: catppuccinMocha.rosewater |
| `&.cm-focused .cm-selectionBackground, .cm-selectionBackground, ::selection` | backgroundColor: catppuccinMocha.surface2 + '60' (with alpha) |
| `.cm-panels` | backgroundColor: catppuccinMocha.mantle, color: catppuccinMocha.text |
| `.cm-panels.cm-panels-top` | borderBottom: `1px solid ${catppuccinMocha.surface0}` |
| `.cm-panels.cm-panels-bottom` | borderTop: `1px solid ${catppuccinMocha.surface0}` |
| `.cm-searchMatch` | backgroundColor: catppuccinMocha.surface2 + '40', outline: `1px solid ${catppuccinMocha.surface2}` |
| `.cm-searchMatch.cm-searchMatch-selected` | backgroundColor: catppuccinMocha.surface2 + '80' |
| `.cm-activeLine` | backgroundColor: catppuccinMocha.surface0 + '40' |
| `.cm-selectionMatch` | backgroundColor: catppuccinMocha.surface2 + '30' |
| `.cm-matchingBracket` | color: catppuccinMocha.green, fontWeight: 'bold', outline: `1px solid ${catppuccinMocha.surface2}` |
| `.cm-gutters` | backgroundColor: catppuccinMocha.mantle, color: catppuccinMocha.overlay0, borderRight: 'none' |
| `.cm-activeLineGutter` | backgroundColor: catppuccinMocha.surface0 + '60', color: catppuccinMocha.subtext0 |
| `.cm-foldPlaceholder` | backgroundColor: 'transparent', border: 'none', color: catppuccinMocha.overlay1 |
| `.cm-tooltip` | border: `1px solid ${catppuccinMocha.surface0}`, backgroundColor: catppuccinMocha.surface0, color: catppuccinMocha.text |
| `.cm-tooltip.cm-tooltip-autocomplete > ul > li[aria-selected]` | backgroundColor: catppuccinMocha.surface1, color: catppuccinMocha.text |

**Part 2 -- Syntax Highlighting** using `HighlightStyle.define([...])`:

Map @lezer/highlight tags to syntaxColors from the shared constants:

| Tag(s) | Color | Style |
|--------|-------|-------|
| tags.keyword | syntaxColors.keyword (mauve) | |
| tags.controlKeyword | syntaxColors.controlFlow (mauve) | |
| tags.operatorKeyword | syntaxColors.keyword (mauve) | |
| tags.definitionKeyword | syntaxColors.keyword (mauve) | |
| tags.moduleKeyword | syntaxColors.keyword (mauve) | |
| tags.operator | syntaxColors.operator (sky) | |
| tags.comment, tags.lineComment, tags.blockComment | syntaxColors.comment (overlay1) | fontStyle: 'italic' |
| tags.string, tags.special(tags.string) | syntaxColors.string (green) | |
| tags.regexp | syntaxColors.regex (peach) | |
| tags.number, tags.integer, tags.float | syntaxColors.number (peach) | |
| tags.bool | syntaxColors.constant (peach) | |
| tags.null | syntaxColors.constant (peach) | |
| tags.atom | syntaxColors.constant (peach) | |
| tags.variableName | syntaxColors.variable (text) | |
| tags.definition(tags.variableName) | catppuccinMocha.lavender | |
| tags.function(tags.variableName) | syntaxColors.function (blue) | |
| tags.typeName | syntaxColors.type (yellow) | |
| tags.className | syntaxColors.type (yellow) | |
| tags.propertyName | syntaxColors.property (lavender) | |
| tags.function(tags.propertyName) | syntaxColors.function (blue) | |
| tags.tagName | syntaxColors.tag (blue) | |
| tags.attributeName | syntaxColors.attribute (yellow) | |
| tags.angleBracket | syntaxColors.punctuation (overlay2) | |
| tags.bracket | syntaxColors.punctuation (overlay2) | |
| tags.paren | syntaxColors.punctuation (overlay2) | |
| tags.squareBracket | syntaxColors.punctuation (overlay2) | |
| tags.separator | syntaxColors.punctuation (overlay2) | |
| tags.meta | catppuccinMocha.pink | |
| tags.heading | catppuccinMocha.red | fontWeight: 'bold' |
| tags.link | catppuccinMocha.sapphire | textDecoration: 'underline' |
| tags.emphasis | fontStyle: 'italic' (no color change) | |
| tags.strong | fontWeight: 'bold' (no color change) | |
| tags.invalid | color: catppuccinMocha.red | textDecoration: 'line-through' |

**Export:** Combine both into a single exported extension array:
```typescript
export const loomDarkTheme: Extension = [
  loomEditorTheme,
  syntaxHighlighting(loomHighlightStyle),
];
```

Import `Extension` type from `@codemirror/state`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>loom-dark.ts exports a complete CodeMirror theme extension with both editor chrome and syntax highlighting using Catppuccin Mocha colors on charcoal base. TypeScript compiles without errors.</done>
  <depth>
    - Ensure @codemirror/language is available (check node_modules, may be a transitive dependency)
    - Ensure @lezer/highlight is available (check node_modules, typically installed with @codemirror/language)
    - EditorView.theme's second argument `{ dark: true }` is critical -- marks this as a dark theme for base theme rules
    - Cover all tags that oneDark covers (reference @codemirror/theme-one-dark source for completeness)
    - Use alpha hex suffixes (e.g., '40', '60', '80') for overlay effects, not CSS rgba()
  </depth>
</task>

<task type="auto">
  <name>Task 2: Wire custom theme into CodeEditorSurface</name>
  <files>src/components/code-editor/view/subcomponents/CodeEditorSurface.tsx</files>
  <action>
Update CodeEditorSurface.tsx to use the new Loom dark theme instead of oneDark.

**Changes:**

1. Remove import: `import { oneDark } from '@codemirror/theme-one-dark';`

2. Add import: `import { loomDarkTheme } from '../themes/loom-dark';`
   (Adjust path relative to CodeEditorSurface.tsx location)

3. Update the `theme` prop on the `<CodeMirror>` component:
   - Change `theme={isDarkMode ? oneDark : undefined}` to `theme={isDarkMode ? loomDarkTheme : undefined}`

4. No other changes needed -- the component structure, props, and MarkdownPreview branch remain unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>CodeEditorSurface uses loomDarkTheme instead of oneDark. No import of @codemirror/theme-one-dark remains. File editor shows charcoal background with Catppuccin Mocha syntax colors.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- CodeMirror file editor background matches app charcoal (#1b1a19)
- Syntax highlighting in CodeMirror uses Catppuccin Mocha colors (mauve keywords, green strings, blue functions)
- Gutters use darker charcoal with muted line numbers
- Selection uses charcoal surface2 with alpha
- No oneDark blue-gray artifacts remain
- Syntax colors match Shiki code blocks (both use same Catppuccin Mocha palette from shared constants)
</verification>

<success_criteria>
- Custom CodeMirror theme created with full editor chrome and syntax highlighting
- oneDark import removed from CodeEditorSurface
- Editor visually integrated with app charcoal palette
- Token colors consistent with Shiki (both reference syntaxColors from shared constants)
</success_criteria>

<output>
After completion, create `.planning/phases/12-specialty-surfaces/12-03-SUMMARY.md`
</output>
