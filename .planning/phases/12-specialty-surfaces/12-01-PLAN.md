---
phase: 12-specialty-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/catppuccin-mocha.ts
  - src/components/shell/constants/constants.ts
autonomous: true
requirements: [SURF-01, SURF-02]

must_haves:
  truths:
    - Terminal background matches app charcoal base (#1b1a19) -- no black box appearance
    - Terminal uses Catppuccin Mocha ANSI palette for command output colors
    - Terminal renders with JetBrains Mono font at 14px
    - Terminal uses bar cursor style
    - Terminal line height is 1.2
  artifacts:
    - src/shared/catppuccin-mocha.ts
    - src/components/shell/constants/constants.ts (updated)
  key_links:
    - catppuccin-mocha.ts exports are used by constants.ts terminal theme
---

<objective>
Create the shared Catppuccin Mocha color constants file and re-theme the xterm.js terminal with Catppuccin Mocha ANSI colors on the app's warm charcoal base.

Purpose: Establish the single source of truth for Catppuccin Mocha colors that all other plans will import, and deliver the terminal as the first visible specialty surface integration.
Output: Shared color constants module, terminal with integrated warm charcoal appearance and Catppuccin Mocha ANSI palette.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-specialty-surfaces/12-CONTEXT.md
@.planning/phases/12-specialty-surfaces/12-RESEARCH.md
@src/components/shell/constants/constants.ts
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared Catppuccin Mocha color constants</name>
  <files>src/shared/catppuccin-mocha.ts</files>
  <action>
Create `src/shared/catppuccin-mocha.ts` as the single source of truth for all Catppuccin Mocha colors used across specialty surfaces. This module is imported by terminal (this plan), Shiki, CodeMirror, and diff viewer (subsequent plans).

**Structure:**
1. `catppuccinMocha` object with ALL 26 Catppuccin Mocha colors, BUT with base/surface/text colors warm-shifted to match the app's charcoal palette:
   - `base`: `#1b1a19` (app's --surface-base, NOT Catppuccin's blue-tinted #1e1e2e)
   - `mantle`: `#191817` (slightly darker than base, for code block backgrounds)
   - `crust`: `#151413` (darkest level)
   - `surface0`: `#252423` (app's --secondary)
   - `surface1`: `#2b2a29` (app's --surface-elevated)
   - `surface2`: `#343332` (lighter surface)
   - `text`: `#e4dbd9` (app's --foreground)
   - `subtext1`: `#bdb9b9` (app's --foreground-secondary)
   - `subtext0`: `#989494` (app's --muted-foreground)
   - `overlay2`: `#7a7676`
   - `overlay1`: `#636060`
   - `overlay0`: `#4d4a4a`
   - All accent/token colors stay pure Catppuccin Mocha (no warm shift):
     rosewater=#f5e0dc, flamingo=#f2cdcd, pink=#f5c2e7, mauve=#cba6f7,
     red=#f38ba8, maroon=#eba0ac, peach=#fab387, yellow=#f9e2af,
     green=#a6e3a1, teal=#94e2d5, sky=#89dceb, sapphire=#74c7ec,
     blue=#89b4fa, lavender=#b4befe

2. `syntaxColors` object mapping semantic syntax roles to Catppuccin colors:
   - keyword: mauve (#cba6f7)
   - string: green (#a6e3a1)
   - type: yellow (#f9e2af)
   - function: blue (#89b4fa)
   - variable: text (#e4dbd9)
   - comment: overlay1 (#636060)
   - number: peach (#fab387)
   - operator: sky (#89dceb)
   - controlFlow: mauve (#cba6f7)
   - constant: peach (#fab387)
   - tag: blue (#89b4fa)
   - attribute: yellow (#f9e2af)
   - property: lavender (#b4befe)
   - regex: peach (#fab387)
   - punctuation: overlay2 (#7a7676)

3. `shikiBaseReplacements` record mapping original Catppuccin Mocha base hex values to warm-shifted equivalents (used by Shiki's colorReplacements). Map the 12 base/surface/text colors from their original Catppuccin hex to warm-shifted hex.

Export all three objects with `as const` for type safety.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>src/shared/catppuccin-mocha.ts exports catppuccinMocha, syntaxColors, and shikiBaseReplacements with correct types and all 26 Catppuccin Mocha colors (warm-shifted base/surface/text, pure accent tokens)</done>
</task>

<task type="auto">
  <name>Task 2: Re-theme xterm.js terminal with Catppuccin Mocha</name>
  <files>src/components/shell/constants/constants.ts</files>
  <action>
Update `src/components/shell/constants/constants.ts` to import from the shared Catppuccin Mocha constants and replace all hardcoded VS Code Dark+ colors.

**Changes:**
1. Add import: `import { catppuccinMocha } from '@/shared/catppuccin-mocha';`
   (If the project doesn't use `@/` path alias, check tsconfig/vite config for the correct import path -- may need `../../shared/catppuccin-mocha` or similar relative path.)

2. Update TERMINAL_OPTIONS:
   - `cursorStyle: 'bar'` (currently missing -- add this property)
   - `fontFamily: "'JetBrains Mono', 'Fira Code', 'Cascadia Code', monospace"` (replace current Menlo/Monaco family)
   - `lineHeight: 1.2` (currently missing -- add this property)
   - `fontSize: 14` (already correct)

3. Replace the entire `theme` object with Catppuccin Mocha ANSI colors from the shared constants:
   - background: catppuccinMocha.base (#1b1a19)
   - foreground: catppuccinMocha.text (#e4dbd9)
   - cursor: catppuccinMocha.rosewater (#f5e0dc)
   - cursorAccent: catppuccinMocha.base (#1b1a19)
   - selectionBackground: catppuccinMocha.surface2 + '80' alpha (#34333280)
   - selectionForeground: catppuccinMocha.text (#e4dbd9)
   - ANSI black: catppuccinMocha.surface1 (#2b2a29)
   - ANSI red: catppuccinMocha.red (#f38ba8)
   - ANSI green: catppuccinMocha.green (#a6e3a1)
   - ANSI yellow: catppuccinMocha.yellow (#f9e2af)
   - ANSI blue: catppuccinMocha.blue (#89b4fa)
   - ANSI magenta: catppuccinMocha.pink (#f5c2e7)
   - ANSI cyan: catppuccinMocha.teal (#94e2d5)
   - ANSI white: catppuccinMocha.subtext1 (#bdb9b9)
   - brightBlack: catppuccinMocha.overlay0 (#4d4a4a)
   - brightRed: catppuccinMocha.red (#f38ba8)
   - brightGreen: catppuccinMocha.green (#a6e3a1)
   - brightYellow: catppuccinMocha.yellow (#f9e2af)
   - brightBlue: catppuccinMocha.blue (#89b4fa)
   - brightMagenta: catppuccinMocha.pink (#f5c2e7)
   - brightCyan: catppuccinMocha.teal (#94e2d5)
   - brightWhite: catppuccinMocha.text (#e4dbd9)

4. Remove the `extendedAnsi` array entirely (it contained default system colors that are no longer needed -- xterm.js generates 256-color extended palette from the 16 base ANSI colors).

Keep all other TERMINAL_OPTIONS properties unchanged (cursorBlink, allowProposedApi, convertEol, scrollback, tabStopWidth, windowsMode, macOptionIsMeta, macOptionClickForcesSelection).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Terminal uses JetBrains Mono at 14px, bar cursor, 1.2 line height, Catppuccin Mocha ANSI colors on charcoal base (#1b1a19). No VS Code Dark+ colors remain. No hardcoded hex values -- all colors imported from shared constants.</done>
  <depth>
    - Verify import path resolves correctly (check tsconfig paths or vite resolve alias)
    - Ensure allowTransparency stays false (transparency not needed with matching background)
    - Selection background uses alpha transparency for readability over highlighted text
    - Bright ANSI variants use the same accent colors as normal variants (Catppuccin convention for Mocha)
  </depth>
</task>

</tasks>

<verification>
- `npm run typecheck` passes with no errors related to catppuccin-mocha imports
- Terminal background visually matches app charcoal base (both #1b1a19)
- Terminal text uses warm white (#e4dbd9) matching app foreground
- ANSI colors in terminal output (ls --color, git diff, etc.) use Catppuccin Mocha palette
- Cursor is a blinking bar, not block
- Font is JetBrains Mono at 14px with 1.2 line height
</verification>

<success_criteria>
- Shared Catppuccin Mocha constants file created and type-checks
- Terminal appears integrated with the app -- no visual seam between terminal area and surrounding UI
- All 16 ANSI colors use Catppuccin Mocha palette values
- Terminal typography: JetBrains Mono, 14px, 1.2 line height, bar cursor
</success_criteria>

<output>
After completion, create `.planning/phases/12-specialty-surfaces/12-01-SUMMARY.md`
</output>
