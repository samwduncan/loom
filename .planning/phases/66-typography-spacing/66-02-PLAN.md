---
phase: 66-typography-spacing
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/src/components/sidebar/SessionItem.tsx
  - src/src/components/chat/styles/thinking-disclosure.css
  - .planning/V2_CONSTITUTION.md
  - src/e2e/typography.spec.ts
autonomous: true
requirements:
  - TYPO-02
  - TYPO-03
  - TYPO-05
  - TYPO-08

must_haves:
  truths:
    - "ThinkingDisclosure trigger text renders at 14px minimum on mobile"
    - "Session titles truncate at 1 line with ellipsis on mobile"
    - "Session list density is optimized with py-2 items and compact group gaps"
    - "Keyboard opening does not cause text to shrink or jump"
    - "V2_CONSTITUTION Section 14 documents typography conventions"
  artifacts:
    - path: "src/e2e/typography.spec.ts"
      provides: "Playwright regression tests for mobile font sizes"
      min_lines: 40
    - path: ".planning/V2_CONSTITUTION.md"
      provides: "Section 14 Typography conventions"
      contains: "## 14. Typography"
  key_links:
    - from: "src/e2e/typography.spec.ts"
      to: "src/src/styles/tokens.css"
      via: "Playwright getComputedStyle assertions verify token values render correctly"
      pattern: "getComputedStyle"
---

<objective>
Verify and fix sidebar density/truncation, ThinkingDisclosure font floor, and keyboard stability. Create the Playwright typography regression test and formalize conventions in V2_CONSTITUTION Section 14.

Purpose: Complete the remaining 4 TYPO requirements (verify-first items that may already pass + convention documentation), and establish automated regression testing so font-size violations are caught in CI.
Output: Verified sidebar density, ThinkingDisclosure font floor, keyboard stability, Playwright typography.spec.ts, V2_CONSTITUTION Section 14.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md (TYPO-02, TYPO-03, TYPO-05, TYPO-08)
@.planning/phases/66-typography-spacing/66-CONTEXT.md
@.planning/phases/66-typography-spacing/66-RESEARCH.md
@.planning/V2_CONSTITUTION.md
@.planning/phases/66-typography-spacing/66-01-SUMMARY.md

<interfaces>
<!-- SessionItem typography and layout (lines 159-228) -->
From src/src/components/sidebar/SessionItem.tsx:
```tsx
// Line 159-166: Main container classes
className={cn(
  'px-3 py-2 cursor-pointer',
  'min-h-[44px] md:min-h-0',
  ...
)}

// Line 191-193: Title div -- already has truncate
<div className={cn(
  'text-[length:var(--text-body)] text-foreground',
  'truncate min-w-0 flex-1',
)}>

// Line 221: Timestamp
<span className="text-[length:0.75rem] text-muted">
```

<!-- ThinkingDisclosure trigger (thinking-disclosure.css lines 18-31) -->
From src/src/components/chat/styles/thinking-disclosure.css:
```css
.thinking-disclosure-trigger {
  font-size: 0.875rem;   /* Already 14px -- TYPO-02 may already pass */
}
/* Mobile touch target (lines 102-106): */
@media (max-width: 767px) {
  .thinking-disclosure-trigger {
    min-height: 44px;
  }
}
```

<!-- V2_CONSTITUTION table of contents (line 25) -->
From .planning/V2_CONSTITUTION.md:
```
13. [Touch Target & Focus Standards](#13-touch-target--focus-standards)
```
Section 14 does not yet exist. New section will be added after Section 13 (before Appendix A at line 811).

<!-- Existing E2E test patterns -->
From src/e2e/touch-targets.spec.ts (pattern reference for viewport + getComputedStyle approach).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify-first items (TYPO-02, TYPO-03, TYPO-05, TYPO-08) and fix if needed</name>
  <read_first>
    - src/src/components/chat/styles/thinking-disclosure.css (full file -- verify TYPO-02 current font-size)
    - src/src/components/sidebar/SessionItem.tsx (full file -- verify TYPO-03 truncation and TYPO-05 density)
    - src/src/components/sidebar/DateGroupHeader.tsx (verify gap/spacing for TYPO-05)
    - src/src/hooks/useKeyboardOffset.ts (verify TYPO-08 -- no font-size manipulation)
    - src/src/components/chat/composer/composer.css (verify no font-size affected by keyboard offset)
    - .planning/phases/66-typography-spacing/66-CONTEXT.md (D-09, D-10, D-14, D-15, D-21, D-22)
  </read_first>
  <files>
    src/src/components/sidebar/SessionItem.tsx,
    src/src/components/chat/styles/thinking-disclosure.css
  </files>
  <action>
**Verify-first approach:** Read each file and determine if a fix is needed before making changes. These items may already pass.

**TYPO-02: ThinkingDisclosure trigger text at 14px (per D-09).**

Read `thinking-disclosure.css` line 28. The trigger already has `font-size: 0.875rem` (14px). Verify there are no mobile media query overrides that shrink it. If no override shrinks it below 14px on mobile, this already passes -- add a comment `/* TYPO-02: 14px minimum -- verified */` next to the font-size declaration for documentation.

If any mobile override DOES shrink it, add this inside the existing `@media (max-width: 767px)` block (lines 102-106):
```css
font-size: 0.875rem; /* TYPO-02: 14px floor on mobile */
```

**TYPO-03: Session title truncation on mobile (per D-10).**

Read `SessionItem.tsx` lines 191-193. The title div already has `truncate min-w-0 flex-1` classes. The `truncate` class applies `overflow: hidden; text-overflow: ellipsis; white-space: nowrap;` which enforces single-line with ellipsis. Verify this by checking no other class or parent overrides `white-space`.

If truncation is already working (it should be -- `truncate` + `min-w-0` + `flex-1` is the standard Tailwind pattern for flexbox truncation), no change needed. Document verification in the summary.

If for some reason wrapping occurs on narrow viewports, add `line-clamp-1` as a defensive addition.

**TYPO-05: Session list density (per D-14, D-15).**

Read `SessionItem.tsx` line 160. Already has `px-3 py-2` (8px vertical padding) with `min-h-[44px]` (Phase 65). This matches the TYPO-05 requirement of "py-2 items."

For the "4px gaps between groups" part of TYPO-05: Read `DateGroupHeader.tsx` -- it has `py-1.5` (6px vertical padding). Check the parent container in the sidebar to understand the gap between DateGroupHeader and first SessionItem. The gap is controlled by the flex container in the session list. If the current gap is already close to 4px (likely since there's no explicit gap between DateGroupHeader and the following items, just the padding), no change is needed. If the gap is larger than needed, reduce DateGroupHeader bottom padding.

In practice: The DateGroupHeader has `py-1.5` (6px top + 6px bottom = 12px total), and SessionItem has `py-2` (8px top). The visual gap between the header text baseline and the first item text is approximately 14px (6px header bottom + 8px item top), which provides adequate visual separation without wasting space. This is acceptable per D-15 ("adjust if needed, but don't over-compress").

**TYPO-08: Keyboard stability (per D-21, D-22).**

Read `useKeyboardOffset.ts`. The hook adjusts `--keyboard-offset` CSS variable which controls `padding-bottom` on `.app-shell` (in base.css). No mechanism exists that would shrink font-size during keyboard events. The font-size of the composer token count was already fixed from 0.6875rem to var(--text-xs) (12px) in Plan 01 (TYPO-01/D-08), which covers the edge case D-22 identified.

This requirement passes by architecture -- the keyboard hook adjusts layout padding, not font sizes. Document verification in the summary. No code change needed for TYPO-08.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - thinking-disclosure.css contains `font-size: 0.875rem` in `.thinking-disclosure-trigger` (14px, no mobile override shrinks it)
    - SessionItem.tsx title div contains both `truncate` and `min-w-0` classes
    - SessionItem.tsx contains `py-2` in the main container className
    - No file in this plan introduces viewport-dependent font-size shrinking during keyboard events
    - `cd /home/swd/loom/src && npx vitest run` exits 0
  </acceptance_criteria>
  <done>TYPO-02 verified: ThinkingDisclosure trigger already at 14px with no mobile override shrinking it. TYPO-03 verified: SessionItem title uses truncate + min-w-0 + flex-1 for single-line ellipsis. TYPO-05 verified: SessionItem uses py-2 (8px) with 44px min-height; DateGroupHeader gap is adequate. TYPO-08 verified structurally: useKeyboardOffset adjusts layout padding only, never font-size; composer token count fixed to 12px in Plan 01. Full confirmation of keyboard stability requires real-device UAT.</done>
</task>

<task type="auto">
  <name>Task 2: Create Playwright typography regression test and V2_CONSTITUTION Section 14</name>
  <read_first>
    - src/e2e/touch-targets.spec.ts (reference for mobile viewport testing patterns with Playwright)
    - src/e2e/helpers.ts (shared E2E test helpers)
    - .planning/V2_CONSTITUTION.md (full file -- Section 13 ends around line 808, Appendix A starts at line 811)
    - .planning/phases/66-typography-spacing/66-VALIDATION.md (Wave 0 requirements for typography.spec.ts)
  </read_first>
  <files>
    src/e2e/typography.spec.ts,
    .planning/V2_CONSTITUTION.md
  </files>
  <action>
**1. Create `src/e2e/typography.spec.ts` -- Playwright regression test (Wave 0 gap closure).**

Create a new Playwright test file that verifies TYPO-01 through TYPO-07 at mobile viewport (375x812 iPhone dimensions). Use the same pattern as `touch-targets.spec.ts` for viewport configuration and page navigation.

The test should:

a. Set viewport to `{ width: 375, height: 812 }` (iPhone 16 Pro Max portrait).

b. Navigate to the app and wait for the chat view to render.

c. **TYPO-01 check:** Verify no visible text element has computed font-size below 12px. Use `page.evaluate()` to walk all visible text nodes, get `getComputedStyle(element).fontSize`, parse the pixel value, and assert >= 12. Exclude elements with `display: none`, `visibility: hidden`, or `opacity: 0`. Exclude elements whose text content is empty. Allow exceptions for elements with class containing `text-[10px]` that are explicitly documented as acceptable (CollapsibleMessage count badge, TabBar keyboard hints with `lg:` guard, dev-only components).

d. **TYPO-04 check:** Verify `.markdown-body` elements (if present in the loaded page) have computed font-size >= 15px at mobile viewport. If no messages are loaded (empty chat), skip this assertion with a descriptive message.

e. **TYPO-07 check:** Verify `.markdown-body` elements (if present) have computed line-height that when divided by font-size yields >= 1.6 ratio. Same skip logic as TYPO-04 for empty chat.

f. **TYPO-02 check:** If `.thinking-disclosure-trigger` elements exist, verify computed font-size >= 14px.

g. **TYPO-03 check:** If session items exist in the sidebar, verify the title div has `overflow: hidden` and `text-overflow: ellipsis` computed styles (evidence of truncation).

Structure the test with `test.describe('Typography mobile compliance', () => { ... })` containing individual `test()` calls for each check.

**Important:** The test must be resilient to the app state (may or may not have messages, may or may not have sessions). Use conditional assertions with skip for elements that may not exist.

**NOTE:** This test depends on Plan 01's mobile font-size overrides being in place. The TYPO-04 check (>= 15px for .markdown-body) and TYPO-07 check (line-height ratio >= 1.6) will only pass after Plan 01's base.css media query is committed.

**2. Add Section 14 (Typography) to V2_CONSTITUTION.md (per D-23).**

Read the full V2_CONSTITUTION.md. Insert a new Section 14 between the end of Section 13 (Touch Target & Focus Standards, which ends with the Reference Implementations block ending at the `---` separator before Appendix A) and Appendix A.

Update the Table of Contents (around line 25) to add:
```
14. [Typography](#14-typography)
```

Add the following section:

```markdown
## 14. Typography

### 14.1 Minimum Font Size (12px)

No text element in the application may render below 12px (0.75rem) on any viewport.

**Rules:**
- All font-size declarations MUST use design tokens: `var(--text-xs)` (12px), `var(--text-sm)` (13px), `var(--text-body)` (14px), `var(--text-code)` (13px), or heading tokens
- BANNED: Hardcoded `font-size` values in rem, px, or em in component CSS/TSX (use tokens)
- BANNED: `font-size` below `var(--text-xs)` (0.75rem / 12px) for any text element
- **Exceptions:** Badge count indicators (`text-[10px]`) in CollapsibleMessage and desktop-only keyboard hints (behind `lg:` guard) are the only permitted sub-12px usages

### 14.2 Mobile Body Text (15px / 18px Code)

Chat message content uses larger font sizes on mobile (< 768px) for readability at arm's length.

**Token values:**
| Token | Desktop | Mobile (< 768px) |
|-------|---------|-------------------|
| Body text (`.markdown-body`, `.active-message`) | 14px (0.875rem) | 15px (0.9375rem) |
| Code blocks (`.markdown-body code`, `.active-message code`) | 13px (0.8125rem) | 18px (1.125rem) |

**Rules:**
- Mobile overrides are defined in `base.css` via `@media (max-width: 767px)` — NOT in individual component files
- Both `.markdown-body` (finalized messages) and `.active-message` (streaming) MUST be targeted in the same media query block to prevent CLS on stream completion
- No `clamp()` for font sizes — clean breakpoint split only (D-02)

### 14.3 Line-Height Minimums

| Context | Minimum | Tailwind | Notes |
|---------|---------|----------|-------|
| Body/reading text (mobile) | 1.6 | `leading-relaxed` (1.625) | Multi-line chat messages, session descriptions |
| Body/reading text (desktop) | 1.5 | `leading-normal` | Standard desktop density |
| Labels, headings, single-line | 1.4 | — | Buttons, pills, badges, headers |
| Compact UI elements | 1.0 | — | Tool chips, scroll pill, disclosure arrows — intentional |

**Rules:**
- Do NOT apply `line-height: 1.6` globally — it wastes space on compact UI elements
- The four intentional `line-height: 1` usages (tool-card-shell, thinking-disclosure arrow, scroll pill, tool chip) MUST NOT be changed

### 14.4 Typography Token Reference

| Token | Value | Usage |
|-------|-------|-------|
| `--text-xs` | 0.75rem (12px) | Minimum readable: DateGroupHeader, badges, status bar, command palette headings |
| `--text-sm` | 0.8125rem (13px) | Compact text: git panel, tool cards |
| `--text-code` | 0.8125rem (13px) | Code blocks desktop (monospace) |
| `--text-body` | 0.875rem (14px) | Body text, default font-size |
| `--text-h3` | 1.125rem (18px) | Subheadings |
| `--text-h2` | 1.5rem (24px) | Section headings |
| `--text-h1` | 2rem (32px) | Page headings |

### 14.5 Reference Implementations

- **Token usage**: `DateGroupHeader.tsx` — `text-[length:var(--text-xs)]`
- **Token usage (CSS)**: `composer.css` — `font-size: var(--text-xs);`
- **Mobile override**: `base.css` — `@media (max-width: 767px)` block targeting `.markdown-body, .active-message`
- **Code block token**: `CodeBlock.tsx` — `text-[length:var(--text-code)]`
- **Streaming parity**: `streaming-markdown.css` — uses `var(--text-code)` for code, matches finalized via shared mobile media query
```
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx playwright test e2e/typography.spec.ts --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/e2e/typography.spec.ts` exists with >= 40 lines
    - typography.spec.ts contains `getComputedStyle` for font-size assertions
    - typography.spec.ts sets viewport width to 375 (mobile)
    - typography.spec.ts contains test cases for TYPO-01, TYPO-02, TYPO-03, TYPO-04, TYPO-07
    - V2_CONSTITUTION.md Table of Contents contains `14. [Typography]`
    - V2_CONSTITUTION.md contains `## 14. Typography`
    - V2_CONSTITUTION.md Section 14 contains `--text-xs` token documentation
    - V2_CONSTITUTION.md Section 14 contains `0.9375rem` (mobile body text reference)
    - V2_CONSTITUTION.md Section 14 contains `line-height: 1` intentional exclusion note
    - `cd /home/swd/loom/src && npx playwright test e2e/typography.spec.ts` exits 0 (or tests skip gracefully for missing content)
  </acceptance_criteria>
  <done>Playwright typography regression test created at src/e2e/typography.spec.ts covering TYPO-01 through TYPO-07 at mobile viewport. V2_CONSTITUTION.md updated with Section 14 (Typography) documenting minimum font sizes, mobile body text tokens, line-height minimums, token reference table, and reference implementations.</done>
</task>

</tasks>

<verification>
1. TYPO-02: `grep 'font-size: 0.875rem' src/src/components/chat/styles/thinking-disclosure.css` confirms 14px trigger font-size
2. TYPO-03: `grep 'truncate' src/src/components/sidebar/SessionItem.tsx` confirms truncation class
3. TYPO-05: `grep 'py-2' src/src/components/sidebar/SessionItem.tsx` confirms density padding
4. TYPO-08: `grep -c 'font-size' src/src/hooks/useKeyboardOffset.ts` returns 0 (no font manipulation)
5. Typography E2E: `cd src && npx playwright test e2e/typography.spec.ts` passes
6. Constitution: `grep '## 14. Typography' .planning/V2_CONSTITUTION.md` returns match
7. Full test suite: `cd src && npx vitest run` exits 0
8. Touch target regression: `cd src && npx playwright test e2e/touch-targets.spec.ts` exits 0
</verification>

<success_criteria>
- TYPO-02 verified or fixed: ThinkingDisclosure trigger at 14px on mobile
- TYPO-03 verified: Session titles truncate with ellipsis at 1 line on mobile
- TYPO-05 verified: SessionItem uses py-2 density, DateGroupHeader gap is appropriate
- TYPO-08 verified structurally: Keyboard hook adjusts layout only, not font-size; full confirmation requires real-device UAT
- Playwright typography.spec.ts exists and covers mobile font-size regression
- V2_CONSTITUTION.md Section 14 documents all typography conventions
- Full Vitest and Playwright suites pass
</success_criteria>

<output>
After completion, create `.planning/phases/66-typography-spacing/66-02-SUMMARY.md`
</output>
