---
phase: 64-scroll-performance
plan: 03
type: execute
wave: 2
depends_on: ["64-01", "64-02"]
files_modified:
  - src/src/hooks/useChatScroll.ts
  - src/src/hooks/useChatScroll.test.ts
autonomous: false
requirements: [SCROLL-07, SCROLL-08, SCROLL-10]

must_haves:
  truths:
    - "Tapping the iOS status bar scrolls the chat message list to top with smooth animation"
    - "After all jank fixes, user can scroll rapidly through 50+ messages on iPhone 16 Pro Max with zero visible stutter"
    - "Virtualization is NOT needed if jank fixes achieve 60fps at 50+ messages (measure-then-decide gate)"
  artifacts:
    - path: "src/src/hooks/useChatScroll.ts"
      provides: "statusTap handler wired into the scroll hook"
      contains: "statusTap"
  key_links:
    - from: "src/src/hooks/useChatScroll.ts"
      to: "window statusTap event"
      via: "addEventListener on window for Capacitor statusTap event"
      pattern: "addEventListener.*statusTap"
---

<objective>
Verify statusTap integration works end-to-end, validate the virtualization gate (SCROLL-07 measure-then-decide), and perform real device 60fps validation on iPhone 16 Pro Max.

Purpose: statusTap is a quick iOS polish feature. The virtualization gate and 60fps validation are the acceptance tests for the entire phase -- they prove the jank fixes actually worked on real hardware.

Output: Verified statusTap in useChatScroll tests, virtualization gate decision documented, real device validation checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-scroll-performance/64-CONTEXT.md
@.planning/phases/64-scroll-performance/64-RESEARCH.md

# Prior plan summaries needed -- these plans create the code this plan validates
@.planning/phases/64-scroll-performance/64-01-SUMMARY.md
@.planning/phases/64-scroll-performance/64-02-SUMMARY.md

@src/src/hooks/useChatScroll.ts
@src/src/hooks/useChatScroll.test.ts
@src/src/lib/platform.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify statusTap integration and add SCROLL-08 E2E-style unit test</name>
  <files>src/src/hooks/useChatScroll.ts, src/src/hooks/useChatScroll.test.ts</files>
  <read_first>
    src/src/hooks/useChatScroll.ts
    src/src/hooks/useChatScroll.test.ts
    src/src/lib/platform.ts
  </read_first>
  <action>
    **Verify statusTap is already wired in useChatScroll.ts (from Plan 01 Task 1):**

    The statusTap handler should already be in useChatScroll.ts from Plan 01. Verify:
    1. There is a useEffect that checks `IS_NATIVE` from `@/lib/platform`
    2. It adds a `statusTap` event listener on `window`
    3. The handler calls `scrollContainerRef.current?.scrollTo({ top: 0, behavior: 'smooth' })`
    4. Cleanup removes the listener

    If the statusTap handler is NOT present (Plan 01 may have deprioritized it), add it now:
    ```typescript
    // SCROLL-08 / D-17: iOS status bar tap scrolls to top
    useEffect(() => {
      if (!IS_NATIVE) return;

      const handleStatusTap = () => {
        const el = scrollContainerRef.current;
        if (el) {
          el.scrollTo({ top: 0, behavior: 'smooth' });
        }
      };

      window.addEventListener('statusTap', handleStatusTap);
      return () => window.removeEventListener('statusTap', handleStatusTap);
    }, [scrollContainerRef]);
    ```

    **Add/verify SCROLL-08 test in useChatScroll.test.ts:**

    Ensure there is a test that:
    1. Mocks IS_NATIVE to true (vi.mock('@/lib/platform', () => ({ IS_NATIVE: true })))
    2. Creates the hook with a scroll container ref
    3. Dispatches a `statusTap` CustomEvent on window
    4. Asserts `scrollContainerRef.current.scrollTo` was called with `{ top: 0, behavior: 'smooth' }`

    Also add a negative test:
    1. Mocks IS_NATIVE to false
    2. Dispatches statusTap
    3. Asserts scrollTo was NOT called

    **Run full test suite to confirm no regressions from Plans 01+02:**
    After verification, run the full Vitest suite to confirm everything from the prior plans is still green.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/hooks/useChatScroll.test.ts -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `useChatScroll.ts` contains `addEventListener('statusTap'`
    - `useChatScroll.ts` contains `scrollTo({ top: 0, behavior: 'smooth' })`
    - `useChatScroll.ts` contains `IS_NATIVE` check gating the statusTap listener
    - `useChatScroll.test.ts` contains a test with `statusTap` in its name or description
    - `useChatScroll.test.ts` contains both a positive test (IS_NATIVE=true, scrollTo called) and negative test (IS_NATIVE=false, scrollTo not called)
    - All hook tests pass: `cd src && npx vitest run src/src/hooks/useChatScroll.test.ts -x` exits 0
    - Full suite passes: `cd src && npx vitest run` exits 0
  </acceptance_criteria>
  <done>
    statusTap handler verified in useChatScroll.ts with both positive and negative unit tests. iOS status bar tap triggers smooth scroll to top when running in Capacitor native shell. Full test suite green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real device 60fps validation and virtualization gate decision</name>
  <files>none (manual validation only)</files>
  <read_first>
    .planning/phases/64-scroll-performance/64-01-SUMMARY.md
    .planning/phases/64-scroll-performance/64-02-SUMMARY.md
  </read_first>
  <action>
    Present the user with a comprehensive real-device testing checklist covering SCROLL-07, SCROLL-08, SCROLL-09, SCROLL-10. All code changes are complete -- this is pure validation on physical iPhone 16 Pro Max.

    Build and deploy the app so the user can test on their device. Run `cd /home/swd/loom/src && npm run build` to ensure a fresh production build is available.

    The user will test the following on their iPhone 16 Pro Max and report results:

    **SCROLL-10: 60fps validation**
    1. Open a session with 50+ messages
    2. Safari Web Inspector -> Performance monitor
    3. Scroll rapidly up and down -- expect sustained 60fps, no stutter
    4. Test with 200+ messages for headroom (D-20)

    **SCROLL-09: Rubber band bounce**
    5. Message list: scroll past top/bottom -- expect native rubber band bounce
    6. Session list sidebar: same test
    7. Page-level: expect NO bounce (html/body overscroll-behavior: none)

    **SCROLL-08: statusTap**
    8. Scroll down in long conversation, tap iOS status bar -- expect smooth scroll to top

    **SCROLL-03: Auto-scroll during streaming**
    9. Send a message generating long response -- auto-scroll follows
    10. During streaming, scroll up -- auto-scroll disengages
    11. Click pill -- auto-scroll re-engages

    **Edge case (D-21)**
    12. During streaming: scroll up, scroll back down -- no oscillation or jumps

    **SCROLL-07: Virtualization gate**
    13. If 60fps at 50+ messages: virtualization NOT needed (report "approved")
    14. If >5% frame drops: report specifics for follow-up planning
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - User reports 60fps sustained at 50+ messages on iPhone 16 Pro Max
    - User reports native rubber band bounce on message list and session list
    - User reports status bar tap scrolls to top
    - User reports auto-scroll during streaming works without jank
    - User reports no scroll oscillation or jumping during edge cases
    - User provides virtualization gate decision (expected: not needed)
  </acceptance_criteria>
  <done>
    Real device validation complete. 60fps confirmed at 50+ messages on iPhone 16 Pro Max. Rubber band bounce, statusTap, auto-scroll, and edge cases all verified. Virtualization gate decision documented.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/swd/loom/src && npx vitest run` -- full suite green (prerequisite for device testing)
2. Real device testing on iPhone 16 Pro Max (manual, covers SCROLL-07, SCROLL-08, SCROLL-09, SCROLL-10)
3. `grep "statusTap" src/src/hooks/useChatScroll.ts` -- confirms statusTap handler present
</verification>

<success_criteria>
- statusTap integration verified with unit tests
- Real device testing on iPhone 16 Pro Max confirms 60fps at 50+ messages
- Rubber band bounce works on message list and session list
- Status bar tap scrolls to top
- Auto-scroll during streaming works without jank
- Virtualization gate decision made (expect: not needed)
- All automated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/64-scroll-performance/64-03-SUMMARY.md`
</output>
