---
phase: 64-scroll-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/hooks/useChatScroll.ts
  - src/src/hooks/useChatScroll.test.ts
  - src/src/components/chat/view/MessageList.tsx
  - src/src/hooks/useScrollAnchor.ts
  - src/src/hooks/useScrollAnchor.test.ts
  - src/src/components/dev/ProofOfLife.tsx
  - src/src/components/dev/ProofOfLife.test.tsx
  - src/e2e/scroll-anchor.spec.ts
autonomous: true
requirements: [SCROLL-01, SCROLL-02, SCROLL-03, SCROLL-06]

must_haves:
  truths:
    - "No React setState calls fire inside scroll event handlers -- atBottom detection uses IntersectionObserver on a sentinel element"
    - "No DOM measurements (scrollHeight, scrollTop, clientHeight) are read inside scroll event handlers"
    - "Auto-scroll during streaming uses ResizeObserver on content wrapper, not per-message-length useEffect"
    - "Dead useScrollAnchor.ts hook is deleted with no remaining imports"
    - "ScrollToBottomPill shows/hides correctly with debounced state updates"
  artifacts:
    - path: "src/src/hooks/useChatScroll.ts"
      provides: "Extracted scroll hook with IO sentinel, ResizeObserver auto-follow, ref-based state"
      exports: ["useChatScroll", "UseChatScrollOptions", "UseChatScrollReturn"]
    - path: "src/src/hooks/useChatScroll.test.ts"
      provides: "Unit tests for useChatScroll covering IO sentinel, auto-scroll, session switch, pill debounce"
      min_lines: 80
    - path: "src/src/components/chat/view/MessageList.tsx"
      provides: "Refactored message list consuming useChatScroll hook instead of inline scroll logic"
  key_links:
    - from: "src/src/hooks/useChatScroll.ts"
      to: "src/src/components/chat/view/MessageList.tsx"
      via: "hook consumed in MessageList"
      pattern: "useChatScroll\\("
    - from: "src/src/hooks/useChatScroll.ts"
      to: "src/src/components/chat/view/ScrollToBottomPill.tsx"
      via: "showPill and unreadCount drive pill visibility"
      pattern: "showPill|unreadCount"
    - from: "src/src/components/chat/view/MessageList.tsx"
      to: "src/src/hooks/useChatScroll.ts"
      via: "imports and calls useChatScroll"
      pattern: "import.*useChatScroll"
---

<objective>
Create the `useChatScroll` hook that eliminates the #1 scroll jank source (setState in scroll handlers) and the #3 source (per-message-length auto-scroll), integrate it into MessageList, and delete the dead useScrollAnchor hook.

Purpose: This is the core fix -- moving all scroll-driven logic from synchronous React state updates to async IntersectionObserver + ResizeObserver. Without this, scroll will never hit 60fps on iOS.

Output: New `useChatScroll.ts` hook, refactored `MessageList.tsx`, deleted `useScrollAnchor.ts`, updated `ProofOfLife.tsx`, passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-scroll-performance/64-CONTEXT.md
@.planning/phases/64-scroll-performance/64-RESEARCH.md
@.planning/phases/64-scroll-performance/64-VALIDATION.md

@src/src/components/chat/view/MessageList.tsx
@src/src/hooks/useScrollAnchor.ts
@src/src/hooks/useScrollAnchor.test.ts
@src/src/components/chat/view/ScrollToBottomPill.tsx
@src/src/components/dev/ProofOfLife.tsx
@src/src/components/dev/ProofOfLife.test.tsx
@src/e2e/scroll-anchor.spec.ts
@src/src/stores/stream.ts
@src/src/lib/platform.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/src/components/chat/view/ScrollToBottomPill.tsx:
```typescript
export interface ScrollToBottomPillProps {
  visible: boolean;
  onClick: () => void;
  unreadCount?: number;
}
```

From src/src/hooks/useScrollAnchor.ts (patterns to port):
```typescript
// IO sentinel with rootMargin (lines 87-109): port the observer pattern
// Anti-oscillation guard (isAutoScrollingRef, lines 93-97): port exactly
// User gesture detection (wheel/touchmove, lines 116-152): port for auto-scroll disengage
// ResizeObserver on content wrapper (lines 156-181): port rAF throttle, CHANGE scrollIntoView -> scrollTop assignment
// Stream start re-engagement (lines 72-80): port for isAtBottomRef reset on stream start
```

From src/src/stores/stream.ts:
```typescript
// Selector: useStreamStore((state) => state.isStreaming)
```

From src/src/components/chat/view/MessageList.tsx (current scroll logic to replace):
```typescript
// Lines 71-72: const [atBottom, setAtBottom] = useState(true); const [unreadCount, setUnreadCount] = useState(0);
// Lines 83-90: unread tracking in render body (state-during-render)
// Lines 92-98: session switch reset
// Lines 160-166: auto-follow useEffect (fires on messages.length)
// Lines 169-182: handleScroll callback (setState + DOM reads in scroll handler)
// Lines 184-193: scroll listener attachment
// Lines 195-201: scrollToBottom callback
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useChatScroll hook with IO sentinel, ResizeObserver auto-follow, and ref-based state</name>
  <files>src/src/hooks/useChatScroll.ts, src/src/hooks/useChatScroll.test.ts</files>
  <read_first>
    src/src/hooks/useScrollAnchor.ts
    src/src/hooks/useScrollAnchor.test.ts
    src/src/components/chat/view/MessageList.tsx
    src/src/components/chat/view/ScrollToBottomPill.tsx
    src/src/stores/stream.ts
    src/src/lib/platform.ts
  </read_first>
  <behavior>
    - Test: IO sentinel entering viewport sets isAtBottomRef.current to true and schedules pill hide (showPill -> false after 200ms debounce)
    - Test: IO sentinel exiting viewport (while NOT auto-scrolling) sets isAtBottomRef.current to false and schedules pill show (showPill -> true after 200ms debounce)
    - Test: IO sentinel exiting viewport while isAutoScrollingRef is true does NOT change isAtBottomRef (anti-oscillation guard)
    - Test: ResizeObserver callback when isAtBottomRef is true triggers rAF-throttled scrollTop assignment (container.scrollTop = container.scrollHeight - container.clientHeight)
    - Test: ResizeObserver callback when isAtBottomRef is false does NOT trigger scroll
    - Test: ResizeObserver callback skips if rAF frame already pending (throttle guard)
    - Test: Session change (sessionId prop) resets isAtBottomRef to true, unreadCountRef to 0, and pill state
    - Test: scrollToBottom() sets container.scrollTop, resets unread, and schedules pill hide
    - Test: Wheel/touchmove on container disengages auto-scroll (sets isAutoScrollingRef to false)
    - Test: Stream start (isStreaming false->true) re-engages auto-scroll (isAutoScrollingRef to true, isAtBottomRef to true)
    - Test: Unread count increments when messageCount increases while isAtBottomRef is false
    - Test: statusTap window event (when IS_NATIVE) triggers scrollTo({ top: 0, behavior: 'smooth' }) (SCROLL-08 prep, but tested here)
  </behavior>
  <action>
    Create `src/src/hooks/useChatScroll.ts` implementing the hook per decisions D-01 through D-08, D-17.

    **Hook API (per D-01):**
    ```typescript
    export interface UseChatScrollOptions {
      scrollContainerRef: RefObject<HTMLDivElement | null>;
      sessionId: string;
      isStreaming: boolean;
      messageCount: number;
    }

    export interface UseChatScrollReturn {
      sentinelRef: (node: HTMLDivElement | null) => void;
      showPill: boolean;
      unreadCount: number;
      scrollToBottom: () => void;
      contentWrapperRef: RefObject<HTMLDivElement | null>;
      isAtBottomRef: RefObject<boolean>;
    }
    ```

    **Internal state architecture (per D-02, D-03):**
    - `isAtBottomRef = useRef(true)` -- hot path, no re-renders
    - `isAutoScrollingRef = useRef(false)` -- anti-oscillation guard
    - `unreadCountRef = useRef(0)` -- hot path unread tracking
    - `[showPill, setShowPill] = useState(false)` -- cold path, debounced 200ms
    - `[displayUnreadCount, setDisplayUnreadCount] = useState(0)` -- cold path, debounced 200ms
    - `debouncedPillUpdate` ref with setTimeout(200ms) that syncs refs to state

    **IntersectionObserver sentinel (per D-02, D-04):**
    - Use callback ref pattern (setSentinelNode state) so observer re-creates when sentinel attaches
    - Observer root: scrollContainerRef.current
    - rootMargin: `'0px 0px -150px 0px'` -- single 150px threshold (Claude's Discretion simplification of D-05's dual 200px/100px hysteresis). RESEARCH.md analysis shows a single threshold combined with the 200ms debounce on schedulePillUpdate() provides equivalent anti-flicker behavior: the debounce window absorbs rapid sentinel enter/exit cycles during programmatic scroll, making the dual-threshold unnecessary. 150px is a middle ground between the show (200px) and hide (100px) values from D-05.
    - On entry.isIntersecting: set isAtBottomRef.current = true, reset unreadCountRef to 0, schedulePillUpdate()
    - On !entry.isIntersecting AND !isAutoScrollingRef.current: set isAtBottomRef.current = false, schedulePillUpdate()
    - Disconnect old observer in useEffect cleanup

    **ResizeObserver auto-follow (per D-06, D-07, D-08):**
    - Observe contentWrapperRef.current (first child of scroll container)
    - Callback: if !isAtBottomRef.current, return. If rafPending, return. Set rafPending=true, rAF(() => { rafPending=false; container.scrollTop = container.scrollHeight - container.clientHeight; })
    - NOT scrollIntoView -- direct scrollTop assignment per D-08
    - Re-setup on session change

    **User gesture detection (port from useScrollAnchor lines 116-152):**
    - wheel + touchmove listeners on container: set isAutoScrollingRef.current = false
    - scroll listener: detect upward scroll (currentScrollTop < lastScrollTop) while isAutoScrollingRef is true -> disengage
    - All listeners passive: true

    **Stream start re-engagement (port from useScrollAnchor lines 72-80):**
    - useEffect watching isStreaming: on false->true edge, set isAtBottomRef.current = true, isAutoScrollingRef.current = true, reset unread

    **Session switch reset:**
    - State-during-render pattern: if sessionId !== prevSessionId, reset isAtBottomRef=true, unreadCountRef=0, isAutoScrollingRef=false, schedulePillUpdate()

    **Unread tracking:**
    - State-during-render pattern: if messageCount > prevMessageCount && !isAtBottomRef.current, increment unreadCountRef by delta, schedulePillUpdate()

    **Scroll position save/restore:**
    - Save: throttled to sessionStorage (200ms trailing) inside the scroll listener (read scrollTop only in this handler, which runs at ~60fps but does NO React state work)
    - Restore: on session switch, read from sessionStorage, apply in rAF

    **statusTap (per D-17, SCROLL-08):**
    - useEffect: if IS_NATIVE (from platform.ts), addEventListener('statusTap', ...) on window
    - Handler: scrollContainerRef.current?.scrollTo({ top: 0, behavior: 'smooth' })

    **scrollToBottom callback:**
    - container.scrollTo({ top: container.scrollHeight, behavior: 'smooth' })
    - Reset unreadCountRef to 0, isAtBottomRef to true, schedulePillUpdate()

    Create `src/src/hooks/useChatScroll.test.ts` with tests covering all behaviors listed above. Port useful test patterns from useScrollAnchor.test.ts (IO mock, rAF mock, container mock). Use vi.useFakeTimers() for debounce testing.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/hooks/useChatScroll.test.ts -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/src/hooks/useChatScroll.ts` exists and exports `useChatScroll`, `UseChatScrollOptions`, `UseChatScrollReturn`
    - No call to `setAtBottom` or `setUnreadCount` anywhere in useChatScroll.ts (only `setShowPill` and `setDisplayUnreadCount` inside the debounced schedulePillUpdate)
    - `useChatScroll.ts` contains `new IntersectionObserver` with rootMargin containing `-150px`
    - `useChatScroll.ts` contains `new ResizeObserver` with rAF throttle guard (`rafPending` pattern)
    - `useChatScroll.ts` contains `container.scrollTop = container.scrollHeight - container.clientHeight` (NOT scrollIntoView)
    - `useChatScroll.ts` contains `isAtBottomRef` and `isAutoScrollingRef` as useRef declarations
    - `useChatScroll.ts` contains `addEventListener('statusTap'` for iOS status bar tap
    - `useChatScroll.ts` does NOT contain `scrollIntoView`
    - `useChatScroll.test.ts` has at least 10 test cases
    - All tests pass: `cd src && npx vitest run src/src/hooks/useChatScroll.test.ts -x` exits 0
  </acceptance_criteria>
  <done>
    useChatScroll hook created with IO sentinel for atBottom detection (ref-based, no setState in scroll path), ResizeObserver for auto-follow (rAF-throttled scrollTop assignment), debounced pill state, user gesture detection, stream re-engagement, session switch reset, unread tracking, scroll position save/restore, and statusTap support. All unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate useChatScroll into MessageList, delete useScrollAnchor, update ProofOfLife and E2E tests</name>
  <files>src/src/components/chat/view/MessageList.tsx, src/src/hooks/useScrollAnchor.ts, src/src/hooks/useScrollAnchor.test.ts, src/src/components/dev/ProofOfLife.tsx, src/src/components/dev/ProofOfLife.test.tsx, src/e2e/scroll-anchor.spec.ts</files>
  <read_first>
    src/src/hooks/useChatScroll.ts
    src/src/components/chat/view/MessageList.tsx
    src/src/hooks/useScrollAnchor.ts
    src/src/components/dev/ProofOfLife.tsx
    src/src/components/dev/ProofOfLife.test.tsx
    src/e2e/scroll-anchor.spec.ts
    src/src/components/chat/view/ScrollToBottomPill.tsx
  </read_first>
  <action>
    **MessageList.tsx refactor (per D-01, D-02, D-03, D-06):**

    Remove these lines/blocks from MessageList.tsx:
    - Lines 71-72: `const [atBottom, setAtBottom] = useState(true)` and `const [unreadCount, setUnreadCount] = useState(0)` -- replaced by hook
    - Lines 83-90: unread tracking state-during-render block -- moved into hook
    - Lines 92-98: session switch reset block -- moved into hook (hook handles via its own state-during-render)
    - Lines 160-166: auto-follow useEffect (`if (isStreaming && atBottom && el) { el.scrollTop = el.scrollHeight; }`) -- replaced by ResizeObserver in hook
    - Lines 169-182: handleScroll useCallback -- replaced entirely by hook internals
    - Lines 184-193: scroll listener useEffect -- replaced by hook
    - Lines 195-201: scrollToBottom useCallback -- replaced by hook return value

    Add to MessageList.tsx:
    - Import: `import { useChatScroll } from '@/hooks/useChatScroll';`
    - Remove import of `useCallback` if no longer needed (check other usages -- `handleFinalized` and `mergedRef` still use it, so KEEP useCallback import)
    - Remove `useState` import for `atBottom` and `unreadCount` -- but keep for `showActiveMessage`, `prevCount`, `prevSession` (check carefully)
    - Call hook:
      ```typescript
      const {
        sentinelRef,
        showPill,
        unreadCount,
        scrollToBottom,
        contentWrapperRef,
      } = useChatScroll({
        scrollContainerRef: scrollRef,
        sessionId,
        isStreaming,
        messageCount: messages.length,
      });
      ```
    - Keep: mergedRef callback (still needed for external scrollContainerRef), infinite scroll IO (lines 100-129), scroll position save on session switch (lines 131-141), scroll-to-saved-position useLayoutEffect (lines 144-158), ActiveMessage handling (lines 74-81)
    - WAIT: The scroll position save/restore is now in the hook. Remove the separate save/restore logic from MessageList (lines 131-158) since the hook handles it. Keep `saveTimeoutRef` removal.
    - Actually, KEEP the scroll-to-saved-position useLayoutEffect (lines 144-158) in MessageList since it only runs on initial session load and is a one-time operation that sets scrollTop based on sessionStorage. The hook's save logic runs during scrolling. These are complementary -- the hook saves on scroll, the useLayoutEffect restores on mount.
    - Remove `saveTimeoutRef` and the save-on-scroll logic (lines 177-181 inside handleScroll) since the hook now handles this.

    Update JSX:
    - Add `<div ref={contentWrapperRef} ...>` around the content wrapper div (the `mx-auto max-w-3xl py-4` div)
    - Add bottom sentinel: `<div ref={sentinelRef} style={{ height: 1 }} aria-hidden="true" />` after the content wrapper div, before ScrollToBottomPill
    - Update ScrollToBottomPill: `visible={showPill}` (was `visible={!atBottom}`), `onClick={scrollToBottom}`, `unreadCount={unreadCount}`

    Remove `SCROLL_STORAGE_PREFIX` constant if scroll position save is fully handled by the hook. If the hook uses the same prefix, it's fine to remove from MessageList.

    **Delete useScrollAnchor (per D-13):**
    - Delete `src/src/hooks/useScrollAnchor.ts`
    - Delete `src/src/hooks/useScrollAnchor.test.ts`

    **Update ProofOfLife.tsx (per D-14):**
    - Replace `import { useScrollAnchor } from '@/hooks/useScrollAnchor';` with `import { useChatScroll } from '@/hooks/useChatScroll';`
    - Find the line `const { sentinelRef, showPill, scrollToBottom } = useScrollAnchor(scrollContainerRef);` (line ~56)
    - Replace with:
      ```typescript
      const { sentinelRef, showPill, scrollToBottom } = useChatScroll({
        scrollContainerRef,
        sessionId: localId,
        isStreaming,
        messageCount: messages.length,
      });
      ```
    - Ensure ProofOfLife still renders the sentinel div with the sentinelRef callback

    **Update ProofOfLife.test.tsx:**
    - Update any mocks that reference `useScrollAnchor` to reference `useChatScroll`
    - Update mock return value to match `UseChatScrollReturn` interface (add `contentWrapperRef`, `isAtBottomRef`)

    **Update E2E test (scroll-anchor.spec.ts):**
    - Verify test selectors still match after refactor. Key selectors:
      - `[data-testid="message-list-scroll"]` -- still present (unchanged)
      - `[data-testid="scroll-to-bottom-pill"]` -- still present (unchanged)
    - If the sentinel div is now positioned differently (inside vs outside content wrapper), update any assertions about scroll behavior
    - The E2E tests test behavior (auto-scroll, pill visibility) not implementation, so they should pass without changes if selectors match
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/src/hooks/useScrollAnchor.ts` does NOT exist (deleted)
    - `src/src/hooks/useScrollAnchor.test.ts` does NOT exist (deleted)
    - `MessageList.tsx` imports `useChatScroll` (grep confirms `import.*useChatScroll`)
    - `MessageList.tsx` does NOT contain `setAtBottom` or `handleScroll` (grep confirms zero matches)
    - `MessageList.tsx` does NOT contain `el.scrollHeight - el.scrollTop - el.clientHeight` (no DOM reads in scroll path)
    - `MessageList.tsx` does NOT contain the auto-follow useEffect: `isStreaming && atBottom && el` pattern gone
    - `MessageList.tsx` contains `sentinelRef` ref attached to a div element
    - `MessageList.tsx` contains `contentWrapperRef` ref attached to the content wrapper div
    - `ProofOfLife.tsx` imports `useChatScroll` (not `useScrollAnchor`)
    - No file in `src/src/` imports `useScrollAnchor` (grep confirms zero matches)
    - Full test suite passes: `cd src && npx vitest run` exits 0
  </acceptance_criteria>
  <done>
    MessageList.tsx refactored to use useChatScroll hook -- ~120 lines of inline scroll logic removed, replaced by hook consumption. useScrollAnchor.ts and its tests deleted. ProofOfLife.tsx updated to use useChatScroll. All unit tests and E2E tests pass with no regressions.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/swd/loom/src && npx vitest run src/src/hooks/useChatScroll.test.ts -x` -- all new hook tests pass
2. `cd /home/swd/loom/src && npx vitest run` -- full test suite green (no regressions from refactor)
3. `grep -r "useScrollAnchor" src/src/` -- zero matches (dead code fully removed)
4. `grep -r "setAtBottom\|setUnreadCount" src/src/components/chat/view/MessageList.tsx` -- zero matches (no setState in scroll path)
5. `grep "scrollIntoView" src/src/hooks/useChatScroll.ts` -- zero matches (only scrollTop assignment used)
</verification>

<success_criteria>
- useChatScroll hook exists with IO sentinel, ResizeObserver auto-follow, ref-based state, debounced pill updates
- MessageList.tsx consumes the hook with zero inline scroll logic
- useScrollAnchor.ts fully deleted with no orphaned imports
- All 1485+ existing tests still pass
- No setState in any scroll event handler path
</success_criteria>

<output>
After completion, create `.planning/phases/64-scroll-performance/64-01-SUMMARY.md`
</output>
