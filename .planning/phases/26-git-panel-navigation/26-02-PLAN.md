---
phase: 26-git-panel-navigation
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/src/components/git/ChangesView.tsx
  - src/src/components/git/ChangedFileRow.tsx
  - src/src/components/git/CommitComposer.tsx
  - src/src/components/git/GitPanel.tsx
  - src/src/components/git/git-panel.css
  - src/src/components/git/ChangesView.test.tsx
  - src/src/components/git/CommitComposer.test.tsx
autonomous: true
requirements:
  - GIT-02
  - GIT-03
  - GIT-04
  - GIT-05
  - GIT-06
  - GIT-07
  - GIT-08
  - GIT-09
  - GIT-16
  - GIT-17
  - GIT-21

must_haves:
  truths:
    - "User sees changed files grouped by status (staged/modified/untracked/deleted)"
    - "User can check/uncheck files to select them for commit"
    - "User can click Select All / Deselect All to batch toggle"
    - "User can click a changed file to view its diff in the code editor"
    - "User can type a commit message and commit staged changes"
    - "Successful commit shows toast, clears message, refreshes changes list"
    - "Failed commit shows error toast with reason"
    - "User can generate an AI commit message from staged changes"
    - "User can discard changes per file with confirmation dialog"
  artifacts:
    - path: "src/src/components/git/ChangesView.tsx"
      provides: "File list grouped by status with checkboxes and batch toggle"
      exports: ["ChangesView"]
    - path: "src/src/components/git/ChangedFileRow.tsx"
      provides: "Individual file row with status icon, name, checkbox, discard button"
      exports: ["ChangedFileRow"]
    - path: "src/src/components/git/CommitComposer.tsx"
      provides: "Commit message textarea + commit button + generate message button"
      exports: ["CommitComposer"]
  key_links:
    - from: "src/src/components/git/ChangedFileRow.tsx"
      to: "useOpenInEditor / useFileDiff"
      via: "Click handler opens file in diff mode"
      pattern: "setActiveTab.*files"
    - from: "src/src/components/git/CommitComposer.tsx"
      to: "/api/git/commit"
      via: "useGitOperations.commit"
      pattern: "commit\\(message.*files\\)"
    - from: "src/src/components/git/CommitComposer.tsx"
      to: "/api/git/generate-commit-message"
      via: "useGitOperations.generateCommitMessage"
      pattern: "generateCommitMessage"
---

<objective>
Build the Changes view: file list with staging checkboxes, commit composer with AI message generation, and discard-per-file with confirmation.

Purpose: This is the primary git interaction surface -- users stage files and commit from here. It's the most complex view in the git panel.

Output: ChangesView, ChangedFileRow, CommitComposer components with full staging/commit/discard flow.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-git-panel-navigation/26-RESEARCH.md
@.planning/phases/26-git-panel-navigation/26-01-SUMMARY.md

<interfaces>
<!-- Types and hooks from Plan 01 -->

From src/src/types/git.ts (created in Plan 01):
```typescript
export type GitFileStatus = 'modified' | 'added' | 'deleted' | 'untracked';
export interface GitFileChange { path: string; status: GitFileStatus }
export interface GitStatusData { branch: string; hasCommits: boolean; files: GitFileChange[] }
```

From src/src/hooks/useGitStatus.ts (created in Plan 01):
```typescript
export function useGitStatus(projectName: string): {
  data: GitStatusData | null;
  loading: boolean;
  error: string | null;
  refetch: () => void;
};
```

From src/src/hooks/useGitOperations.ts (created in Plan 01):
```typescript
export function useGitOperations(projectName: string): {
  commit: (message: string, files: string[]) => Promise<void>;
  discard: (file: string) => Promise<void>;
  deleteUntracked: (file: string) => Promise<void>;
  generateCommitMessage: () => Promise<string>;
  // ... other ops
};
```

From src/src/hooks/useOpenInEditor.ts (existing):
```typescript
export function useOpenInEditor(): (filePath: string) => void;
```

From src/src/stores/ui.ts (existing):
```typescript
// setActiveTab action available via useUIStore
```

From shadcn AlertDialog (existing):
```typescript
// Import from @/components/ui/alert-dialog
// AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChangesView and ChangedFileRow with staging checkboxes</name>
  <files>
    src/src/components/git/ChangesView.tsx,
    src/src/components/git/ChangedFileRow.tsx,
    src/src/components/git/git-panel.css,
    src/src/components/git/ChangesView.test.tsx
  </files>
  <behavior>
    - ChangesView groups files by status (modified, added, deleted, untracked) with section headers
    - Each file row shows: status icon (M/A/D/?), filename, relative path, checkbox
    - Checking a checkbox adds file to client-side staged set
    - Unchecking removes from staged set
    - Select All checks all files; Deselect All unchecks all
    - Clicking file name/path area (not checkbox) opens diff in editor via useOpenInEditor + setActiveTab('files')
    - Discard button on each file row opens confirmation AlertDialog; confirming calls discard/deleteUntracked
    - Empty state shown when no changed files
  </behavior>
  <action>
    1. Create `src/src/components/git/ChangedFileRow.tsx`:
       - Props: `{ file: GitFileChange; isStaged: boolean; onToggleStage: (path: string) => void; onClickFile: (path: string) => void; onDiscard: (file: GitFileChange) => void }`
       - Renders: checkbox (input type=checkbox), status icon letter (M/A/D/? with color: M=yellow, A=green, D=red, ?=gray using design tokens), filename (basename from path), directory (dirname, muted color), discard/trash icon button
       - Clicking the row (except checkbox/discard) calls onClickFile
       - Checkbox calls onToggleStage
       - Discard button calls onDiscard (parent handles confirmation)
       - Use `git-file-row` CSS class for hover state
       - Use lucide-react icons: `Trash2` for discard

    2. Create `src/src/components/git/ChangesView.tsx`:
       - Props: `{ files: GitFileChange[]; refetchStatus: () => void }`
       - Local state: `stagedFiles: Set<string>` (useState) -- client-side staging model per research
       - Groups files by status using a simple reduce/filter
       - Renders section headers ("Modified", "Added", "Deleted", "Untracked") only for non-empty groups
       - Each section: list of `<ChangedFileRow>` components
       - "Select All" / "Deselect All" buttons at top
       - Discard handler: shows shadcn AlertDialog with "This cannot be undone" message (GIT-21)
         - For untracked files: calls `deleteUntracked`
         - For other files: calls `discard`
         - After discard: calls `refetchStatus()`
       - File click handler: uses `useOpenInEditor` to open in diff editor + `useUIStore` setActiveTab('files')
       - Below file list: renders `<CommitComposer>` (placeholder import -- Task 2 creates it)
       - Empty state: "No changes" message when files array is empty
       - Pass stagedFiles set and count to CommitComposer

    3. Update `src/src/components/git/git-panel.css`:
       - Add `.git-file-row` hover background (surface-hover token)
       - Add `.git-status-icon` with color variants: `[data-status="modified"]`, `[data-status="added"]`, `[data-status="deleted"]`, `[data-status="untracked"]`
       - Add `.git-section-header` for group headers

    4. Wire ChangesView into GitPanel.tsx:
       - When activeView === 'changes' and data is loaded, render `<ChangesView files={data.files} refetchStatus={refetch} />`
       - Remove the placeholder div

    5. Create `src/src/components/git/ChangesView.test.tsx`:
       - Mock useProjectContext, useGitOperations, useOpenInEditor, useUIStore
       - Test: renders file rows grouped by status
       - Test: checking checkbox adds to staged set (verify via aria-checked)
       - Test: Select All checks all files
       - Test: Deselect All unchecks all files
       - Test: clicking file row calls openInEditor
       - Test: discard button shows confirmation dialog
       - Test: empty state renders "No changes"
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/git/ChangesView.test.tsx --reporter=verbose</automated>
  </verify>
  <done>ChangesView renders files grouped by status with working staging checkboxes, Select All/Deselect All, click-to-diff, and discard with confirmation. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CommitComposer with commit flow and AI message generation</name>
  <files>
    src/src/components/git/CommitComposer.tsx,
    src/src/components/git/CommitComposer.test.tsx
  </files>
  <behavior>
    - Textarea auto-resizes as user types commit message
    - Commit button disabled when no staged files or message is empty
    - Clicking Commit calls useGitOperations.commit with message and staged files
    - Successful commit: toast.success, clear message, clear staged files, refetch status
    - Failed commit: toast.error with reason
    - Generate Message button calls useGitOperations.generateCommitMessage and fills textarea
    - Generate Message button shows loading state during generation
    - Generate Message button disabled when no files selected
  </behavior>
  <action>
    1. Create `src/src/components/git/CommitComposer.tsx`:
       - Props: `{ stagedFiles: Set<string>; onCommitSuccess: () => void; projectName: string }`
       - Local state: `commitMessage: string`, `isCommitting: boolean`, `isGenerating: boolean`
       - Uses `useGitOperations(projectName)` for commit and generateCommitMessage
       - Textarea with:
         - Placeholder "Commit message..."
         - Auto-resize: set rows dynamically based on content line count (min 2, max 8)
         - Value bound to commitMessage state
       - "Generate Message" button (Sparkles icon from lucide):
         - Disabled when stagedFiles.size === 0 or isGenerating
         - onClick: set isGenerating true, call generateCommitMessage(), set result to commitMessage, set isGenerating false
         - Shows spinner/loading indicator during generation
         - Error: toast.error("Failed to generate commit message")
       - "Commit" button:
         - Disabled when stagedFiles.size === 0 or commitMessage.trim() === '' or isCommitting
         - onClick: set isCommitting true, call commit(commitMessage, [...stagedFiles])
         - Success: toast.success("Changes committed"), setCommitMessage(''), call onCommitSuccess()
         - Error: toast.error with error message (GIT-09)
         - Finally: set isCommitting false
       - Use sonner toast (already wired)
       - Commit button shows loading spinner when isCommitting

    2. Wire CommitComposer into ChangesView:
       - Render at bottom of ChangesView, below file list
       - Pass stagedFiles, projectName (from useProjectContext), and onCommitSuccess handler
       - onCommitSuccess: clear stagedFiles set, call refetchStatus

    3. Create `src/src/components/git/CommitComposer.test.tsx`:
       - Mock useGitOperations
       - Test: commit button disabled when no staged files
       - Test: commit button disabled when message empty
       - Test: commit button enabled when both message and staged files present
       - Test: successful commit calls toast.success and clears message
       - Test: failed commit calls toast.error
       - Test: generate message fills textarea with returned message
       - Test: generate button disabled when no staged files
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/git/CommitComposer.test.tsx --reporter=verbose</automated>
  </verify>
  <done>CommitComposer renders with auto-resize textarea, commit with toast feedback, and AI message generation. Full commit flow works end-to-end. Tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /home/swd/loom/src && npx vitest run --reporter=verbose
```
All existing tests plus new ChangesView and CommitComposer tests pass.
</verification>

<success_criteria>
- Changed files displayed grouped by status (modified/added/deleted/untracked)
- Checkboxes toggle file staging for commit
- Select All / Deselect All batch operations work
- Clicking a file opens its diff in the code editor (Files tab)
- Commit message textarea auto-resizes
- Commit button correctly disabled/enabled based on staged files and message
- Successful commit: toast, clear, refresh
- Failed commit: error toast
- AI message generation fills textarea
- Discard per-file with confirmation dialog
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-git-panel-navigation/26-02-SUMMARY.md`
</output>
