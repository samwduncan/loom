---
phase: 26-git-panel-navigation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/components/sidebar/SessionItem.tsx
  - src/src/components/sidebar/SessionList.tsx
  - src/src/components/sidebar/sidebar.css
  - src/src/components/sidebar/SessionItem.test.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "User can double-click a session title to enter inline edit mode"
    - "User can confirm rename with Enter or cancel with Escape"
    - "Renamed session title persists in sidebar and survives tab switches"
    - "User sees confirmation dialog before deleting a session"
    - "Deleted session is removed from sidebar immediately"
    - "If deleted session was active, sidebar switches to most recent remaining session"
  artifacts:
    - path: "src/src/components/sidebar/SessionItem.tsx"
      provides: "Session row with inline rename on double-click"
      exports: ["SessionItem"]
    - path: "src/src/components/sidebar/SessionList.tsx"
      provides: "Session list with delete confirmation dialog"
      exports: ["SessionList"]
  key_links:
    - from: "src/src/components/sidebar/SessionItem.tsx"
      to: "timeline store updateSessionTitle"
      via: "onRename callback"
      pattern: "updateSessionTitle"
    - from: "src/src/components/sidebar/SessionList.tsx"
      to: "timeline store removeSession"
      via: "handleDelete with AlertDialog confirmation"
      pattern: "removeSession"
    - from: "src/src/components/sidebar/SessionList.tsx"
      to: "react-router navigate"
      via: "Navigate to most recent session after delete"
      pattern: "navigate.*chat"
---

<objective>
Add session rename (double-click inline edit) and delete confirmation dialog to the sidebar session list.

Purpose: Completes the NAV requirements -- users can organize their sessions without leaving Loom. These are the last features needed for the M3 milestone.

Output: Updated SessionItem with inline rename, updated SessionList with delete confirmation.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-git-panel-navigation/26-RESEARCH.md

<interfaces>
<!-- Existing interfaces to modify -->

From src/src/components/sidebar/SessionItem.tsx:
```typescript
interface SessionItemProps {
  id: string;
  title: string;
  updatedAt: string;
  providerId: ProviderId;
  isActive: boolean;
  hasDraft?: boolean;
  onClick: () => void;
  onContextMenu: (e: MouseEvent<HTMLDivElement>) => void;
}
```

From src/src/stores/timeline.ts:
```typescript
// Already exists:
updateSessionTitle: (sessionId: string, title: string) => void;
removeSession: (sessionId: string) => void;
```

From src/src/components/sidebar/SessionList.tsx:
```typescript
// handleRename is currently a stub that just closes context menu
// handleDelete calls DELETE API then removeSession (no confirmation)
```

From shadcn AlertDialog:
```typescript
// Import from @/components/ui/alert-dialog
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session rename via double-click inline edit</name>
  <files>
    src/src/components/sidebar/SessionItem.tsx,
    src/src/components/sidebar/SessionList.tsx,
    src/src/components/sidebar/sidebar.css,
    src/src/components/sidebar/SessionItem.test.tsx
  </files>
  <behavior>
    - Double-clicking session title enters inline edit mode (NAV-01)
    - Input is auto-focused and pre-filled with current title
    - Enter confirms rename if value changed and non-empty
    - Escape cancels edit, restoring original title
    - Clicking outside the input also confirms (blur handler)
    - Renamed title updates immediately in sidebar via updateSessionTitle
    - Single click still navigates to session (not edit)
  </behavior>
  <action>
    1. Update `src/src/components/sidebar/SessionItem.tsx`:
       - Add new props: `onRename: (id: string, newTitle: string) => void`
       - Add local state: `isEditing: boolean` (useState, default false), `editValue: string` (useState)
       - Add `onDoubleClick` handler to the title div (line 1 of the session row):
         - Sets isEditing to true, editValue to current title
         - e.stopPropagation() to prevent parent onClick from firing
       - When isEditing is true:
         - Replace the title text with an `<input>` element
         - Input: type="text", value=editValue, autoFocus, className for inline styling
         - onChange: update editValue
         - onKeyDown:
           - Enter: if editValue.trim() !== '' && editValue !== title, call onRename(id, editValue.trim()). Set isEditing to false.
           - Escape: set isEditing to false (discard changes)
         - onBlur: same logic as Enter (confirm on focus loss)
         - Use `useCallback` for handlers
       - Input styling: transparent background, same font size as title, border-bottom accent, full width
       - IMPORTANT: Use the "null-check ref init" pattern if using a ref for focus (Phase 25 precedent), or just use autoFocus attribute on the input (simpler, avoids refs-during-render lint issue)

    2. Update `src/src/components/sidebar/SessionList.tsx`:
       - Wire the handleRename callback to call `updateSessionTitle(sessionId, newTitle)`:
         - Currently handleRename is a stub that just closes the context menu
         - Now: close context menu, then set a local state flag `renamingSessionId: string | null`
         - Pass `isEditing={renamingSessionId === session.id}` to SessionItem
         - OR simpler approach: let SessionItem manage its own isEditing state and just pass onRename that calls updateSessionTitle
       - Actually, simplest approach: SessionItem internally handles double-click → isEditing. onRename prop calls updateSessionTitle. The context menu "Rename" action also needs to trigger editing.
       - Add a new prop to SessionItem: `startEditing?: boolean` or use a callback ref pattern
       - Better: Add `editingSessionId: string | null` state to SessionList. Context menu "Rename" sets it. SessionItem receives `isEditing={editingSessionId === session.id}` and `onStartEdit` / `onStopEdit` callbacks. Double-click in SessionItem calls onStartEdit(id).
       - handleRename in SessionList: sets `editingSessionId = contextMenu.sessionId`, closes context menu
       - SessionItem.onRename: calls updateSessionTitle, then onStopEdit

    3. Update sidebar.css:
       - `.session-rename-input` -- transparent bg, same font, underline border, full width

    4. Create/extend `src/src/components/sidebar/SessionItem.test.tsx`:
       - Test: double-click on title enters edit mode (input appears)
       - Test: typing in input and pressing Enter calls onRename with new value
       - Test: pressing Escape cancels edit (input disappears, original title restored)
       - Test: blur confirms edit
       - Test: empty input on Enter does NOT call onRename
       - Test: same value as original does NOT call onRename
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/sidebar/SessionItem.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Double-click inline edit works on SessionItem. Context menu "Rename" also triggers edit mode. Enter confirms, Escape cancels, blur confirms. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session delete with confirmation dialog and navigation</name>
  <files>
    src/src/components/sidebar/SessionList.tsx,
    src/src/components/sidebar/SessionItem.test.tsx
  </files>
  <behavior>
    - Right-click > Delete shows confirmation dialog before deletion (NAV-02)
    - Confirmation dialog: "Delete session? This cannot be undone." with Cancel and Delete buttons
    - Confirming deletes via API, removes from store, closes dialog
    - If deleted session was the active one, navigates to most recent remaining session (NAV-03)
    - If no sessions remain after delete, navigates to /chat (empty state)
    - Cancel closes dialog without deleting
  </behavior>
  <action>
    1. Update `src/src/components/sidebar/SessionList.tsx`:
       - Add local state: `deleteSessionId: string | null` (for confirmation dialog)
       - Modify handleDelete:
         - Instead of immediately calling the API, set `deleteSessionId = contextMenu.sessionId` and close context menu
         - This triggers the AlertDialog to render
       - Add shadcn AlertDialog (sibling pattern per Phase 21 decision):
         - `open={deleteSessionId !== null}`
         - `onOpenChange={(open) => { if (!open) setDeleteSessionId(null) }}`
         - Title: "Delete session?"
         - Description: "This will permanently delete this session and all its messages. This cannot be undone."
         - Cancel button: sets deleteSessionId to null
         - Delete button (destructive variant): calls `confirmDelete()`
       - `confirmDelete` async function:
         - Calls DELETE API: `apiFetch('/api/projects/${projectName}/sessions/${deleteSessionId}', { method: 'DELETE' })`
         - Calls `removeSession(deleteSessionId)`
         - If the deleted session was active (`deleteSessionId === activeSessionId`):
           - Find the most recent remaining session (sessions sorted by updatedAt)
           - If found: `navigate('/chat/${mostRecent.id}')`
           - If none remain: `navigate('/chat')`
         - Sets deleteSessionId to null
         - Error: toast.error("Failed to delete session")
       - Import AlertDialog components from @/components/ui/alert-dialog
       - Import toast from sonner for error feedback

    2. Extend `src/src/components/sidebar/SessionItem.test.tsx` (or create SessionList.test.tsx if cleaner):
       - Test: right-click > Delete shows confirmation dialog
       - Test: clicking Cancel closes dialog without deletion
       - Test: clicking Delete calls API and removes session from list
       - Test: deleting active session navigates to most recent remaining
       - Test: deleting last session navigates to /chat
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/sidebar/SessionItem.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Delete session shows confirmation dialog. Confirming deletes and navigates correctly. Canceling preserves session. Tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /home/swd/loom/src && npx vitest run --reporter=verbose
```
All existing tests plus new/extended SessionItem tests pass.
</verification>

<success_criteria>
- Double-click on session title enters inline edit mode
- Enter confirms rename, Escape cancels
- Context menu "Rename" also triggers inline edit
- Delete shows confirmation dialog before deletion
- Deleted session removed from list immediately
- If active session deleted, navigates to most recent remaining
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-git-panel-navigation/26-04-SUMMARY.md`
</output>
