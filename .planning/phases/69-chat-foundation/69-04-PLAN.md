---
phase: 69-chat-foundation
plan: 04
type: execute
wave: 2
depends_on: [69-01, 69-02]
files_modified:
  - mobile/components/chat/MessageList.tsx
  - mobile/components/chat/MessageBubble.tsx
  - mobile/components/chat/ProviderAvatar.tsx
  - mobile/components/chat/StreamingIndicator.tsx
  - mobile/components/chat/ScrollToBottomPill.tsx
  - mobile/components/composer/Composer.tsx
  - mobile/components/composer/ComposerInput.tsx
  - mobile/components/composer/SendButton.tsx
  - mobile/components/composer/ComposerStatus.tsx
  - mobile/components/empty/EmptyChat.tsx
  - mobile/hooks/useMessageList.ts
  - mobile/hooks/useScrollToBottom.ts
  - mobile/app/(stack)/chat/[id].tsx
autonomous: true
requirements: [CHAT-04]
must_haves:
  truths:
    - "User can type a message in the composer and tap send"
    - "Sent message appears as a user bubble (surface-raised, rounded-2xl)"
    - "Streaming markdown response renders in real-time from Claude"
    - "Background warms subtly during streaming (dynamic color)"
    - "Streaming indicator (2px accent line) pulses during active streaming"
    - "Scroll-to-bottom pill appears when scrolled up, bounces with Standard spring"
    - "Composer syncs perfectly with iOS keyboard via KeyboardStickyView"
    - "Send button toggles accent (send) to destructive (stop) during streaming"
  artifacts:
    - path: "mobile/components/chat/MessageList.tsx"
      provides: "FlashList of messages with scroll anchoring"
      exports: ["MessageList"]
    - path: "mobile/components/chat/MessageBubble.tsx"
      provides: "User bubble (raised) and assistant free-flowing layout"
      exports: ["MessageBubble"]
    - path: "mobile/components/composer/Composer.tsx"
      provides: "Glass backdrop composer with input + send + status"
      exports: ["Composer"]
    - path: "mobile/app/(stack)/chat/[id].tsx"
      provides: "Chat screen wiring MessageList + Composer + dynamic color"
  key_links:
    - from: "mobile/components/chat/MessageList.tsx"
      to: "mobile/hooks/useMessageList.ts"
      via: "messages array from hook"
      pattern: "useMessageList"
    - from: "mobile/components/composer/Composer.tsx"
      to: "mobile/lib/websocket-init.ts"
      via: "getWsClient().send() for message sending"
      pattern: "getWsClient.*send"
    - from: "mobile/app/(stack)/chat/[id].tsx"
      to: "mobile/hooks/useDynamicColor.ts"
      via: "backgroundStyle applied to chat container"
      pattern: "useDynamicColor"
    - from: "mobile/hooks/useMessageList.ts"
      to: "mobile/stores/index.ts"
      via: "useStreamStore for live streaming content + useTimelineStore for session messages"
      pattern: "useStreamStore|useTimelineStore"
---

<objective>
Build the chat screen with message list, streaming markdown rendering, Soul-doc composer, dynamic color, and all message layout per the visual contract.

Purpose: This is the core messaging loop (CHAT-04) -- the reason the app exists. User sends a message, sees it as a bubble, watches Claude stream a markdown response with warmth-shifted background, and can scroll through the conversation.

Output: Complete chat screen with MessageList, MessageBubble, Composer, streaming integration, and dynamic color.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/NATIVE-APP-SOUL.md
@.planning/BACKEND_API_CONTRACT.md
@.planning/phases/69-chat-foundation/69-CONTEXT.md
@.planning/phases/69-chat-foundation/69-RESEARCH.md
@.planning/phases/69-chat-foundation/69-01-SUMMARY.md
@.planning/phases/69-chat-foundation/69-02-SUMMARY.md

<interfaces>
From mobile/lib/websocket-init.ts (Plan 01):
```typescript
export function getWsClient(): WebSocketClient | null;
export function initializeWebSocket(): Promise<void>;
```

From mobile/lib/springs.ts (Plan 01):
```typescript
export const SPRING: {
  micro: WithSpringConfig;      // 18/220/0.8  (~150ms) -- button press
  standard: WithSpringConfig;   // 20/150/1.0  (~250ms) -- card press, message entrance
  navigation: WithSpringConfig; // 22/130/1.0  (~300ms) -- screen push
};
```

From mobile/hooks/useDynamicColor.ts (Plan 01):
```typescript
export function useDynamicColor(): {
  backgroundStyle: AnimatedStyleProp;  // interpolated bg color
  accentPulse: SharedValue<number>;    // 0.6-1.0 during streaming
  enterStreaming: () => void;
  exitStreaming: () => void;
  enterError: () => void;
};
```

From mobile/components/chat/MarkdownRenderer.tsx (Plan 01):
```typescript
export function MarkdownRenderer({ content, isStreaming }: { content: string; isStreaming: boolean }): JSX.Element;
```

From mobile/stores/index.ts:
```typescript
export const useStreamStore;     // content, isStreaming, thinkingBlocks, toolCalls
export const useTimelineStore;   // sessions, activeSessionId, messages for session
export const useConnectionStore; // status, isConnected
```

From mobile/components/primitives/:
```typescript
export function GlassSurface({ children, intensity, className }: GlassSurfaceProps): JSX.Element;
export function Button({ title, variant, disabled, className }: ButtonProps): JSX.Element;
export function LoomText({ variant, children, className }: LoomTextProps): JSX.Element;
```

From shared/types/websocket.ts:
```typescript
interface ClientMessage {
  type: 'claude-command';
  command: string;
  options: {
    projectPath?: string;
    sessionId?: string;
    model?: string;
  };
}
```

From .planning/BACKEND_API_CONTRACT.md:
```typescript
// GET /api/projects/:projectName/sessions/:sessionId/messages
// Response: { messages: Message[] }
// Message has: role ('user'|'assistant'), content (string), timestamp, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build message components (MessageBubble, ProviderAvatar, StreamingIndicator, ScrollToBottomPill, EmptyChat) and message list hook</name>
  <read_first>
    - mobile/components/chat/MarkdownRenderer.tsx (from Plan 01)
    - mobile/lib/springs.ts (from Plan 01)
    - mobile/lib/colors.ts (from Plan 01)
    - mobile/hooks/useDynamicColor.ts (from Plan 01)
    - mobile/stores/index.ts
    - mobile/components/primitives/GlassSurface.tsx
    - mobile/components/primitives/TextHierarchy.tsx
    - shared/types/websocket.ts (for message types)
    - shared/stores/stream.ts (first 60 lines for stream store interface)
    - shared/stores/timeline.ts (first 60 lines for timeline store interface)
    - .planning/NATIVE-APP-SOUL.md (Chat Thread section, Composer section, Elevation Examples: "Sending a Message")
    - .planning/BACKEND_API_CONTRACT.md (Messages endpoint)
    - .planning/phases/69-chat-foundation/69-CONTEXT.md (D-18 through D-26)
  </read_first>
  <files>
    mobile/components/chat/MessageBubble.tsx
    mobile/components/chat/ProviderAvatar.tsx
    mobile/components/chat/StreamingIndicator.tsx
    mobile/components/chat/ScrollToBottomPill.tsx
    mobile/components/empty/EmptyChat.tsx
    mobile/hooks/useMessageList.ts
    mobile/hooks/useScrollToBottom.ts
  </files>
  <action>
**Step 1: Create mobile/hooks/useMessageList.ts** -- manages message data for a session:
```typescript
interface UseMessageListReturn {
  messages: DisplayMessage[];
  isLoading: boolean;
  isStreaming: boolean;
  streamingContent: string;
  fetchMessages: (projectName: string, sessionId: string) => Promise<void>;
}

interface DisplayMessage {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  timestamp: string;
  showTimestamp: boolean;  // true if 5+ min gap from previous message
  isStreaming: boolean;    // true for current streaming message
}
```
- Fetches messages from API: GET /api/projects/:name/sessions/:id/messages
- Subscribes to useStreamStore for live streaming content (content tokens accumulating)
- During streaming: appends a virtual "assistant" message with isStreaming=true and content from stream store
- Timestamp logic per D-20: show timestamp when 5+ minute gap between consecutive messages
- Returns isStreaming from stream store

**Step 2: Create mobile/hooks/useScrollToBottom.ts** -- scroll position tracking:
- Accepts FlashList ref
- Tracks whether user is "near bottom" (within 100px of end)
- `isAtBottom: boolean` -- true when within threshold
- `showPill: boolean` -- true when NOT at bottom AND messages exist
- `scrollToBottom()` -- animates scroll to end
- Returns { isAtBottom, showPill, scrollToBottom, onScroll handler }

**Step 3: Create mobile/components/chat/MessageBubble.tsx** -- per D-18, D-19:
- Props: `message: DisplayMessage`, `isLastMessage: boolean`
- **User messages (per D-18):**
  - Container: surface-raised bg (rgb(54, 50, 48)), rounded-2xl, p-4
  - Layout: right-aligned in row (alignSelf: 'flex-end', maxWidth: '85%')
  - Text: left-aligned within bubble, 15px regular, text-primary
  - Entrance animation per D-21: Standard spring (opacity 0->1, translateY 20->0)
  - Using Animated.View with entering={FadeInDown.springify().damping(20).stiffness(150)}
- **Assistant messages (per D-18):**
  - No container -- text directly on surface-base
  - Layout: full width, px-4 (16px horizontal padding)
  - Content rendered via MarkdownRenderer component
  - First token entrance per D-21: 150ms opacity fade-in (no spring, withTiming)
  - Avatar at top-left (ProviderAvatar component)
- **Turn spacing per D-19:**
  - 24px between different roles (gap-6 / marginTop: 24)
  - 8px between consecutive same-role messages (gap-2 / marginTop: 8)
  - Logic: check previous message role, apply appropriate margin

**Step 4: Create mobile/components/chat/ProviderAvatar.tsx** -- per D-19:
- 24px circular container
- Claude: terracotta accent color circle with "C" text (or Lucide Bot icon)
- Positioned at top-left of assistant messages
- Only renders for assistant messages, first message in a same-role group

**Step 5: Create mobile/components/chat/StreamingIndicator.tsx** -- per D-26:
- Thin 2px line at bottom of message area
- Accent color (rgb(196, 108, 88))
- Pulsing opacity animation: 0.3 to 0.8 over 1.5s cycle
- Implementation: Animated.View with height 2, width '100%', bg accent
  - useAnimatedStyle with withRepeat(withTiming(0.8, { duration: 1500 }), -1, true) for opacity
- Disappears when streaming completes (conditional render on isStreaming)

**Step 6: Create mobile/components/chat/ScrollToBottomPill.tsx** -- per D-22:
- Glass surface (GlassSurface component), small pill shape
- Accent text: down arrow icon + "Scroll to bottom" or just down arrow
- Positioned absolutely at bottom-center of message area, above composer
- Entrance: bounces once with Standard spring (translateY 10->0, opacity 0->1)
- onPress: calls scrollToBottom, pill fades out (100ms opacity)
- Only visible when showPill is true (from useScrollToBottom)

**Step 7: Create mobile/components/empty/EmptyChat.tsx** -- per D-23:
- Centered in chat area
- Provider avatar (24px) at top
- Model name below: "Claude" (15px Body, text-secondary)
- "How can I help?" (15px Body, text-primary)
- If session has project context: project name below in Caption (12px, text-muted)
- Standard spring entrance (opacity 0->1, translateY 20->0)
  </action>
  <verify>
    <automated>cd /home/swd/loom && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
    Verify: All 7 files exist with correct exports.
  </verify>
  <acceptance_criteria>
    - mobile/hooks/useMessageList.ts exports useMessageList with messages, isStreaming, streamingContent, fetchMessages
    - mobile/hooks/useMessageList.ts subscribes to useStreamStore for live content
    - mobile/hooks/useMessageList.ts implements 5-minute timestamp gap logic
    - mobile/hooks/useScrollToBottom.ts exports useScrollToBottom with isAtBottom, showPill, scrollToBottom
    - mobile/components/chat/MessageBubble.tsx renders user messages with surface-raised (rgb(54, 50, 48)) background and rounded-2xl
    - mobile/components/chat/MessageBubble.tsx renders assistant messages without container, using MarkdownRenderer
    - mobile/components/chat/MessageBubble.tsx applies 24px spacing between different roles and 8px for same role
    - mobile/components/chat/MessageBubble.tsx contains Standard spring entrance animation for user messages
    - mobile/components/chat/ProviderAvatar.tsx renders 24px circular avatar
    - mobile/components/chat/StreamingIndicator.tsx has height 2 with accent color and pulsing opacity 0.3-0.8
    - mobile/components/chat/ScrollToBottomPill.tsx imports GlassSurface
    - mobile/components/chat/ScrollToBottomPill.tsx contains Standard spring entrance with bounce
    - mobile/components/empty/EmptyChat.tsx contains "How can I help?" text
    - `npx vitest run` passes
  </acceptance_criteria>
  <done>All message display components built per Soul doc. Message bubbles, avatar, streaming indicator, scroll pill, and empty state all implement correct typography, spacing, and spring animations.</done>
</task>

<task type="auto">
  <name>Task 2: Build composer (glass backdrop, expanding input, send/stop, status bar) and wire chat screen</name>
  <read_first>
    - mobile/components/chat/MessageBubble.tsx (just created)
    - mobile/components/chat/StreamingIndicator.tsx (just created)
    - mobile/components/chat/ScrollToBottomPill.tsx (just created)
    - mobile/components/empty/EmptyChat.tsx (just created)
    - mobile/hooks/useMessageList.ts (just created)
    - mobile/hooks/useScrollToBottom.ts (just created)
    - mobile/hooks/useDynamicColor.ts (from Plan 01)
    - mobile/lib/websocket-init.ts (from Plan 01)
    - mobile/lib/springs.ts (from Plan 01)
    - mobile/lib/colors.ts (from Plan 01)
    - mobile/components/primitives/GlassSurface.tsx
    - mobile/app/(stack)/chat/[id].tsx (current placeholder)
    - mobile/app/(stack)/_layout.tsx
    - .planning/NATIVE-APP-SOUL.md (Composer section, Chat Thread section, Elevation Examples: "Sending a Message")
    - .planning/phases/69-chat-foundation/69-CONTEXT.md (D-14 through D-17, D-22, D-25, D-26)
    - .planning/phases/69-chat-foundation/69-RESEARCH.md (Pattern 5 for KeyboardStickyView)
  </read_first>
  <files>
    mobile/components/composer/Composer.tsx
    mobile/components/composer/ComposerInput.tsx
    mobile/components/composer/SendButton.tsx
    mobile/components/composer/ComposerStatus.tsx
    mobile/components/chat/MessageList.tsx
    mobile/app/(stack)/chat/[id].tsx
  </files>
  <action>
**Step 1: Create mobile/components/composer/ComposerInput.tsx** -- per D-14:
- Single-line expanding TextInput (max 6 lines before internal scroll)
- 15px body text, text-primary color
- Placeholder: "Message" in text-muted
- Internal container: rounded-2xl, border border-subtle, px-4 py-3
- Height animation per Soul doc: Standard spring on height change (never instant). Track content height via onContentSizeChange, animate with Reanimated withSpring(newHeight, SPRING.standard)
- Focus: border shifts border-subtle to border-interactive (200ms color transition via withTiming)
- Left: Attachment button (+) -- 36px circular, surface-raised bg, Lucide Plus icon 20px, text-muted. Per D-15: tapping shows "Coming soon" toast or empty action sheet. Non-functional.
- onChangeText: propagates to parent
- onSubmitEditing: calls onSend if text is non-empty

**Step 2: Create mobile/components/composer/SendButton.tsx** -- per D-14, Soul doc Composer section:
- 36px diameter circular button
- **Empty state:** surface-raised bg, up-arrow icon (Lucide ArrowUp, 20px, text-muted)
- **Has text state:** accent bg with Glow shadow, up-arrow icon in accent-fg. Transition: 200ms withTiming from surface-raised to accent.
- **Streaming state:** destructive bg, square icon (Lucide Square, 16px, white). Per Soul doc: send button transforms to stop button. 200ms color transition.
- Press animation: Micro spring scale (0.85 to 1.0) per Soul doc "Sending a Message" elevation example
- Impact Light haptic on press (import from expo-haptics)
- onPress: if streaming, sends abort/stop command. If has text, sends message.

**Step 3: Create mobile/components/composer/ComposerStatus.tsx** -- per D-14:
- Below input, ~24px tall
- Caption text (12px, text-muted)
- Content: token count + model name + connection dot
- Token count: from stream store (or "0 tokens" when idle)
- Model name: "Claude" (or from stream store model name)
- Connection dot: 6px circle, success (connected) or destructive (disconnected), with 200ms color transition
- Layout: flex-row, justify-between, px-4

**Step 4: Create mobile/components/composer/Composer.tsx** -- the main composer assembly:
- Background: Glass surface (GlassSurface component) per D-14. Per D-17: try glass first. If glass causes performance issues (detected during device testing), fall back to opaque surface-raised.
- Layout (inside glass):
  - ComposerInput (expanding, with attachment button)
  - SendButton (absolute positioned right of input, vertically centered)
  - ComposerStatus (below input)
- Wraps in KeyboardStickyView from react-native-keyboard-controller per D-16:
  ```tsx
  import { KeyboardStickyView } from 'react-native-keyboard-controller';
  <KeyboardStickyView>
    <GlassSurface className="mx-0 rounded-none border-t border-border-subtle">
      {/* composer content */}
    </GlassSurface>
  </KeyboardStickyView>
  ```
  Note: KeyboardStickyView matches iOS system keyboard curve exactly -- NOT a spring (Soul doc specifies this).
- Safe area bottom inset: handled by useSafeAreaInsets, add as bottom padding
- On send: clear input text, contract to single line (Standard spring), call websocket send
- During streaming: SendButton shows stop icon, input disabled

**Step 5: Create mobile/components/chat/MessageList.tsx** -- per Soul doc Chat Thread:
- Uses @shopify/flash-list FlashList (or FlatList as fallback)
- Props: messages array, isStreaming, streamingContent
- Renders MessageBubble for each message
- During streaming: renders an extra assistant MessageBubble at the end with streaming content
- Scroll behavior: auto-scroll to bottom when new content arrives AND user is at bottom
  - Use onContentSizeChange + scrollToEnd when isAtBottom
  - Do NOT auto-scroll if user has scrolled up (check via useScrollToBottom)
- StreamingIndicator at bottom (visible during streaming)
- ScrollToBottomPill overlay (visible when scrolled up)
- estimatedItemSize NOT needed with FlashList v2 (auto-sizes)
- Per research anti-pattern: do NOT use inverted list. Use normal order with scrollToEnd.

**Step 6: Rewrite mobile/app/(stack)/chat/[id].tsx** -- the chat screen:
- Gets session ID from route params: `const { id } = useLocalSearchParams()`
- Layout:
  - Animated.View with dynamic color backgroundStyle (from useDynamicColor)
  - Navigation header: session title (17px semibold centered), back arrow (left, navigates back), model indicator (right, "Claude" in Caption)
  - MessageList (fills space between header and composer)
  - Composer (KeyboardStickyView)
- On mount:
  - Set active session in timeline store
  - Fetch messages for this session via useMessageList.fetchMessages
  - Subscribe to streaming state -- when streaming starts, call enterStreaming(). When ends, call exitStreaming(). When error, call enterError().
- On send message:
  - Get wsClient via getWsClient()
  - Send: `wsClient.send({ type: 'claude-command', command: text, options: { sessionId: id, projectPath } })`
  - projectPath from timeline store (session's project path)
- Empty state (per D-23): when no messages, show EmptyChat component
- Header back arrow: uses router.back() or drawer navigation

**Important per Soul doc "Sending a Message" elevation example (full sequence):**
1. Press send -> Micro spring scale 0.85, Impact Light haptic
2. Release -> scale 1.0, text clears
3. User bubble springs in from below (Standard spring)
4. Input contracts to single line
5. ~200ms later, assistant text fades in (150ms opacity)
6. Streaming accent line pulses
7. Background warms (useDynamicColor.enterStreaming)
8. Streaming ends -> accent line fades, warmth reverts (exitStreaming)
  </action>
  <verify>
    <automated>cd /home/swd/loom && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
    Verify: All 6 files exist with correct exports.
    Verify: mobile/app/(stack)/chat/[id].tsx imports useDynamicColor, MessageList, Composer.
  </verify>
  <acceptance_criteria>
    - mobile/components/composer/ComposerInput.tsx contains TextInput with multiline support and max 6 lines
    - mobile/components/composer/ComposerInput.tsx contains Standard spring height animation (withSpring with SPRING.standard)
    - mobile/components/composer/ComposerInput.tsx contains border transition from subtle to interactive on focus
    - mobile/components/composer/SendButton.tsx has 36px diameter (width: 36, height: 36, borderRadius: 18)
    - mobile/components/composer/SendButton.tsx contains accent bg when text present, destructive bg when streaming
    - mobile/components/composer/SendButton.tsx contains Micro spring scale (0.85) on press
    - mobile/components/composer/SendButton.tsx imports from expo-haptics for Impact Light
    - mobile/components/composer/ComposerStatus.tsx shows token count, model name, connection dot
    - mobile/components/composer/Composer.tsx imports KeyboardStickyView from react-native-keyboard-controller
    - mobile/components/composer/Composer.tsx imports GlassSurface from primitives
    - mobile/components/chat/MessageList.tsx imports FlashList from @shopify/flash-list (or FlatList as fallback)
    - mobile/components/chat/MessageList.tsx imports MessageBubble, StreamingIndicator, ScrollToBottomPill
    - mobile/components/chat/MessageList.tsx handles auto-scroll to bottom during streaming
    - mobile/app/(stack)/chat/[id].tsx imports useDynamicColor and applies backgroundStyle to container
    - mobile/app/(stack)/chat/[id].tsx imports Composer wrapped in KeyboardStickyView
    - mobile/app/(stack)/chat/[id].tsx calls getWsClient().send with type 'claude-command'
    - mobile/app/(stack)/chat/[id].tsx calls enterStreaming/exitStreaming based on stream state
    - `npx vitest run` passes
  </acceptance_criteria>
  <done>Complete chat screen with message list, glass composer, streaming markdown, dynamic color warmth shift, and perfect keyboard sync. Full "Sending a Message" elevation sequence implemented.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes (all existing tests green)
- `cd mobile && npx tsc --noEmit` compiles
- Chat screen renders messages with correct Soul-doc layout
- User messages in raised bubbles, assistant messages free-flowing
- Composer has glass backdrop, expanding input, send/stop toggle
- Keyboard moves in sync with composer (KeyboardStickyView)
- Dynamic color shifts during streaming
- Streaming indicator pulses at bottom
</verification>

<success_criteria>
1. User can send a message via the composer and see it as a raised bubble per D-18
2. Streaming markdown response renders in real-time via MarkdownRenderer per D-04
3. Background warms during streaming, cools on error per D-05
4. Streaming indicator (2px accent line) pulses during streaming per D-26
5. Scroll-to-bottom pill appears when scrolled up, bounces on appearance per D-22
6. Composer glass backdrop, expanding input (max 6 lines), send/stop toggle per D-14
7. Keyboard syncs perfectly via KeyboardStickyView per D-16
8. Empty chat shows avatar + "How can I help?" per D-23
</success_criteria>

<output>
After completion, create `.planning/phases/69-chat-foundation/69-04-SUMMARY.md`
</output>
