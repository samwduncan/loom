---
phase: 18-activity-scroll-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/components/chat/view/StatusLine.tsx
  - src/src/components/chat/view/StreamingCursor.tsx
  - src/src/components/chat/view/ChatView.tsx
  - src/src/components/chat/styles/streaming-cursor.css
  - src/src/lib/websocket-init.ts
  - src/src/lib/stream-multiplexer.ts
  - src/src/components/chat/view/ActiveMessage.tsx
  - src/src/components/chat/view/ThinkingDisclosure.tsx
  - src/src/components/chat/styles/status-line.css
autonomous: true
requirements:
  - ACT-01
  - ACT-02
  - ACT-03
  - ACT-04
  - POL-02
  - POL-03
  - POL-04

must_haves:
  truths:
    - "Activity status line shows current tool action text below messages, above composer"
    - "Activity text fades in/out smoothly and truncates long paths"
    - "Activity text updates are debounced to 200ms in the multiplexer"
    - "Status line never causes scroll height changes (outside scroll container)"
    - "Streaming cursor pulses at end of active text with primary accent color"
    - "Streaming cursor in thinking blocks uses muted color variant"
  artifacts:
    - path: "src/src/components/chat/view/StatusLine.tsx"
      provides: "Activity status line component"
      exports: ["StatusLine"]
    - path: "src/src/components/chat/view/StreamingCursor.tsx"
      provides: "Reusable streaming cursor component"
      exports: ["StreamingCursor"]
    - path: "src/src/components/chat/styles/status-line.css"
      provides: "Status line fade and collapse CSS"
    - path: "src/src/components/chat/styles/streaming-cursor.css"
      provides: "Updated cursor CSS with pulse animation and muted variant"
  key_links:
    - from: "src/src/components/chat/view/StatusLine.tsx"
      to: "src/src/stores/stream.ts"
      via: "useStreamStore selector for activityText and isStreaming"
      pattern: "useStreamStore.*activityText"
    - from: "src/src/components/chat/view/ChatView.tsx"
      to: "src/src/components/chat/view/StatusLine.tsx"
      via: "Grid row between messages and permission banner"
      pattern: "grid-rows.*StatusLine"
    - from: "src/src/lib/websocket-init.ts"
      to: "src/src/stores/stream.ts"
      via: "Debounced setTimeout in onActivityText callback"
      pattern: "setTimeout.*setActivityText"
    - from: "src/src/components/chat/view/ActiveMessage.tsx"
      to: "src/src/components/chat/view/StreamingCursor.tsx"
      via: "StreamingCursor component replaces inline span"
      pattern: "StreamingCursor"
---

<objective>
Create the activity status line showing what the agent is doing, refactor the streaming cursor into a React component with muted variant, and add activity text debouncing in the multiplexer layer.

Purpose: Give users real-time feedback about agent actions and visual streaming presence, completing the "alive" feel of the chat experience.
Output: StatusLine component in ChatView grid, StreamingCursor React component with primary/muted variants, debounced activity text in multiplexer.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/src/components/chat/view/ChatView.tsx
@src/src/stores/stream.ts
@src/src/lib/websocket-init.ts
@src/src/lib/stream-multiplexer.ts
@src/src/components/chat/view/ActiveMessage.tsx
@src/src/components/chat/view/ThinkingDisclosure.tsx
@src/src/components/chat/styles/streaming-cursor.css

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/src/stores/stream.ts:
```typescript
export interface StreamState {
  isStreaming: boolean;
  activityText: string;
  setActivityText: (text: string) => void;
  // ... other fields
}
export const useStreamStore = create<StreamState>()(/* ... */);
```

From src/src/lib/stream-multiplexer.ts:
```typescript
export interface MultiplexerCallbacks {
  onActivityText: (text: string) => void;
  // ... other callbacks
}
```

From src/src/lib/websocket-init.ts (current, no debounce):
```typescript
onActivityText: (text) => streamStore().setActivityText(text),
```

From src/src/components/chat/view/ChatView.tsx (current grid):
```typescript
className="relative grid h-full grid-rows-[1fr_auto_auto]"
// Row 1: MessageList, Row 2: PermissionBanner, Row 3: ChatComposer
```

From src/src/components/chat/view/ActiveMessage.tsx (current cursor):
```typescript
<span className="streaming-cursor" data-testid="streaming-cursor" />
```

From src/src/components/chat/view/ThinkingDisclosure.tsx (thinking text):
```typescript
<p key={block.id} className="italic text-muted font-mono text-sm">
  {block.text}
</p>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatusLine component + ChatView grid integration + activity debounce</name>
  <files>
    src/src/components/chat/view/StatusLine.tsx,
    src/src/components/chat/styles/status-line.css,
    src/src/components/chat/view/ChatView.tsx,
    src/src/lib/websocket-init.ts
  </files>
  <action>
1. Create `src/src/components/chat/view/StatusLine.tsx`:
   - `React.memo` wrapped component with two Zustand selectors: `useStreamStore(s => s.activityText)` and `useStreamStore(s => s.isStreaming)`.
   - Compute `visible = isStreaming && Boolean(activityText)`.
   - Render: `<div>` with `cn('status-line px-4 py-1 text-xs text-muted truncate transition-opacity', visible ? 'opacity-100' : 'opacity-0')`. Use `style={{ transitionDuration: 'var(--duration-normal)' }}` per POL-04.
   - `data-testid="status-line"`. When not visible, the div is still in the DOM (grid row auto collapses to min content = the transparent text).
   - Display `activityText` as text content.

2. Create `src/src/components/chat/styles/status-line.css`:
   - Minimal CSS for the status-line if needed beyond Tailwind. Primarily the opacity transition is handled inline. May not need a separate CSS file if Tailwind covers everything — use judgment. If creating, keep it minimal: just the fade timing using `var(--duration-normal)`.

3. Update `src/src/components/chat/view/ChatView.tsx`:
   - Import `StatusLine`.
   - Change grid from `grid-rows-[1fr_auto_auto]` to `grid-rows-[1fr_auto_auto_auto]`.
   - Insert `<StatusLine />` between `MessageList` and `PermissionBanner` in the JSX.

4. Update `src/src/lib/websocket-init.ts` — debounce onActivityText:
   - Add module-scoped `let activityDebounceTimer: ReturnType<typeof setTimeout> | null = null;`.
   - Replace the direct `onActivityText: (text) => streamStore().setActivityText(text)` with a debounced version:
     ```
     onActivityText: (text) => {
       if (activityDebounceTimer !== null) clearTimeout(activityDebounceTimer);
       activityDebounceTimer = setTimeout(() => {
         streamStore().setActivityText(text);
         activityDebounceTimer = null;
       }, 200);
     },
     ```
   - Also clear the timer in the `onStreamEnd` callback (if activity timer is pending, clear it and set activityText to '').
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>StatusLine renders activity text from stream store with opacity fade, ChatView grid has 4 rows with StatusLine between messages and permission banner, activity text debounced to 200ms in websocket-init module scope</done>
</task>

<task type="auto">
  <name>Task 2: StreamingCursor React component + ActiveMessage/ThinkingDisclosure integration</name>
  <files>
    src/src/components/chat/view/StreamingCursor.tsx,
    src/src/components/chat/styles/streaming-cursor.css,
    src/src/components/chat/view/ActiveMessage.tsx,
    src/src/components/chat/view/ThinkingDisclosure.tsx
  </files>
  <action>
1. Create `src/src/components/chat/view/StreamingCursor.tsx`:
   - Props: `variant?: 'primary' | 'muted'` (default 'primary').
   - Render: `<span className={cn('streaming-cursor', variant === 'muted' && 'streaming-cursor--muted')} data-testid="streaming-cursor" aria-hidden="true" />`.
   - Named export, no memo needed (trivial render).

2. Update `src/src/components/chat/styles/streaming-cursor.css`:
   - Change the existing `.streaming-cursor` animation from `cursor-blink` (step-end, hard on/off) to `cursor-pulse` (smooth ease-in-out, opacity 0.4 to 1.0) per POL-02.
   - Add `.streaming-cursor--muted` class: `background-color: var(--text-muted)`.
   - Update `@keyframes cursor-pulse`: `0%, 100% { opacity: 1; } 50% { opacity: 0.4; }`.
   - Remove old `@keyframes cursor-blink`.
   - Keep the `prefers-reduced-motion` rule (animation: none; opacity: 1).
   - Keep the `[data-phase="finalizing"]` hiding rules.
   - All durations MUST use design tokens (POL-04): the existing `var(--duration-normal)` and `var(--ease-out)` are correct.

3. Update `src/src/components/chat/view/ActiveMessage.tsx`:
   - Import `StreamingCursor` from `./StreamingCursor`.
   - Replace `<span className="streaming-cursor" data-testid="streaming-cursor" />` (line ~339) with `<StreamingCursor />`.

4. Update `src/src/components/chat/view/ThinkingDisclosure.tsx`:
   - Import `StreamingCursor`.
   - Add streaming cursor after the thinking text content. The cursor should appear at the end of the last thinking block text when the thinking block is still actively streaming (check if `isStreaming` from store or if the block has `isComplete` flag).
   - Use `<StreamingCursor variant="muted" />` for the muted color in thinking.
   - Only show cursor when the thinking content is still being generated (not on finalized thinking blocks). Use the `isComplete` field on the thinking block or the global `isStreaming` state to determine this.

5. POL-03 verification: The thinking text already has `italic text-muted font-mono text-sm` styling (confirmed at line 80 of ThinkingDisclosure.tsx). No changes needed — just verify it's still correct after adding the cursor.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>StreamingCursor component renders with primary (default) and muted variants, ActiveMessage uses StreamingCursor component instead of inline span, ThinkingDisclosure shows muted cursor during active thinking, cursor pulse is smooth (0.4-1.0 opacity) not hard blink, all CSS durations use design tokens</done>
</task>

</tasks>

<verification>
- StatusLine appears below messages during streaming with activity text
- StatusLine fades out when streaming stops
- StatusLine never causes scroll jump (outside scroll container)
- Streaming cursor pulses smoothly in active messages
- Streaming cursor in thinking blocks uses muted color
- Activity text debounced (no flicker during rapid tool switches)
- All animations/transitions use design tokens (POL-04)
- `cd /home/swd/loom/src && npx vitest run --reporter=verbose` passes
- `cd /home/swd/loom/src && npx eslint src/` passes
</verification>

<success_criteria>
- Activity status line visible during streaming, invisible when idle
- Streaming cursor is a reusable React component with variant prop
- Activity text debounced to 200ms at multiplexer level
- All CSS uses design tokens only
- Existing tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/18-activity-scroll-polish/18-01-SUMMARY.md`
</output>
