---
phase: 15-tool-card-shell-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/lib/format-elapsed.ts
  - src/src/lib/format-elapsed.test.ts
  - src/src/hooks/useElapsedTime.ts
  - src/src/components/chat/tools/ToolCardShell.tsx
  - src/src/components/chat/tools/ToolCardShell.test.tsx
  - src/src/components/chat/tools/tool-card-shell.css
autonomous: true
requirements: [TOOL-01, TOOL-03, TOOL-04, TOOL-05]

must_haves:
  truths:
    - "ToolCardShell renders a consistent header with tool name, status dot, status label, and elapsed time"
    - "Expand/collapse animates smoothly via CSS Grid grid-template-rows 0fr/1fr with spring cubic-bezier"
    - "Error cards render with red accent border and background tint"
    - "Elapsed time formats correctly: sub-second 0.3s, seconds 1.2s, long-running 1m 23s"
    - "Timer updates live every 100ms during executing state and freezes on resolve/error"
  artifacts:
    - path: "src/src/lib/format-elapsed.ts"
      provides: "Elapsed time formatting utility"
      exports: ["formatElapsed"]
    - path: "src/src/hooks/useElapsedTime.ts"
      provides: "Hook wrapping setInterval + cleanup for live elapsed time"
      exports: ["useElapsedTime"]
    - path: "src/src/components/chat/tools/ToolCardShell.tsx"
      provides: "Shared card wrapper with header, expand animation, error treatment"
      exports: ["ToolCardShell"]
    - path: "src/src/components/chat/tools/tool-card-shell.css"
      provides: "CSS for card shell expand animation, error treatment, header layout"
  key_links:
    - from: "src/src/hooks/useElapsedTime.ts"
      to: "src/src/lib/format-elapsed.ts"
      via: "import formatElapsed"
      pattern: "formatElapsed"
    - from: "src/src/components/chat/tools/ToolCardShell.tsx"
      to: "src/src/hooks/useElapsedTime.ts"
      via: "import useElapsedTime"
      pattern: "useElapsedTime"
    - from: "src/src/components/chat/tools/ToolCardShell.tsx"
      to: "src/src/types/stream.ts"
      via: "import ToolCallState"
      pattern: "ToolCallState"
---

<objective>
Build the elapsed time utility, useElapsedTime hook, and ToolCardShell wrapper component with CSS Grid expand/collapse animation, consistent header layout, and error state visual treatment.

Purpose: Provides the shared infrastructure that all tool cards (default and per-tool in Phase 16) render inside. Establishes the state machine visual transitions (data-status CSS), elapsed time display, and error force-expand pattern.
Output: 3 production files (format-elapsed.ts, useElapsedTime.ts, ToolCardShell.tsx) + CSS + 2 test files
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-tool-card-shell-state-machine/15-CONTEXT.md
@.planning/phases/15-tool-card-shell-state-machine/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/src/types/stream.ts:
```typescript
export type ToolCallStatus = 'invoked' | 'executing' | 'resolved' | 'rejected';

export interface ToolCallState {
  id: string;
  toolName: string;
  status: ToolCallStatus;
  input: Record<string, unknown>;
  output: string | null;
  isError: boolean;
  startedAt: string;
  completedAt: string | null;
}
```

From src/src/lib/tool-registry.ts:
```typescript
export interface ToolCardProps {
  toolName: string;
  input: Record<string, unknown>;
  output: string | null;
  isError: boolean;
  status: ToolCallStatus;
}

export interface ToolConfig {
  displayName: string;
  icon: ComponentType;
  getChipLabel: (input: Record<string, unknown>) => string;
  stateColors?: Partial<Record<ToolCallStatus, string>>;
  renderCard: ComponentType<ToolCardProps>;
}
```

From src/src/lib/motion.ts:
```typescript
export const EASING = {
  spring: 'cubic-bezier(0.34, 1.56, 0.64, 1)',
} as const;
export const DURATION = {
  spring: 500,
} as const;
```

From src/src/components/chat/view/ThinkingDisclosure.tsx (reference pattern):
- CSS Grid `grid-template-rows: 0fr/1fr` for expand/collapse
- Inner `<div>` with `overflow: hidden` wrapping content
- `gridTemplateRows` set via inline style driven by `isExpanded` state

From src/src/components/chat/tools/tool-chip.css (existing status dot classes):
- `.tool-chip-dot--invoked`, `--executing`, `--resolved`, `--rejected`
- `--accent-primary`, `--status-warning`, `--status-success`, `--status-error` tokens
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatElapsed utility + useElapsedTime hook</name>
  <files>src/src/lib/format-elapsed.ts, src/src/lib/format-elapsed.test.ts, src/src/hooks/useElapsedTime.ts</files>
  <behavior>
    - formatElapsed(0) returns "0.0s"
    - formatElapsed(300) returns "0.3s"
    - formatElapsed(1200) returns "1.2s"
    - formatElapsed(5432) returns "5.4s"
    - formatElapsed(59999) returns "60.0s" (stays in seconds until 60s)
    - formatElapsed(60000) returns "1m 0s"
    - formatElapsed(83000) returns "1m 23s"
    - formatElapsed(125000) returns "2m 5s"
    - useElapsedTime with completedAt returns frozen formatted value (no interval)
    - useElapsedTime without completedAt returns live updating value (100ms interval)
    - useElapsedTime cleans up interval on unmount
  </behavior>
  <action>
    Create `src/src/lib/format-elapsed.ts`:
    - Export `formatElapsed(ms: number): string`
    - Sub-60s: `(ms / 1000).toFixed(1)` + "s" (always one decimal)
    - 60s+: `${minutes}m ${seconds}s` (floor both, no decimals)

    Create `src/src/lib/format-elapsed.test.ts`:
    - Test all boundary cases from behavior block

    Create `src/src/hooks/useElapsedTime.ts`:
    - Export `useElapsedTime(startedAt: string | null, completedAt: string | null): string`
    - If startedAt is null, return empty string (no tool timing yet)
    - If completedAt is set, compute once and return (no interval). Use `useState` initializer for instant value.
    - If completedAt is null (still executing), `setInterval` at 100ms calling `Date.now() - new Date(startedAt).getTime()` through `formatElapsed`
    - Store interval in `useRef`, clear on cleanup and when completedAt transitions to non-null
    - Belt-and-suspenders: inside interval callback, check a ref tracking completedAt; if set, clearInterval from within
    - Constitution: named export only, no default export
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/lib/format-elapsed.test.ts -x</automated>
  </verify>
  <done>formatElapsed handles all time ranges correctly. useElapsedTime hook manages interval lifecycle with proper cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: ToolCardShell component + CSS</name>
  <files>src/src/components/chat/tools/ToolCardShell.tsx, src/src/components/chat/tools/ToolCardShell.test.tsx, src/src/components/chat/tools/tool-card-shell.css</files>
  <action>
    Create `src/src/components/chat/tools/tool-card-shell.css`:
    - `.tool-card-shell`: `margin-top: 4px` (gap between chip and card per user decision)
    - `.tool-card-shell-header`: flexbox row, `align-items: center`, `gap: var(--space-2)`, `padding: 8px 12px`, `background: var(--surface-raised)`, `border: 1px solid var(--border-subtle)`, `border-radius: 6px 6px 0 0` (top corners only when body visible, full radius when collapsed)
    - Left group: tool icon + display name (`font-weight: 500`, `color: var(--text-primary)`)
    - Right group: `margin-left: auto`, status dot (reuse `.tool-chip-dot--*` classes), status label (`text-xs`, `color: var(--text-muted)`), elapsed time (`text-xs`, `color: var(--text-secondary)`, `font-variant-numeric: tabular-nums` for stable width)
    - Status labels: "Starting" (invoked), "Running" (executing), "Done" (resolved), "Failed" (rejected)
    - `.tool-card-shell-body`: `display: grid`, `grid-template-rows: 0fr`, `transition: grid-template-rows var(--duration-spring) var(--ease-spring)`
    - `.tool-card-shell-body[data-expanded="true"]`: `grid-template-rows: 1fr`
    - `.tool-card-shell-body > div`: `overflow: hidden` (required for grid animation)
    - `.tool-card-shell-body-inner`: `padding: 8px 12px`, `background: var(--surface-raised)`, `border: 1px solid var(--border-subtle)`, `border-top: none`, `border-radius: 0 0 6px 6px`
    - Error treatment: `.tool-card-shell[data-status="rejected"]` gets `border-color: oklch(from var(--status-error) l c h / 0.3)` on both header and body-inner. Header also gets `background: oklch(from var(--status-error) l c h / 0.05)`
    - `@media (prefers-reduced-motion: reduce)`: set `transition: none` on `.tool-card-shell-body`

    Create `src/src/components/chat/tools/ToolCardShell.tsx`:
    - Named export `ToolCardShell`, wrapped in `memo()`
    - Props: `{ toolCall: ToolCallState, config: ToolConfig, isExpanded: boolean, children: React.ReactNode }`
    - Import `useElapsedTime` from `@/hooks/useElapsedTime`
    - Import Lucide `AlertTriangle` for error state icon in header
    - Call `useElapsedTime(toolCall.startedAt, toolCall.completedAt)` for elapsed display
    - Header renders: config.icon, config.displayName, status dot (same CSS classes as tool-chip-dot), status text label, elapsed time (with middle dot separator: `· {elapsed}`)
    - Error state: render `AlertTriangle` icon next to tool name in header
    - Body wraps `children` in the CSS Grid animation container. Content always rendered (not conditionally mounted) -- the `grid-template-rows: 0fr` + `overflow: hidden` hides it visually
    - `data-status={toolCall.status}` on root element for CSS error treatment
    - `data-expanded={isExpanded}` on body element for CSS grid animation
    - Constitution: `cn()` for conditional classes, no hardcoded colors

    Create `src/src/components/chat/tools/ToolCardShell.test.tsx`:
    - Test: renders header with tool name and status label
    - Test: renders elapsed time in header
    - Test: body has data-expanded="true" when isExpanded=true, "false" when false
    - Test: error state renders AlertTriangle icon
    - Test: error state has data-status="rejected" on root
    - Test: children render inside body
    - Use `makeToolCall` helper similar to ToolChip.test.tsx
    - Mock `useElapsedTime` to return "1.2s" for predictable tests
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/components/chat/tools/ToolCardShell.test.tsx -x</automated>
  </verify>
  <done>ToolCardShell renders consistent header with status/timer, animates expand/collapse via CSS Grid, error cards show red treatment with AlertTriangle icon. All tests pass.</done>
</task>

</tasks>

<verification>
cd /home/swd/loom/src && npx vitest run --reporter=verbose
</verification>

<success_criteria>
- formatElapsed produces correct output for sub-second, second, and minute ranges
- useElapsedTime manages interval lifecycle with proper cleanup
- ToolCardShell renders header with icon, name, status dot, status label, elapsed time
- CSS Grid expand/collapse animation works with spring easing
- Error state shows red border/background treatment and AlertTriangle icon
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/15-tool-card-shell-state-machine/15-01-SUMMARY.md`
</output>
