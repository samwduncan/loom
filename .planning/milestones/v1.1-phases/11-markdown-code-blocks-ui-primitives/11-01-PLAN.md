---
phase: 11-markdown-code-blocks-ui-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/package.json
  - src/package-lock.json
  - src/components.json
  - src/src/styles/tokens.css
  - src/src/styles/index.css
  - src/src/components/ui/badge.tsx
  - src/src/components/ui/collapsible.tsx
  - src/src/components/ui/dialog.tsx
  - src/src/components/ui/dropdown-menu.tsx
  - src/src/components/ui/kbd.tsx
  - src/src/components/ui/scroll-area.tsx
  - src/src/components/ui/separator.tsx
  - src/src/components/ui/sonner.tsx
  - src/src/components/ui/tooltip.tsx
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "react-markdown, remark-gfm, rehype-raw, and shiki are installed in src/ workspace"
    - "shadcn/ui is initialized with components.json pointing to correct paths"
    - "All 9 shadcn primitives are installed and import without errors"
    - "Every shadcn component uses OKLCH tokens -- no default shadcn HSL colors remain"
    - "Custom Kbd component exists and renders styled keyboard shortcut hints"
  artifacts:
    - path: "src/package.json"
      provides: "react-markdown, remark-gfm, rehype-raw, shiki dependencies"
      contains: "react-markdown"
    - path: "src/components.json"
      provides: "shadcn/ui configuration"
      contains: "components"
    - path: "src/src/components/ui/dialog.tsx"
      provides: "Dialog primitive"
      min_lines: 10
    - path: "src/src/components/ui/kbd.tsx"
      provides: "Custom Kbd component"
      min_lines: 8
  key_links:
    - from: "src/src/components/ui/*.tsx"
      to: "src/src/styles/index.css"
      via: "Tailwind utility classes referencing @theme inline tokens"
      pattern: "bg-surface|text-muted|border-border"
---

<objective>
Install all Phase 11 dependencies (react-markdown, remark-gfm, rehype-raw, shiki) and set up shadcn/ui with 9 primitives restyled to OKLCH tokens.

Purpose: Establish the component library foundation and package dependencies that Plans 02 and 03 consume. shadcn components are used across all subsequent M2 phases (dialogs for permissions, tooltips for tool info, collapsibles for tool groups, sonner for copy feedback, etc.).

Output: All npm packages installed, shadcn/ui initialized with components.json, 9 primitives in src/src/components/ui/ restyled to OKLCH.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-markdown-code-blocks-ui-primitives/11-RESEARCH.md

@src/package.json
@src/src/styles/tokens.css
@src/src/styles/index.css
@src/vite.config.ts
@src/tsconfig.app.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and initialize shadcn/ui</name>
  <files>src/package.json, src/package-lock.json, src/components.json</files>
  <action>
1. Install markdown and syntax highlighting packages:
   ```
   cd src/ && npm install react-markdown@^10.1.0 remark-gfm@^4.0.1 rehype-raw@^7.0.0 shiki@^3.23.0
   ```

2. Initialize shadcn/ui in the `src/` workspace:
   ```
   cd src/ && npx shadcn@latest init
   ```
   When prompted (or via flags), configure:
   - Style: "new-york"
   - RSC: false
   - TypeScript: true
   - Tailwind CSS: `src/styles/index.css`
   - Base color: "neutral"
   - CSS variables: yes
   - Components alias: `@/components`
   - Utils alias: `@/utils`
   - UI alias: `@/components/ui`
   - Lib alias: `@/lib`
   - Hooks alias: `@/hooks`
   - Icon library: lucide

   If interactive prompts block, create `src/components.json` manually with the config from the RESEARCH.md (components.json section) and install supporting deps:
   ```
   cd src/ && npm install class-variance-authority lucide-react tw-animate-css
   ```

3. Install all 8 shadcn primitives (kbd is custom, not from shadcn catalog):
   ```
   cd src/ && npx shadcn@latest add dialog tooltip scroll-area collapsible sonner dropdown-menu badge separator
   ```

4. Verify all packages installed and components generated:
   - Check `src/package.json` has react-markdown, remark-gfm, rehype-raw, shiki
   - Check `src/src/components/ui/` has the 8 generated component files
   - Run `cd src && npx tsc --noEmit` to ensure no type errors

IMPORTANT: shadcn may add CSS variable declarations to index.css that conflict with existing OKLCH tokens. If it does, remove them -- the existing `@theme inline` block already defines all needed semantic tokens. The key mapping is:
- shadcn `bg-background` -> already maps via `--color-background: var(--surface-base)`
- shadcn `text-foreground` -> already maps via `--color-foreground: var(--text-primary)`
- shadcn `bg-primary` -> already maps via `--color-primary: var(--accent-primary)`
- shadcn `bg-muted` -> may need adding as `--color-muted-background` if shadcn components use `bg-muted`
- shadcn `bg-card` -> may need adding as `--color-card: var(--surface-raised)`
- shadcn `bg-popover` -> may need adding as `--color-popover: var(--surface-overlay)`
- shadcn `bg-accent` -> already maps via `--color-accent: var(--accent-primary)`

Add any missing semantic token mappings to `@theme inline` in index.css. Do NOT let shadcn write its own `:root` color block -- delete it if generated.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && node -e "const p=require('./package.json'); const deps={...p.dependencies,...p.devDependencies}; ['react-markdown','remark-gfm','rehype-raw','shiki'].forEach(d => { if(!deps[d]) { console.error('MISSING: '+d); process.exit(1); } }); console.log('All deps present')" && npx tsc --noEmit</automated>
  </verify>
  <done>react-markdown, remark-gfm, rehype-raw, shiki in package.json. shadcn/ui initialized with components.json. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle shadcn components to OKLCH tokens and create Kbd</name>
  <files>src/src/components/ui/badge.tsx, src/src/components/ui/collapsible.tsx, src/src/components/ui/dialog.tsx, src/src/components/ui/dropdown-menu.tsx, src/src/components/ui/kbd.tsx, src/src/components/ui/scroll-area.tsx, src/src/components/ui/separator.tsx, src/src/components/ui/sonner.tsx, src/src/components/ui/tooltip.tsx, src/src/styles/index.css, src/src/styles/tokens.css</files>
  <action>
1. Audit every generated shadcn component in `src/src/components/ui/` for:
   - Any `hsl(var(--...))` color references -> replace with OKLCH token utilities (`bg-surface-raised`, `text-foreground`, `border-border`, etc.)
   - Any hardcoded colors -> replace with design token utilities
   - Verify all imports resolve (shadcn may generate `@/lib/utils` -- redirect to `@/utils/cn` since that's where `cn()` lives)

2. Fix import paths if needed. shadcn generates `import { cn } from "@/lib/utils"` by default. The project has `cn` at `@/utils/cn`. Either:
   - Update components.json utils alias to point to `@/utils/cn`, OR
   - Create a re-export at `src/src/lib/utils.ts` that does `export { cn } from '@/utils/cn'`
   The re-export approach is cleaner (doesn't fight shadcn conventions).

3. Ensure `@theme inline` in index.css has all semantic tokens shadcn components need. Add missing ones:
   - `--color-card: var(--surface-raised)` (for card backgrounds)
   - `--color-card-foreground: var(--text-primary)`
   - `--color-popover: var(--surface-overlay)` (for dropdown/dialog overlays)
   - `--color-popover-foreground: var(--text-primary)`
   - `--color-muted-background: var(--surface-raised)` (if shadcn uses `bg-muted` for backgrounds)
   - `--color-accent-foreground: var(--text-primary)` (for hover states in dropdowns)
   - `--color-destructive-foreground: var(--text-primary)`
   - `--color-input: var(--border-interactive)` (for input borders)

4. If shadcn init added a `:root` block or `@layer base` block with color definitions to index.css, REMOVE it entirely. All colors come from tokens.css.

5. Add `@import "tw-animate-css"` to index.css if shadcn didn't add it automatically (needed for shadcn animation classes).

6. Create custom `Kbd` component at `src/src/components/ui/kbd.tsx`:
   ```tsx
   import { cn } from '@/utils/cn'
   import type { HTMLAttributes } from 'react'

   export function Kbd({ className, children, ...props }: HTMLAttributes<HTMLElement>) {
     return (
       <kbd
         className={cn(
           'inline-flex items-center rounded-sm border border-border bg-surface-raised px-1.5 py-0.5',
           'font-mono text-xs text-muted',
           className,
         )}
         {...props}
       >
         {children}
       </kbd>
     )
   }
   ```

7. Verify no default shadcn colors remain by searching for `hsl` in ui/ directory.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && grep -r "hsl(" src/components/ui/ && echo "FAIL: HSL colors found" && exit 1 || echo "PASS: No HSL colors" && npx tsc --noEmit</automated>
  </verify>
  <done>All 9 shadcn components (dialog, tooltip, scroll-area, collapsible, sonner, dropdown-menu, badge, separator) plus custom Kbd use OKLCH tokens exclusively. No hsl() references in ui/ directory. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `cd src && npx tsc --noEmit` passes (no type errors)
- `cd src && grep -r "hsl(" src/components/ui/` returns nothing (no HSL colors)
- `cd src && node -e "require('./package.json')"` confirms all 4 packages present
- `src/components.json` exists with correct aliases
- `src/src/components/ui/` contains 9 component files (8 shadcn + 1 custom kbd)
</verification>

<success_criteria>
1. react-markdown ^10.1.0, remark-gfm ^4.0.1, rehype-raw ^7.0.0, shiki ^3.23.0 installed
2. shadcn/ui initialized, components.json configured for project paths
3. All 9 primitives installed and use OKLCH tokens -- zero HSL or hardcoded colors
4. TypeScript compiles without errors
5. Existing tests still pass (`cd src && npx vitest run`)
</success_criteria>

<output>
After completion, create `.planning/phases/11-markdown-code-blocks-ui-primitives/11-01-SUMMARY.md`
</output>
