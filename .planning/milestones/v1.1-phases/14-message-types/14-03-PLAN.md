---
phase: 14-message-types
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - src/src/components/chat/view/UserMessage.tsx
  - src/src/components/chat/view/UserMessage.test.tsx
  - src/src/components/chat/view/ImageThumbnailGrid.tsx
  - src/src/components/chat/view/ImageLightbox.tsx
  - src/src/components/chat/view/ImageLightbox.test.tsx
  - src/src/components/chat/view/MarkdownRenderer.tsx
autonomous: true
requirements: [MSG-01, MSG-09]

must_haves:
  truths:
    - "User messages show bg-card background instead of bg-primary-muted"
    - "User messages show hover timestamp below the bubble with zero layout shift"
    - "User-sent images display as thumbnails above text content in the bubble"
    - "Clicking an image thumbnail opens a lightbox overlay with the full-size image"
    - "Lightbox dismisses on backdrop click, Escape, or X button"
    - "Assistant inline images in markdown are clickable for lightbox"
  artifacts:
    - path: "src/src/components/chat/view/UserMessage.tsx"
      provides: "Enhanced user message with timestamp and images"
      exports: ["UserMessage"]
    - path: "src/src/components/chat/view/ImageThumbnailGrid.tsx"
      provides: "Horizontal image thumbnail row"
      exports: ["ImageThumbnailGrid"]
    - path: "src/src/components/chat/view/ImageLightbox.tsx"
      provides: "Dialog-based fullscreen image viewer"
      exports: ["ImageLightbox"]
  key_links:
    - from: "src/src/components/chat/view/UserMessage.tsx"
      to: "ImageThumbnailGrid + ImageLightbox"
      via: "render when message.attachments exists"
      pattern: "message\\.attachments.*ImageThumbnailGrid"
    - from: "src/src/components/chat/view/MarkdownRenderer.tsx"
      to: "ImageLightbox"
      via: "img component override with onClick"
      pattern: "img:.*onClick.*setLightbox"
    - from: "src/src/components/chat/view/ImageLightbox.tsx"
      to: "shadcn Dialog"
      via: "Dialog wrapper with transparent content"
      pattern: "Dialog.*open.*onOpenChange"
---

<objective>
Enhance UserMessage with bg-card background, hover timestamp, and image thumbnail support. Create ImageLightbox for fullscreen image viewing. Add img override to MarkdownRenderer for assistant inline images.

Purpose: Complete the user message styling and add image display capabilities across all message contexts.
Output: Polished user messages with timestamps, image thumbnails in user bubbles, lightbox overlay, and clickable assistant inline images.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-message-types/14-CONTEXT.md
@.planning/phases/14-message-types/14-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (dependency): ImageAttachment type added to message.ts -->
```typescript
export interface ImageAttachment {
  id: string;
  url: string;
  name: string;
  width?: number;
  height?: number;
}

export interface Message {
  // ... existing fields ...
  attachments?: ImageAttachment[];
}
```

<!-- MessageContainer now accepts role='user' with bg-card (changed from bg-primary-muted in Plan 01) -->

From src/src/lib/formatTime.ts:
```typescript
export function formatRelativeTime(dateStr: string): string;
// Returns: "just now", "2m ago", "1h ago", "3d ago", "Mar 5"
```

From src/src/components/ui/dialog.tsx:
```typescript
// shadcn Dialog, DialogContent, DialogOverlay — already installed and OKLCH-restyled
function Dialog({ ...props }: React.ComponentProps<typeof DialogPrimitive.Root>)
function DialogContent({ className, children, ...props })
```

From src/src/components/chat/view/MarkdownRenderer.tsx:
```typescript
interface MarkdownRendererProps {
  content: string;
  toolCalls?: ToolCallState[];
}
// Has component overrides system via react-markdown Components type
// Currently overrides: code, a, table, pre
// Needs: img override for lightbox click
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UserMessage enhancements + ImageThumbnailGrid + ImageLightbox</name>
  <files>
    src/src/components/chat/view/UserMessage.tsx,
    src/src/components/chat/view/UserMessage.test.tsx,
    src/src/components/chat/view/ImageThumbnailGrid.tsx,
    src/src/components/chat/view/ImageLightbox.tsx,
    src/src/components/chat/view/ImageLightbox.test.tsx
  </files>
  <behavior>
    - UserMessage renders with bg-card background (not bg-primary-muted — handled by MessageContainer Plan 01)
    - Hover over user message bubble shows relative timestamp ("2m ago") below bubble, right-aligned
    - Timestamp uses absolute positioning with opacity 0->1 transition, zero layout shift
    - When message.attachments exists and has items, ImageThumbnailGrid renders above text content
    - Thumbnails: max 200px width, aspect ratio preserved, rounded-lg, cursor-pointer, horizontal row with gap-2
    - Images keyed on attachment.id (not URL) for lifecycle stability
    - Click thumbnail opens ImageLightbox (shadcn Dialog, bg-black/80 backdrop, max-w-[90vw] max-h-[90vh])
    - Lightbox dismisses on backdrop click, Escape, or X button
    - No zoom controls in lightbox
  </behavior>
  <action>
    1. **Create ImageLightbox.tsx:**
       - Use shadcn `Dialog` + `DialogContent` with transparent/borderless styling
       - Props: `src: string | null`, `alt?: string`, `open: boolean`, `onClose: () => void`
       - DialogContent: `className="border-none bg-transparent p-0 shadow-none max-w-[90vw] sm:max-w-[90vw]"` (override shadcn default max-width)
       - Overlay/backdrop: add `className="bg-black/80"` to DialogOverlay
       - Image: `<img src={src} alt={alt} className="max-h-[90vh] max-w-[90vw] object-contain rounded-lg" />`
       - Close via `onOpenChange={(v) => !v && onClose()}`
       - Return null if src is null

    2. **Create ImageThumbnailGrid.tsx:**
       - Props: `attachments: ImageAttachment[]`, `onImageClick: (attachment: ImageAttachment) => void`
       - Render horizontal row: `flex flex-wrap gap-2 mb-2`
       - Each image: `<img key={attachment.id} src={attachment.url} alt={attachment.name} className="max-w-[200px] max-h-[200px] rounded-lg cursor-pointer object-cover hover:opacity-90 transition-opacity" onClick={() => onImageClick(attachment)} />`
       - Key on `attachment.id` NOT `attachment.url` (handles optimistic -> persisted URL change)

    3. **Update UserMessage.tsx:**
       - Import `formatRelativeTime` from `@/lib/formatTime`
       - Import `ImageThumbnailGrid` and `ImageLightbox`
       - Add local state: `const [lightboxAttachment, setLightboxAttachment] = useState<ImageAttachment | null>(null)`
       - Wrap bubble in `group relative` div for hover timestamp
       - Timestamp: `<span className="absolute -bottom-5 right-0 text-xs text-muted opacity-0 transition-opacity group-hover:opacity-100">{formatRelativeTime(message.metadata.timestamp)}</span>`
       - Add margin-bottom to outer wrapper when timestamp exists: `mb-5` (space for absolute-positioned timestamp)
       - Inside the bubble div, before text content: render `ImageThumbnailGrid` if `message.attachments?.length`
       - Render `ImageLightbox` with lightboxAttachment state

    4. **Write tests:**
       - UserMessage.test.tsx: Test bg-card class presence (via MessageContainer), test timestamp renders on hover (check opacity classes), test ImageThumbnailGrid renders when attachments present, test ImageThumbnailGrid does NOT render when no attachments
       - ImageLightbox.test.tsx: Test Dialog opens when open=true and src provided, test onClose called on close interaction, test returns null when src is null
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/chat/view/UserMessage.test.tsx src/src/components/chat/view/ImageLightbox.test.tsx --reporter=verbose</automated>
  </verify>
  <done>User messages show bg-card background, hover timestamp with zero layout shift, image thumbnails when attachments exist. ImageLightbox opens on click with full-size image. Dismiss works via backdrop/Escape/X.</done>
</task>

<task type="auto">
  <name>Task 2: MarkdownRenderer img override for assistant inline images</name>
  <files>
    src/src/components/chat/view/MarkdownRenderer.tsx,
    src/src/components/chat/view/MarkdownRenderer.test.tsx
  </files>
  <action>
    1. **Update MarkdownRenderer.tsx:**
       - Import `ImageLightbox` from `@/components/chat/view/ImageLightbox`
       - Add local state at component level: `const [lightboxSrc, setLightboxSrc] = useState<string | null>(null)` (each MarkdownRenderer instance manages its own lightbox — Dialog portals to body so nesting is fine)
       - Add `img` to the components override object:
         ```
         img: ({ src, alt, ...props }) => (
           <img
             src={src}
             alt={alt ?? ''}
             className="max-w-full rounded-lg cursor-pointer hover:opacity-90 transition-opacity my-2"
             onClick={() => src && setLightboxSrc(src)}
             {...props}
           />
         )
         ```
       - Render `<ImageLightbox src={lightboxSrc} alt="" open={lightboxSrc !== null} onClose={() => setLightboxSrc(null)} />` at the end of the component return, after the `<Markdown>` element

    2. **Update MarkdownRenderer.test.tsx** if it exists: Add a test case that renders markdown with an `![alt](url)` image and verifies the img element has cursor-pointer class and the correct src attribute. Full lightbox interaction testing is covered by ImageLightbox.test.tsx.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/chat/view/MarkdownRenderer.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Assistant inline images in markdown render with click-to-lightbox behavior. Images display at natural size with max-width constraint. Lightbox shared component used consistently.</done>
</task>

</tasks>

<verification>
- `cd /home/swd/loom/src && npx vitest run --reporter=verbose` — full suite green
- `cd /home/swd/loom/src && npx tsc --noEmit` — no TypeScript errors
</verification>

<success_criteria>
- User messages display with bg-card background
- Hovering a user message shows relative timestamp below the bubble with no layout shift
- User messages with image attachments show thumbnails above text
- Clicking any image thumbnail opens a lightbox overlay
- Lightbox shows full-size image with dark backdrop, dismisses properly
- Assistant inline images in markdown are clickable for lightbox viewing
</success_criteria>

<output>
After completion, create `.planning/phases/14-message-types/14-03-SUMMARY.md`
</output>
