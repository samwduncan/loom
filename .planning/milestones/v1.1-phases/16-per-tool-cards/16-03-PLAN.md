---
phase: 16-per-tool-cards
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/src/components/chat/tools/GlobToolCard.tsx
  - src/src/components/chat/tools/GlobToolCard.test.tsx
  - src/src/components/chat/tools/GrepToolCard.tsx
  - src/src/components/chat/tools/GrepToolCard.test.tsx
  - src/src/lib/tool-registry.ts
autonomous: true
requirements: [TOOL-14, TOOL-15]

must_haves:
  truths:
    - "GlobToolCard shows glob pattern and bulleted file list with count"
    - "GlobToolCard truncates at ~20 files with Show N more files"
    - "GrepToolCard shows pattern and file-grouped matches with highlighted terms"
    - "GrepToolCard truncates at ~5 files with Show N more files"
    - "All 6 tool cards registered with Lucide icons and per-tool renderCard"
  artifacts:
    - path: "src/src/components/chat/tools/GlobToolCard.tsx"
      provides: "File list card for glob results"
      exports: ["GlobToolCard"]
    - path: "src/src/components/chat/tools/GrepToolCard.tsx"
      provides: "Search results card for grep output"
      exports: ["GrepToolCard"]
    - path: "src/src/lib/tool-registry.ts"
      provides: "Updated registry with all 6 per-tool cards and Lucide icons"
  key_links:
    - from: "src/src/components/chat/tools/GrepToolCard.tsx"
      to: "src/src/lib/grep-parser.ts"
      via: "parseGrepOutput import"
      pattern: "import.*parseGrepOutput.*grep-parser"
    - from: "src/src/lib/tool-registry.ts"
      to: "src/src/components/chat/tools/BashToolCard.tsx"
      via: "renderCard registration"
      pattern: "renderCard.*BashToolCard"
    - from: "src/src/lib/tool-registry.ts"
      to: "src/src/components/chat/tools/ReadToolCard.tsx"
      via: "renderCard registration"
      pattern: "renderCard.*ReadToolCard"
---

<objective>
Build GlobToolCard and GrepToolCard, then update tool-registry.ts to register all 6 per-tool cards with Lucide icons.

Purpose: Complete the per-tool card set with file list and search result cards, then wire everything into the registry so ToolChip renders the correct card for each tool.
Output: 2 card components with tests, updated tool-registry.ts with Lucide icons and all 6 per-tool renderCard assignments.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-per-tool-cards/16-CONTEXT.md
@.planning/phases/16-per-tool-cards/16-RESEARCH.md
@.planning/phases/16-per-tool-cards/16-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/src/components/chat/tools/TruncatedContent.tsx:
```typescript
interface TruncatedContentProps {
  items: string[];
  threshold: number;
  unit: string;
  renderItem: (item: string, index: number) => React.ReactNode;
}
export const TruncatedContent: React.FC<TruncatedContentProps>;
```

From src/src/lib/grep-parser.ts:
```typescript
export interface GrepMatch { lineNo: number; content: string; }
export interface GrepFileGroup { filePath: string; matches: GrepMatch[]; }
export function parseGrepOutput(output: string): GrepFileGroup[] | null;
```

From src/src/lib/tool-registry.ts:
```typescript
export interface ToolCardProps {
  toolName: string;
  input: Record<string, unknown>;
  output: string | null;
  isError: boolean;
  status: ToolCallStatus;
}
export function registerTool(toolName: string, config: ToolConfig): void;
```

Lucide icons to use (from RESEARCH.md):
- Bash -> Terminal
- Read -> FileText
- Edit -> FilePen
- Write -> FilePlus
- Glob -> FolderSearch
- Grep -> Search
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GlobToolCard + GrepToolCard</name>
  <files>
    src/src/components/chat/tools/GlobToolCard.tsx,
    src/src/components/chat/tools/GlobToolCard.test.tsx,
    src/src/components/chat/tools/GrepToolCard.tsx,
    src/src/components/chat/tools/GrepToolCard.test.tsx
  </files>
  <action>
    **1. Create `src/src/components/chat/tools/GlobToolCard.tsx`:**
    - Extract `pattern` from `input.pattern` (string, fallback to "*")
    - Output is newline-separated file paths
    - **Pattern display:** Monospace `text-muted text-xs` showing the glob pattern
    - **File list:** Parse output lines, filter empty. Each line is a file path.
    - File type icon per entry: use Lucide `Folder` for paths ending with `/`, `FileCode` for code extensions (.ts, .js, .py, .rs, .go, .java, .c, .cpp, .rb), `FileText` for everything else. Icon size 14px, `text-muted`.
    - Left-truncate long paths: if path > 60 chars, show `...{last 57 chars}`
    - **Count summary:** "N files found" in `text-xs text-muted` below pattern
    - **Truncation:** Use TruncatedContent with threshold=20, unit="files"
    - `renderItem`: flex row with icon + truncated path
    - Handle null output (executing state): show pattern only, no file list
    - Wrap in `memo()`, named export

    **2. Create `src/src/components/chat/tools/GrepToolCard.tsx`:**
    - Extract `pattern` from `input.pattern` (string)
    - Parse output via `parseGrepOutput(output)` from `@/lib/grep-parser`
    - If parseGrepOutput returns null: fall back to plain monospace `<pre>` rendering of raw output
    - **Pattern display:** Monospace `text-muted text-xs` showing search pattern
    - **Summary:** "N matches in M files" in `text-xs text-muted`
    - **Results grouped by file:**
      - File path as section header: `text-xs text-muted font-mono` with Lucide `File` icon
      - Each match: line number gutter (`text-muted w-12 text-right`) + content with match term highlighted
    - **Match highlighting:** Extract pattern from `input.pattern`, create regex, wrap matches in `<mark className="bg-primary/20 text-primary rounded-sm px-0.5">`. If regex invalid, skip highlighting.
    - **Truncation by file count:** Show first 5 files, "Show N more files" for the rest. Use local state (not TruncatedContent -- items are GrepFileGroup[], not strings)
    - Handle null output: show pattern only
    - `font-mono text-sm` for match content
    - Wrap in `memo()`, named export

    **3. Tests:**
    - `GlobToolCard.test.tsx`: renders pattern, renders file list, shows count, truncates at 20 files, handles null output, file type icons (code vs text vs folder)
    - `GrepToolCard.test.tsx`: renders pattern, renders grouped matches, highlights match terms, summary shows count, truncates at 5 files, falls back to plain text on invalid output, handles null output
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/chat/tools/GlobToolCard.test.tsx src/src/components/chat/tools/GrepToolCard.test.tsx --reporter=verbose</automated>
  </verify>
  <done>GlobToolCard renders pattern + file list + count + truncation at 20 files. GrepToolCard renders pattern + grouped matches + highlighted terms + truncation at 5 files. Both handle null output and edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Registry migration -- Lucide icons + per-tool card wiring</name>
  <files>
    src/src/lib/tool-registry.ts
  </files>
  <action>
    **Update `src/src/lib/tool-registry.ts`:**

    This file must be converted from `.ts` to `.tsx` OR keep as `.ts` using `createElement` for Lucide icons. Since the file already uses `createElement` pattern and the docstring explains why, KEEP as `.ts` with `createElement`.

    **1. Replace emoji icon components with Lucide icons:**
    - Import `Terminal, FileText, FilePen, FilePlus, FolderSearch, Search` from `lucide-react`
    - Replace BashIcon: `createElement(Terminal, { size: 14 })`
    - Replace ReadIcon: `createElement(FileText, { size: 14 })`
    - Replace EditIcon: `createElement(FilePen, { size: 14 })`
    - Replace WriteIcon: `createElement(FilePlus, { size: 14 })`
    - Replace GlobIcon: `createElement(FolderSearch, { size: 14 })`
    - Replace GrepIcon: `createElement(Search, { size: 14 })`
    - Remove DefaultIcon emoji (fallback config can use a Lucide `Wrench` or `Settings` icon)
    - Remove the old emoji icon functions

    **2. Import and wire all 6 per-tool card components:**
    - Import `BashToolCard` from `@/components/chat/tools/BashToolCard`
    - Import `ReadToolCard` from `@/components/chat/tools/ReadToolCard`
    - Import `EditToolCard` from `@/components/chat/tools/EditToolCard`
    - Import `WriteToolCard` from `@/components/chat/tools/WriteToolCard`
    - Import `GlobToolCard` from `@/components/chat/tools/GlobToolCard`
    - Import `GrepToolCard` from `@/components/chat/tools/GrepToolCard`
    - Update each `registerTool` call: change `renderCard: DefaultToolCard` to the per-tool card
    - DefaultToolCard remains as the fallback in `getToolConfig` for unregistered tools

    **3. Verify no breakage:**
    - Existing chip label functions (bashChipLabel, filePathChipLabel, patternChipLabel) remain unchanged
    - DefaultToolCard still exists and is used in the fallback `getToolConfig` return
    - All existing exports preserved
    - Run existing tool-registry tests to verify no regression
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All 6 tools use Lucide icons and per-tool card components. DefaultToolCard remains as fallback for unknown tools. Full test suite green with no regressions.</done>
</task>

</tasks>

<verification>
Full test suite passes:
```bash
cd /home/swd/loom/src && npx vitest run --reporter=verbose
```
All per-tool cards registered, emoji icons replaced with Lucide.
</verification>

<success_criteria>
- GlobToolCard renders pattern + bulleted file list with Lucide file-type icons + count + truncation at 20 files
- GrepToolCard renders pattern + file-grouped matches + highlighted match terms + truncation at 5 files
- GrepToolCard falls back to plain text when output doesn't match ripgrep format
- All 6 registerTool calls use Lucide icons (no emoji) and per-tool renderCard components
- DefaultToolCard preserved as fallback for unknown tools
- Full test suite green, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/16-per-tool-cards/16-03-SUMMARY.md`
</output>
