---
phase: 16-per-tool-cards
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/src/components/chat/tools/ReadToolCard.tsx
  - src/src/components/chat/tools/ReadToolCard.test.tsx
  - src/src/components/chat/tools/WriteToolCard.tsx
  - src/src/components/chat/tools/WriteToolCard.test.tsx
  - src/src/components/chat/tools/EditToolCard.tsx
  - src/src/components/chat/tools/EditToolCard.test.tsx
autonomous: true
requirements: [TOOL-11, TOOL-12, TOOL-13]

must_haves:
  truths:
    - "ReadToolCard shows file path with icon and syntax-highlighted content with line numbers"
    - "ReadToolCard truncates at ~100 lines with Show more"
    - "EditToolCard shows unified diff with green additions and red deletions with dual line numbers"
    - "WriteToolCard shows file path with icon and syntax-highlighted preview of first ~20 lines"
    - "All three cards degrade gracefully when output is null or input fields missing"
  artifacts:
    - path: "src/src/components/chat/tools/ReadToolCard.tsx"
      provides: "File content card with Shiki highlighting"
      exports: ["ReadToolCard"]
    - path: "src/src/components/chat/tools/EditToolCard.tsx"
      provides: "Unified diff card with colored lines"
      exports: ["EditToolCard"]
    - path: "src/src/components/chat/tools/WriteToolCard.tsx"
      provides: "File write preview card"
      exports: ["WriteToolCard"]
  key_links:
    - from: "src/src/components/chat/tools/ReadToolCard.tsx"
      to: "src/src/lib/shiki-highlighter.ts"
      via: "highlightCode + getLanguageFromPath imports"
      pattern: "import.*getLanguageFromPath.*shiki-highlighter"
    - from: "src/src/components/chat/tools/EditToolCard.tsx"
      to: "src/src/lib/diff-parser.ts"
      via: "computeDiff import"
      pattern: "import.*computeDiff.*diff-parser"
    - from: "src/src/components/chat/tools/ReadToolCard.tsx"
      to: "src/src/components/chat/tools/TruncatedContent.tsx"
      via: "TruncatedContent import"
      pattern: "import.*TruncatedContent"
---

<objective>
Build ReadToolCard, WriteToolCard, and EditToolCard -- the file-content-focused tool cards.

Purpose: ReadToolCard and WriteToolCard display syntax-highlighted file content; EditToolCard shows colored unified diffs. These are the most complex cards due to Shiki integration and diff rendering.
Output: 3 card components with tests, all rendering inside ToolCardShell.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-per-tool-cards/16-CONTEXT.md
@.planning/phases/16-per-tool-cards/16-RESEARCH.md
@.planning/phases/16-per-tool-cards/16-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/src/components/chat/tools/TruncatedContent.tsx:
```typescript
interface TruncatedContentProps {
  items: string[];
  threshold: number;
  unit: string;
  renderItem: (item: string, index: number) => React.ReactNode;
}
export const TruncatedContent: React.FC<TruncatedContentProps>;
```

From src/src/lib/diff-parser.ts:
```typescript
export interface DiffLine {
  type: 'added' | 'removed' | 'context';
  content: string;
  oldLineNo: number | null;
  newLineNo: number | null;
}
export function computeDiff(oldStr: string, newStr: string, contextLines?: number): DiffLine[];
```

From src/src/lib/shiki-highlighter.ts:
```typescript
export function highlightCode(lang: string, code: string): Promise<string>;
export function getLanguageFromPath(filePath: string): string;
```

From src/src/lib/tool-registry.ts:
```typescript
export interface ToolCardProps {
  toolName: string;
  input: Record<string, unknown>;
  output: string | null;
  isError: boolean;
  status: ToolCallStatus;
}
```

From src/src/components/chat/view/CodeBlock.tsx:
```typescript
// Existing pattern: async Shiki highlight with useDeferredValue fallback
// Line numbers via CSS counter on .shiki .line elements
// Copy button with 2s Copied! feedback
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReadToolCard + WriteToolCard</name>
  <files>
    src/src/components/chat/tools/ReadToolCard.tsx,
    src/src/components/chat/tools/ReadToolCard.test.tsx,
    src/src/components/chat/tools/WriteToolCard.tsx,
    src/src/components/chat/tools/WriteToolCard.test.tsx
  </files>
  <action>
    **1. Create `src/src/components/chat/tools/ReadToolCard.tsx`:**
    - Extract `file_path` from `input.file_path` (string, fallback to "unknown")
    - Infer language via `getLanguageFromPath(filePath)`
    - Output is the file content string
    - **File path header row:** Lucide `FileText` icon (14px) + file path in monospace, `text-muted text-xs`, inside the card body (ToolCardShell already provides the tool name header)
    - **Content area:** Use the same Shiki highlighting pattern as CodeBlock:
      - `useState` for html, `useDeferredValue` for deferred swap
      - `useEffect` calling `highlightCode(language, visibleCode)` -- highlight only the VISIBLE lines (pre-truncation), not the full output
      - Plain monospace fallback while Shiki loads
      - Line numbers via CSS counter on `.shiki .line` elements (same Tailwind classes as CodeBlock)
      - `dangerouslySetInnerHTML` with SAFETY comment for Shiki output
    - **Truncation:** Split output into lines, use TruncatedContent with threshold=100, unit="lines"
    - BUT: Shiki needs the full visible text as one string, not per-line rendering. So instead of TruncatedContent for line rendering, manage truncation locally:
      - `expanded` state, `visibleLines = expanded ? allLines : allLines.slice(0, 100)`
      - Join visible lines, highlight as single block
      - Show "Show N more lines" / "Show less" buttons below the code block
    - Copy button for full file content (not just visible)
    - `overflow-x: auto` for long lines
    - Wrap in `memo()`, named export

    **2. Create `src/src/components/chat/tools/WriteToolCard.tsx`:**
    - Nearly identical to ReadToolCard but with threshold=20 and "Show full file" button text
    - Extract `file_path` from `input.file_path`, infer language
    - Output is the written file content
    - Lucide `FilePlus` icon for file path header
    - Same Shiki highlighting with local truncation (threshold=20)
    - Wrap in `memo()`, named export

    **3. Tests for both:**
    - `ReadToolCard.test.tsx`: renders file path, renders output content, truncates at 100 lines with show more button, handles null output, handles missing file_path
    - `WriteToolCard.test.tsx`: renders file path, renders preview content, truncates at 20 lines, handles null output
    - Mock `highlightCode` to return immediately (don't test Shiki in unit tests -- just verify the fallback plain text renders)
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/chat/tools/ReadToolCard.test.tsx src/src/components/chat/tools/WriteToolCard.test.tsx --reporter=verbose</automated>
  </verify>
  <done>ReadToolCard shows file path + icon + highlighted content + truncation at 100 lines. WriteToolCard shows file path + icon + preview at 20 lines. Both degrade gracefully with null output.</done>
</task>

<task type="auto">
  <name>Task 2: EditToolCard with unified diff view</name>
  <files>
    src/src/components/chat/tools/EditToolCard.tsx,
    src/src/components/chat/tools/EditToolCard.test.tsx
  </files>
  <action>
    **1. Create `src/src/components/chat/tools/EditToolCard.tsx`:**
    - Extract `file_path`, `old_string`, `new_string` from `input` (all strings, with null checks)
    - If both old_string and new_string present: compute diff via `computeDiff(oldString, newString, 3)`
    - If only new_string (old_string empty/missing): treat as pure addition (all lines green)
    - If only old_string (new_string missing): treat as pure deletion (all lines red)
    - If neither: fall back to displaying output as plain text

    **File path header:** Lucide `FilePen` icon + file path in monospace

    **Diff rendering:**
    - Container: `bg-surface-0 rounded-md overflow-hidden font-mono text-sm`
    - Each DiffLine rendered as a flex row:
      - Old line number gutter: `w-10 text-right pr-2 text-muted select-none shrink-0` (empty for additions)
      - New line number gutter: `w-10 text-right pr-2 text-muted select-none shrink-0` (empty for deletions)
      - Sign column: `w-4 select-none shrink-0` showing +, -, or space
      - Content: `whitespace-pre overflow-x-auto`
    - Line background colors:
      - Added: `bg-diff-added` (uses --diff-added-bg token)
      - Removed: `bg-diff-removed` (uses --diff-removed-bg token)
      - Context: no background (transparent)
    - NO Shiki highlighting inside diffs (per user decision -- line coloring only)

    **No truncation for diffs** (per user decision -- diffs are compact, show full diff)

    - Wrap in `memo()`, named export

    **2. Create `src/src/components/chat/tools/EditToolCard.test.tsx`:**
    - Test: renders file path with icon
    - Test: addition lines have bg-diff-added class and + sign
    - Test: deletion lines have bg-diff-removed class and - sign
    - Test: context lines have no diff background
    - Test: dual line numbers displayed correctly
    - Test: handles empty old_string (pure addition)
    - Test: handles missing input fields gracefully (falls back to plain text)
    - Test: identical strings show no diff lines
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/chat/tools/EditToolCard.test.tsx --reporter=verbose</automated>
  </verify>
  <done>EditToolCard renders unified diff with green additions (bg-diff-added), red deletions (bg-diff-removed), dual line numbers, 3 context lines. Handles edge cases (empty strings, missing fields).</done>
</task>

</tasks>

<verification>
All card tests pass:
```bash
cd /home/swd/loom/src && npx vitest run --reporter=verbose
```
Full test suite remains green.
</verification>

<success_criteria>
- ReadToolCard renders file path + Lucide icon, Shiki-highlighted content, line numbers, truncation at 100 lines
- WriteToolCard renders file path + Lucide icon, Shiki-highlighted preview at 20 lines
- EditToolCard renders unified diff with green/red line coloring, dual line number gutters, 3 context lines
- All three handle null output and missing input fields gracefully
- No Shiki inside diffs (line coloring only, per user decision)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/16-per-tool-cards/16-02-SUMMARY.md`
</output>
