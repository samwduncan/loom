---
phase: 24-code-editor
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - src/src/components/editor/DiffEditor.tsx
  - src/src/hooks/useFileDiff.ts
  - src/src/components/chat/tools/ReadToolCard.tsx
  - src/src/components/chat/tools/EditToolCard.tsx
  - src/src/components/chat/tools/WriteToolCard.tsx
  - src/src/hooks/useOpenInEditor.ts
autonomous: true
requirements: [ED-14, ED-15, ED-16]

must_haves:
  truths:
    - "User can view file diffs in a merge view within the editor"
    - "Clicking a file path in a Read/Edit/Write tool card opens that file in the editor on the Files tab"
  artifacts:
    - path: "src/src/components/editor/DiffEditor.tsx"
      provides: "CodeMirror merge view wrapper"
      min_lines: 30
    - path: "src/src/hooks/useFileDiff.ts"
      provides: "Fetch diff data from git endpoint"
      exports: ["useFileDiff"]
    - path: "src/src/hooks/useOpenInEditor.ts"
      provides: "Shared hook for opening files from any component"
      exports: ["useOpenInEditor"]
  key_links:
    - from: "src/src/components/chat/tools/ReadToolCard.tsx"
      to: "src/src/hooks/useOpenInEditor.ts"
      via: "click handler on file path"
      pattern: "useOpenInEditor"
    - from: "src/src/hooks/useFileDiff.ts"
      to: "/api/git/file-with-diff"
      via: "apiFetch GET"
      pattern: "file-with-diff"
    - from: "src/src/components/editor/DiffEditor.tsx"
      to: "react-codemirror-merge"
      via: "CodeMirrorMerge wrapper"
      pattern: "CodeMirrorMerge"
---

<objective>
Add diff view capability to the editor and make file paths in chat tool cards clickable to open files in the editor. This completes Phase 24 by connecting the editor to the git panel (for Phase 26) and the chat interface.

Purpose: Users need to view diffs from git operations, and they need a quick path from seeing a file mentioned in chat to viewing/editing it.
Output: DiffEditor component, useFileDiff hook, clickable file paths in Read/Edit/Write tool cards.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-code-editor/24-RESEARCH.md
@.planning/phases/24-code-editor/24-01-SUMMARY.md
@.planning/phases/24-code-editor/24-02-SUMMARY.md

@src/src/components/chat/tools/ReadToolCard.tsx
@src/src/components/chat/tools/EditToolCard.tsx
@src/src/components/chat/tools/WriteToolCard.tsx

<interfaces>
<!-- From Plan 01 -->
```typescript
// src/src/components/editor/loom-dark-theme.ts
export const loomDarkTheme: Extension[];
```

<!-- File store -->
```typescript
openFile(path: string): void
setActiveFile(path: string | null): void
```

<!-- UI store -->
```typescript
setActiveTab(tab: TabId): void
```

<!-- Backend diff endpoint -->
GET /api/git/file-with-diff?project=:projectName&file=:filePath
Response: { currentContent: string, oldContent: string, isDeleted: boolean, isUntracked: boolean }

<!-- react-codemirror-merge API -->
```typescript
import CodeMirrorMerge from 'react-codemirror-merge';
const Original = CodeMirrorMerge.Original;
const Modified = CodeMirrorMerge.Modified;
// Usage: <CodeMirrorMerge><Original value={old} /><Modified value={new} /></CodeMirrorMerge>
```

<!-- ReadToolCard file path rendering (from current ReadToolCard.tsx line ~110-112) -->
```tsx
<span className="font-[family-name:var(--font-mono)] text-xs text-[var(--text-muted)] truncate">
  {filePath}
</span>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DiffEditor and useFileDiff hook</name>
  <files>
    src/src/components/editor/DiffEditor.tsx
    src/src/hooks/useFileDiff.ts
  </files>
  <action>
1. Create `src/src/hooks/useFileDiff.ts`:
   - Hook: `useFileDiff(projectName: string, filePath: string | null)`.
   - Returns `{ oldContent: string | null, newContent: string | null, loading: boolean, error: string | null, isDeleted: boolean, isUntracked: boolean }`.
   - Fetch via `apiFetch<{ currentContent: string; oldContent: string; isDeleted: boolean; isUntracked: boolean }>(`/api/git/file-with-diff?project=${encodeURIComponent(projectName)}&file=${encodeURIComponent(filePath)}`)`.
   - Use `useEffect` with AbortController. Only fetch when filePath is non-null.
   - Map response: `oldContent` → `oldContent`, `currentContent` → `newContent`.

2. Create `src/src/components/editor/DiffEditor.tsx`:
   - Props: `{ oldContent: string, newContent: string, filePath: string }`.
   - Import `CodeMirrorMerge` from `react-codemirror-merge` and its `Original`/`Modified` sub-components.
   - Import `loomDarkTheme` from `./loom-dark-theme`.
   - Import `lineNumbers` from `@codemirror/view`, `search` from `@codemirror/search`.
   - Load language dynamically via `loadLanguageForFile` (same pattern as CodeEditor).
   - Build extensions for both sides: `[loomDarkTheme, lineNumbers(), search(), languageExtension]`.
   - Set both sides to `readOnly` (diffs are read-only).
   - Render `EditorBreadcrumb` above the merge view with `filePath` and a "Diff" badge.
   - Apply diff-specific token colors from tokens.css: `--diff-added-bg` and `--diff-removed-bg`. Style via CSS in editor.css:
     - `.cm-mergeView .cm-changedLine` with green/red backgrounds using the diff tokens.
   - Export as named export and default export (for potential lazy loading from git panel in Phase 26).
   - The DiffEditor is not wired into the main CodeEditor in this plan -- it will be activated by the git panel (Phase 26). This plan builds the component so it's ready. However, add a `diffMode` state to the file store or CodeEditor to support toggling between edit and diff mode in the future. For now, just export the component.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>DiffEditor renders side-by-side diff with OKLCH theme and proper diff colors. useFileDiff fetches diff data from git endpoint. Both compile clean and are ready for Phase 26 wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Make tool card file paths clickable to open in editor</name>
  <files>
    src/src/hooks/useOpenInEditor.ts
    src/src/components/chat/tools/ReadToolCard.tsx
    src/src/components/chat/tools/EditToolCard.tsx
    src/src/components/chat/tools/WriteToolCard.tsx
  </files>
  <action>
1. Create `src/src/hooks/useOpenInEditor.ts`:
   - Hook: `useOpenInEditor()`.
   - Returns `(filePath: string) => void`.
   - Inside: `const openFile = useFileStore((s) => s.openFile)` and `const setActiveTab = useUIStore((s) => s.setActiveTab)`.
   - The callback: `setActiveTab('files')` then `openFile(filePath)`.
   - Wrap in `useCallback` with stable deps.

2. Update `src/src/components/chat/tools/ReadToolCard.tsx`:
   - In `FileContentCard` (the shared component used by ReadToolCard and WriteToolCard), make the file path in the header clickable.
   - Import `useOpenInEditor` hook.
   - Change the file path `<span>` (line ~110-112) to a `<button>` with:
     - `onClick={() => openInEditor(filePath)}`
     - Add `hover:text-foreground hover:underline cursor-pointer transition-colors` classes
     - Keep existing classes: `font-[family-name:var(--font-mono)] text-xs text-[var(--text-muted)] truncate`
     - `type="button"` and `aria-label={`Open ${filePath} in editor`}`
   - This automatically covers both ReadToolCard and WriteToolCard since they both use FileContentCard.

3. Update `src/src/components/chat/tools/EditToolCard.tsx`:
   - EditToolCard has its own file path display (not using FileContentCard). Find the file path element and make it clickable using the same pattern:
     - Import `useOpenInEditor`.
     - Change the file path span/element to a button with click handler.
     - Same hover styling as above.

4. Do NOT modify the test files in this task -- the existing tool card tests mock the store and don't test click-to-open behavior. The hook itself is simple enough to not need a dedicated test file. If existing tests break due to the new hook import, add a mock for `useOpenInEditor` that returns a no-op function.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/chat/tools/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>File paths in Read, Edit, and Write tool cards are clickable buttons that switch to the Files tab and open the file in the editor. Existing tool card tests still pass.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd src && npx tsc --noEmit`
2. Tool card tests pass: `cd src && npx vitest run src/src/components/chat/tools/ --reporter=verbose`
3. Full suite passes: `cd src && npx vitest run --reporter=verbose`
4. Lint clean: `cd src && npx eslint src/src/components/editor/ src/src/hooks/useOpenInEditor.ts src/src/hooks/useFileDiff.ts src/src/components/chat/tools/ReadToolCard.tsx src/src/components/chat/tools/EditToolCard.tsx src/src/components/chat/tools/WriteToolCard.tsx --max-warnings 0`
</verification>

<success_criteria>
- DiffEditor renders side-by-side merge view with OKLCH theme and diff-colored backgrounds
- useFileDiff fetches diff data from backend git endpoint
- Clicking a file path in ReadToolCard switches to Files tab and opens file in editor
- Clicking a file path in EditToolCard switches to Files tab and opens file in editor
- Clicking a file path in WriteToolCard switches to Files tab and opens file in editor
- All existing tool card tests pass
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-code-editor/24-03-SUMMARY.md`
</output>
