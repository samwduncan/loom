---
phase: 20-content-layout-tab-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/types/ui.ts
  - src/src/types/file.ts
  - src/src/stores/ui.ts
  - src/src/stores/ui.test.ts
  - src/src/stores/file.ts
  - src/src/stores/file.test.ts
autonomous: true
requirements: [LAY-07, LAY-09]

must_haves:
  truths:
    - "TabId union type includes 'chat' | 'files' | 'shell' | 'git' and excludes old values"
    - "File store exists as importable skeleton with type contracts for future consumers"
    - "UI store persist migration handles version bump without hydration error"
  artifacts:
    - path: "src/src/types/ui.ts"
      provides: "Updated TabId union type"
      contains: "'chat' | 'files' | 'shell' | 'git'"
    - path: "src/src/types/file.ts"
      provides: "File store type definitions"
      exports: ["FileTab", "FileState", "FileActions", "FileStore"]
    - path: "src/src/stores/file.ts"
      provides: "5th Zustand store skeleton for file/editor state"
      exports: ["useFileStore"]
    - path: "src/src/stores/file.test.ts"
      provides: "File store minimal unit tests"
      min_lines: 15
    - path: "src/src/stores/ui.test.ts"
      provides: "Updated UI store tests with new TabId values"
      contains: "'files'"
  key_links:
    - from: "src/src/stores/file.ts"
      to: "src/src/types/file.ts"
      via: "import FileStore type"
      pattern: "import.*from.*types/file"
    - from: "src/src/stores/ui.ts"
      to: "src/src/types/ui.ts"
      via: "import TabId type"
      pattern: "import.*TabId.*from.*types/ui"
---

<objective>
Define the type contracts and stores that Phase 20's layout system depends on.

Purpose: Establish the data layer (TabId type change, file store skeleton) before building UI components. Plan 02 consumes these types/stores directly.
Output: Updated TabId union, new file store skeleton with type contracts, updated UI store persist migration, all tests green.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-content-layout-tab-system/20-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/src/types/ui.ts (CURRENT -- will be modified):
```typescript
export type SidebarState = 'expanded' | 'collapsed-hidden';
export type TabId = 'chat' | 'dashboard' | 'settings';
export interface ModalState { type: string; props: Record<string, unknown>; }
export interface CompanionState { isVisible: boolean; animation: CompanionAnimation; }
export interface ThemeConfig { fontSize: number; density: 'compact' | 'comfortable' | 'spacious'; }
```

From src/src/stores/ui.ts (CURRENT -- persist config to update):
```typescript
// Persist version is 2, partialize includes theme/sidebarCollapsed/thinkingExpanded
// activeTab is NOT persisted (good -- keep it that way)
// migrate handles version < 2 only -- needs version 3 migration for stale TabId
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update TabId type + UI store migration + update existing tests</name>
  <files>src/src/types/ui.ts, src/src/stores/ui.ts, src/src/stores/ui.test.ts</files>
  <behavior>
    - Test: setActiveTab('files') sets activeTab to 'files'
    - Test: setActiveTab('shell') sets activeTab to 'shell'
    - Test: setActiveTab('git') sets activeTab to 'git'
    - Test: reset() returns activeTab to 'chat'
    - Test: Old test values ('settings', 'dashboard') replaced with new TabId values
  </behavior>
  <action>
    1. In `src/src/types/ui.ts`: Change TabId from `'chat' | 'dashboard' | 'settings'` to `'chat' | 'files' | 'shell' | 'git'`. Leave all other types unchanged.

    2. In `src/src/stores/ui.ts`: Increment persist version to 3. The migration for version < 3 should just return `state` unchanged -- activeTab is NOT persisted, so there are no stale values to fix. Add a comment: `// v3: TabId changed from 'chat'|'dashboard'|'settings' to 'chat'|'files'|'shell'|'git' — no persisted fields affected`. Do NOT add `activeTab: 'chat'` to the migration body -- that would be dead code since activeTab is excluded from partialize. Keep activeTab OUT of partialize.

    3. In `src/src/stores/ui.test.ts`: Update the existing `setActiveTab` test to use new TabId values ('files', 'shell', 'git' instead of 'settings', 'dashboard'). Update the reset test's setActiveTab call to use a new valid value. Add a test confirming all four TabId values are accepted.

    Run tests: `cd /home/swd/loom/src && npx vitest run src/stores/ui.test.ts -x`
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/stores/ui.test.ts -x</automated>
  </verify>
  <done>TabId is 'chat' | 'files' | 'shell' | 'git'. UI store persist version is 3 with no-op migration (comment explains why). All existing UI store tests pass with updated TabId values. TypeScript compiles with no errors on these files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create file store types + minimal store skeleton + tests</name>
  <files>src/src/types/file.ts, src/src/stores/file.ts, src/src/stores/file.test.ts</files>
  <behavior>
    - Test: store exists and is importable (useFileStore is a function)
    - Test: reset() returns all state to initial values (empty openTabs, null activeFilePath, empty expandedDirs, null selectedPath)
  </behavior>
  <action>
    1. Create `src/src/types/file.ts` with:
       - `FileTab` interface: `{ filePath: string; isDirty: boolean }`
       - `FileState` interface with data fields: `expandedDirs: string[]` (NOT Set -- avoids JSON serialization pitfall from research), `selectedPath: string | null`, `openTabs: FileTab[]`, `activeFilePath: string | null`
       - `FileActions` interface with actions: `toggleDir(path: string): void`, `selectPath(path: string | null): void`, `openFile(path: string): void`, `closeFile(path: string): void`, `setDirty(path: string, isDirty: boolean): void`, `setActiveFile(path: string | null): void`, `reset(): void`
       - Export `FileStore = FileState & FileActions`

    2. Create `src/src/stores/file.ts`:
       - `create<FileStore>()` -- NO persist middleware (file state is ephemeral per session)
       - Implement ONLY the `reset()` action (returns to initial empty state)
       - All other actions (`openFile`, `closeFile`, `setDirty`, `toggleDir`, `selectPath`, `setActiveFile`) should be no-ops: `openFile: () => {}` etc. Add a comment above the no-ops: `// Stub actions — full implementation deferred to Phase 23 when consumers exist`
       - Export `useFileStore`

    3. Create `src/src/stores/file.test.ts` with 2 tests:
       - "useFileStore is importable and returns a store": call `useFileStore.getState()`, assert it has `openTabs` (array), `activeFilePath` (null), `expandedDirs` (array), `selectedPath` (null)
       - "reset() returns to initial state": mutate state via setState, call reset(), verify all fields are back to initial values
       - Use Zustand getState()/setState() pattern consistent with ui.test.ts. Call reset() in beforeEach.

    Run tests: `cd /home/swd/loom/src && npx vitest run src/stores/file.test.ts -x`
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/stores/file.test.ts -x</automated>
  </verify>
  <done>File store skeleton exists at src/src/stores/file.ts with useFileStore export. Full type contracts at src/src/types/file.ts (FileTab, FileState, FileActions, FileStore). 2 minimal tests pass. Actions are stubs -- full implementation deferred to Phase 23. Store uses string[] for expandedDirs (not Set). No persist middleware.</done>
</task>

</tasks>

<verification>
cd /home/swd/loom/src && npx vitest run src/stores/ui.test.ts src/stores/file.test.ts --reporter=verbose
cd /home/swd/loom/src && npx tsc --noEmit
</verification>

<success_criteria>
- TabId type is `'chat' | 'files' | 'shell' | 'git'` (old values removed)
- UI store persist version is 3 with no-op migration (activeTab not persisted)
- File store skeleton exists with type contracts and reset() action
- All other file store actions are stubs (deferred to Phase 23)
- All store tests green (UI + file)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/20-content-layout-tab-system/20-01-SUMMARY.md`
</output>
