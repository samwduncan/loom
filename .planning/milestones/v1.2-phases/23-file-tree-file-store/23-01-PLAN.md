---
phase: 23-file-tree-file-store
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/stores/file.ts
  - src/src/types/file.ts
  - src/src/hooks/useFileTree.ts
  - src/src/components/file-tree/FileIcon.tsx
  - src/src/components/file-tree/FileTreePanel.tsx
  - src/src/components/file-tree/file-tree.css
  - src/src/stores/file.test.ts
  - src/src/hooks/useFileTree.test.ts
  - src/src/components/file-tree/FileIcon.test.tsx
  - src/src/components/file-tree/FileTreePanel.test.tsx
autonomous: true
requirements:
  - FT-01
  - FT-03
  - FT-04
  - FT-05
  - FT-14
  - FT-16

must_haves:
  truths:
    - "File store actions (toggleDir, openFile, closeFile, setDirty, setActiveFile, selectPath) work correctly with proper state transitions"
    - "useFileTree hook fetches tree data from backend API and re-fetches on loom:projects-updated CustomEvent"
    - "FileIcon returns correct lucide-react icon for each file extension and folder state"
    - "FileTreePanel renders a split layout with tree sidebar (~240px) and editor placeholder"
  artifacts:
    - path: "src/src/stores/file.ts"
      provides: "Implemented file store actions (no more stubs)"
      exports: ["useFileStore"]
    - path: "src/src/types/file.ts"
      provides: "FileTreeNode type matching backend response shape"
      contains: "FileTreeNode"
    - path: "src/src/hooks/useFileTree.ts"
      provides: "File tree data fetching + WebSocket refresh"
      exports: ["useFileTree"]
    - path: "src/src/components/file-tree/FileIcon.tsx"
      provides: "Extension-to-icon mapping"
      exports: ["FileIcon", "getFileIcon"]
    - path: "src/src/components/file-tree/FileTreePanel.tsx"
      provides: "Split layout panel (tree left, editor placeholder right)"
      exports: ["FileTreePanel"]
  key_links:
    - from: "src/src/hooks/useFileTree.ts"
      to: "/api/projects/:projectName/files"
      via: "apiFetch in useEffect"
      pattern: "apiFetch.*api/projects.*files"
    - from: "src/src/hooks/useFileTree.ts"
      to: "window loom:projects-updated"
      via: "addEventListener in useEffect"
      pattern: "loom:projects-updated"
---

<objective>
Implement file store actions, backend data hook, file icon mapping, and the FileTreePanel split layout shell.

Purpose: Establish the foundation layer -- types, store, data fetching, and layout -- that all subsequent file tree components build on. Replaces stub store actions with real implementations.
Output: Working file store, useFileTree hook that fetches from backend and refreshes on WebSocket events, FileIcon component, and FileTreePanel with 240px tree sidebar + editor placeholder.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-file-tree-file-store/23-RESEARCH.md

@src/src/stores/file.ts
@src/src/types/file.ts
@src/src/hooks/useProjectContext.ts
@src/src/lib/api-client.ts
@src/src/components/content-area/view/ContentArea.tsx

<interfaces>
<!-- Existing types the executor needs -->

From src/src/types/file.ts:
```typescript
export interface FileTab {
  filePath: string;
  isDirty: boolean;
}

export interface FileState {
  expandedDirs: string[];
  selectedPath: string | null;
  openTabs: FileTab[];
  activeFilePath: string | null;
}

export interface FileActions {
  toggleDir: (path: string) => void;
  selectPath: (path: string | null) => void;
  openFile: (path: string) => void;
  closeFile: (path: string) => void;
  setDirty: (path: string, isDirty: boolean) => void;
  setActiveFile: (path: string | null) => void;
  reset: () => void;
}

export type FileStore = FileState & FileActions;
```

From src/src/hooks/useProjectContext.ts:
```typescript
export function useProjectContext(): { projectName: string; isLoading: boolean };
```

Backend response shape (from server/index.js):
```typescript
interface BackendFileNode {
  name: string;           // "src"
  path: string;           // "/home/swd/loom/src" (absolute)
  type: 'file' | 'directory';
  size: number;
  modified: string | null;
  permissions: string;
  permissionsRwx: string;
  children?: BackendFileNode[];
}
// GET /api/projects/:projectName/files returns BackendFileNode[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: File store implementation + FileTreeNode type</name>
  <files>src/src/types/file.ts, src/src/stores/file.ts, src/src/stores/file.test.ts</files>
  <behavior>
    - toggleDir: adds path to expandedDirs if not present, removes if present
    - selectPath: sets selectedPath to the given path (or null)
    - openFile: sets activeFilePath, adds to openTabs if not already open, does not duplicate existing tab
    - closeFile: removes tab from openTabs, if closing active file switches activeFilePath to last remaining tab (or null)
    - setDirty: updates isDirty on the matching tab by filePath
    - setActiveFile: sets activeFilePath to given path (or null)
    - reset: restores all state to initial values
  </behavior>
  <action>
1. Add `FileTreeNode` interface to `src/src/types/file.ts` matching the backend response shape: `name: string`, `path: string`, `type: 'file' | 'directory'`, `size: number`, `modified: string | null`, `children?: FileTreeNode[]`. Keep existing types unchanged.

2. Replace ALL stub actions in `src/src/stores/file.ts` with real implementations:
   - `toggleDir(path)`: toggle path in expandedDirs array (add if absent, filter out if present)
   - `selectPath(path)`: set selectedPath
   - `openFile(path)`: set activeFilePath and selectedPath to path; if no tab exists for path, append `{ filePath: path, isDirty: false }` to openTabs
   - `closeFile(path)`: filter tab out of openTabs; if closing the active file, set activeFilePath to last remaining tab's filePath (or null if empty)
   - `setDirty(path, isDirty)`: map over openTabs, update matching tab's isDirty
   - `setActiveFile(path)`: set activeFilePath

3. Write tests in `src/src/stores/file.test.ts` covering all behaviors listed above. Use `useFileStore.getState()` and `useFileStore.setState()` for direct store testing (no component rendering needed). Reset store between tests with `useFileStore.getState().reset()`.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/stores/file.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 7 store actions implemented (no stubs remain). FileTreeNode type exported. All store tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useFileTree hook + FileIcon + FileTreePanel layout</name>
  <files>src/src/hooks/useFileTree.ts, src/src/hooks/useFileTree.test.ts, src/src/components/file-tree/FileIcon.tsx, src/src/components/file-tree/FileIcon.test.tsx, src/src/components/file-tree/FileTreePanel.tsx, src/src/components/file-tree/FileTreePanel.test.tsx, src/src/components/file-tree/file-tree.css</files>
  <behavior>
    - useFileTree: returns { tree, fetchState, retry, projectRoot }; fetchState transitions idle -> loading -> success (or error); re-fetches on loom:projects-updated event; retry resets to loading and re-fetches
    - FileIcon: renders correct icon for directories (Folder/FolderOpen), .ts/.tsx/.js/.jsx (FileCode), .json (FileJson), .css/.md (FileText), images (FileImage), config files (Cog), unknown (File)
    - FileTreePanel: renders flex container with 240px tree sidebar (border-right) and flex-1 editor placeholder showing "Select a file to view"
  </behavior>
  <action>
1. Create `src/src/hooks/useFileTree.ts`:
   - Accept `projectName: string` parameter
   - State: `tree: FileTreeNode[]`, `fetchState: 'idle' | 'loading' | 'error' | 'success'`, `projectRoot: string | null`
   - On mount (and when projectName changes): fetch `GET /api/projects/${encodeURIComponent(projectName)}/files` via `apiFetch`
   - On success, set tree data and derive projectRoot from tree (common parent: take any top-level entry's path, strip last segment). On error, set fetchState to 'error'
   - Listen for `loom:projects-updated` CustomEvent on window -- re-fetch when fired
   - Expose `retry` function that resets fetchState to 'loading' and re-fetches
   - Guard against unmount with ref (don't setState after unmount)
   - Return early with idle state if projectName is empty

2. Write tests in `src/src/hooks/useFileTree.test.ts`:
   - Mock `apiFetch` via vi.mock('@/lib/api-client')
   - Test: initial fetch sets loading then success with tree data
   - Test: fetch error sets fetchState to 'error'
   - Test: retry triggers re-fetch
   - Test: dispatching `loom:projects-updated` CustomEvent triggers re-fetch
   - Test: empty projectName returns idle state without fetching

3. Create `src/src/components/file-tree/FileIcon.tsx`:
   - Export `getFileIcon(name: string, isDirectory: boolean, isExpanded: boolean): React.ElementType` function
   - Export `FileIcon` component that takes `{ name, isDirectory, isExpanded?, className? }` props and renders the icon
   - Icon map: directories -> Folder/FolderOpen; .ts/.tsx/.js/.jsx -> FileCode; .json -> FileJson; .css/.md/.txt -> FileText; .png/.jpg/.jpeg/.gif/.svg/.webp -> FileImage; .yaml/.yml/.toml/.env -> Cog; default -> File (all from lucide-react)
   - Size: 16px default

4. Write tests in `src/src/components/file-tree/FileIcon.test.tsx`:
   - Test getFileIcon returns correct component for each extension category
   - Test directory open vs closed icons differ

5. Create `src/src/components/file-tree/FileTreePanel.tsx`:
   - Flex container (`flex h-full`)
   - Left: `w-60 shrink-0 border-r border-[var(--color-surface-border)]` div with flex-col layout. For now, render a placeholder div saying "File tree loading..." (the actual FileTree component comes in Plan 02)
   - Right: `flex-1 min-w-0 flex items-center justify-center` div with "Select a file to view" text in text-[var(--color-text-tertiary)]
   - Add a manual refresh button (lucide RefreshCw icon) in the tree header area per research pitfall 3

6. Write tests in `src/src/components/file-tree/FileTreePanel.test.tsx`:
   - Test: renders split layout with tree sidebar and editor placeholder
   - Test: editor placeholder shows "Select a file to view"

7. Create `src/src/components/file-tree/file-tree.css`:
   - Tree indentation: `.file-node { padding-left: calc(var(--depth, 0) * 12px); }` using CSS custom property
   - Hover state: `.file-node:hover { background: var(--color-surface-hover); }`
   - Active state: `.file-node-active { background: var(--color-surface-raised); }`
   - Transition: `background 100ms ease`
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/hooks/useFileTree.test.ts src/src/components/file-tree/FileIcon.test.tsx src/src/components/file-tree/FileTreePanel.test.tsx --reporter=verbose</automated>
  </verify>
  <done>useFileTree hook fetches tree data and re-fetches on WebSocket events. FileIcon maps 20+ extensions to correct icons. FileTreePanel renders 240px/flex-1 split layout. All tests pass.</done>
</task>

</tasks>

<verification>
cd /home/swd/loom/src && npx vitest run --reporter=verbose
</verification>

<success_criteria>
- File store has zero stub actions remaining -- all 7 actions are implemented
- FileTreeNode type exists in types/file.ts matching backend response shape
- useFileTree hook fetches from /api/projects/:name/files and re-fetches on loom:projects-updated
- FileIcon renders correct lucide-react icon for 20+ file extensions
- FileTreePanel renders split layout (240px tree sidebar + editor placeholder)
- All new tests pass, existing test suite unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/23-file-tree-file-store/23-01-SUMMARY.md`
</output>
