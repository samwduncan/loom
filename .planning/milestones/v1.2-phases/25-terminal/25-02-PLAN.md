---
phase: 25-terminal
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/src/components/terminal/TerminalView.tsx
  - src/src/components/terminal/TerminalView.test.tsx
  - src/src/components/terminal/TerminalHeader.tsx
  - src/src/components/terminal/TerminalHeader.test.tsx
  - src/src/components/terminal/TerminalOverlay.tsx
  - src/src/components/terminal/TerminalOverlay.test.tsx
  - src/src/components/terminal/TerminalPanel.tsx
  - src/src/components/terminal/styles/terminal.css
  - src/src/components/content-area/view/ContentArea.tsx
autonomous: true
requirements: [TERM-01, TERM-03, TERM-04, TERM-05, TERM-09, TERM-10, TERM-11, TERM-13, TERM-15]

must_haves:
  truths:
    - "User sees a working terminal in the Shell tab with ANSI color support"
    - "Terminal auto-resizes when browser window or panel dimensions change"
    - "URLs in terminal output are clickable"
    - "Connection state indicator shows connecting/connected/disconnected in header"
    - "Restart and Disconnect buttons work in header"
    - "Disconnected state shows overlay with reconnect button"
    - "Terminal is lazy-loaded and not in initial bundle"
    - "Terminal stays mounted when switching tabs (CSS show/hide)"
    - "Copy/paste works via Cmd+C/V"
  artifacts:
    - path: "src/src/components/terminal/TerminalView.tsx"
      provides: "xterm.js instance management with ref-based lifecycle"
      exports: ["TerminalView"]
    - path: "src/src/components/terminal/TerminalHeader.tsx"
      provides: "Connection indicator, mode label, restart/disconnect buttons"
      exports: ["TerminalHeader"]
    - path: "src/src/components/terminal/TerminalOverlay.tsx"
      provides: "Disconnected state overlay with reconnect button"
      exports: ["TerminalOverlay"]
    - path: "src/src/components/terminal/TerminalPanel.tsx"
      provides: "Outer shell composing header + view + overlay"
      exports: ["TerminalPanel"]
  key_links:
    - from: "src/src/components/terminal/TerminalView.tsx"
      to: "src/src/hooks/useShellWebSocket.ts"
      via: "Hook provides sendInput/sendResize, view wires xterm.onData -> sendInput"
      pattern: "useShellWebSocket|sendInput|onData"
    - from: "src/src/components/terminal/TerminalView.tsx"
      to: "@xterm/xterm"
      via: "Terminal instance in ref, open() on container ref"
      pattern: "new Terminal|terminal\\.open"
    - from: "src/src/components/content-area/view/ContentArea.tsx"
      to: "src/src/components/terminal/TerminalPanel.tsx"
      via: "React.lazy() import replacing PanelPlaceholder for shell tab"
      pattern: "LazyTerminalPanel|lazy.*TerminalPanel"
    - from: "src/src/components/terminal/TerminalPanel.tsx"
      to: "src/src/components/terminal/TerminalView.tsx"
      via: "Composition: Panel renders Header + View + Overlay"
      pattern: "TerminalHeader|TerminalView|TerminalOverlay"
---

<objective>
Build the terminal UI components (TerminalView, TerminalHeader, TerminalOverlay, TerminalPanel), wire xterm.js with the WebSocket hook, and integrate into ContentArea as a lazy-loaded panel.

Purpose: Delivers the complete terminal experience -- user can open Shell tab, see a working terminal, run commands, see connection state, and restart/reconnect.
Output: 4 terminal components, terminal CSS, ContentArea wiring, all tests.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-terminal/25-RESEARCH.md
@.planning/phases/25-terminal/25-01-SUMMARY.md

@src/src/components/content-area/view/ContentArea.tsx
@src/src/components/content-area/view/PanelPlaceholder.tsx
@src/src/hooks/useProjectContext.ts
@src/src/stores/ui.ts
@src/src/styles/tokens.css

<interfaces>
<!-- From Plan 01 outputs (read from SUMMARY) -->

From src/src/types/shell.ts:
```typescript
export type ShellConnectionState = 'disconnected' | 'connecting' | 'connected';

export type ShellClientMessage =
  | { type: 'init'; projectPath: string; provider: string; isPlainShell: boolean; cols: number; rows: number }
  | { type: 'input'; data: string }
  | { type: 'resize'; cols: number; rows: number };

export type ShellServerMessage =
  | { type: 'output'; data: string }
  | { type: 'auth_url'; url: string; autoOpen: boolean };
```

From src/src/components/terminal/terminal-theme.ts:
```typescript
export function getTerminalTheme(): ITheme;
```

From src/src/hooks/useShellWebSocket.ts:
```typescript
export function useShellWebSocket(params: { projectPath: string; isPlainShell?: boolean }): {
  state: ShellConnectionState;
  sendInput: (data: string) => void;
  sendResize: (cols: number, rows: number) => void;
  connect: (cols: number, rows: number) => void;
  disconnect: () => void;
  restart: () => void;
  authUrl: { url: string; autoOpen: boolean } | null;
};
```

From src/src/hooks/useProjectContext.ts:
```typescript
export function useProjectContext(): { projectName: string; isLoading: boolean };
```

From src/src/components/content-area/view/ContentArea.tsx:
```typescript
// Current shell panel entry in panels useMemo:
{ id: 'shell', content: <PanelPlaceholder name="Shell" icon={Terminal} /> }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TerminalView -- xterm.js instance with resize and WebSocket wiring</name>
  <files>src/src/components/terminal/TerminalView.tsx, src/src/components/terminal/TerminalView.test.tsx, src/src/components/terminal/styles/terminal.css</files>
  <behavior>
    - Renders a div container for xterm.js to attach to
    - Creates Terminal instance in ref on mount, calls terminal.open(container)
    - Loads FitAddon and WebLinksAddon
    - Calls fitAddon.fit() after open, then connect(cols, rows) to start WS
    - Wires terminal.onData -> sendInput (keystrokes to backend)
    - Wires WS onOutput -> terminal.write (backend output to screen)
    - ResizeObserver on container calls debounced fitAddon.fit() + sendResize
    - Only fits when container is visible (offsetParent !== null) -- prevents 0-col issue when hidden
    - Fits on re-show (when activeTab changes to 'shell')
    - Disposes terminal and observer on unmount
  </behavior>
  <action>
1. Create `src/src/components/terminal/styles/terminal.css`:
   - Import `@xterm/xterm/css/xterm.css` at the top.
   - Style `.terminal-container` with `height: 100%; width: 100%; overflow: hidden;` and design token background (`var(--surface-base)`).
   - Override `.xterm` viewport padding if needed for consistency.

2. Create `src/src/components/terminal/TerminalView.tsx`:
   - Props: `{ onOutput?: (data: string) => void }` (optional callback for auth URL banner -- parent TerminalPanel handles this via the hook).
   - Actually, TerminalView receives the WS hook results as props from TerminalPanel: `{ wsState, sendInput, sendResize, connect, writeData }`. The parent manages the hook and passes down what TerminalView needs. This avoids TerminalView needing project context.
   - Wait -- simpler: TerminalView receives the full hook return plus a `writeRef` pattern. Parent calls `useShellWebSocket`, passes a `writeToTerminal` callback ref that TerminalView populates with `terminal.write`. When WS receives output, parent calls `writeRef.current(data)`.
   - REVISED APPROACH: TerminalPanel owns useShellWebSocket. TerminalView takes props: `{ onData: (data: string) => void; onResize: (cols: number, rows: number) => void; onReady: (write: (data: string) => void, cols: number, rows: number) => void }`.
     - `onData`: called when user types (terminal.onData) -- parent routes to sendInput
     - `onResize`: called when terminal resizes -- parent routes to sendResize
     - `onReady`: called after terminal.open() + fitAddon.fit() with the write function and initial cols/rows -- parent uses this to connect WS and wire output
   - Refs: `terminalRef` (Terminal instance), `containerRef` (div), `fitAddonRef` (FitAddon).
   - useEffect mount: create Terminal with options (fontSize 14, fontFamily from ThemeConfig `codeFontFamily` via UI store, cursorBlink true, scrollback 5000, theme from getTerminalTheme()). Load FitAddon + WebLinksAddon. Call terminal.open(container). Call fitAddon.fit(). Wire terminal.onData -> props.onData. Call props.onReady(terminal.write.bind(terminal), terminal.cols, terminal.rows).
   - ResizeObserver on container: debounce 100ms, check `containerRef.current.offsetParent !== null` before calling fitAddon.fit(), then call props.onResize(terminal.cols, terminal.rows).
   - Also re-fit when the Shell tab becomes active. Use useUIStore activeTab selector -- useEffect on activeTab === 'shell' calls fitAddon.fit() after a 0ms setTimeout (allows browser to compute layout after display change).
   - Cleanup: dispose terminal, disconnect observer.
   - Import terminal.css.

3. Create `src/src/components/terminal/TerminalView.test.tsx`:
   - Mock `@xterm/xterm` Terminal class: open, write, dispose, onData (returns disposable), loadAddon, cols=80, rows=24, options={}.
   - Mock `@xterm/addon-fit` FitAddon: fit stub.
   - Mock `@xterm/addon-web-links` WebLinksAddon: constructor stub.
   - Mock `getTerminalTheme` to return a simple theme object.
   - Mock UI store for codeFontFamily.
   - Test: component renders container div.
   - Test: Terminal is created and opened on container.
   - Test: FitAddon and WebLinksAddon are loaded.
   - Test: onData callback wired to terminal.onData.
   - Test: onReady called with write function, cols, rows.
   - Test: cleanup disposes terminal.
  </action>
  <verify>
    <automated>cd src && npx vitest run src/components/terminal/TerminalView.test.tsx -x</automated>
  </verify>
  <done>TerminalView renders xterm.js with addons, wires input/output/resize callbacks, handles visibility-aware fitting, and cleans up on unmount. Tests pass with mocked xterm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TerminalHeader, TerminalOverlay, TerminalPanel, and ContentArea wiring</name>
  <files>src/src/components/terminal/TerminalHeader.tsx, src/src/components/terminal/TerminalHeader.test.tsx, src/src/components/terminal/TerminalOverlay.tsx, src/src/components/terminal/TerminalOverlay.test.tsx, src/src/components/terminal/TerminalPanel.tsx, src/src/components/content-area/view/ContentArea.tsx</files>
  <behavior>
    - TerminalHeader shows connection state dot (yellow pulse for connecting, green for connected, red for disconnected)
    - TerminalHeader shows "Shell" label and "Plain Shell" mode indicator
    - TerminalHeader has Restart button (RotateCw icon) and Disconnect button (Unplug icon) with tooltips
    - Restart calls restart(), Disconnect calls disconnect()
    - TerminalOverlay renders absolute-positioned overlay when state is 'disconnected'
    - Overlay shows "Session ended" message and Reconnect button
    - Reconnect button calls restart()
    - Auth URL banner renders when authUrl is non-null, clickable, dismissible
    - TerminalPanel composes: TerminalHeader + TerminalView + TerminalOverlay
    - TerminalPanel owns useShellWebSocket + useProjectContext, passes props down
    - ContentArea replaces shell PanelPlaceholder with lazy-loaded TerminalPanel
  </behavior>
  <action>
1. Create `src/src/components/terminal/TerminalHeader.tsx`:
   - Props: `{ state: ShellConnectionState; onRestart: () => void; onDisconnect: () => void }`.
   - Renders a horizontal bar: terminal icon (Terminal from lucide), "Shell" label, connection dot, mode label "Plain Shell", spacer, Restart button (RotateCw), Disconnect button (Unplug).
   - Connection dot: `div` with rounded-full, sized w-2 h-2. Colors via design tokens:
     - connecting: `bg-[var(--status-warning)]` + animate-pulse
     - connected: `bg-[var(--status-success)]`
     - disconnected: `bg-[var(--status-error)]`
   - Buttons: icon buttons with `title` attribute for tooltip. Disabled when state is 'disconnected' (Disconnect) or 'connecting' (both).
   - Style: `h-10 px-3 flex items-center gap-2 border-b border-[var(--border-default)]` with surface-raised background.
   - Constitution: cn() for classes, design tokens only, named export.

2. Create `src/src/components/terminal/TerminalHeader.test.tsx`:
   - Test: renders "Shell" label.
   - Test: renders green dot when state='connected'.
   - Test: renders yellow pulsing dot when state='connecting'.
   - Test: renders red dot when state='disconnected'.
   - Test: Restart button calls onRestart.
   - Test: Disconnect button calls onDisconnect.

3. Create `src/src/components/terminal/TerminalOverlay.tsx`:
   - Props: `{ visible: boolean; onReconnect: () => void; authUrl: { url: string; autoOpen: boolean } | null; onDismissAuth: () => void }`.
   - When `visible`: absolute overlay over terminal area. Semi-transparent backdrop (`bg-[var(--surface-base)]/80`). Centered content: "Session ended" text + Reconnect button (styled like a primary action with accent color).
   - When `authUrl` is non-null (regardless of visible): show a dismissible banner at top of terminal area with clickable URL and dismiss X button. If `autoOpen`, call `window.open(url, '_blank')` via useEffect.
   - Constitution: cn(), tokens only, named export.

4. Create `src/src/components/terminal/TerminalOverlay.test.tsx`:
   - Test: not rendered when visible=false and authUrl=null.
   - Test: shows "Session ended" and Reconnect button when visible=true.
   - Test: Reconnect button calls onReconnect.
   - Test: auth URL banner renders when authUrl is set.
   - Test: clicking auth URL link opens in new tab.
   - Test: dismiss button calls onDismissAuth.

5. Create `src/src/components/terminal/TerminalPanel.tsx`:
   - Owns `useProjectContext()` for projectPath (uses projectName as the path since that's what the backend expects -- verify against ChatComposer which sends `projectPath: projectName`).
   - Owns `useShellWebSocket({ projectPath: projectName })`.
   - Manages a `writeRef = useRef<((data: string) => void) | null>(null)`.
   - Wires WS output to terminal: when WS sends output, write to terminal via writeRef. This needs the hook to accept an onOutput callback, OR the panel polls -- better approach: the hook exposes a way to register an output handler. Simplest: pass `onOutput` to connect(), or the hook calls a ref-based callback.
   - APPROACH: The `useShellWebSocket` hook from Plan 01 should accept an `onOutput` callback in its params (or via a ref). The panel sets `onOutput` to `writeRef.current`. This is cleanest -- the hook calls onOutput whenever it gets output data.
   - Actually, looking at Plan 01's hook design: the hook internally manages the ShellWebSocketClient. Add an `onOutput` param to the hook: `useShellWebSocket({ projectPath, isPlainShell, onOutput })`. The hook wires `client.onOutput = onOutput` (via a ref so it doesn't cause reconnects on callback identity change).
   - NOTE TO EXECUTOR: If Plan 01's hook doesn't accept onOutput, add it. The hook should accept `onOutput: (data: string) => void` as a stable-ref callback.
   - `onReady` callback from TerminalView: saves write function to writeRef, then calls `connect(cols, rows)`.
   - `onData` from TerminalView: calls `sendInput(data)`.
   - `onResize` from TerminalView: calls `sendResize(cols, rows)`.
   - Auth URL: pass `authUrl` to TerminalOverlay. `onDismissAuth` clears it (if hook exposes a dismiss, use that; otherwise local state).
   - Renders: flex-col h-full. TerminalHeader at top. Relative div flex-1 containing TerminalView + TerminalOverlay.
   - Named export `TerminalPanel`.

6. Update `src/src/components/content-area/view/ContentArea.tsx`:
   - Add `lazy` and `Suspense` imports from React.
   - Create `LazyTerminalPanel` using the same pattern as LazyCodeEditor in FileTreePanel:
     ```typescript
     const LazyTerminalPanel = lazy(() =>
       import('@/components/terminal/TerminalPanel').then((mod) => ({
         default: mod.TerminalPanel,
       })),
     );
     ```
   - Create a `TerminalSkeleton` function component: simple div with `h-full flex items-center justify-center text-[var(--text-muted)]` and "Loading terminal..." text.
   - Replace the shell panel entry in the `panels` useMemo:
     FROM: `{ id: 'shell', content: <PanelPlaceholder name="Shell" icon={Terminal} /> }`
     TO: `{ id: 'shell', content: <Suspense fallback={<TerminalSkeleton />}><LazyTerminalPanel /></Suspense> }`
   - Remove the `Terminal` import from lucide-react if it's no longer used (check if Git placeholder still uses it -- it doesn't, Git uses GitBranch). Remove the unused import.
   - Keep the `PanelPlaceholder` import for the Git tab placeholder.
  </action>
  <verify>
    <automated>cd src && npx vitest run src/components/terminal/TerminalHeader.test.tsx src/components/terminal/TerminalOverlay.test.tsx --reporter=verbose</automated>
  </verify>
  <done>TerminalHeader shows connection state with colored dots, restart/disconnect buttons. TerminalOverlay shows disconnected overlay and auth URL banners. TerminalPanel composes all pieces with WS hook. ContentArea lazy-loads TerminalPanel replacing PanelPlaceholder. All tests pass.</done>
</task>

</tasks>

<verification>
Run all terminal tests:
```bash
cd src && npx vitest run src/components/terminal/ --reporter=verbose
```

Run ContentArea tests to verify no regressions:
```bash
cd src && npx vitest run src/components/content-area/ --reporter=verbose
```

Full suite:
```bash
cd src && npx vitest run
```

Build check (lazy loading produces correct chunks):
```bash
cd src && npx tsc --noEmit
```
</verification>

<success_criteria>
- Shell tab shows a functional xterm.js terminal with ANSI color support and OKLCH theme
- Terminal auto-resizes on window/panel resize, re-fits on tab switch
- URLs in terminal output are clickable (WebLinksAddon loaded)
- Header shows connection state dot (yellow/green/red) and Plain Shell label
- Restart button reconnects, Disconnect button closes WS
- Disconnected state shows overlay with Reconnect button and "Session ended"
- Auth URL banner shows when backend sends auth_url message
- Terminal is lazy-loaded via React.lazy (not in initial bundle)
- Terminal stays mounted on tab switch (CSS show/hide preserved)
- Copy/paste works via Cmd+C/V (xterm.js native)
- All new tests pass, no regressions in existing suite
</success_criteria>

<output>
After completion, create `.planning/phases/25-terminal/25-02-SUMMARY.md`
</output>
