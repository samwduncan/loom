---
phase: 42-session-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/types/session.ts
  - src/src/lib/sessionGrouping.ts
  - src/src/lib/sessionGrouping.test.ts
  - src/src/hooks/useSessionSearch.ts
  - src/src/hooks/useSessionSearch.test.ts
  - src/src/hooks/useSessionPins.ts
  - src/src/hooks/useSessionPins.test.ts
  - src/src/components/sidebar/SearchInput.tsx
autonomous: true
requirements:
  - SESS-07
  - SESS-08

must_haves:
  truths:
    - "filterProjectGroups returns only projects/date-groups containing title matches"
    - "filterProjectGroups returns all groups unmodified when query is empty"
    - "useSessionPins persists pin set to localStorage and survives page reload"
    - "groupSessionsByProject hoists pinned sessions into a Pinned pseudo-date-group at the top of each project"
    - "SearchInput renders an accessible text input with clear button"
  artifacts:
    - path: "src/src/hooks/useSessionSearch.ts"
      provides: "filterProjectGroups pure function + useSessionSearch hook with query state"
      exports: ["filterProjectGroups", "useSessionSearch"]
    - path: "src/src/hooks/useSessionPins.ts"
      provides: "localStorage-backed pin management hook"
      exports: ["useSessionPins"]
    - path: "src/src/lib/sessionGrouping.ts"
      provides: "Updated groupSessionsByProject accepting pinnedIds parameter"
      exports: ["groupSessionsByProject", "isJunkSession"]
    - path: "src/src/types/session.ts"
      provides: "SessionDateGroup union extended with Pinned"
      contains: "Pinned"
    - path: "src/src/components/sidebar/SearchInput.tsx"
      provides: "Controlled search input component"
      exports: ["SearchInput"]
  key_links:
    - from: "src/src/hooks/useSessionSearch.ts"
      to: "src/src/types/session.ts"
      via: "ProjectGroup type import for filtering"
      pattern: "import.*ProjectGroup.*from.*session"
    - from: "src/src/lib/sessionGrouping.ts"
      to: "src/src/types/session.ts"
      via: "SessionDateGroup union includes Pinned"
      pattern: "'Pinned'"
---

<objective>
Build the data layer for session search and pinning: pure filtering function, localStorage pin hook, grouping function updated to hoist pinned sessions, and a SearchInput presentational component.

Purpose: Establishes all the logic and state management that Plan 02 will wire into the SessionList UI.
Output: 3 new files (useSessionSearch, useSessionPins, SearchInput), 2 modified files (sessionGrouping, session types), plus tests.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-session-discovery/42-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from the existing codebase. -->

From src/src/types/session.ts:
```typescript
export type SessionDateGroup = 'Today' | 'Yesterday' | 'This Week' | 'This Month' | 'Older';

export interface ProjectSessionGroup {
  label: SessionDateGroup;
  sessions: Session[];
}

export interface ProjectGroup {
  projectName: string;
  displayName: string;
  projectPath: string;
  sessionCount: number;
  visibleCount: number;
  dateGroups: ProjectSessionGroup[];
}

export interface Session {
  id: string;
  title: string;
  messages: Message[];
  providerId: ProviderId;
  createdAt: string;
  updatedAt: string;
  metadata: SessionMetadata;
}
```

From src/src/lib/sessionGrouping.ts:
```typescript
export interface ProjectSessionInput {
  projectName: string;
  displayName: string;
  projectPath: string;
  sessions: Session[];
}

export function groupSessionsByProject(projects: ProjectSessionInput[]): ProjectGroup[];
export function isJunkSession(session: Session): boolean;
```

From src/src/hooks/useMultiProjectSessions.ts:
```typescript
interface UseMultiProjectSessionsResult {
  projectGroups: ProjectGroup[];
  isLoading: boolean;
  error: string | null;
  expandedProjects: Set<string>;
  toggleProject: (name: string) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session search hook and pin persistence hook</name>
  <files>
    src/src/types/session.ts,
    src/src/lib/sessionGrouping.ts,
    src/src/lib/sessionGrouping.test.ts,
    src/src/hooks/useSessionSearch.ts,
    src/src/hooks/useSessionSearch.test.ts,
    src/src/hooks/useSessionPins.ts,
    src/src/hooks/useSessionPins.test.ts
  </files>
  <behavior>
    - filterProjectGroups('') returns input groups unchanged
    - filterProjectGroups('deploy') returns only projects/date-groups containing sessions with 'deploy' in title (case-insensitive)
    - filterProjectGroups filters out empty date-groups and empty projects after filtering
    - filterProjectGroups recalculates visibleCount per project
    - useSessionPins.togglePin adds/removes session ID from pin set
    - useSessionPins.isPinned returns true for pinned IDs
    - Pin set persists to localStorage under 'loom-pinned-sessions' key
    - Pin set loads from localStorage on init, returns empty Set on corrupt data
    - groupSessionsByProject with pinnedIds hoists pinned sessions into 'Pinned' pseudo-date-group at top of each project
    - groupSessionsByProject without pinnedIds (or empty set) behaves identically to current behavior
    - Pinned sessions are removed from their original date group (no duplicates)
  </behavior>
  <action>
    1. Update `src/src/types/session.ts`: Add `'Pinned'` to the `SessionDateGroup` union type. This is a backward-compatible addition since existing code only creates the 5 existing labels.

    2. Create `src/src/hooks/useSessionSearch.ts`:
       - Export `filterProjectGroups(groups: ProjectGroup[], query: string): ProjectGroup[]` as a pure function (for testability outside React).
       - When query is empty/whitespace, return groups unchanged.
       - When query is non-empty, for each project: filter each dateGroup's sessions by `session.title.toLowerCase().includes(query.toLowerCase())`, drop empty dateGroups, recalculate `visibleCount`. Drop projects with 0 visible sessions.
       - Export `useSessionSearch()` hook returning `{ query, setQuery, filterGroups: (groups) => ProjectGroup[] }`. The hook owns the query string state and returns a bound filter function.

    3. Create `src/src/hooks/useSessionPins.ts`:
       - localStorage key: `'loom-pinned-sessions'`
       - `loadPins(): Set<string>` reads from localStorage, returns empty Set on error.
       - `savePins(pins: Set<string>): void` writes JSON array to localStorage.
       - Export `useSessionPins()` hook returning `{ pinnedIds: Set<string>, togglePin: (id: string) => void, isPinned: (id: string) => boolean }`.
       - Uses `useState` initialized from `loadPins()`. `togglePin` adds/removes from set and calls `savePins`. Returns new Set reference on each toggle to trigger re-render.

    4. Modify `src/src/lib/sessionGrouping.ts`:
       - Add optional `pinnedIds?: Set<string>` parameter to `groupSessionsByProject`.
       - After junk filtering, if `pinnedIds` has entries: partition visible sessions into pinned vs unpinned. If pinned array is non-empty, prepend a `{ label: 'Pinned', sessions: pinned }` group before the date-bucketed groups. The unpinned sessions go through `groupIntoDateBuckets` as before.
       - Sort pinned sessions descending by updatedAt (same as date buckets).
       - When pinnedIds is undefined or empty, behavior is identical to current (no breaking change).

    5. Extend `src/src/lib/sessionGrouping.test.ts` with new tests covering the pinnedIds parameter behavior.

    6. Create test files:
       - `src/src/hooks/useSessionSearch.test.ts` testing the pure `filterProjectGroups` function directly (no React rendering needed).
       - `src/src/hooks/useSessionPins.test.ts` testing localStorage read/write with mocked localStorage.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/hooks/useSessionSearch.test.ts src/src/hooks/useSessionPins.test.ts src/src/lib/sessionGrouping.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - filterProjectGroups correctly filters and recalculates counts
    - useSessionPins reads/writes localStorage correctly
    - groupSessionsByProject hoists pinned sessions into Pinned group
    - All new and existing sessionGrouping tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: SearchInput presentational component</name>
  <files>
    src/src/components/sidebar/SearchInput.tsx,
    src/src/components/sidebar/sidebar.css
  </files>
  <action>
    Create `src/src/components/sidebar/SearchInput.tsx`:
    - Controlled component with props `{ value: string; onChange: (value: string) => void }`.
    - Layout: relative container with `Search` icon (lucide, size 14) positioned left, input element with left padding for icon, and conditional `X` clear button positioned right when value is non-empty.
    - Styling: Use design tokens only -- `bg-surface-base`, `border-border`, `text-foreground`, `placeholder:text-muted`, focus ring using `--accent-primary`. Padding `px-2 py-1.5` on container.
    - Accessibility: `aria-label="Search sessions"` on input, `aria-label="Clear search"` on clear button, `type="button"` on clear button.
    - Named export only per Constitution 2.2.

    Add to `sidebar.css` if needed: any search-specific styles that can't be expressed as Tailwind utilities (likely none needed -- prefer Tailwind classes).
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - SearchInput component exists with correct props interface
    - TypeScript compiles with no errors
    - Component uses design tokens and lucide icons consistently with rest of sidebar
  </done>
</task>

</tasks>

<verification>
- All new test files pass: `cd /home/swd/loom/src && npx vitest run src/src/hooks/useSessionSearch.test.ts src/src/hooks/useSessionPins.test.ts src/src/lib/sessionGrouping.test.ts --reporter=verbose`
- TypeScript compiles cleanly: `cd /home/swd/loom/src && npx tsc --noEmit`
- Existing tests not broken: `cd /home/swd/loom/src && npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- filterProjectGroups correctly filters sessions by title substring match (case-insensitive)
- useSessionPins persists pin toggles to localStorage and loads on init
- groupSessionsByProject accepts pinnedIds and hoists pinned sessions to top
- SessionDateGroup type includes 'Pinned'
- SearchInput component renders with icon, input, and conditional clear button
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/42-session-discovery/42-01-SUMMARY.md`
</output>
