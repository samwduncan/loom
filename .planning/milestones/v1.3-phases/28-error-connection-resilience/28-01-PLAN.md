---
phase: 28-error-connection-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/components/shared/ConnectionStatusIndicator.tsx
  - src/src/components/shared/ConnectionStatusIndicator.test.tsx
  - src/src/components/shared/ConnectionBanner.tsx
  - src/src/components/shared/ConnectionBanner.test.tsx
  - src/src/components/shared/connection-banner.css
  - src/src/components/sidebar/Sidebar.tsx
  - src/src/components/app-shell/AppShell.tsx
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-05

must_haves:
  truths:
    - "User sees a persistent dot/icon showing connected/reconnecting/disconnected state"
    - "User sees an error banner within 3 seconds when backend crashes"
    - "User sees a reconnection overlay when WebSocket drops"
    - "After reconnection succeeds, banner auto-dismisses"
  artifacts:
    - path: "src/src/components/shared/ConnectionStatusIndicator.tsx"
      provides: "Dot indicator for connected/reconnecting/disconnected"
      min_lines: 30
    - path: "src/src/components/shared/ConnectionBanner.tsx"
      provides: "Error banner + reconnection overlay"
      min_lines: 50
    - path: "src/src/components/shared/ConnectionBanner.test.tsx"
      provides: "Tests for banner states"
      min_lines: 40
  key_links:
    - from: "src/src/components/shared/ConnectionStatusIndicator.tsx"
      to: "useConnectionStore"
      via: "selector subscription"
      pattern: "useConnectionStore.*providers.*claude.*status"
    - from: "src/src/components/shared/ConnectionBanner.tsx"
      to: "useConnectionStore"
      via: "selector subscription"
      pattern: "useConnectionStore.*providers.*claude"
    - from: "src/src/components/app-shell/AppShell.tsx"
      to: "ConnectionBanner"
      via: "import and render"
      pattern: "ConnectionBanner"
---

<objective>
Build the visual connection health UI: a persistent status indicator dot and an error/reconnection banner.

Purpose: Users must always know whether Loom is connected. When things break, they need immediate, clear feedback -- not silence.
Output: ConnectionStatusIndicator component (sidebar), ConnectionBanner component (app shell overlay)
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/src/types/provider.ts:
```typescript
export type ConnectionStatus = 'disconnected' | 'connecting' | 'connected' | 'reconnecting';
export interface ProviderConnection {
  status: ConnectionStatus;
  lastConnected: string | null;
  reconnectAttempts: number;
  error: string | null;
  modelId: string | null;
}
```

From src/src/stores/connection.ts:
```typescript
export const useConnectionStore = create<ConnectionState>()(...)
// Selectors: state.providers.claude.status, state.providers.claude.error, state.providers.claude.reconnectAttempts
```

From src/src/components/app-shell/AppShell.tsx:
```typescript
// Grid layout: sidebar | content | artifact
// ConnectionBanner should render as a fixed/absolute overlay ABOVE the grid, not inside it
```

From src/src/components/sidebar/Sidebar.tsx:
```typescript
// Status indicator goes in the sidebar header, next to "Loom" wordmark
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConnectionStatusIndicator and ConnectionBanner components</name>
  <files>
    src/src/components/shared/ConnectionStatusIndicator.tsx,
    src/src/components/shared/ConnectionStatusIndicator.test.tsx,
    src/src/components/shared/ConnectionBanner.tsx,
    src/src/components/shared/ConnectionBanner.test.tsx,
    src/src/components/shared/connection-banner.css
  </files>
  <behavior>
    - ConnectionStatusIndicator renders a colored dot: green (connected), yellow pulsing (connecting/reconnecting), red (disconnected)
    - ConnectionStatusIndicator shows tooltip with status text on hover
    - ConnectionBanner shows nothing when status is "connected"
    - ConnectionBanner shows error banner with red background when status is "disconnected" and error is set (ERR-01: backend crash)
    - ConnectionBanner shows reconnection overlay with attempt count when status is "reconnecting" (ERR-02)
    - ConnectionBanner auto-dismisses when status returns to "connected"
    - ConnectionBanner error text includes the error string from connection store
  </behavior>
  <action>
    Create ConnectionStatusIndicator.tsx:
    - Read `useConnectionStore(s => s.providers.claude.status)` via selector
    - Render a 8px dot with color mapped: connected=green (--success), connecting/reconnecting=yellow (--warning) with CSS pulse animation, disconnected=red (--error)
    - Wrap in a span with `title` attribute showing status text ("Connected", "Reconnecting...", "Disconnected")
    - Use cn() for class composition, design tokens only (no hardcoded colors)
    - memo() the component

    Create ConnectionBanner.tsx:
    - Read `useConnectionStore(s => s.providers.claude)` via selector for status, error, reconnectAttempts
    - Render nothing when status === 'connected' or status === 'connecting' (initial connect)
    - When status === 'disconnected' AND error is set: render a fixed banner at top of viewport (z-[var(--z-toast)]) with red/error styling, showing the error message text. This covers ERR-01 (backend crash detection).
    - When status === 'reconnecting': render a semi-transparent overlay with centered card showing "Reconnecting..." with attempt count and a subtle spinner/pulse animation. This covers ERR-02.
    - When status === 'disconnected' AND no error: render a banner saying "Connection lost" with a manual "Reconnect" button that calls wsClient.connect()
    - Import connection-banner.css for the pulse animation keyframes
    - memo() the component

    Create connection-banner.css:
    - @keyframes pulse for the reconnecting dot
    - Overlay backdrop styles using design tokens

    Constitution compliance: cn() for classes (3.6), token-based styling (3.1), selector-only store access (4.2), named exports (2.2).
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/shared/ConnectionStatusIndicator.test.tsx src/src/components/shared/ConnectionBanner.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - ConnectionStatusIndicator renders correct color dot for each of the 4 connection states
    - ConnectionBanner shows error banner on disconnect+error, reconnection overlay on reconnecting, nothing on connected
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire components into AppShell and Sidebar</name>
  <files>
    src/src/components/sidebar/Sidebar.tsx,
    src/src/components/app-shell/AppShell.tsx
  </files>
  <action>
    Modify Sidebar.tsx:
    - Import ConnectionStatusIndicator
    - Add it to the sidebar header, right after the "Loom" wordmark span, as a flex item with gap-2
    - Only show when sidebar is expanded (it's already inside the expanded branch)

    Modify AppShell.tsx:
    - Import ConnectionBanner
    - Render it BEFORE the grid div (as a sibling in the fragment), so it overlays the entire app
    - No Suspense needed -- it's a small sync component

    Both changes are minimal -- just imports and JSX insertion.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/components/shared/ConnectionStatusIndicator.test.tsx src/src/components/shared/ConnectionBanner.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - ConnectionStatusIndicator visible in sidebar header next to "Loom" wordmark
    - ConnectionBanner renders as overlay above the entire app shell
    - TypeScript compiles with no errors
    - Existing tests still pass
  </done>
</task>

</tasks>

<verification>
- `cd /home/swd/loom/src && npx vitest run --reporter=verbose` -- all tests pass
- `cd /home/swd/loom/src && npx tsc --noEmit` -- no type errors
- `cd /home/swd/loom/src && npx eslint src/src/components/shared/ConnectionStatusIndicator.tsx src/src/components/shared/ConnectionBanner.tsx` -- no lint errors
</verification>

<success_criteria>
- Green dot visible in sidebar when connected
- Error banner appears when connection store has disconnected + error state
- Reconnection overlay appears when connection store has reconnecting state
- Banner/overlay auto-dismisses on reconnect success
- All new and existing tests pass, TypeScript compiles, ESLint clean
</success_criteria>

<output>
After completion, create `.planning/phases/28-error-connection-resilience/28-01-SUMMARY.md`
</output>
