---
phase: 37-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vite.config.ts
  - src/package.json
  - src/src/lib/shiki-highlighter.ts
  - src/src/lib/shiki-highlighter.test.ts
  - src/src/hooks/useAutoCollapse.ts
  - src/src/hooks/useAutoCollapse.test.ts
autonomous: true
requirements: [PERF-04, PERF-05, PERF-03, PERF-01]

must_haves:
  truths:
    - "Bundle analysis report exists as dist/bundle-report.html with interactive treemap"
    - "Main chunk is split into vendor sub-chunks instead of one 1.1MB blob"
    - "Shiki highlight cache is bounded at 500 entries with LRU eviction"
    - "useAutoCollapse uses a single shared IntersectionObserver instead of one per message"
  artifacts:
    - path: "src/vite.config.ts"
      provides: "manualChunks config + visualizer plugin"
      contains: "manualChunks"
    - path: "src/src/lib/shiki-highlighter.ts"
      provides: "Bounded LRU cache for highlight results"
      contains: "MAX_CACHE_SIZE"
    - path: "src/src/hooks/useAutoCollapse.ts"
      provides: "Single-observer pattern for message collapse"
    - path: "src/dist/bundle-report.html"
      provides: "Interactive bundle treemap"
  key_links:
    - from: "src/vite.config.ts"
      to: "dist/*.js"
      via: "rollup manualChunks"
      pattern: "manualChunks"
    - from: "src/src/lib/shiki-highlighter.ts"
      to: "cache"
      via: "LRU eviction in cacheSet"
      pattern: "MAX_CACHE_SIZE"
---

<objective>
Bundle optimization, cache bounding, and observer consolidation for Loom performance.

Purpose: Fix the three code-level performance issues identified in research: (1) 1.1MB main chunk needs vendor splitting, (2) unbounded shiki highlight cache leaks memory, (3) per-message IntersectionObservers accumulate with message count. Also install bundle analysis tooling to produce the PERF-05 report.

Output: Optimized vite.config.ts with manual chunks + visualizer, bounded shiki cache, consolidated single-observer useAutoCollapse, and bundle analysis HTML report.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-performance/37-RESEARCH.md

<interfaces>
From src/vite.config.ts:
```typescript
// Current build config (no manualChunks, no plugins beyond react + tailwind)
build: {
  outDir: 'dist',
},
```

From src/src/lib/shiki-highlighter.ts:
```typescript
// Unbounded cache -- line 102
const cache = new Map<string, string>();
// Used by highlightCode() via cache.get() and cache.set()
export async function highlightCode(lang: string, code: string): Promise<string>;
export async function preloadLanguages(): Promise<void>;
export function getHighlighter(): Promise<HighlighterCore>;
export function getLanguageFromPath(filePath: string): string;
```

From src/src/hooks/useAutoCollapse.ts:
```typescript
// Creates one IntersectionObserver per message (lines 67-104)
// Returns: { observeRef, isCollapsed, toggleExpand }
export function useAutoCollapse(scrollContainerRef: RefObject<HTMLElement | null>): AutoCollapseReturn;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bundle analysis tooling + vendor chunk splitting</name>
  <files>src/vite.config.ts, src/package.json</files>
  <behavior>
    - Test 1: `npm run build:analyze` script exists in package.json
    - Test 2: Running `ANALYZE=true vite build` produces dist/bundle-report.html
    - Test 3: Build output shows separate vendor chunks (vendor-react, vendor-markdown, vendor-shiki) instead of single 1.1MB main
    - Test 4: Application still loads correctly after chunk splitting (no import order errors)
  </behavior>
  <action>
    1. Install rollup-plugin-visualizer as devDependency: `cd src && npm install -D rollup-plugin-visualizer`

    2. Update src/vite.config.ts:
       - Add conditional visualizer plugin when ANALYZE env var is set (filename: 'dist/bundle-report.html', gzipSize: true, template: 'treemap')
       - Add build.rollupOptions.output.manualChunks with these groups:
         - 'vendor-react': ['react', 'react-dom', 'react-router-dom']
         - 'vendor-markdown': ['react-markdown', 'remark-gfm', 'rehype-raw']
         - 'vendor-shiki': ['shiki']
         - 'vendor-radix': Match any module path containing '@radix-ui'
         - 'vendor-zustand': ['zustand', 'immer']
       - Use a function form for manualChunks to handle @radix-ui pattern matching (check if id includes '/node_modules/@radix-ui/')

    3. Add `"build:analyze": "ANALYZE=true vite build"` to package.json scripts

    4. Run `cd src && npm run build` to verify the split works without errors. Run `cd src && ANALYZE=true npx vite build` to generate the bundle report.

    5. Check the build output: the old ~1.1MB index chunk should be split into smaller pieces. Document the new chunk sizes in the commit message.

    Note: Keep manualChunks to these 5 broad groups per research guidance. Don't try to split individual components -- that risks circular dependency issues (Pitfall 4 from research).
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - rollup-plugin-visualizer installed as devDep
    - build:analyze script exists in package.json
    - vite.config.ts has manualChunks splitting vendor libraries into 5 groups
    - vite.config.ts has conditional visualizer plugin
    - `npm run build` succeeds with no errors and shows multiple vendor chunks
    - dist/bundle-report.html exists after ANALYZE build
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded shiki cache + consolidated IntersectionObserver</name>
  <files>src/src/lib/shiki-highlighter.ts, src/src/lib/shiki-highlighter.test.ts, src/src/hooks/useAutoCollapse.ts, src/src/hooks/useAutoCollapse.test.ts</files>
  <behavior>
    - Test 1: shiki cache evicts oldest entry when exceeding MAX_CACHE_SIZE (500)
    - Test 2: shiki cache.size never exceeds MAX_CACHE_SIZE after many inserts
    - Test 3: useAutoCollapse creates exactly 1 IntersectionObserver regardless of how many messages are observed
    - Test 4: useAutoCollapse still collapses/expands messages correctly with single observer
    - Test 5: useAutoCollapse cleans up the single observer on unmount
  </behavior>
  <action>
    1. **shiki-highlighter.ts** -- Replace the unbounded `const cache = new Map<string, string>()` with a bounded LRU cache:
       - Add `const MAX_CACHE_SIZE = 500;`
       - Create a `cacheSet(key: string, value: string)` helper that checks `cache.size >= MAX_CACHE_SIZE` and deletes the oldest entry (first key via `cache.keys().next().value`) before inserting
       - Replace both `cache.set(cacheKey, html)` and `cache.set(cacheKey, code)` calls in `highlightCode` with `cacheSet(cacheKey, ...)`
       - Export MAX_CACHE_SIZE for testing (or test via highlightCode behavior)

    2. **shiki-highlighter.test.ts** -- Add tests:
       - Test that the cache is bounded: call highlightCode with 501+ unique inputs and verify the cache size doesn't exceed 500 (access via exported `getCacheSize()` helper or by checking behavior)
       - Add a `getCacheSize()` export for testability, or test indirectly by verifying that old entries aren't returned from cache after eviction

    3. **useAutoCollapse.ts** -- Refactor from N observers to 1 shared observer:
       - Create a single IntersectionObserver in useEffect (not per-message)
       - Maintain a `Map<Element, string>` to map observed elements back to their messageId
       - The single observer callback receives entries[] array -- iterate all, look up messageId from element map
       - `observeRef` callback: observer.observe(el) and register in element->messageId map; on null, observer.unobserve and remove from map
       - Keep all existing behavior: 300ms debounce on collapse, immediate expand, pin/unpin via toggleExpand, stable ref callbacks
       - Cleanup: disconnect the single observer on unmount, clear all maps

    4. **useAutoCollapse.test.ts** -- Update/add tests:
       - Verify only 1 IntersectionObserver constructor call regardless of observed message count
       - Verify collapse/expand behavior unchanged
       - Verify cleanup disconnects the observer

    Run existing tests to verify no regressions: `cd src && npx vitest run --reporter=verbose`
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/src/lib/shiki-highlighter.test.ts src/src/hooks/useAutoCollapse.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Shiki highlight cache bounded at 500 entries with LRU eviction (oldest-first)
    - useAutoCollapse uses a single shared IntersectionObserver for all messages
    - All existing collapse/expand/pin behavior preserved
    - Tests pass for both modules
    - No regressions in full test suite
  </done>
</task>

</tasks>

<verification>
- `cd src && npm run build` succeeds with vendor chunk splitting visible in output
- `cd src && ANALYZE=true npx vite build` produces dist/bundle-report.html
- `cd src && npx vitest run --reporter=verbose` -- full suite green
- Main chunk size reduced from 1.1MB to under 500KB (vendor libs extracted)
</verification>

<success_criteria>
- Bundle report exists with interactive treemap showing all chunks
- Main chunk split into 5+ vendor chunks
- Shiki cache bounded at 500 entries
- Single IntersectionObserver for all auto-collapse messages
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/37-performance/37-01-SUMMARY.md`
</output>
