---
phase: 10-pre-archive-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/components/chat/tools/ToolCard.tsx
  - src/src/lib/tool-registry.ts
  - src/src/hooks/useProjectContext.ts
  - src/src/components/chat/tools/tool-chip.css
requirements:
  - ENF-01
autonomous: true

must_haves:
  truths:
    - "ToolCard.tsx does not exist (orphaned dead file removed)"
    - "tool-registry.ts DefaultToolCard uses CSS classes, not inline styles"
    - "No unused test-only exports exist in production code"
    - "ENF-01 traceability is consistent (Complete everywhere)"
  artifacts:
    - path: "src/src/lib/tool-registry.ts"
      provides: "DefaultToolCard with CSS-only styling"
      contains: "createElement\\('pre'"
    - path: "src/src/components/chat/tools/tool-chip.css"
      provides: "Tool card pre styles"
      contains: ".tool-card pre"
  key_links:
    - from: "src/src/lib/tool-registry.ts"
      to: "src/src/components/chat/tools/tool-chip.css"
      via: "CSS class .tool-card pre"
      pattern: "tool-card"
---

<objective>
Remove orphaned code, eliminate Constitution violations, and clean up test-only exports before archiving M1.

Purpose: Close all tech debt items from the v1.0 audit so the milestone archives clean.
Output: 4 targeted fixes across 3-4 files, zero new functionality.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ToolCard.tsx orphan and strip inline styles from tool-registry.ts</name>
  <files>src/src/components/chat/tools/ToolCard.tsx, src/src/lib/tool-registry.ts</files>
  <action>
1. Delete `src/src/components/chat/tools/ToolCard.tsx` entirely. It is never imported by any component — `DefaultToolCard` in tool-registry.ts serves the same purpose using createElement.

2. In `src/src/lib/tool-registry.ts`, remove the `style` prop from both `createElement('pre', ...)` calls (lines 104 and 114). The CSS file `tool-chip.css` already has `.tool-card pre { margin: 0; white-space: pre-wrap; word-break: break-all; }` at line 115-119, so these inline styles are redundant AND a Constitution violation (no-banned-inline-style).

Before (line 104):
```
createElement('pre', { style: { margin: 0, whiteSpace: 'pre-wrap', wordBreak: 'break-all' } }, truncatedInput)
```

After:
```
createElement('pre', null, truncatedInput)
```

Same change on line 114 for the output pre element.

3. Verify `ToolCard` is not referenced in any import statement (it should not be — only test describe blocks mention the name as a string, which is fine).
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx eslint src/lib/tool-registry.ts --no-warn && ! test -f src/components/chat/tools/ToolCard.tsx && ! grep -r "from.*ToolCard" src/ && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>ToolCard.tsx deleted, tool-registry.ts has zero inline styles, all existing tests pass, ESLint clean</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused _resetProjectContextForTesting export</name>
  <files>src/src/hooks/useProjectContext.ts</files>
  <action>
Remove the `_resetProjectContextForTesting()` function export from `src/src/hooks/useProjectContext.ts` (lines 33-36). This function is exported but never actually called in any test — it only appears in `vi.fn()` module mock factories in SessionList.test.tsx, Sidebar.test.tsx, and useSessionList.test.ts. Those mocks will continue to work fine without this export because they define their own vi.fn() regardless of what the real module exports.

Do NOT touch `_resetInitForTesting()` in websocket-init.ts — that one IS actually imported and called in websocket-init.test.ts (line 127). It is a legitimate test utility.

After removing the function, verify the 3 test files with mocks still pass. The mock factories use `vi.mock('@/hooks/useProjectContext', ...)` which replaces the entire module — so the mock shape is independent of the real module's exports.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && grep -c "_resetProjectContextForTesting" src/hooks/useProjectContext.ts; npm test -- --run --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|Tests)" | tail -5</automated>
  </verify>
  <done>_resetProjectContextForTesting removed from production code, grep returns 0 matches, all tests pass</done>
</task>

</tasks>

<verification>
- `find src/src -name "ToolCard.tsx"` returns nothing
- `grep -r "style:" src/src/lib/tool-registry.ts` returns nothing
- `grep "_resetProjectContextForTesting" src/src/hooks/useProjectContext.ts` returns nothing
- `cd src && npm test -- --run` passes all tests
- `cd src && npx eslint src/ --no-warn` passes (no Constitution violations)
</verification>

<success_criteria>
1. ToolCard.tsx is deleted (no orphaned exports)
2. tool-registry.ts DefaultToolCard pre elements have no style prop (CSS classes handle it)
3. _resetProjectContextForTesting is removed from useProjectContext.ts
4. _resetInitForTesting remains in websocket-init.ts (it IS used)
5. All existing tests pass
6. ESLint passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-pre-archive-cleanup/10-01-SUMMARY.md`
</output>
