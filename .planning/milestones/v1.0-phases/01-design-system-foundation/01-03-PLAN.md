---
phase: 01-design-system-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/src/App.tsx
  - src/src/components/dev/TokenPreview.tsx
autonomous: false
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04
  - DS-05
  - DS-06

must_haves:
  truths:
    - "A token preview page at /dev/tokens renders ALL token categories visually"
    - "Three surface levels are perceptibly distinct when viewed side by side"
    - "All text hierarchy tokens (primary, secondary, muted) are legible on all 3 surface tiers"
    - "Dusty rose accent punches against charcoal surfaces with high contrast"
    - "Inter, Instrument Serif, and JetBrains Mono render correctly in font specimen section"
    - "Spring Lab section demonstrates all 3 spring configs with interactive animations"
    - "Status colors are distinguishable and readable on dark surfaces"
    - "Spacing grid visualization shows 4px increments"
    - "Z-index tiers are listed with their intended usage"
  artifacts:
    - path: "src/src/components/dev/TokenPreview.tsx"
      provides: "Comprehensive token preview and validation page"
      min_lines: 200
  key_links:
    - from: "src/src/App.tsx"
      to: "src/src/components/dev/TokenPreview.tsx"
      via: "React Router route at /dev/tokens"
      pattern: "path.*dev/tokens"
    - from: "TokenPreview.tsx"
      to: "src/src/styles/tokens.css"
      via: "CSS custom property usage (var(--surface-base), etc.)"
      pattern: "var\\(--"
    - from: "TokenPreview.tsx"
      to: "src/src/lib/motion.ts"
      via: "import spring configs for Spring Lab"
      pattern: "import.*SPRING_"
---

<objective>
Create a comprehensive token preview page that serves as the primary validation artifact for Phase 1. This page renders every token category visually so the user can verify colors, surfaces, typography, spacing, motion, and z-index are all correct.

Purpose: Phase 1 is primarily visual/CSS. Automated tests cannot verify color warmth, surface distinction, or font rendering. The token preview IS the test suite. It also becomes a permanent dev tool for future reference.
Output: TokenPreview component at /dev/tokens route demonstrating all token values.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-CONTEXT.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
@.planning/V2_CONSTITUTION.md

<!-- Depends on Plan 01-01 and 01-02 output -->
@.planning/phases/01-design-system-foundation/01-01-SUMMARY.md
@.planning/phases/01-design-system-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build comprehensive token preview page</name>
  <files>src/src/components/dev/TokenPreview.tsx, src/src/App.tsx</files>
  <action>
    Create `src/components/dev/TokenPreview.tsx` — a comprehensive design token visualization and stress test page. This is a permanent dev tool, not temporary. Register it at the `/dev/tokens` route in App.tsx using React Router.

    The page MUST include ALL of the following sections (use Tailwind utility classes throughout, referencing the semantic tokens from @theme inline — `bg-surface-base`, `text-foreground`, `bg-primary`, etc.):

    **Section 1: Surface Hierarchy** (DS-06 verification)
    - Three large rectangles side by side: base, raised, overlay
    - Each labeled with its token name and OKLCH value
    - Each containing sample text in all 3 text tiers (primary, secondary, muted) so contrast is verifiable
    - A border sample between each surface pair showing `--border-subtle` and `--border-interactive`
    - NOTE: Surfaces must be perceptibly distinct without box-shadow. If they look identical, the lightness steps are too small.

    **Section 2: Color Palette** (DS-01 verification)
    - **Accent colors**: Dusty rose (primary) with hover/muted/fg variants shown as color swatches. Amber/gold (secondary) swatch.
    - **Status colors**: Success, error, warning, info — each as a colored pill with white text and dark text variants to test readability.
    - **Rose variants**: rose-accent, rose-text, rose-focus-glow, rose-selection displayed as swatches.
    - **FX gradient colors**: All 4 gradient tokens (rose, violet, teal, amber) shown as gradient swatches.
    - **Diff colors**: Added/removed backgrounds shown as sample code diff lines.
    - **Code surface**: A mock code block on `--code-surface` with `--code-inline-bg` inline code sample.
    - Each swatch shows the token name and its raw OKLCH value.

    **Section 3: Typography** (DS-05 verification)
    - **Font specimens**: A paragraph in each font family:
      - Inter Variable (font-sans): "The quick brown fox jumps over the lazy dog" at 14px, 16px, 18px, 24px
      - Instrument Serif (font-serif): Same pangram showing the editorial heading personality
      - JetBrains Mono (font-mono): `const greeting = "Hello, World!";` showing code rendering
    - **Heading scale**: H1 (32px), H2 (24px), H3 (18px), body (14px) — using Instrument Serif for H1/H2 and Inter for H3/body per the personality spec.
    - **Density comparison**: Side by side panels — one with compact (8px) spacing and one with comfortable (12px) spacing, showing how the same content feels at different densities.

    **Section 4: Spacing Scale** (DS-03 verification)
    - Visual grid showing each spacing token as a colored bar with width equal to the space value
    - Labels: --space-1 (4px), --space-2 (8px), through --space-16 (64px)
    - A visual alignment check: boxes at each spacing value to verify 4px grid compliance

    **Section 5: Z-Index Dictionary** (DS-04 verification)
    - Table listing all 8 z-index tiers with token name, numeric value, and intended usage
    - Small visual demonstration: overlapping colored squares at different z-index values showing stacking order

    **Section 6: Motion Tokens** (DS-02 verification)
    - **CSS Easing demo**: Three boxes that animate on hover/click, each using a different easing curve (spring, out, inOut). Show the cubic-bezier value. Animate translateX on click.
    - **Duration demo**: Three boxes that fade in/out at different durations (fast: 100ms, normal: 200ms, slow: 400ms).

    **Section 7: Spring Lab** (DS-02 spring physics verification)
    - Import SPRING_GENTLE, SPRING_SNAPPY, SPRING_BOUNCY from `@/lib/motion`
    - Three interactive demo elements (e.g., draggable circles or click-to-animate boxes):
      - Gentle: Slow, smooth, no overshoot — labeled "Sidebar transitions"
      - Snappy: Fast, precise, minimal overshoot — labeled "Button press"
      - Bouncy: Playful, visible overshoot — labeled "Notifications"
    - Each shows its stiffness/damping values
    - Use CSS spring easing approximation (`--ease-spring` cubic-bezier) for the demo since Framer Motion is not yet installed. The visual should approximate the spring feel. Add a note: "Full spring physics via LazyMotion in M3".
    - A "Play All" button that triggers all three animations simultaneously for comparison.

    **Section 8: Glass & FX Tokens**
    - A glassmorphic card demo showing `--glass-blur`, `--glass-saturate`, `--glass-bg-opacity` applied
    - An aurora/ambient shimmer demo: a subtle animated gradient behind content using the FX gradient tokens and animation speed tokens

    **Page Layout:**
    - Use `--surface-base` as page background
    - Section headers in Instrument Serif (font-serif)
    - Body text in Inter (font-sans)
    - Use semantic Tailwind utilities only (bg-surface-base, text-foreground, etc.)
    - Each section is a `--surface-raised` card with `--border-subtle` borders
    - Page must be scrollable (this is a dev tool, not the app shell)
    - Use `cn()` utility for all className composition

    **Route registration in App.tsx:**
    - Add `<Route path="/dev/tokens" element={<TokenPreview />} />` inside the BrowserRouter
    - Import TokenPreview as a named import
    - Keep the existing placeholder routes

    IMPORTANT: Use only named exports per Constitution Section 2.2. Use `cn()` for all className composition per Constitution Section 3.6. NO hardcoded Tailwind color utilities (no bg-gray-*, text-red-*, etc.) — only semantic token utilities.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx tsc --noEmit --project tsconfig.app.json 2>&1 | tail -5 && grep -q "TokenPreview" src/App.tsx && grep -q "dev/tokens" src/App.tsx && echo "PASS: Route registered" && wc -l src/components/dev/TokenPreview.tsx | awk '{if ($1 >= 200) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}' || echo "FAIL"</automated>
  </verify>
  <done>TokenPreview.tsx is a 200+ line comprehensive dev tool at /dev/tokens showing: surface hierarchy (3 tiers side by side with text on each), complete color palette (accent, status, rose, FX, diff, code), typography specimens (all 3 fonts at multiple sizes + heading scale + density comparison), spacing grid (4px increments visualized), z-index table, motion/easing demos, Spring Lab with all 3 spring configs, and glass/FX demos. All using semantic Tailwind utilities only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of design token system</name>
  <files>src/src/components/dev/TokenPreview.tsx</files>
  <action>
    Present the token preview page to the user for visual inspection. Start the dev server if not already running (`cd src && npm run dev`). The user accesses http://100.86.4.57:5173/dev/tokens in their browser.

    This checkpoint verifies ALL 6 DS requirements visually:
    - DS-01: OKLCH color tokens (palette section)
    - DS-02: Motion tokens + spring configs (motion section + Spring Lab)
    - DS-03: Spacing scale (spacing section)
    - DS-04: Z-index dictionary (z-index section)
    - DS-05: Font families (typography section)
    - DS-06: Surface hierarchy (surfaces section)

    Guide the user through each section for verification. If the user reports issues (e.g., "surfaces too similar", "rose not warm enough"), adjust the OKLCH values in tokens.css and reload until approved.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && curl -s http://localhost:5184/dev/tokens | grep -q "Surface" && echo "PASS: Token preview page serves" || echo "FAIL: Token preview not accessible"</automated>
  </verify>
  <done>User has visually verified all design tokens: surfaces are perceptibly distinct with warm charcoal feel, dusty rose punches against charcoal, all 3 fonts load correctly (Inter body, Instrument Serif headings, JetBrains Mono code), spacing grid aligns to 4px, z-index tiers match spec, motion demos feel qualitatively different, and overall vibe matches "Warp terminal meets high-end journal".</done>
</task>

</tasks>

<verification>
- /dev/tokens route loads without errors
- All 8 sections render with correct token values
- Surface hierarchy shows 3 perceptibly distinct levels
- All font families load (check Network tab for 4 .woff2 files)
- Spring Lab animations are visually distinct
- No hardcoded color utilities in TokenPreview.tsx (grep for bg-gray, text-red, etc. returns 0)
- TypeScript compiles without errors
</verification>

<success_criteria>
- Token preview page is a permanent dev tool at /dev/tokens
- All 6 DS requirements (DS-01 through DS-06) are visually verifiable on the preview page
- User confirms: surfaces are perceptible, colors have the right personality, fonts load correctly, motion feels right
- The preview page itself follows all Constitution rules (cn(), semantic utilities, named exports)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-03-SUMMARY.md`
</output>
