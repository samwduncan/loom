---
phase: 03-app-shell-error-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/src/stores/ui.ts
  - src/src/components/app-shell/AppShell.tsx
  - src/src/components/sidebar/Sidebar.tsx
  - src/src/components/shared/PlaceholderView.tsx
  - src/src/App.tsx
  - src/src/components/app-shell/AppShell.test.tsx
  - src/src/components/sidebar/Sidebar.test.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03]

must_haves:
  truths:
    - "App renders a 3-column CSS Grid filling 100dvh with no document scrollbar"
    - "Sidebar shows Loom wordmark in Instrument Serif italic with collapse toggle"
    - "Collapsing sidebar hides it completely (0px) and shows an expand trigger at left edge"
    - "Routes /chat/:sessionId?, /dashboard, /settings render inside the content grid area"
    - "/dev/tokens route renders outside the AppShell as a standalone page"
    - "Below 768px viewport width, sidebar auto-collapses"
  artifacts:
    - path: "src/src/stores/ui.ts"
      provides: "Minimal UI store stub with sidebarOpen state and toggleSidebar action"
      exports: ["useUIStore"]
    - path: "src/src/components/app-shell/AppShell.tsx"
      provides: "CSS Grid shell with data-sidebar-state attribute"
      exports: ["AppShell"]
    - path: "src/src/components/sidebar/Sidebar.tsx"
      provides: "Sidebar with branded header and collapse toggle"
      exports: ["Sidebar"]
    - path: "src/src/components/shared/PlaceholderView.tsx"
      provides: "Generic reusable placeholder for empty route states"
      exports: ["PlaceholderView"]
    - path: "src/src/App.tsx"
      provides: "Restructured routing with AppShell as layout route"
      exports: ["App"]
  key_links:
    - from: "src/src/components/app-shell/AppShell.tsx"
      to: "src/src/stores/ui.ts"
      via: "useUIStore selector for sidebarOpen and sidebarState"
      pattern: "useUIStore\\(.*sidebar"
    - from: "src/src/components/sidebar/Sidebar.tsx"
      to: "src/src/stores/ui.ts"
      via: "useUIStore selector for toggleSidebar"
      pattern: "useUIStore\\(.*toggle"
    - from: "src/src/App.tsx"
      to: "src/src/components/app-shell/AppShell.tsx"
      via: "React Router layout route with Outlet"
      pattern: "Route.*element.*AppShell"
---

<objective>
Create the CSS Grid app shell, sidebar placeholder with collapse toggle, minimal UI store, and React Router route structure for Phase 3.

Purpose: Establishes the spatial skeleton that all future components render inside -- sidebar, content area, and reserved artifact column. Without this shell, no component has a layout context.
Output: Working 3-column grid layout with sidebar toggle, route placeholders, and passing tests.
</objective>

<execution_context>
@/home/swd/.claude/get-shit-done/workflows/execute-plan.md
@/home/swd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-app-shell-error-boundaries/03-CONTEXT.md
@.planning/phases/03-app-shell-error-boundaries/03-RESEARCH.md

<interfaces>
<!-- Existing utilities and tokens the executor needs -->

From src/src/utils/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From src/src/styles/tokens.css (relevant tokens):
```css
--surface-base: oklch(0.20 0.010 32);
--surface-raised: oklch(0.23 0.008 32);
--border-subtle: oklch(1 0 0 / 0.07);
--z-sticky: 10;
--duration-fast: 100ms;
--text-primary: oklch(0.90 0.02 30);
--text-muted: oklch(0.60 0.008 30);
```

From src/src/styles/index.css (Tailwind utility mappings):
```css
--color-surface-base: var(--surface-base);
--color-surface-raised: var(--surface-raised);
--color-foreground: var(--text-primary);
--color-muted: var(--text-muted);
--color-border: var(--border-subtle);
```

From src/src/App.tsx (current structure to replace):
```typescript
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { TokenPreview } from '@/components/dev/TokenPreview';
// Currently has HomePage + /dev/tokens routes
// Phase 3 restructures to use AppShell as layout route
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Minimal UI store stub + AppShell CSS Grid + Sidebar</name>
  <files>src/src/stores/ui.ts, src/src/components/app-shell/AppShell.tsx, src/src/components/sidebar/Sidebar.tsx, src/src/components/app-shell/AppShell.test.tsx, src/src/components/sidebar/Sidebar.test.tsx</files>
  <behavior>
    - Test: AppShell renders a grid container with data-sidebar-state attribute
    - Test: AppShell grid container has h-dvh and overflow-hidden classes
    - Test: Sidebar renders "Loom" wordmark in Instrument Serif italic font
    - Test: Sidebar has role="complementary" and aria-label
    - Test: Sidebar collapse toggle button exists and is accessible
    - Test: Clicking collapse toggle calls toggleSidebar from UI store
    - Test: When sidebarOpen is false, sidebar renders expand trigger button instead of full sidebar
    - Test: Expand trigger has z-[var(--z-overlay)] positioning (above sticky headers, below modals)
  </behavior>
  <action>
    1. Create `src/src/stores/` directory and `src/src/stores/ui.ts`:
       - Install zustand: `cd /home/swd/loom/src && npm install zustand`
       - Create minimal store with: `sidebarOpen: boolean` (default `true`), `sidebarState: 'expanded' | 'collapsed-hidden'` (derived from sidebarOpen), `toggleSidebar: () => void`
       - Use selector-only pattern per Constitution. Export `useUIStore` as named export.
       - This is a STUB -- Phase 4 (STATE-01) will expand this store with the full UIStore schema. Keep it minimal.

    2. Create `src/src/components/app-shell/AppShell.tsx`:
       - First, add `--sidebar-expanded-width: 280px;` to `src/src/styles/tokens.css` in the layout/spacing section. This parameterizes the sidebar width for future global changes.
       - CSS Grid container: `grid-template-columns: var(--sidebar-width, var(--sidebar-expanded-width, 280px)) 1fr var(--artifact-width, 0px)`, `grid-template-rows: 1fr`
       - Apply `h-dvh overflow-hidden bg-surface-base` on the grid container (SHELL-02 enforcement)
       - Set `data-sidebar-state` attribute from UI store's sidebarState
       - CSS for sidebar state: use a style block or Tailwind arbitrary selectors:
         - `[data-sidebar-state="expanded"]` sets `--sidebar-width: var(--sidebar-expanded-width)` (NOT hardcoded 280px)
         - `[data-sidebar-state="collapsed-hidden"]` sets `--sidebar-width: 0px`
       - Add `@media (max-width: 767px)` rule that forces `--sidebar-width: 0px`
       - Grid children: sidebar slot (first column), main content with `<Outlet />` (second column), artifact column reserved (third, empty)
       - Each grid child gets `overflow-hidden min-w-0` to prevent grid blowout (pitfall from RESEARCH.md)
       - Use `React.memo` on the component
       - Use inline style for gridTemplateColumns and gridTemplateRows (these are dynamic layout values, not color/spacing -- ESLint allows dynamic dimensions)

    3. Create `src/src/components/sidebar/Sidebar.tsx`:
       - When `sidebarOpen` is true (expanded state):
         - `<aside>` with `role="complementary"` and `aria-label="Navigation"`
         - `bg-surface-raised` background, `border-r border-border` right border (Constitution 7.13)
         - `overflow-hidden flex flex-col h-full`
         - Header section: "Loom" text in `font-serif italic text-lg text-foreground`, collapse chevron button (ChevronLeft arrow -- use a simple SVG inline or Unicode character like `\u276E`)
         - Header has `border-b border-border` bottom separator
         - Rest of sidebar empty (Phase 3 placeholder per CONTEXT.md)
       - When `sidebarOpen` is false (collapsed state):
         - Render a fixed-position expand trigger button at left edge, vertically centered
         - `fixed left-0 top-1/2 -translate-y-1/2` positioning
         - `z-[var(--z-overlay)]` (40) -- must sit above all scrolling content and sticky headers, but below modals. Architect review elevated this from --z-sticky (10) to avoid conflicts with future sticky headers in the content area.
         - `bg-surface-raised rounded-r-md border-r border-border` styling
         - Chevron right icon (Unicode `\u276F` or simple SVG)
         - `aria-label="Expand sidebar"`
       - Use `React.memo` on the component
       - Read sidebarOpen and toggleSidebar from useUIStore with selectors

    4. Write CSS rules for sidebar state. Add to `src/src/styles/index.css` (or embed in AppShell via a small CSS module / style tag):
       - `[data-sidebar-state="expanded"] { --sidebar-width: var(--sidebar-expanded-width); }`
       - `[data-sidebar-state="collapsed-hidden"] { --sidebar-width: 0px; }`
       - `@media (max-width: 767px) { [data-sidebar-state] { --sidebar-width: 0px; } }`
       - Prefer adding these CSS rules to `src/src/styles/index.css` after the @theme blocks, since AppShell uses inline style for the grid-template-columns referencing var(--sidebar-width).
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run src/components/app-shell/AppShell.test.tsx src/components/sidebar/Sidebar.test.tsx --reporter=verbose</automated>
  </verify>
  <done>AppShell renders 3-column CSS Grid with h-dvh and overflow-hidden. Sidebar shows "Loom" in Instrument Serif italic with collapse/expand toggle. data-sidebar-state attribute drives CSS variable for sidebar width. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: React Router route structure with placeholders</name>
  <files>src/src/components/shared/PlaceholderView.tsx, src/src/App.tsx</files>
  <behavior>
    - Test: Navigating to /chat renders inside AppShell content area with "Start a conversation" placeholder
    - Test: Navigating to /chat/some-session-id renders inside AppShell with chat placeholder
    - Test: Navigating to /dashboard renders "Dashboard" placeholder inside AppShell
    - Test: Navigating to /settings renders "Settings" placeholder inside AppShell
    - Test: Navigating to /dev/tokens renders TokenPreview OUTSIDE AppShell (standalone)
    - Test: Root path / redirects to /chat
    - Test: PlaceholderView renders title and optional message with consistent styling
  </behavior>
  <action>
    1. Create `src/src/components/shared/PlaceholderView.tsx`:
       - A generic reusable placeholder component for empty route states
       - Props: `{ title: string; message?: string }`
       - Renders: centered content (`flex items-center justify-center h-full`), title in `text-muted text-lg`, optional message in `text-muted text-sm` below title
       - Named export only. Uses cn() for classnames.
       - This keeps App.tsx clean and enforces a consistent empty-state design across all placeholder routes.

    2. Restructure `src/src/App.tsx`:
       - Import AppShell, TokenPreview, PlaceholderView
       - Create thin route wrapper components that use PlaceholderView:
         - `ChatPlaceholder`: reads `sessionId` from `useParams()`. Renders `<PlaceholderView title="Start a conversation" />` when no sessionId, or `<PlaceholderView title="Chat" message={sessionId} />` when sessionId is present.
         - `DashboardPlaceholder`: `<PlaceholderView title="Dashboard" />`
         - `SettingsPlaceholder`: `<PlaceholderView title="Settings" />`
       - Route structure using React Router layout route pattern:
         ```
         <BrowserRouter>
           <Routes>
             <Route element={<AppShell />}>
               <Route index element={<Navigate to="/chat" replace />} />
               <Route path="/chat/:sessionId?" element={<ChatPlaceholder />} />
               <Route path="/dashboard" element={<DashboardPlaceholder />} />
               <Route path="/settings" element={<SettingsPlaceholder />} />
             </Route>
             <Route path="/dev/tokens" element={<TokenPreview />} />
           </Routes>
         </BrowserRouter>
         ```
       - Import Navigate from react-router-dom for the index redirect
       - Remove the old HomePage component entirely
       - All exports must be named (no default export)

    2. Update AppShell.test.tsx to add routing integration tests if not already present:
       - Wrap test renders in MemoryRouter
       - Verify /chat renders ChatPlaceholder text
       - Verify /dashboard renders DashboardPlaceholder text
       - Verify / redirects to /chat content

    Note: The placeholder components are intentionally trivial per CONTEXT.md decision ("centered label only -- route name in --text-muted"). Do NOT add any functionality beyond the placeholder text.
  </action>
  <verify>
    <automated>cd /home/swd/loom/src && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>App.tsx restructured with AppShell as layout route. All 4 routes render inside AppShell content area. /dev/tokens renders outside AppShell. Root / redirects to /chat. Full test suite passes including ESLint and TypeScript checks.</done>
</task>

</tasks>

<verification>
1. `cd /home/swd/loom/src && npx vitest run --reporter=verbose` -- all tests pass
2. `cd /home/swd/loom/src && npx eslint src/ --max-warnings=0` -- zero lint errors
3. `cd /home/swd/loom/src && npx tsc --noEmit` -- zero type errors
4. Visual: `cd /home/swd/loom/src && npm run dev` -- navigate to http://localhost:5184:
   - 3-column grid visible (sidebar + content + 0px artifact)
   - Sidebar shows "Loom" in italic serif font with collapse chevron
   - Clicking collapse hides sidebar, expand trigger appears at left edge
   - /chat shows "Start a conversation"
   - /dashboard shows "Dashboard"
   - /settings shows "Settings"
   - /dev/tokens shows token preview (standalone, no shell)
   - No document scrollbar at any viewport size
</verification>

<success_criteria>
- CSS Grid shell fills 100dvh with no document scrollbar (SHELL-02)
- Grid has 3 columns: sidebar 280px | content 1fr | artifact 0px (SHELL-01)
- Sidebar collapses to 0px with expand trigger at left edge
- data-sidebar-state attribute drives layout via CSS variables
- React Router serves /chat/:sessionId?, /dashboard, /settings inside content area (SHELL-03)
- /dev/tokens renders outside AppShell
- All tests pass, lint clean, types clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-app-shell-error-boundaries/03-01-SUMMARY.md`
</output>
